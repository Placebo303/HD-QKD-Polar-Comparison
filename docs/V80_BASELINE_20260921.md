# V80 Baseline — 2026-09-21 (THE planning authority)

> Amendment 2026-09-21 (R1 EXECUTED + independent Pre-RESULT PASS_WITH_FINDINGS, docs-only, NO new execution/data/commit): the R1 histogram re-run (Acceptance ID G-R1) EXECUTED under main-thread conversation-grant authorization (this-cycle mode; §7/§8 signature blocks intentionally blank; paperwork deviation ratified administratively per the F-3 precedent — nil scientific impact verified independently) in fresh root `workspace/r1_histogram_5e2a91c4/` (18 files): 3/3 sources OK, gates (a)–(f) all PASS, determinism vs A1 exact on 12 fields, wall 115.7/169.1/246.3 s (trio 531.1 ≤ 5400), peak RSS 0.68 GiB, 0 decoder/DE/graph calls, no repair path used. Independent review `docs/research_cycles/V80-NBLDPC-JAN21/R1_PRERESULT_REVIEW.md`: PASS_WITH_FINDINGS, no blocking findings; every operator number recomputed from the persisted sparse COOs; `H_MM_old`/`H_plug`/`K_AB` bit-identical to the A1 census (same TRAIN realization). §0.2: P-V1 (Miller–Madow estimand) CLOSED for the three Jan-21 sources — `K_B_train = 1024` (full B support) MEASURED on all three, converting the prior inference to fact and satisfying the K_B≳253 conditional; P-V2 (f_eff slope) CLOSED as settled-re-raise per estimator-verification T5 (frozen convention stands; fixed-vs-arm-slope gap gate-irrelevant). §3 per-source design points SUPERSEDED to the corrected basis (defective-MM values retained as labeled history): H_corr = 0.8012690084416184 / 0.8272902027770036 / 0.8333327179427281 (Δ = −0.0023389196758668573 / −0.0016714841284448667 / −0.0012518869160380586, saturating the pre-registered bound at K_B = 1024); m_max 1M 201→200, 1.5M 207 unchanged, 2M 209→cap 208 unchanged; f@208 = 1.34552190 (1M, OUT) / 1.30320049 (1.5M, nominal OUT; threshold-H gap vs CI halfwidth = 0.92 ⇒ statistical status INDETERMINATE-vs-OUT — planning treatment OUT on the frozen point-estimate gate (b)) / 1.29375096 (2M, IN on f-margin); N_req@208 = inf / inf / 2298. Certifiability on key-eligible 200/276/364 (zero-failure still required): 1M NO at 208/200/199 (inf/4447/1541 > 200) and at m_max 200; 1.5M NO at 208/200/199 (inf/327/288 > 276) and at m_max 207 (5315 > 276) — the defective-basis "YES-by-2-blocks PROVISIONAL" at m=199 (274 ≤ 276) FLIPS to NO (288 > 276); 2M m=200/199 remain YES-on-count (271/244 ≤ 364), m=208 is f-IN but NOT certifiable on count (2298 > 364), capped m_max 208 NO. Bootstrap CIs (200 resamples, seed 20260921, on the corrected statistic): hw 0.002851/0.002215/0.001947; each CI sits entirely below its point estimate (A1 F-2 duplicate-support pattern; hw valid; CI_hi is not an upper bound on H). 2M consistency vs frozen `gamma_f03.npz` lineage: no FINDING (ΔH_L1/L2 = −0.000632/−0.000523 ≪ 0.01; p_b L_inf 0.000197 < 1e-3; ΔN +29589 expected vintage delta). §6-2: X1 bundle-materialization entry blocker CLOSED — sparse TRAIN `N_ab` + `p_b` persisted and checksum-verified for all three sources in the R1 root (bundle build = seconds of numpy via `ChannelAdapter`, zero code change via `bind_empirical_bundle()`); X1's 15 arms still require their OWN entry gate (fresh Pre-EXECUTE + explicit grant) — nothing about X1 is authorized here. §8 working code gains `comparison_bench/src/comparison_bench/cli/r1_histogram_rerun.py` + `comparison_bench/tests/test_r1_histogram_rerun.py` (fake-only, 13 tests). Claim ceiling unchanged: corrected design points + materialized bundle inputs ONLY; no FER/SKR/route/qualification/publication claim; corrected points do NOT authorize X1.

> Amendment 2026-09-21 (archive close-out, docs-only, NO execution/data/commit/`.ttbin`/workspace): four earlier OpenSpec changes archived to `openspec/changes/archive/2026-09-21-*/` as superseded-before-execution (frozen, never granted, never executed; spec deltas NOT merged into `openspec/specs/`); §0.2-D1 four-change question CLOSED-BY-ARCHIVE, §7-8 RESOLVED (no waiver needed); §0.1 banner note updated to archived status; §3 stale x1 entry repointed to archive path + naming deviation recorded; §6 notes frozen-not-granted `R1_HISTOGRAM_RERUN_PACKET.md` (G-R1) as the next DECIDE step (NOT authorized here); AGENTS.md §6 non-merge gloss conflict flagged for main thread.
> Amendment 2026-09-21 (pre-launch-review P1–P5+6 follow-up, docs-only, NO execution/data/commit): per review arithmetic reproduction — §3 2M-margin cell corrected to MM-basis f-margin 0.00819 / H-margin 0.00526 (0.0021 retained only as plug-in-basis residue [A1R], provenance CLOSED); stale list extended (§3 note: memory :4255 + four-proposal banner sentence falsified by `v80-prior-cost-accounting/`); §0.1 banner-supersession note added; §7-8 corrected to change-landed (`63004a0c`) with four-earlier-SHALLs question still OPEN; X1 15-arm entry-block CONFIRMED (no authorization conferred); N_req razor-thin sensitivity recorded (1.5M@207 2699.955…/1M@201/2M@208). [DEC]§A/§D 0.12–0.26× withdrawal aligned; interpretation governed by Baseline §5-i.
> Amendment 2026-09-21 (A1-recompute EXECUTED + prior-cost change landed, docs-only, NO execution/data/commit): A1 arithmetic recompute EXECUTED via repo interpreter ([REC]); N_req 15487/2700/1754 + m=200/199 rows CONFIRMED at full precision (§3/§5); headroom canonical 4.3075 (full-precision; rounded 4.3072 differs 0.0003 immaterial); amortized 0.18/0.099 b/blk (0.036/0.020 rows) does NOT force m≤201; §0.2-D1 disclosure-route/data-role gap CLOSED-BY-CHANGE (`v80-prior-cost-accounting/` proposal/design/tasks/spec, 7 SHALLs); four-earlier-changes SHALL question LEFT OPEN (new change explicitly does not retire them); 0.12–0.26× WITHDRAWN as planning number (§5-i; 1.50× binds); report-only stale hits + ROADMAP re-derivation + 0.0021 provenance flagged (§3 note). Decision-doc §A 0.12–0.26× alignment DONE by the P1–P5+6 follow-up amendment above.
> Amendment 2026-09-21 (Step-1 DONE, docs-only): §6-step-1 executed via `docs/PRIOR_COST_ACCOUNTING_DECISION_20260921.md` — disclosure route sacrifice-the-sample DEFAULT, key-eligible counts 200/276/364, §5 (ii)+(iii) SETTLED, (i) partially settled, (iv) OPEN, §7 items 2–6 RATIFIED. No execution/data/commit authorized; rest unchanged.
> Amendment 2026-09-21 (REVISE corrections, docs-only, NO execution/data/commit): independent review returned ADVISORY_VERDICT REVISE. Applied now — R5 N_req→15487/2700/1754 + per-source m=200 certifiability fix (§3/§5; numbers as supplied by review; independent full-precision recompute against `workspace/p3_census_3954637c/census_table.json` H_MM recorded as commands in §3 but NOT executed in this task under the no-execution constraint); A2 36-bit consequence conditioned on per-block model (§2, §6-2/§7-4 qualified); S3 f-direction corrected (§2/§4); S2 ~9σ→halfwidth-ratio (§3); OVERTURNED ledger fixes (§4 + [DEC]§A/§D); R1 retained-clause index (§0.1); R6 applied in `docs/X1_ENTRY_BLOCKER_ASSESSMENT_20260921.md`. PENDING concurrent verification (placeholders only, conclusions NOT preempted): Miller–Madow estimand question; f_eff slope/arm-invariance question. FLAGGED for main thread (not resolved here): new OpenSpec change (or explicit waiver) required — disclosure-route + data-role decisions exceed documentation-only consolidation while four v80 OpenSpec changes still carry effective SHALLs under proposal-only banners; formal F-3 signature + tolerance confirmation remain OPEN until user signs (§0.2/§7). No `.ttbin` access, no workspace write, no commit/push.
> Source-key: [P1]=`docs/research_cycles/V80-NBLDPC-JAN21/P1_PACKET.md` [P2]=`P2_PACKET.md`
> [P3]=`P3_CENSUS_PACKET.md` [S05]=`P3_STAGE05_PACKET.md` [X1]=`X1_CROSS_SOURCE_PACKET.md`
> [RM]=`docs/ROADMAP-20260921.md` [AUD]=`docs/PRIOR_COST_ACCOUNTING_AUDIT_20260921.md`
> [REV]=`docs/PRIOR_COST_CLAIM_REVIEW_20260921.md` [SIB]=`docs/SIBLING_PRIOR_PARAMETERIZATION_AUDIT_20260921.md`
> [A1R]=`P3_A1_REVIEW.md` [S05R]=`P3_STAGE05_REVIEW.md` [MEM]=`AGENT_PROJECT_MEMORY.md:4226-4238`
> [DL]=`docs/decision-log.md:4785-4797` [SEM]=`docs/TTBIN_MEMBER_SEMANTICS_20260921.md`
> [ENV]=`docs/TTBIN_ENV_SETUP_20260921.md` [COR]=`docs/CORRELATION_ALIGNMENT_CAPABILITY_20260921.md`
> [DEC]=`docs/PRIOR_COST_ACCOUNTING_DECISION_20260921.md` (Step-1 decision, 2026-09-21)
> [REC]=`docs/A1_ARITHMETIC_RECOMPUTE_20260921.md` (A1 full-precision recompute evidence, EXECUTED 2026-09-21)
> [PC]=`openspec/changes/v80-prior-cost-accounting/` (proposal/design/tasks/spec, 7 SHALLs)

## §0 Authority & supersession

- This document is the ONE planning authority for V80 NB-LDPC on Jan-21. It supersedes AS PLANNING
  AUTHORITY: `P1/P2/P3/X1/Stage-0.5` packet + prompt files and OpenSpec changes
  `v80-p1-rate-adaptive-rescue`, `v80-p2-finite-length-decomposition`, `v80-p3-real-hfull-census`,
  `v80-x1-cross-source-cliff` (each carries a supersede banner; retained for history, do not cite frozen clauses without checking here).
- NOT superseded (remain authoritative): executed evidence roots
  (`workspace/p3_stage05_ee32030a/`, `workspace/p3_census_3954637c/`), the two review verdicts
  ([S05R], [A1R]), working code (§8), and all historical cycles (untouched).
- Execution status snapshot: Stage 0.5 + A1 EXECUTED, reviewed PASS_WITH_FINDINGS, pending main-thread
  acceptance + F-3 ratification (§7-2). P1 / P2 / X1 FROZEN, NOT GRANTED, unexecuted — their §§ below are
  frozen contracts, not results.

## §0.1 Retained-clause index (REVISE R1 — resolves §0/§1–§2 citation tension)

- Clarification: §0 "do not cite frozen clauses without checking here" moves PLANNING AUTHORITY here; it does
  not erase the frozen packets. Citations like [P1]§2 below are PROVENANCE pointers; binding force comes from
  restatement here or from the explicitly RETAINED-FROZEN packet contract referenced here. No gate or
  prohibition is silently dropped: every still-binding clause below is either restated in this baseline or
  marked RETAINED-FROZEN (packet contract stands, execution still needs its own grant); SUPERSEDED rows are
  explicit and exhaustive for the listed packets.
- Banner-supersession + archive note (item 6, docs-only; updated 2026-09-21 archive close-out): the four old proposals (`v80-p1-rate-adaptive-rescue`, `v80-p2-finite-length-decomposition`, `v80-p3-real-hfull-census`, `v80-x1-cross-source-cliff`) are now ARCHIVED at `openspec/changes/archive/2026-09-21-*/` as superseded-before-execution (frozen, never granted, never executed; each archived `proposal.md` carries an appended ARCHIVED note; spec deltas intentionally NOT merged into `openspec/specs/`). Their banner closing sentence "No new OpenSpec change created (consolidation is documentation-only)" is SUPERSEDED both by the archive and by the existence of `openspec/changes/v80-prior-cost-accounting/` (landed `63004a0c`, 7 SHALLs) and must not be read as a claim about the current change set. This §0.1 cuts their planning force; their frozen clauses bind ONLY via the retained-clause index below.

| Source | Still-binding clause (compact) | Disposition in this baseline |
|---|---|---|
| [P1]§3 | Nested construction: base = leading-200 rows of same-instance A208; rescue rows [200,208); COLD full-matrix re-decode; NO warm-start; NO non-nested fallback; NO two-segment | RETAINED-FROZEN (P1 §§3/6–7; baseline §§5–7 keep P1 frozen-unexecuted; §6-2 consumes X1 for per-source base) |
| [P1]§6 | Gates AND: final fails/240 = 0 HARD; f ≤ 1.3 own basis; headroom ≥ 21.5 b (N ≤ 570; r ≤ 57.0% at 2M) | RETAINED-FROZEN (P1 §6; §9 keeps (a)(b)(c) on each source's OWN basis) |
| [P1]§7 | Budgets/scope/stop: wall ≤ 3600 s/arm; per-decode ≤ 300 s terminal; RSS < 2 GiB; 1 CPU; fresh `workspace/p1_*` roots; no retry/resume/adaptive; ≤1 engineering repair+rerun (inputs unchanged, failure retained); STOP on science-input change; FORBIDDEN list (evidence roots, real data, gate changes, pooling incl. 6+4 sums, undetected-merging, f_super-as-f_eff, warm-start, second matrix, two-segment) | RETAINED-FROZEN |
| [P1]§2 | Out-of-box arms m_base = 202+8 (210 rows, f = 1.30668) and m_base = 200 two-segment (216 rows, f = 1.34185): DELETED | SUPERSEDED-by-deletion (stays deleted; only m_base = 200+8 single-segment inside box) |
| [P1]§9 | Per-source extension: 1M m_base = 193/total 201; 1.5M m_base = 199/total 207 INDETERMINATE-pending-X1; anti-cherry-picking (headlines LEAD with 1M; 2M-only headline FORBIDDEN); gates (a)(b)(c) on OWN source basis | RETAINED-FROZEN (baseline §7-6/§7-7) |
| [P2]§2 | Arm (i) MC-DE at joint rate (m = 416/n = 2048, f = 1.25741): JOINT (u1,u2) sampler (NOT L2 bundle); q = 32, λ = {2:1}; screen 4000/60 + confirm 16000/100; seeds {2026094951, 2026094952}; gate Δ ≤ 0.05; budget ≤ 96 DE calls + ≤ 1200 s total, per-call ≤ 300 s | RETAINED-FROZEN (baseline §7-7 keeps P2 frozen until X1) |
| [P2]§3 | Arm (ii) curve: frozen channel/code family (n = 1024, b2f verbatim, v28 300/3, exact_match, 240 paired blocks, instance 2026092001); partition 180–188 OUT OF SCOPE / 192,196,204 NEW (priority 196→204→192) / m = 200 OWNED BY P1 (must NOT re-measure) / 202,208 BY CITATION (b2f F202 6/240, F208 0/240); bar-12 early-stop; pins fc = 0 + rank-full + twice-identical GATED, girth recorded; NO cross-m monotonicity assumption (report monotone/non-monotone/censored, no inference) | RETAINED-FROZEN |
| [P2]§§4–6 | Precedence (P1 owns m = 200 measurement; P2 consumes BY REFERENCE; P2-first ⇒ SKIP m = 200 mark PENDING; both ⇒ P1 first) + non-interchangeable constructions; roots `workspace/p2_*`; V26 kernel read-only; fake-only tests; STOP + ≤1 repair; FORBIDDEN (real data, constant/gate changes, pooling, f_super-as-f_eff, extrapolation, second kernel, m = 200 in P2) | RETAINED-FROZEN |
| [X1]§3 | Grid (15 arms): 1M {185,189,193,197,201} / 1.5M {191,195,199,203,207} / 2M {192,196,200,204,208}; 240 blocks; seeds 2026095601+idx `o1_blk:{seed}`; SINGLE instance 2026092001; per-source bundles | RETAINED-FROZEN (restated baseline §6-2) |
| [X1]§4 | Prior/decoder per source (b2f verbatim; v28 max_iter 300/streak 3 exact_match; per-source bundles; cross-source reuse FORBIDDEN; standalone per-m constructs fc = 0/rank-full/twice-identical GATED) | RETAINED-FROZEN |
| [X1]§5 | Metrics on arm's OWN source H (f_super/f_eff; undetected separate; NO pooling across sources/m/instances) | RETAINED (baseline I5 + §6-2) |
| [X1]§6 | Gates (a) ≤ 12/240 route gate; (b) f_super ≤ 1.3 own corrected H; (c) N-rule + FORBIDDEN certifiable-literature-comparable presentation | RETAINED-FROZEN |
| [X1]§7 | Budgets/stop: ≤ 1800 s/arm (≤ 27000 s total); per-decode ≤ 300 s; RSS < 4 GiB; 1 CPU; `workspace/x1_*` roots; STOP incl. any `.ttbin` read STOP-BLOCKED; no retry/resume/adaptive; ≤1 repair; overlaps (X1-2M re-measures F202/F208 as single-batch curve, citations as consistency checks never pooled; X1-2M m = 200 STANDALONE ≠ P1 nested leading-200; X1-2M {192,196,204} vs P2 arm-(ii) schedule ONCE) | RETAINED-FROZEN |
| [X1]§1-l5 | Premise "per-source joint channels materialized by the A1 census" | SUPERSEDED-as-premise (FALSE — assessment T2 proves histograms summarized never persisted; entry-blocked until 1M/1.5M bundles materialized via new DECIDE read or main-thread-approved vintage change) |
| [P3]§5 | Estimator F03 (H_full = H_L1+H_L2 verbatim, plug-in no backoff, b/GF32-sym); anchor = V49 TRAIN-pool plug-in 0.83256272 train-side (+0.0204 train→hold gap); split side must be stated; V19 0.549955 never averaged; comparability rule | RETAINED (baseline §§1–3; S3 direction corrected §2/§4) |
| [P3]§6 | Uncertainty: N/support/occupancy reported; MM frozen formula; hold gap Δ = NLL_HOLD − H_TRAIN; bootstrap CI ≥ 200 resamples frozen seed REQUIRED below anchor; uncertainty statement mandatory for design-point use; support bar 0.02 (confirmation status → §0.2) | RETAINED (baseline §3 values; tolerance confirmation §0.2/§7) |
| [P3]§7 | Memory/stationarity: per-frame weights; drift max−min + slope (no pre-registered threshold); lag-1/2 ACF; 60/20/20 consecutive-time TRAIN/VAL/HOLD with manifest first, HOLD single use, VAL recorded-unused; DECIDE-grade falsification statement | RETAINED (baseline §3 roles/results/F-4 power limit) |
| [P3]§8 | Reporting schema + labels (ALIGNMENT-FITTED / IMPOSED-NOT-MEASURED; comparable-by-anchor YES/DIRECTIONAL-ONLY/NO; never pooled) | RETAINED (baseline §3) |
| [P3]§§9–9.4 | Certification arithmetic (content/budget/f/headroom/N-rule; m_max table; structural uncertifiability at m_max; "lower H buys headroom" FALSE at fixed m; control tolerance ±0.01) | RETAINED with updates (baseline §2 rule + §3 MM-governs points; R5 numbers; tolerance confirmation §0.2) |
| [P3]§§2/9.2 | Block counts 500/691/911 (pool 2103) with zero prior reserve | SUPERSEDED for planning by key-eligible 200/276/364 ([DEC]§C; baseline §2/§5) — history only |
| [P3]§11 | Roots/scope/stop: fresh `workspace/p3_census_*`; `results/` + `comparison_bench/outputs_comparison/` forbidden; `src/` read-only; STOP list; FORBIDDEN (excluded derived artifacts, evidence-root overwrite, decoder/DE/graph, longrun/minrerun/routeA_*, constant/gate changes, pooling, undetected-merging, TRAIN-as-held-out, V19-averaging, invented params, commit/push, default-offset runs, embedding alignment into `compute_ttbin_metrics`); derived-data exclusion (§2 excluded list); zero decoder/DE/graph | RETAINED (baseline I4/I6 + §3) |
| [P3]§4.4 | Old both-member-read probe form | SUPERSEDED in form by span-continuity probe (role kept; split out as Stage 0.5) |
| [S05]§§5–7 | G3 span tolerance (proposed 0.5 s, TO BE CONFIRMED at authorization); G4 config-parse gate (verbatim dump + type; absent keys recorded never invented; PM/EB negative: dict 20 keys, zero pm/eb keys ⇒ header-PM/EB REFUTED at payload level); read counts/budget (2 reads/dataset, ≤ 300 s/read, ≤ 1800 s total, RSS < 4 GiB, 0 decoder/DE/graph; wall-partial INCOMPLETE never continued; ≤1 repair); G1/G2/batch rule; SHG-A anomaly; §8 future-script guards | RETAINED-FROZEN as proposed/measured (executed root `workspace/p3_stage05_ee32030a/` + [S05R]; formal tolerance confirmation OPEN → §0.2) |

## §0.2 Governance — REVISE disposition, pending verifications, open decisions (REVISE Governance)

- Review status: independent review returned ADVISORY_VERDICT REVISE (this amendment). Applied: R5/A2/S3/S2/OVERTURNED-ledger/R6/R1 as scoped in the amendment note. No execution, no data, no gate change by this amendment.
- Pending concurrent verification (placeholders ONLY — conclusions NOT preempted; separate task owns the verdicts):
  (P-V1) Miller–Madow estimand question — affected claims recorded as UNVERIFIED-pending-verification; over-confident wording removed (§3: 1.5M in-box status UNVERIFIED; no MM-estimand conclusion asserted here).
  (P-V2) f_eff slope/arm-invariance question — the frozen `f_eff = f_super + 4.785675·FER` rule and all N_req numbers that depend on the slope are recorded as UNVERIFIED-pending-verification for slope purposes; no slope conclusion asserted here (numbers retained as frozen-planning arithmetic until the verification lands).
- Open main-thread decisions (flagged, NOT resolved here): (D1) CLOSED-BY-CHANGE for disclosure-route + data-role — `openspec/changes/v80-prior-cost-accounting/` NOW EXISTS (proposal/design/tasks/spec, 7 SHALLs: prior-cost disclosure; sacrifice exclusion + forgone-key reporting; key-eligible 200/276/364 with corrected-count certifiability; charging model + amortization basis; f_super/f_eff invariance; estimator estimand + `K_B` auditability; claim ceiling) and carries the [DEC]§B/§C requirements at spec level. Four-earlier-changes SHALL question CLOSED-BY-ARCHIVE 2026-09-21: the four v80 OpenSpec changes (`v80-p1-rate-adaptive-rescue`, `v80-p2-finite-length-decomposition`, `v80-p3-real-hfull-census`, `v80-x1-cross-source-cliff`) are archived at `openspec/changes/archive/2026-09-21-*/` as superseded-before-execution (frozen, never granted, never executed; md5-verified, nothing lost; each archived `proposal.md` carries an appended ARCHIVED note, append-only); `openspec/specs/` is UNMODIFIED — spec deltas intentionally NOT merged (merging would promote unimplemented SHALLs into live spec); their frozen clauses bind ONLY via the §0.1 retained-clause index. So the new change closes the disclosure-route/data-role governance gap and the archive closes the banner/SHALL tension (no waiver needed). Note: non-merge conflicts with the one-line AGENTS.md §6 gloss ("merge delta specs into main") — flagged for the main thread, not followed here. (D2) formal F-3 signature + tolerance confirmation (below). (D3) §5-iv parameterization. (D4) operating point.
- Status-consistency rule (resolves §0/§3 vs §7 conflict flagged by review): §7 items 2–3 are main-thread RATIFIED-in-principle; the FORMAL close-outs remain OPEN until the user signs — i.e. §3 wording stands ("F-3 auth gap OPEN", control PASS "conditional on [TO BE CONFIRMED] tolerance") while §7 records the ratification direction. Formally OPEN: (i) F-3 signature block (A1-only grant, verbatim chat grant `允许你开始Stage 0.5与A1`); (ii) tolerance confirmation (±0.01 control / 0.02 support). §7-2/§7-3 are qualified accordingly.

## §1 Invariants — never violated

| # | Rule | Authority |
|---|---|---|
| I1 | `.ttbin` pair members are NESTED/SUPERSET (vendor auto-follow). Open `X.ttbin` ONLY; never both, never concatenate (doubling ⇒ pairs/counts_ab/H all wrong). Guard: span-continuity assertion (span>0 AND \|span−mtime-gap\|≤tol, else STOP-BLOCKED) | [P3]§2, [SEM]§C–E |
| I2 | Duration is MEASURED (event-stream span), never the filename tag. Jan-12 measured 29.9999524 s vs `3s` tag (QUARANTINED, tag_disputed); Jan-21-2M 2.9999997 s | [P3]§2, [SEM]§D |
| I3 | Mandatory correlation delay alignment BEFORE any pairing: `compute_cross_correlation_histogram(events,ch_a,ch_b,bin_width_ps=100,max_lag_ps=819200)` (16384 bins) → argmax → `offset_ps=+lag_center_ps[pk]`. Gates: p2bg≥100, single dominant mode (no secondary >50% outside ±1000 ps), crude σ 10–500 ps; else STOP-BLOCKED with NO fallback to 0/borrowed/recorded. Sign: offset added to side A (`src/qkd_io/ttbin_pipeline.py:219-249`) | [P3]§3A, [COR]§D |
| I4 | `src/` FROZEN (`AGENTS.md` §5.1; `git diff -- src/` empty). Alias shim BEFORE any TimeTagger import; repo-root PYTHONPATH (§8) | [ENV] verification |
| I5 | No pooling across construction instances or sources; `undetected` never merged into success; never quote f_super as f_eff | [RM]§5, [P1]§7 |
| I6 | Protected roots `results/`, `comparison_bench/outputs_comparison/` never written; additive `workspace/<id>_<uuid>` roots only; fresh root absence proven at Pre-EXECUTE | [P1]§7,[P3]§11 |

## §2 Frozen accounting

- Basis: superframe n=1024 GF(32) symbols, n_bits=5120, anchor H_full=0.83256272 b/sym, content=852.544 b ([P1]§2; `PROGRAM_PLAN.md`§1.3).
- `f_super=(5·(m1+m2)+64)/852.544 ≤ 1.3` ⇒ leak ≤1108.31 b ⇒ **m1+m2 ≤ 208** (hard cap). A208: leak 1104, f=1.294947, headroom **4.3075 b canonical** ([REV]§5.4; [RM] rounds 4.31; [REC]§5: canonical is full-precision `1.3·(1024·0.83256272)−1104=4.3075`; the rounded `1.3·852.544−1104=4.3072` differs by 0.0003 b, immaterial). A202 f=1.259759; A200 leak 1064 f=1.24803 ([P1]§2).
- `f_eff=f_super+4.785675·FER` on each arm's OWN m/H basis. `λ_total=leak_EC+64` (≡ε_EV≈2⁻⁶³). `net=A·5·n−leak`; V80 nominal superframe 5120−1104=**+4016 b** ([RM]§3 P5; [MEM] rejects the legacy negative-SKR sentence).
- Certifiability (zero-failure arms ONLY): `N ≥ ceil(3·4.785675/(1.3−f_super))`. A208→**2842** (pool 2103 < 2842: structurally uncertifiable, [RM]§1.2/§8); f≈1.25→288; n=2048 m=416 f=1.25741→338 ([RM]§2–3).
- **OPEN ACCOUNTING ITEM — prior/calibration cost NOT counted in λ_total (CONFIRMED gap; `SECURITY_MODEL.md` zero hits for prior terms; no `+prior` term in `v80_o1_campaign.py:415-417`, `nonbinary_qspa.py:264-268`, `metrics/leakage.py:29-34`; [AUD] C1, [DL]).**
  (a) Prior-disclosure cost is CONDITIONAL on the accounting model (REVISE A2; recorded here as a non-default-branch conditional, NOT the headline finding). Under a PER-BLOCK disclosure model ONLY — iff every block bears the full 36 b cost charged against that block's 1.3 gate — 36 b is **8.36×** the 4.3075 b A208 headroom ⇒ `m ≤ 201` (⌊(1108.3075−64−36)/5⌋ = 201), inside the unmapped 188–208 cliff ([REV]§5.4; [MEM]). Under the AMORTIZED alternative — prior disclosed once per same-source batch — the per-block cost is 36/200 ≈ 0.18 b (1M) or 36/364 ≈ 0.099 b (2M) ([DEC]§C counts; [REC]§6 CONFIRMED: ⇒ 0.036 / 0.020 rows, two orders below the 7-row per-block relocation, so m ≤ 201 does NOT follow). Neither budget may be used until the total transcription (bits disclosed, encoding, precision, public-message protocol) and its amortization basis (per-block vs per-batch, batch size) are DEFINED. 36 b itself is NOT a proven security-leakage quantity: the real C03 is a 1024-bin smoothed histogram (≤1023 dof; [AUD] C4), and no two-parameter encoding, precision, or public-message protocol has been established ([REV]§3). Under the RATIFIED sacrifice-the-sample DEFAULT ([DEC]§B) this whole consequence is inert (f/m unaffected).
- f-direction correction (REVISE S3; supersedes the [REV]§5.3/§191 framing): with `f = L/(nH)`, UNDERestimating H makes f LARGER (conservative/pessimistic), NOT optimistic. The V80 anchor is a TRAIN-side plug-in whose measured train→hold gap is +0.0204 b/symbol (TRAIN 0.83256 → HOLD 0.85297; [P3]§5.3) — the holdout H is HIGHER — so using the anchor UNDERSTATES the denominator and OVERSTATES f. The anchor is conservative on f, not optimistic-low.
  (b) Planning counts 500/691/911 reserve zero frames for the prior ⇒ key-eligible **200/276/364** (pool 840 vs 2103; 2.5× overstatement, [REV]§1c; [MEM]).
- **RATIFIED 2026-09-21 ([DEC]): disclosure route is sacrifice-the-sample (DEFAULT; cost in SKR numerator only, f untouched); key-eligible counts are 200/276/364 (corrects [P3]:43); per-source f uses own H_MM.**

## §3 Established results (executed; paths + verdicts)

- **Stage 0.5** — root `workspace/p3_stage05_ee32030a/` (14 files). **10/10 G1–G4, PASS_WITH_FINDINGS** ([S05R]).
  Spans ≈3.0 s all (JAN12 29.9999524, tag_disputed=true); all `fallback_used=false`; config `dict` 20 keys, ZERO pm/eb keys ⇒ PM/EB-from-header REFUTED; SHG-A anomaly (registered [1,5,9,13,17] vs stream [1001,1,5]); wall ~3 s, RSS ~70 MiB; 0 decoder/DE calls ([S05R]§§1–11).
- **A1 trio census** — root `workspace/p3_census_3954637c/` (11 files). **12/12 review items PASS, PASS_WITH_FINDINGS** ([A1R]).
  N=525831/735780/982182; support 2395/2439/2597; MM=(K−1)/(2N·ln2) confirmed; bootstrap 200 resamples seed 20260921, CI hw 0.00285/0.00221/0.00193; control 2M Δ=−0.00115 within ±0.01 ⇒ PASS (conditional on [TO BE CONFIRMED] tolerance); memoryless NOT-FALSIFIED-but-POWER-LIMITED (|acf|≤0.005, slopes ~1e-9; ~1 pair/frame ⇒ blind to frame-internal memory, F-4); F-1 (missing §9 table) closed via `DESIGN_POINT_ARITHMETIC.md`; F-3 auth gap OPEN (§7-2); F-5 block-count deviation declared ([A1R] F-1–F-8).
- **Per-source design points (MM-corrected governs; content=1024·H_MM; [X1]§1; recompute [REV]§5.3):**

| src | H_plug | H_MM | content | m_max | f@208 | f@200 | f@199 | N_req |
|---|---|---|---|---|---|---|---|---|
| 1M | 0.79813 | 0.80361 | 822.897 | 201 | 1.34160 OUT (threshold-H gap 0.0257 ≈9.0× CI hw 0.00285 — halfwidth ratio, NOT σ; bootstrap percentile interval does not bracket the point estimate) | 1.29300 (N 2051) | 1.28692 (N 1098) | @201 15487 |
| 1.5M | 0.82498 | 0.82896 | 848.855 | 207 | 1.30055 nominal f>1.3, statistical in-box status UNVERIFIED (threshold-H gap 0.00037 within CI hw 0.00221; P-V1/P-V2 pending) | 1.25345 (N 309) | 1.24756 (N 274) | @207 2700 |
| 2M | 0.83141 | 0.83458 | 854.610 | 209→cap 208 | 1.29181 IN (MM-basis f-margin 1.3−1.29181=0.00819; H-margin 0.83458−0.829327=0.00526; `0.0021` is plug-in-basis historical residue [A1R] (≈0.8314077735−0.8293269231=0.00208) and must not be read against the MM-basis f; plug-in H < MM H so 0.0021 is the tighter/more-conservative estimate; 2M strictly IN@208 on BOTH bases — no verdict change) | 1.24501 (N 262) | 1.23916 (N 236) | @208 1754 |
| anchor V49 TRAIN | — | 0.83256272 | 852.544 | 208 | 1.294947 | 1.24803 | 1.24216 | 2842 |

Thresholds: A208 needs H≥0.829327; A200 H≥0.799279; A199 H≥0.795523 ([X1]§1). [A1R]§7 plug-in-only table (m_max 199/206/208) is SUPERSEDED for severity by the row above.
- Per-source certifiability vs key-eligible 200/276/364 ([DEC]§C; REVISE R5; [REC] EXECUTED-CONFIRMED at full precision — the blanket "every N exceeds at m=200" is FALSE): 1M m=201 N=15487>200 NO / m=200 N=2051>200 NO / m=199 N=1098>200 NO; 1.5M m=207 N=2700>276 NO / m=200 N=309>276 NO / m=199 N=274≤276 YES-on-count PROVISIONAL (2-block margin; zero-failure still required); 2M m=208 N=1754>364 NO / m=200 N=262≤364 YES-on-count / m=199 N=236≤364 YES-on-count (zero-failure still required); 2M raw m_max=209 gives N=6138 but the frozen cap 208 binds. Explicit verdicts: **2M m=200 YES, m=199 YES; 1.5M m=199 YES-by-2-blocks PROVISIONAL, m=200 NO, m_max NO; 1M NO at m_max/200/199.** [P3]§9.2 structural-uncertifiability holds at m_max, NOT uniformly at m=200.
- Recompute ([REC] EXECUTED 2026-09-21 with the repo interpreter against `workspace/p3_census_3954637c/census_table.json` read-only; full-precision H_MM T2-1M 0.8036079281174853 / T2-1.5M 0.8289616869054485 / T2-2M 0.8345846048587662; rule `N=ceil(3·4.785675/(1.3−f))`, `f=(5m+64)/(1024·H)`; ALL review values reproduced): N_req at each source's own m_max is **15487 (1M@201) / 2700 (1.5M@207) / 1754 (2M@capped-208; raw m_max 209 gives N=6138)** CONFIRMED; m=200/199 rows CONFIRMED (1M 2051/1098; 1.5M 309/274; 2M 262/236); certifiability verdicts above CONFIRMED. The rounded-content table values (822.897/848.855/854.610) differ ≤0.005 b from full-precision contents (822.89452/848.85677/854.61464) — immaterial, no N_req changes ([REC]§§1–4).
- **Alignment (derived, all `ok`, one-bin AGREE Δ=0; [X1]§1, [A1R] findings 1–3):** 1M bin 8191→−50 ps (p2bg 1186.3, σ66.41); 1.5M bin 8192→+50 (748.2, 70.71); 2M bin 8192→+50 (546.1, 70.92). Recorded −50/+50 are measured peak-bin centres, quoted as prior evidence only, never as input.
- **Report-only stale references (deliberately NOT rewritten; history/memory domains stay frozen):** `~9σ` / pre-correction N_req still appear in `X1_CROSS_SOURCE_PACKET.md:43`, `P1_PACKET.md:68`, `openspec/changes/archive/2026-09-21-v80-x1-cross-source-cliff/design.md:12` + `proposal.md:10` (archived 2026-09-21; `proposal.md` now carries an appended ARCHIVED note — as do all four archived proposals), and `AGENT_PROJECT_MEMORY.md:4216/4220` — all SUPERSEDED/history/memory, left as-is (pre-archive paths cited above are stale by relocation, content unchanged). Added by this amendment: `AGENT_PROJECT_MEMORY.md:4255` ("THIS append is NOT committed" — stale; committed in `63004a0c`) and the four superseded proposals' banner closing sentence "No new OpenSpec change created (consolidation is documentation-only)" (falsified by the existence of `openspec/changes/v80-prior-cost-accounting/`; see §0.1 banner-supersession + archive note) — likewise SUPERSEDED/history, deliberately not rewritten. `docs/ROADMAP-20260921.md:264` still carries the pre-correction 500/691/911 claim — needs re-derivation, not a one-word fix (flagged for main thread). Provenance CLOSED (was "unclear", [REC]§7): the §3-table "2M @208 IN (margin 0.0021)" is the plug-in-basis H margin (≈0.8314077735−0.8293269231=0.00208) quoted from [A1R] wording; MM-basis margins above govern. Known cosmetic deviation (for the main thread): the four new archives use plain `2026-09-21-<name>` without the `-superseded` suffix plus top-level `archive.md` disposition record of the 2026-09-11 precedent (`2026-09-11-…-nbpolar-app-transfer-superseded`) — accept as-is or rename at commit time.
- **N_req razor-thin sensitivity (review finding, no verdict change):** at full precision 1.5M@207 N_req=2699.955… sits only 0.04 blocks below its ceil boundary — adopted 2700 is razor-thin and any rounding or small H change could move it; 1M@201=15486.64 (0.64 below boundary); 2M@208=1753.06 (adopted 1754 reflects the frozen cap 208; raw m=209 is arithmetically valid but frozen-invalid). Rounded table values (H/content/f) must never be substituted back into recomputation — recompute only from full-precision H_MM per [REC] commands.

## §4 Corrections ledger (settled — never re-quote withdrawn versions)

| Withdrawn | Settled | Authority |
|---|---|---|
| "1.8× prior-cost ratio" | WITHDRAWN as fact; 5-bit/10-bit symbol arithmetic (7.84/4.23≈1.856) is a PLAUSIBLE INFERENCE for the mix-up, NOT a determined cause — the nonexistent P20Q number cannot prove causation | [AUD]§6, [REV]§6; REVISE OVERTURNED |
| `P20Q 138516÷32768` | Nonexistent (zero hits; digit-substrings only) | [AUD] C3 |
| `PHASE4_P0_PRIOR_CONTRACT.md` | Nonexistent | [AUD] C2 intro |
| CAL framing (CAL=prior) | CAL is Jan-23 `20260123_1M_600k_0dB` (out of scope); real V80 prior = Jan-21 2M TRAIN N=559872 | [REV]§1, [MEM] |
| C03 ≈ 2 params | 1024-bin smoothed histogram (≤1023 dof); "2" = effective-dof reading only | [AUD] C4 |
| 2545 as CAL/2M count | 2545=1M support; 2M=2821; CAL nonzero=139766 (1.88 samples/cell) | [AUD] C5 |
| Plug-in-only severity for 1.5M | MM-corrected: 1.5M nominal f>1.3 with statistical in-box status UNVERIFIED (threshold-H gap 0.00037 within CI hw 0.00221; P-V1/P-V2 pending), 1M IN@200 (was OUT on plug-in) | [X1]§1 vs [A1R]§7; REVISE S2 |
| "Lower H buys f headroom" | FALSE at fixed m (H in denominator); lever is the (H,m_min) pair | [P3]§9.3 |
| "Every packet carries negative SKR ceiling" | CORRECTION (REVISE OVERTURNED): our REJECTION was WRONG. The source ([REV]§5.2: `NO real-data/FER/SKR/qualification/publication claim`; [P3]§12) states a CLAIM-CEILING prohibition on SKR assertions, not a negative SKR value. Original meaning RESTORED: no SKR number has been or may be asserted under those packets. The frozen planning arithmetic `net=5120−1104=+4016 b/superframe` (§2) is unaffected — what was wrong was reading the ceiling sentence as a claim about that value. This does NOT rescue the withdrawn 1.8× figure | [REV]§5.2, [P3]§12 |
| Sibling zero-PE-leakage as proof | Scope exclusion (`public_ec_only_not_secure`, `composable_security_claim_flag=0`; no estimation-leakage variable exists) | [SIB] S3–S4 |
| "Parameterization does not win on disclosure cost" | OVER-TURNED as stated (REVISE): relative cost reduction vs meeting a per-block budget are different propositions. Parameterization DOES reduce the disclosure cost (1024-bin table → small effective-dof statistic); it just does not make it affordable UNDER THE PER-BLOCK MODEL at A208 economics. Under amortization the comparison changes — see §2(a) | REVISE OVERTURNED |
| C03/C04 equality ⇒ full-table equality | UNRESOLVED conflict (REVISE): fact audit reads C03==C04 (bit-identical) as CONFIRMED flat-with-full-table ([AUD]§4), while the adversarial review holds full-table-holdout equality NOT MEASURED (NLL flatness ≠ decoder-equivalence; no V80 arm compares priors; [REV]§4/FXR-1). C03/C04 equality does NOT establish equality with the FULL-TABLE holdout | [AUD]§4 vs [REV]§4 |
| "f optimistic via denominator (TRAIN plug-in anchor, +0.0204 gap)" ([REV]§5.3/§191) | DIRECTION CORRECTED (REVISE S3): with f=L/(nH), UNDERestimating H makes f LARGER (conservative). Anchor TRAIN plug-in + holdout +0.0204 higher ⇒ using the anchor OVERSTATES f — see §2 | [P3]§5.3 |

## §5 Open accounting decisions (settle before any SKR/publication claim)

- (i) PARTIALLY SETTLED 2026-09-21 ([DEC]§A; [PC] design §1 carries the spec-level SHALL): the frozen amortization/reuse statement NOW EXISTS (`gamma_f03.npz` read-only, never refitted, reused O1/O1R/P0/L1B/B2E/B2F/B2G/X1 ⇒ one-time factory calibration — FACT retained); the "0.12–0.26× honest per-source ratio" is WITHDRAWN as a planning number (retained history-only as a withdrawn sensitivity figure: its denominator mixed the sacrificed TRAIN pool and it is incompatible with the cross-source reuse ban [X1]§4); the 1.50× per-source figure (cross-source reuse FORBIDDEN [X1]§4; repo 60/20/20 split, [REV]§2c) is the BINDING number for any SKR claim. Alignment DONE 2026-09-21 (pre-launch-review follow-up): [DEC]§A/§D now carry the same withdrawal inline; where [DEC] and this §5-i differ, this §5-i governs.
- (ii) SETTLED 2026-09-21 ([DEC]§B): disclosure route is sacrifice-the-sample (DEFAULT; cost in SKR numerator only; f untouched). Disclose-the-statistic PERMITTED-BUT-NOT-DEFAULT only with explicit transcription + amortization basis, λ_total accounting and m→201 relocation under the per-block model only (see §2(a); [REV]§5.4).
- (iii) SETTLED 2026-09-21 ([DEC]§C; [REC] EXECUTED-CONFIRMED): block counts corrected to key-eligible 200/276/364 (corrects [P3]:43); certifiability re-run on corrected counts — at m_max N_req 15487/2700/1754 ≫ 200/276/364 CONFIRMED (1M@201 / 1.5M@207 / 2M@capped-208; 2M raw 209 N=6138, cap binds); at m=200: 1M 2051>200 NO, 1.5M 309>276 NO, 2M 262<364 YES-on-count (zero-failure still required); at m=199 1.5M 274<276 YES-on-count PROVISIONAL (2-block margin; arithmetic confirmed, provisional = thin margin + zero-failure requirement), 2M 236<364 YES-on-count (see §3).
- (iv) OPEN: decide whether to parameterize the prior. Decoder-equivalence UNMEASURED: no V80 arm compares priors (FXR-1 forbids entropy-parity reading); Release's O(10)-scalar BSC path not transferable under composable intent ([SIB] transfer table; [REV]§4).

## §6 Forward plan (ordered; each unlocks the next)

0. **R1 histogram re-run — FROZEN, NOT GRANTED (DECIDE packet, authorizes NOTHING here).** `docs/research_cycles/V80-NBLDPC-JAN21/R1_HISTOGRAM_RERUN_PACKET.md` (Acceptance ID **G-R1**) is the frozen next step that closes BOTH open items in one DECIDE pass: the estimator `K_B` defect (corrected `H_corr = H_plug + (K_AB − K_B)/(2·N_train·ln2)`) and the X1 bundle materialization (sparse `N_ab_train` + `p_b_train` per source). It needs its own signature block (`R1_HISTOGRAM_RERUN_PREREG_AND_AUTH.md`) + Pre-EXECUTE + independent Pre-RESULT + main-thread acceptance before anything runs. This baseline records it; it does NOT grant or authorize it.
1. **Amortization statement + disclosure-route decision (docs-only, zero cost).** Track: — (no execution; normal review proportionality). **DONE 2026-09-21 ([DEC]) — fixes (ii)+(iii) numerically.**
   Unlocks: ALL SKR/publication talk; fixes (ii)+(iii) numerically. Authorizes: no execution, no data, no gate change. Does NOT: certify any f_eff, select an operating point, or close (iv).
2. **X1 cross-source cliff curves (15 arms, §3 grid; 1M {185,189,193,197,201} / 1.5M {191,195,199,203,207} / 2M {192,196,200,204,208}; 240 blocks, seeds 2026095601+idx, instance 2026092001, standalone per-m constructs, per-source bundles, b2f prior/decoder; [X1]§§3–7).** Track: EXPLORE (EXPLORE_HEAVY cost: 15×1800 s=27000 s ceiling). **ENTRY-BLOCKED — CONFIRMED by this amendment, no authorization conferred:** all 15 arms remain blocked until 1M/1.5M bundles are materialized ([X1]§4 [BLOCKING]); nothing in this baseline authorizes any X1 arm, any `.ttbin` read (STOP-BLOCKED, [X1]§7), or any vintage substitution. X1-2M {192,196,204} run ONCE vs P2 arm-(ii) (consume by reference, [X1]§7).
   Doubly motivated (CONDITIONAL — per-block disclosure model only, see §2(a); under amortization m≤201 does NOT follow): IF the disclose-statistic route is ever chosen under per-block charging, the operating point relocates to m≤201 — inside the unmapped 188–208 cliff this step maps ([REV]§5.4; P1 §9 needs X1 per source). Unlocks: per-source m_base for P1 rescue (never assume 2M). Authorizes: synthetic per-source FER curves only. Does NOT: establish generality (needs P1-1M rescue PASS, [X1]§8), any FER/SKR/route/qualification claim, or any `.ttbin` read (STOP-BLOCKED, [X1]§7).
3. **Decoder-equivalence arm (parameterized ±1 prior vs full table at frozen design point, one arm ~11–22 min; [REV]§8-1).** Track: EXPLORE (synthetic, same b2f gates). Unlocks: whether parameterization is even available (settles §5-iv). Authorizes: one paired 240-block comparison only. Does NOT: change any frozen prior, gate, or accounting; a C03 NLL win ≠ decodability (FXR-1).

## §7 Open decisions for the main thread (recommendations)

1. Step order §6-1→2→3? **Yes — recommend as frozen** (step 1 zero-cost, step 2 entry-blocked on bundles, step 3 needs no X1 output but benefits from its m_base).
2. F-3 authorization gap (unsigned prereg; verbatim chat grant `允许你开始Stage 0.5与A1`)? **RATIFIED-in-principle 2026-09-21 ([DEC]§D)** as A1-only grant (control margin 8.7×, CI margin 7× ⇒ nil scientific impact), close P3-T5 administratively; any new dataset needs a new signature ([A1R] F-3). Formal signature block remains OPEN until the user signs (see §0.2).
3. `[TO BE CONFIRMED]` tolerances (±0.01 control, 0.02 support)? **RATIFIED-in-principle 2026-09-21 ([DEC]§D: CONFIRMED)** (both pass with large margins; formal only, [A1R] items 5–6). Formal tolerance confirmation remains OPEN until the user signs (see §0.2; §3 conditional wording stands).
4. Disclosure route? **RATIFIED 2026-09-21 ([DEC]§B: sacrifice-the-sample as default** (standard PE treatment; keeps f intact); disclose-statistic only with explicit λ_total accounting and m→201 relocation under the per-block model only (see §2(a); [REV]§5.4).
5. Block counts? **RATIFIED 2026-09-21 ([DEC]§C): correct to 200/276/364** before any S3 packaging ([REV]§1c).
6. Headline source? **RATIFIED 2026-09-21 ([DEC]§D): 2M alone carries A208; any generality headline LEADS with 1M** (anti-cherry-picking; reporting 2M only is forbidden, [P1]§9).
7. P1-2M (m_base=200+8) and P2: keep frozen-unexecuted until X1 lands? **Recommend yes** — P1-§9 per-source arms consume X1; P2 arm-(ii) overlaps scheduled once ([P2]§4, [X1]§7).
8. OpenSpec change for disclosure-route + data-role decisions? **CREATED AND LANDED 2026-09-21 as `openspec/changes/v80-prior-cost-accounting/` (proposal/design/tasks/spec, 7 SHALLs) in commit `63004a0c`** — closes the §0.2-D1 disclosure-route/data-role governance gap. The separate four-earlier-changes question is RESOLVED 2026-09-21 by archive — no waiver needed: `v80-p1-rate-adaptive-rescue`, `v80-p2-finite-length-decomposition`, `v80-p3-real-hfull-census`, `v80-x1-cross-source-cliff` are archived at `openspec/changes/archive/2026-09-21-*/` as superseded-before-execution (each archived `proposal.md` carries an appended ARCHIVED note; spec deltas NOT merged into `openspec/specs/`; frozen clauses bind only via §0.1). `v80-prior-cost-accounting/` remains active and untouched. Naming deviation recorded in §3 (no `-superseded` suffix / `archive.md`; main thread to accept as-is or rename at commit time). Pending verifications P-V1 (MM estimand) / P-V2 (f_eff slope) recorded as placeholders in §0.2; no conclusion preempted here.

## §8 Working code & how to run it

- Modules: `comparison_bench/src/comparison_bench/io/ttbin_compat.py` (`install_timetagger_alias`),
  `io/align_wrapper.py` (frozen §3A: `derive_alignment`, `require_alignment_passed`, `offsets_agree_one_bin`),
  `cli/p3_stage05_probe.py` (Stage 0.5), `cli/p3_census_a1.py` (A1 trio). Tests:
  `comparison_bench/tests/test_p3_census_a1_align.py`, `comparison_bench/tests/test_p3_stage05_probe.py`.
- Env preamble (EVERY entrypoint; [ENV] verification, [S05]§3.1): repo-root PYTHONPATH + alias BEFORE any TimeTagger import:
  `PYTHONPATH=/mnt/d/Code/HD-QKD_Polar_Comparison .venv/bin/python -c "from comparison_bench.src.comparison_bench.io.ttbin_compat import install_timetagger_alias; install_timetagger_alias(); from TimeTagger import FileReader; print('smoke OK')"`
- Tests: `PYTHONPATH=<repo-root> .venv/bin/python -m pytest -p no:cacheprovider -o addopts="" comparison_bench/tests/test_p3_census_a1_align.py comparison_bench/tests/test_p3_stage05_probe.py` (fake-only; 18 + 7 passed at review, [A1R]§12 [S05R]§11).
- Census runs (DECIDE — signed prereg + Pre-EXECUTE + explicit grant REQUIRED, nothing authorized here):
  Stage 0.5: `PYTHONPATH=<root> .venv/bin/python -m comparison_bench.src.comparison_bench.cli.p3_stage05_probe --bases <10;-separated> --root workspace/p3_stage05_<uuid8>`;
  A1: `... cli.p3_census_a1 --bases <3;-separated> --datasets T2-1M;T2-1.5M;T2-2M --root workspace/p3_census_<uuid8> --bootstrap-seed 20260921 --bootstrap-resamples 200`.
