# Route B-lite Archived Study

Route B-lite is archived and no longer part of the default mainline.

It evaluated whether richer binary channel / LLR modeling should replace the Route A baseline. The result was mixed: local gains exist, but they were not stable enough to justify polluting the default implementation path.

The retained value is evidence for future Route C / q-ary Polar design discussions, not a current production route.
