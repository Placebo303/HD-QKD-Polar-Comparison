# D7-E Pre-RESULT Review R1 (independent, read-only)

- reviewer: independent D7-E Pre-RESULT reviewer (separate context from the execution operator; review-only)
- authority: `.workbuddy/tasks/D7_E_CROSS_LAYER_EXECUTE_PRE_RESULT_R1_TASK_PACKET.md` §10 (STEP 7, R01–R22)
- branch: `formal-ir-v72p1-addendum-clean`
- HEAD verified: `34085c159d87bcaf9b46f2dad5961688940223c5` (revoke commit; `git log --oneline -5` = `34085c15`, `b148c9d4`, `40eeb73a`, `9e095382`, `becf60f2`)
- review-only: sole write is this file; no commits, no other edits, no decoder/Model-F content, no new roots/UUID, no verify re-run
- frozen inputs read: packet §§0–3 + §10; `D7_E_PREREG_R1.md`; `D7_E_EXECUTION_PACKET_R1.md`; `D7_E_EXECUTION_PACKET_ADDENDUM_VENV_A1.md`; `D7_E_EXECUTION_PACKET_ADDENDUM_RSS_A2.md`; `D7_E_RSS_TELEMETRY_REWORK_REVIEW_A2.md` (T5); `D7_E_PRE_EXECUTE_REVIEW_VENV_RSS_A2.md` (T6); `D7_E_IMPLEMENTATION_REVIEW_R1.md`; `D7_E_PRE_EXECUTE_REVIEW_VENV_A1.md` (provenance); `D7_E_AUTHORIZATION_RECORD_R1.md` (committed); `D7_E_OPERATOR_RETURN_R1.md` (uncommitted — treated as claim, never as evidence); result root `workspace/d7_e_cross_layer_discriminator_faa5dc1c-d2d6-4329-b88d-68f2c1f51d5c/` (seven files, read-only scalars only); `cycle_state.yaml`
- operator claims verified (not trusted): UUID `faa5dc1c-d2d6-4329-b88d-68f2c1f51d5c`; auth `b148c9d4`, revoke `34085c15`; one invocation, stdout `terminal=D7_E_L2_TO_L1_TRANSFER_LIFT`, stderr empty, outer wall ~73 s; scheduled/invoked/completed 192/192/192, mandatory 128, transfer invoked 64 / blocked 0; all strata 16/16 eligible; labels STRONG 1 / REGRESSION 0 / NO_RECOVERY 2 / CONTROL 0 / AMBIGUOUS 1; terminal `D7_E_L2_TO_L1_TRANSFER_LIFT` (9th of 12); exact 16 / syndrome 16 / crash 0 / nonfinite 0; stored wall 64.9536 s, max call 0.4379 s, peak RSS 132390912, watchdog 0; provenance CHECK_UPDATED 192 / PRIOR_ONLY 0 / WARM 0; verify claim noted but NO recorded frozen-verify literal found (see R18)

## Verdict

`D7_E_PRE_RESULT_REVIEW_BLOCKED_R1`

The artifact is verify-consistent on every independently recomputable invariant (R02–R17, R19–R22 pass; R01 passes with a disclosed inference note), but R18 cannot be closed: no recorded frozen `--verify` literal output (e.g. `VERIFY_OK` + exit code) exists in the operator return or cycle directory, and the reviewer is forbidden from re-running verify. Do not solidify, do not repair, do not rerun. Preserve the root in place. Main thread to adjudicate: either accept this independent recomputation as sufficient under a governed exception, or authorize a single recorded frozen verify plus a re-review (no second scientific run).

The reviewer does not accept the result. No scientific acceptance, qualification, promotion, or G1/G2 permission is granted.

## R01–R22 table (independently recomputed values)

| ID | Check | Recomputed | Result |
|----|-------|------------|--------|
| R01 | Authorization lifecycle + exactly one invocation | Auth commit `b148c9d4` (record + state `false->true` only); revoke commit `34085c15` (state `true->false` only); UUID single `faa5dc1c-...`; single root `workspace/d7_e_cross_layer_discriminator_faa5dc1c-...`; workspace glob yields exactly that one root; `git grep` for the UUID hits only the authorization record (no second UUID/root); operator invocation count 1, no retry/rerun/resume/replacement-root claimed and no contrary evidence (manifest `retries/reruns/resumes 0/0/0`, summary same, single pid `703389`, single start/end `2026-09-11T11:42:02Z/11:43:07Z`); stdout 1 line/119 bytes terminal line present in `/tmp/d7e_exec_*.log`, stderr empty; outer wall ~73–89 s << 1800+30, no exit-124 evidence. Numeric exit code was NOT directly observed (`wait` unavailable after setsid detach; see exit-code assessment). Invocation uniqueness itself is proven; exit-0 inference is disclosed and sufficient at Pre-RESULT (non-blocking). | PASS (with noted inference; not an independent blocker) |
| R02 | Immutable command, UUID, model root, matrix/order, decoder config, estimator identity | Outer argv `timeout -k 30 1800 .venv/bin/python scripts/v72p2d7_gf32_cross_layer_discriminator.py --model-f-root workspace/v72p2d5_model_f_input/20260907_r1 --out-root workspace/d7_e_cross_layer_discriminator_<uuid>` matches the frozen A1/A2 spelling (`.venv/bin/python` form; byte-identical per A1 review); UUID fresh/single (see R01); manifest `model_f_root workspace/v72p2d5_model_f_input/20260907_r1`, files `model_f_input.npz 208467` + `model_f_input_summary.json 752`; matrix `f [1.0,1.2]`, seeds `2026091300..2026091315` (16/f), directions `[L1_TO_L2,L2_TO_L1]`, rows L1 49/59 L2 43/52, `n 64`, graph seeds L1 `2026090501` L2 `2026090502`, mothers `build_dv3_nested_support+assign_gf32_coefficients` per manifest; decoder `v35.decode_row_layered_fftqspa(..., max_iter=90, damping_alpha=1.0, warm_beliefs=None, field=None)` for SOURCE+TARGET, manifest `max_iter 90` `damping_alpha 1.0`; estimator `v72p2d5_gf32_rate_mother.prepare_model_f_prior_candidate(counts_ab, p_b)` (per-column concentration); module grep shows zero `ru_maxrss`, zero `import resource`, zero legacy `build_f_model(` (per-cell path absent from records/code path taken). Manifest/command_log store the inner argv (without the outer `timeout/.venv` wrapper) by writer design — outer proof rests on operator argv + stdout log (see discrepancy note). | PASS |
| R03 | Exact root schema, no subdirectories | Root exists; sorted files exactly the frozen seven (`manifest.json`, `decoder_records.csv`, `transfer_pairs.csv`, `stratum_summary.csv`, `summary.json`, `report.md`, `command_log.txt`); sizes 3192/47144/9070/814/982/392/290 all match operator inspection; `mtime_ns` all match exactly (diff 0 on all seven); `find -mindepth 1 -type d` empty (0 subdirs). | PASS |
| R04 | 192-slot schedule identity, 128 mandatory, at most 64 transfer | `slots_scheduled 192`, `slot_idx 1..192` complete/unique/sorted (`FULL_1_192 True`, `SLOT_UNIQUE True`); full 192-row order check against frozen enumeration (f outer, seeds ascending, 6-per-group `SOURCE/CONTROL/TRANSFER x L1_TO_L2/L2_TO_L1` with conditions `L1_MARGINAL/L2_MARGINAL/L2_TRANSFER/L2_MARGINAL/L1_MARGINAL/L1_TRANSFER`, layers/rows `49/43` for f=1.0 and `59/52` for f=1.2, `n 64`) passes (`ORDER_OK True`); roles SOURCE/CONTROL/TRANSFER 64/64/64; `mandatory_completed 128`; `transfer_invoked 64` `transfer_blocked 0` (≤64). Note: `transfer_eligible True:64` marks SOURCE rows (per code `only source rows carry transfer_eligible`), not TRANSFER rows as the operator parenthetical states — counts correct, attribution slipped (non-blocking; see discrepancies). | PASS (with doc-slip note) |
| R05 | No missing/duplicate/retry/resume/make-up | Missing 0, duplicate 0 (192 unique slots 1..192); `calls_attempted 192` `calls_completed 192` `calls_remaining 0`; manifest+summary `retries/reruns/resumes 0/0/0`; no second UUID/root; blocked 0 so no make-up surface. | PASS |
| R06 | Provenance gate (transfer only for exact CHECK_UPDATED + finite/non-crash/valid-shape) | `belief_provenance CHECK_UPDATED:192`, `PRIOR_ONLY:0`, `WARM_START_UNSPECIFIED:0`; `belief_shape_ok True:192`; `finite True:192`; crash 0 (no `crash:` status); each of 64 pairs maps to source `transfer_slot_idx-2` with SOURCE role + same f/seed/direction, all finite/non-crash/shape-valid/CHECK_UPDATED (`GATE_OK True`); pair `source_provenance CHECK_UPDATED` all 64 and `source_exact` matches the source record. | PASS |
| R07 | Source exactness not a gate | Pair `source_exact False:61 True:3` yet all 64 transfers invoked; stratum 3 has control-exact 0 with transfer-exact 3 (transfer-only 3); eligibility held for source-exact-false majority. Exactness did not gate. | PASS |
| R08 | Corrected per-column estimator; legacy per-cell rejected | Manifest estimator is the corrected `prepare_model_f_prior_candidate` chain; `lambda_star 137.3823795883264`; module contains the A2 VmHWM block only (no `ru_maxrss`/`import resource`); no legacy `build_f_model(` reference in core+runner; records carry no per-cell fields; numeric/static estimator pins covered by the committed implementation review (`test_e02/e03`) which this review does not re-litigate. Code path taken is the corrected one. | PASS |
| R09 | Both transfer equations + direction identity | Code exposes `transfer_prior_l1_to_l2` (`P_transfer(U2\|B)=sum_u1 q1 P(U2\|B,u1)`) and `transfer_prior_l2_to_l1` (`P_transfer(U1\|B)=sum_u2 q2 P(U1|B,u2)`) with `_check_mixer_q` + floor/renorm and direction dispatch raising on unknown; 6-per-group direction identity verified in R04 order check; transient-q comment present. Equation-level numeric proofs remain those of the implementation review; no contrary evidence in records. | PASS |
| R10 | Per-stratum ≥12/16 coverage | All four strata `eligible 16 blocked 0 COVERAGE_OK` (stratum CSV + summary agree); no coverage block; threshold rule satisfied in every stratum. | PASS |
| R11 | Six labels in frozen first-match order | Recomputed from stored counts in first-match order: s1 (16/0/0/0/0) → `NO_TRANSFER_RECOVERY` match; s2 same → `NO_TRANSFER_RECOVERY` match; s3 (16, te=3, ce=0, to=3, co=0) → `AMBIGUOUS_TRANSFER_EFFECT` match (STRONG fails on to<4/te<4, REGRESSION fails, NO_RECOVERY fails on te>1); s4 (16, te=7, ce=3, to=4, co=0, crash/nf 0) → `STRONG_TRANSFER_LIFT` match. Six-label counts STRONG 1 / REGRESSION 0 / NO_RECOVERY 2 / CONTROL 0 / AMBIGUOUS 1 / empty 0 match operator claim. | PASS |
| R12 | Twelve terminals in frozen priority order | Stored `D7_E_L2_TO_L1_TRANSFER_LIFT`, `stop_terminal ""`; manifest `terminal_priority` is the frozen 12 in order; higher priorities excluded: no pre-execution block (run completed), `watchdog_timeouts 0`, `crash 0`/`nonfinite 0`, peak RSS < limit (no resource overrun), 192/192 complete (no incomplete matrix), all `COVERAGE_OK` (no coverage block), no bidirectional lift (f=1.0 has no STRONG; f=1.2 has STRONG only L2→L1), no L1→L2-only lift; position 9 (`...BIDIRECTIONAL, L1_TO_L2, L2_TO_L1(9th), ...`) correctly first-applicable. Stdout terminal line agrees. | PASS |
| R13 | Exact/syndrome/crash/nonfinite isolation | `exact True:16 False:176`; `syndrome_ok True:16 False:176`; per-row `exact==syndrome_ok` on all 192; `status converged_exact:16 converged_no_syndrome:176`; `crash_count 0` `nonfinite_count 0` `finite True:192`; stratum exact/syndrome splits recomputed from pairs match stored (s1/s2 0/0/16-neither; s3 0/3/3-transfer-only/13-neither; s4 3/7/4-transfer-only/3-both/9-neither; syndrome mirrors exact). Columns kept separate throughout. | PASS |
| R14 | Call-count + paired-identity arithmetic | Directions L1_TO_L2/L2_TO_L1 96/96; f=1.0/1.2 96/96; roles 64/64/64; pairs 64; every pair's f/seed/direction matches both its control and transfer records, control role = CONTROL, transfer role = TRANSFER (`PAIR_ID_OK True`); pair `control_exact/transfer_exact` and `control_syndrome_ok/transfer_syndrome_ok` match the corresponding records. | PASS |
| R15 | Wall/timeout/per-call/RSS budgets; VmHWM rule; single-E09 discipline | Stored `stored_wall_s 64.95356635501957 ≤ 1500`; per-row wall sum equals stored (<1e-9); per-call max `0.43789475300582126 < 120`, min `0.0129...`; outer ~73–89 s < 1800+30, no 124 evidence, `watchdog_timeouts 0`; RSS every row `132390912`, `0 < rss < 2147483648`, peak `132390912 < 2147483648`, limit `2147483648`; WSL path is VmHWM-only (zero `ru_maxrss`/`import resource`/`VmRSS`/`statm`/`subprocess`/`psutil` in core+runner; parser strictness covered by T5); manifest limits (`outer 1800`, grace `30`, per-call `120`, stored `1500`) match frozen. Single-E09: authorization record documents exactly one live probe (`.venv/bin/python`, `VmHWM 96688 kB` → `99008512` bytes, `0<...<2GiB`); no second E09 evidenced; reviewer ran no E09/probe. | PASS |
| R16 | Scalar-only persistence; forbidden-payload scan | Record fields 26 and pair fields 24 as listed; `NONSCALAR_VALUES 0` in both CSVs (no value starts with `[`/`{`); `syndrome_ok`/`symbol_errors`/`beliefs_conditioned` present only as scalar booleans/counts; no vector/belief-array/prior-matrix/symbol-vector/syndrome-vector/block-vector/digest field. | PASS |
| R17 | Writer/CSV/JSON/report consistency | Manifest `uuid` matches directory UUID; `model_f_root` is the accepted root; `seven_files` is the frozen seven; `terminal_priority` contains stored terminal; `authorization_consumed true`; `stratum_summary.csv` 4 rows match `summary.json strata` labels 1:1; `report.md` terminal/counts/four stratum lines match; `command_log.txt` inner command + terminal + `calls_completed 192` + stored wall match; `summary` counts vs manifest budgets (192/192, 128/128) consistent. | PASS |
| R18 | Single verify result + root immutability | Immutability: CONFIRMED — all seven sizes and `mtime_ns` match the operator inspection exactly (diff 0), 0 subdirs. Verify: BLOCKED — no recorded frozen `--verify` literal output (`VERIFY_OK` + exit code) exists in `D7_E_OPERATOR_RETURN_R1.md` (72 lines, no verify section) or elsewhere in the cycle directory; the task-budget instruction forbids the reviewer from re-running verify. The reviewer's independent recomputation replicates every verifier invariant (slot order, mandatory 128, eligible-transfer count, uniqueness, no-replacement, pair identity, provenance gate, exact/syndrome isolation, four labels, terminal, wall/RSS, schema) and all pass, so the artifact is verify-consistent — but that does not substitute for the frozen verifier literal. | BLOCKED (immutability passes; recorded verify absent) |
| R19 | Protected-root metadata equality; R1d/G2 absence | Formal R1d `workspace/d6_graph_mother_r1d_*` absent; formal G2 `workspace/v72p2d5_g2` and `.../20260906_r1` absent; accepted Model-F/D7-C/D7-B/D7-D roots exist as directories; scoped `git diff --numstat` and `--cached --numstat` for the three implementation files + state file are empty; scoped status shows only the untracked operator return. Exact pre/post `mtime_ns` equality cannot be byte-closed because the E06 table is dangling (authorization record defers to the operator return, which contains no protected-root values) — non-blocking record-keeping note; no evidence of protected-root change. | PASS (with dangling-table note) |
| R20 | All authorization flags false; no acceptance/promotion mutation | `cycle_state.yaml`: `plan_accepted/implementation_authorized/d7e_execution_authorized/decoder_executed/result_created/formal_execution_authorized/synthetic_execution_authorized/real_execution_authorized/scientific_promotion/g1_authorized/g2_authorized/d7e_result_accepted` all false (`ALL_FALSE` holds); `d7e_execution_attempts/completed 0/0`; `d7e_terminal D7_E_NOT_EXECUTED`; `d7e_pre_result_review PENDING`; `d7e_accepted_scope NO_SCOPE_ACCEPTED`; `next_gate/state D7_E_WSL_RSS_READY_AWAITING_FRESH_EXPLICIT_AUTHORIZATION`. Auth/revoke commits touch only the single key `false->true->false`. No push. | PASS |
| R21 | Supported claims limited to internal consistency + frozen diagnostic terminal | Operator return `No scientific interpretation` section records counts/labels/terminal/resources only and states Pre-RESULT review is still required before any acceptance. No FER/leakage/key/qualification/promotion/G2/deployment claim. | PASS |
| R22 | Unsupported claims explicitly excluded | Same section explicitly excludes FER, leakage, key-rate, qualification, promotion, G2-permission, deployment; prereg §9 boundaries (no FER/leakage/efficiency/key-rate/CAL/real/R1d/G1/G2/general GF32 claim; D7-C ceiling stays counterfactual) preserved. General NB-LDPC success/failure and automatic cross-layer deployment are not claimed. | PASS |

## Exit-code assessment (R01 explicit judgment)

The operator could not capture the numeric exit code directly (`wait` unavailable after setsid detach; wait attempt exit 127) and recorded the exit as inferred-0 from completion evidence (192/192 rows, terminal line, root present, wall≪timeout, verify claim), without retrying for any outcome.

Judgment: for Pre-RESULT consistency purposes this inference is SUFFICIENT and is NOT a FAIL/BLOCKED gap, with one stated dependence. Reasoning: (a) the failure modes that would produce a nonzero exit (timeout 124, watchdog/resource/incomplete terminals, crash/nonfinite) all leave positive traces — none are present (outer wall ~73–89 s vs 1800+30 with no timeout message; `watchdog_timeouts 0`; `crash/nonfinite 0`; 192/192 complete; stored terminal is the mechanism terminal at priority 9, not a failure terminal; root sizes/mtimes immutable and internally consistent); (b) the limitation was disclosed transparently rather than fabricating an observed 0; (c) single-invocation uniqueness (the core of R01) is independently proven by single UUID/root/pid/start-end and zero retry markers. Dependence: the corroborating frozen-verify literal is itself unrecorded (R18 blocker), so the exit inference currently rests on completion evidence plus this review's verify-consistent recomputation rather than on a recorded `VERIFY_OK exit 0`. Closing R18 closes that dependence. No rerun is warranted for the exit-code question alone.

## Discrepancies (blocking vs non-blocking)

Blocking:

- R18 — no recorded frozen `--verify` literal output. `D7_E_OPERATOR_RETURN_R1.md` contains invocation, lifecycle, arithmetic, provenance, payload-scan, and no-interpretation sections but no STEP 6 verify section (no literal output, no exit code, no before/after name/size/mtime table). A repo-wide `VERIFY_OK` search finds only test/source mentions, not a D7-E R1 verify record. Root immutability is confirmed (all seven size/mtime_ns exact, 0 subdirs), and every verifier invariant recomputes cleanly, so this is a procedural evidence gap, not an artifact-falsification finding. Preserve the root; do not solidify; do not repair; do not rerun the science. Main thread options: (i) govern an exception accepting this independent recomputation, or (ii) capture exactly one frozen read-only `--verify` with literal output + exit + before/after metadata and then re-review R18 only (no second scientific invocation).

Non-blocking (correct before any solidification, no rework of the science):

- Operator return line 57 parenthetical inverts `transfer_eligible` attribution: it reads `transfer_eligible True:64 (TRANSFER rows), empty:128 (SOURCE/CONTROL rows)`, but the data and the frozen verifier rule (`only source rows carry transfer_eligible`) show `('SOURCE', True):64, ('CONTROL', ''):64, ('TRANSFER', ''):64`. Counts are correct; only the row-type words are swapped. Fix the words, not the numbers.
- E06 reference dangles: the authorization record says protected-root `mtime_ns` values are in the operator return (`see operator return for values`), but the operator return carries no protected-root table. Existence/absence (R1d/G2 absent; Model-F/D7-C/D7-B/D7-D present) and scoped cleanliness are confirmed; byte-level pre/post equality therefore rests on prohibition + clean-scope evidence rather than on a transcribed table. File the table or drop the cross-reference at the next milestone; do not gate the science on it.
- Manifest `command` and `command_log.txt` line 1 store the inner argv (`scripts/... --model-f-root ... --out-root ...`) without the outer `timeout -k 30 1800 .venv/bin/python` wrapper, by writer design (`sys.argv`). Outer-form proof rests on the operator's recorded argv + cwd + stdout/stderr log + manifest pid/start/end. Clarify the inner/outer distinction in one line before solidification; no artifact change.
- Scoped cleanliness holds (implementation/tests/runner/state diffs empty; only the untracked operator return is new). The broad dirty worktree (~1968 `M` porcelain entries with a small content-diff subset) is the known WSL stat/CRLF noise plus pre-existing unrelated content deltas, preserved per packet; it did not touch the D7-E scope.

## Prohibited-action checklist

- Second UUID / replacement root / retry / rerun / resume / blocked-slot make-up: none (single UUID/root; `retries/reruns/resumes 0`; `transfer_blocked 0` so no make-up surface) — True (absent as required).
- Parameter / estimator / matrix / order / decoder / provenance / RSS-rule / label / terminal / schema / threshold change: none — True.
- R1d / `--phase` / formal G1/G2 / CAL / VAL / real/raw / oracle / flooding / forced sweep / warm start / alternating / joint / feedback: none — True.
- Production code / test / OpenSpec / prereg / packet / predecessor-evidence edit: none (scoped diffs empty) — True.
- VOID-root scientific read: none — True.
- Push / reset / checkout / clean / stash / rebase / amend / broad-stage: none (two local commits only; no push) — True.
- Real-decoder/Model-F invocation from tests/probes: none (reviewer ran scalar-only reads; no decoder bind, no Model-F content read, no E09, no verify) — True.
- Turning this review into acceptance: not done (verdict is BLOCKED; no `accepted`/promotion/G1/G2 field set) — True.
- No-push confirmation: no push performed or authorized — True.

## Return delta

- verdict: BLOCKED (see Verdict section above for the full token)
- R01–R22: table above (R01 PASS with inference note; R02–R17 PASS; R18 BLOCKED on missing recorded verify with immutability confirmed; R19–R22 PASS)
- exit-code assessment: inference sufficient at Pre-RESULT, not a FAIL/BLOCKED gap; dependence on R18 stated above
- discrepancies: one blocking (R18 recorded verify absent); three non-blocking doc/clarity slips listed above
- solidification: NO solidification commit (BLOCKED); result root preserved in place; operator return remains uncommitted; state facts unchanged (`d7e_pre_result_review PENDING`, `d7e_result_accepted false`, `next_gate D7_E_WSL_RSS_READY_AWAITING_FRESH_EXPLICIT_AUTHORIZATION`)
- protected roots: R1d/G2 absent confirmed; accepted roots present; all authorization flags false; next gate unchanged
- review doc path: `docs/research_cycles/V72P2D7-GF32-CROSS-LAYER-DISCRIMINATOR/D7_E_PRE_RESULT_REVIEW_R1.md`

(End of review — independent Pre-RESULT R1; the result is not accepted.)
