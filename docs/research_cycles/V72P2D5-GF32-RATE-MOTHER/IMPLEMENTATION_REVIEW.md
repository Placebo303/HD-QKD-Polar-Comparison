# V72P2D5-GF32-RATE-MOTHER Implementation Review — INDEPENDENT VERDICT: PASS

- status: REVIEWED_PASS
- verdict: PASS (independent reviewer-go; T0/T1 only)
- cycle_id: V72P2D5-GF32-RATE-MOTHER
- scope: T0/T1 only. No full-size V31 construction, no real decoder, no CAL/VAL reads, no output files, no execution authorization change.
- date_utc: 2026-09-05
- reviewer: reviewer-go (independent of implementation agent; read-only inspection plus only authorized verification)
- method: read-only inspection of packet, plan verdict, 3 implementation files, cycle_state.yaml and DRAFT review; ran ONLY `python -m py_compile` on the 3 files and `pytest -p no:cacheprovider comparison_bench/tests/test_v72p2d5_gf32_rate_mother.py -v`. Did NOT build 1000x1024 mother via default V31 builder, did NOT invoke real decoder, did NOT execute structure/g0/p0/g1/g2 phases beyond auth-refusal paths, did NOT read CAL/VAL, did NOT create production output/run_01.
- prior_draft: DRAFT_CANDIDATE with `verdict: NOT_CLAIMED` (implementation agent did NOT self-claim PASS; confirmed).

## 1. Files under review (exact closed set)

1. `comparison_bench/src/comparison_bench/formal_ir/v72p2d5_gf32_rate_mother.py` (1011 lines, untracked new file)
2. `scripts/v72p2d5_gf32_rate_mother.py` (84 lines, untracked new file)
3. `comparison_bench/tests/test_v72p2d5_gf32_rate_mother.py` (681 lines, 25 T0 + 18 T1, untracked new file)
4. this file (verdict overwrite only; authorized)
5. `docs/research_cycles/V72P2D5-GF32-RATE-MOTHER/cycle_state.yaml` (`state=IMPLEMENTATION_CANDIDATE`, `implementation_authorized=true` meaning code-writing only; every phase authorization remains false; `decoder_executed=false`; `val_rows_read=0`; `next_gate=INDEPENDENT_IMPLEMENTATION_REVIEW`)

Unrelated dirty worktree preserved and NOT attributed to implementation: `M AGENT_PROJECT_MEMORY.md` + `M docs/decision-log.md` (2026-09-05 memory/decision entries, pre-existing) plus many untracked baseline artifacts (cascade/outputs/openspec deltas). Implementation delta itself respects the packet closed set (see I14).

## 2. Checklist I1-I14 (all PASS; packet section 7 names)

- I1 prior axis and mapping contract holds: PASS. `build_f_model` takes `(Alice,Bob)` counts, adds scalar `lam` cell-wise, normalizes along `axis=0` so `P_F.sum(axis=0)==1`, all-zero column with `lam<=0` falls back to uniform `1/A`; `marginalize_f_to_p1` reshapes `(A//32,32,B).sum(axis=1)` then norms over U1; `conditionalize_f_to_p2` reshapes then norms over U2 with zero-mass slice fallback to `1/32`; `symbols_to_layers`/`layers_to_symbols` enforce `A=32*U1+U2`; production L2 is `q@P` (`app_fed_l2_prior`), oracle is diagnostic-only (`oracle_l2_prior` docstring says diagnostic-only, name contains oracle). Evidence: T0_01/T0_02 asymmetric hand calc + transpose counterexample, T0_03 col-norm, T0_04/T0_05 hand calcs + fallback, T0_06 one-hot recovers oracle, T0_07 mixture hand calc, T0_08 signature rejects alice/u1_true (3 params), T0_10 roundtrip.
- I2 `lambda*` smoothing placement holds: PASS. `LAMBDA_STAR=137.3823795883264` frozen constant, default `lam` in `build_f_model`; no log-before-norm; no `q@P` on unnormalized table. Evidence: T0_03 uses default smoothing on 1024x1024 random counts and checks col-norm; T0_12 asserts exact constant.
- I3 dual-floor and uniform-fallback semantics holds: PASS. `AUDIT_FLOOR=1e-300` used only in `_ce_stats` CE path (`maximum(p,1e-300)` then `log2`), `DECODER_FLOOR=1e-15` used in `_floor_renorm` for `app_fed`/`oracle` priors with renormalization; zero-mass `(U1,B)` slices fall back to uniform, no sample deletion. Evidence: T0_09 asserts both constants, checks renorm sums to 1, finiteness, `min>=5e-16`, and audit-floor log2 finiteness.
- I4 row-count table recomputation holds: PASS. `_rows_required=ceil(width*ce*f/5.0)`; `CE_L1_MEAN=3.814742`, `CE_L2_ORACLE_MEAN=3.347605`, `CE_JOINT_MEAN=7.162347`; `G1_WIDTH=64`, `G2_WIDTH=256`, `P0_WIDTH=64`; `G1_F=(1.0,1.2)`, `G2_F=(1.0,1.1,1.2)`, `P0_F=(1.0,1.2)`. Recomputed: G1 `49/43`, G2 `235/206` match frozen tables. Evidence: T0_11 8-case ceil check (L1 782/821/860/938, L2 686/720/755/823); T1_08 asserts `frozen_rows 1.0={m1:49,m2:43}` and `1.2={m1:235,m2:206}`.
- I5 mother parameters and graph seeds hold: PASS. `N=1024`, `M_MAX=1000`, `L1_GRAPH_SEED=2026090501`, `L2_GRAPH_SEED=2026090502`, `V31_FAMILY=QC-cyclic-projective`; `build_candidate_mother(n,m_max,seed,build_layer_fn)` is thin: with injected builder calls `build_layer_fn(m_max,n,seed)` once and validates shape `(m_max,n)` + values `0..31`; without it forwards to frozen V31 `build_layer(m_max,n,family=FAMILY_QC,field=FIELD,require_full_rank=True)` with coefficient replay, wrapped in clear `RuntimeError` on failure; single family, no seed search, no fallback family, no per-k independent matrix. Evidence: T0_12 asserts N/M/seeds/prefixes/G0/G1/G2/lambda; no test invokes default V31 path (all structure tests inject `h` or a throwing builder).
- I6 prefix 13-item report plus minimum PASS gate holds: PASS. `audit_prefix` reports rank, zero_rows, zero_columns, degree min/median/max, degree1/degree2, components, largest fraction, isolated, row-degree histogram, 4-cycles (`sum_C(shared,2)`), duplicate/projective-equivalent columns (zero-pattern grouping + GF32 ratio check; all-zero pairs count), coefficients_nonzero, plus `passed`; `audit_frozen_prefixes` checks every frozen k in order. `passed` implements packet 8-check gate: `rank==k`, `zero_rows==0`, `zero_columns==0`, `isolated==0`, `components==1`, `largest==1.0`, `duplicate==0`, `vals_ok` (no relaxation, no tightening). Evidence: T0_13 rank hand calcs, T0_14 zero row/col, T0_15 components, T0_16 4-cycle (1 vs 0), T0_17 projective duplicate (1 vs 0), T0_18 structural pass/fail.
- I7 row-ordering determinism and no-search holds: PASS. `order_rows_for_prefix_coverage` returns one permutation, no row/coefficient change, every row exactly once; greedy priority `(new_vars, rank_inc, conn, -r)` with max (smallest index wins ties); recomputes GF32 rank on small selected set directly; touches only support/coverage/components, never decoder/data/randomness; identity returned when natural order already greedy-optimal. At-most-once-per-layer is procedural (caller discipline), correctly not hard-coded as global state. Evidence: T0_19 completeness, T0_20 content unchanged (sorted rows equal), T0_21 determinism, T0_22 natural-pass identity on diag.
- I8 matched-generator channel and injection-only holds: PASS. `sample_matched_block(p_b,p_f,n,seed)` requires normalized finite nonnegative tables (`p_b` sums to 1, every `p_f` column sums to 1 within 1e-8), uses fixed `default_rng(seed)` for exact reproducibility, samples `B~P(B)` then `A~P_F(.|B)`, returns `bob/alice/u1/u2` with `A=32*U1+U2`; no AWGN/BSC/QSC, no file read, no per-symbol array persistence (returns in-memory dict only; passing probability table allowed per packet). Evidence: T0_23 determinism + mapping, T0_24 empirical freq tolerance (4000-block, `|mean-0.5|<0.05`, conditional exact), T0_25/T1_12 no loader interface.
- I9 `decode_fn` default None and no-fallback holds: PASS. All of `run_g0/p0-cost/g1/g2` declare `decode_fn=None`, call `_require_decode_fn` first (after `_require_authorized`): `None` refuses with `ValueError`, non-callable refuses; no import of production decoder as fallback; `run_structure_phase` takes no `decode_fn` and reports `decoder_calls=0`. Evidence: T1_01 None-refusal for g0/g2; T1_03 unauthorized refuses before decode (`fake.calls==[]`); T1_04/T1_08 exercise only injected `FakeDecoder`.
- I10 CLI explicit-phase and banned-flag holds: PASS. `build_parser` requires `--phase` with `choices=sorted(structure,g0,p0-cost,g1,g2)`; missing `--phase` exits nonzero; `--phase all` rejected by choices; no `--phase all`, autochain, retry, seed change, threshold tuning, `--execute-real`, VAL path, or `run_01` flags/strings; default performs no execution (missing phase prints help and exits); single auth choke point `is_phase_authorized` in core module (CLI calls `_mod.is_phase_authorized`, defines no duplicate logic); CLI reads only authorization state file, no data tables, creates no output; authorized path forwards `authorized=True` and maps exceptions to exit 3 refusal. Evidence: T1_13 missing-phase refuses, T1_14 illegal-phase refuses + help lacks `run_01`/`--real` + structure unauthorized refuses (exit nonzero).
- I11 T0 passes without decoder: PASS. 25/25 green, ~2s, no `decode_fn` passed in any T0 test, no decoder-call tracking in T0. Evidence: pytest `test_T0_01`..`test_T0_25` all PASSED (see section 4).
- I12 T1 passes with fake decoder only: PASS. 18/18 green with injected `FakeDecoder` only (returns zeros, `syndrome_ok=False`, `iterations=1`); call counts `G0=8, P0=12, G1=440, G2=1320` match frozen block/rate/oracle-subset design (L1+L2 two calls per APP-fed block, one per oracle block); with all-zero fake every phase reports failure grades (G2 grade `G2_CURRENT_CONFIGURATION_FAILED`, no threshold tuned to make fakes pass); stage isolation verified via monkeypatch (structure failure never enters g0; g0 never enters p0/g1/g2; g1 never enters g2); no semantic fakes (no hand-crafted passing beliefs). Evidence: T1_01..T1_18 all PASSED; T1_04 exact counts; T1_05/T1_06/T1_07 isolation; T1_08 grade check.
- I13 output roots / four-file / banned-artifact rule holds: PASS. Core + CLI contain none of `write_text/write_bytes/np.save/npz/to_csv/to_parquet/pickle/mkdir` and no `open(...w` write; no `out_dir/path/run_id/output/save` params on any phase function; `run_structure/g0/p0/g1/g2` return in-memory dicts only; future roots `workspace/v72p2d5_*/<run_id>/` with exactly four scalar files are declared but NOT created this round; banned artifacts (complete mother, raw counts, prior tables, syndromes, BP messages, any hash) never stored (mother rebuilt in memory from seed per docstring). Verified no files created: `tmp_path.rglob(*)==[]` after running all phases in T1_09/T1_16, `workspace/v72p2d5_*` glob empty, `comparison_bench/outputs_comparison/v72p2d5_*` empty, `run_01` absent. This review also created no workspace output and no `run_01` (checked `Test-Path .../run_01 == False`).
- I14 plan-unchanged / gate-separation / no-auto-authorization holds: PASS. `git status --porcelain -- openspec/changes/formal-ir-v72p2d5-gf32-rate-mother-plan docs/research_cycles/V72P2D5-GF32-RATE-MOTHER/IMPLEMENTATION_PACKET.md docs/research_cycles/V72P2D5-GF32-RATE-MOTHER/PLAN_REVIEW_VERDICT.md src experiments tools results` is empty; `git status` for `nonbinary_v31/v35/v54` is empty; `cycle_state.yaml` keeps `structure/g0/p0_cost/g1/g2/synthetic/real/formal_execution_authorized=false`, `decoder_executed=false`, `val_rows_read=0`, `scientific_promotion=false`, `state=IMPLEMENTATION_CANDIDATE`, `next_gate=INDEPENDENT_IMPLEMENTATION_REVIEW`; passing this review does NOT auto-authorize structure (all structure/g0/g1/g2 remain false here and in cycle_state). Additive `p0_cost_execution_authorized=false` key (absent in pre-packet state) is required by core `_PHASE_AUTH_KEYS` and test T1_15; it is `false`, not an authorization. Implementation agent made no plan edit to suit code (no threshold/seed/row-count/model/gate change, no hash/checksum/tag added).

## 3. Extra required checks

- OpenSpec five files diff=0: CONFIRMED. `proposal.md/design.md/tasks.md/specs/spec.md/PLAN_FREEZE.md` under `openspec/changes/formal-ir-v72p2d5-gf32-rate-mother-plan/` show zero `git status`/`git diff` entries.
- PLAN_REVIEW_VERDICT/IMPLEMENTATION_PACKET diff=0: CONFIRMED. Both files show zero status/diff entries.
- V31/V35/V54 diff=0: CONFIRMED for `nonbinary_v31.py`/`nonbinary_v35.py`/`nonbinary_v54.py` (empty status); broader `git status` shows no other tracked `comparison_bench/src` modification attributable to implementation (only untracked new D5 files).
- Only allowed 3 .py + 2 cycle docs changed (implementation delta): CONFIRMED with worktree note. Implementation delta = 3 untracked new `.py` (core, CLI, test) + `M cycle_state.yaml` + untracked `IMPLEMENTATION_REVIEW.md` (this file). `M AGENT_PROJECT_MEMORY.md` + `M docs/decision-log.md` and dozens of other `??` baseline artifacts are pre-existing unrelated dirty (memory/decision entries dated 2026-09-05, cascade/outputs/openspec deltas) preserved untouched per delegation workflow; they are NOT implementation edits and must NOT be attributed to D5.
- No production decoder import (AST/grep): CONFIRMED. Case-insensitive scan of core + CLI finds none of `v35/production_decoder/fftqspa/decode_row_layered`; AST import walk finds no `v35/fftqspa/production_decoder` module; `_block_import_smoke` (T1_11) re-imports both files with those names blocked and still succeeds. Default V31 constructor reference (`nonbinary_v31.build_layer`) is a graph builder, not a decoder, and is never exercised by T0/T1 (all structure tests inject `h`).
- Tests never invoke default builder / never run real decoder: CONFIRMED. No test calls `build_candidate_mother` without `build_layer_fn`; `builder` in T1_02 is a throwing stub verified never called when unauthorized (`calls==[]`); no `build_layer`/`require_full_rank` invocation in tests; all decoder interactions go through injected `FakeDecoder`.
- Tests never read CAL/VAL: CONFIRMED. No `parquet/np.load/loadtxt/genfromtxt/read_bytes/pd.read/csv.reader`, `CORE open(==0`, `CLI open(==1` (only `cycle_state.yaml` via `_load_state`), no `path/file/csv/parquet/dir/root/output` params on phase functions; no `read_CAL/read_VAL/load_*` symbols (T0_25/T1_12).
- Tests create no D5 workspace output: CONFIRMED. `tmp_path` empty after full phase run, `workspace/v72p2d5_*` empty, `outputs_comparison/v72p2d5_*` empty, `run_01` absent (T1_09/T1_16 + reviewer `Get-ChildItem`/`Test-Path` re-check).
- All execution authorizations false: CONFIRMED. `cycle_state.yaml` phase keys all `false` (see section 1); `T1_15` re-loads state via `cli._load_state` and asserts each `is_phase_authorized==False`.
- Implementation agent did not self-claim PASS: CONFIRMED. Prior DRAFT header was `status: DRAFT_CANDIDATE`, `verdict: NOT_CLAIMED` with explicit neither-claimed-nor-implied note.

## 4. Test evidence (reviewer re-run, this turn)

- `python -m py_compile` on the three Python files: PASS (exit 0).
- `pytest -p no:cacheprovider comparison_bench/tests/test_v72p2d5_gf32_rate_mother.py -v`: 43 passed (T0 25/25, T1 18/18) in ~2.2s, 1 benign warning (`Unknown config option: cache_dir`). No decoder calls in T0, fake decoder only in T1, no files created, no data reads.
- Operator-reported meter fix (P0/G1/G2 `decoder_calls` now counts actual `decode_fn` invocations: 2 per APP-fed L1+L2 block, 1 per oracle block) matches reviewer-observed T1_04 counts (8/12/440/1320); no plan change involved.

## 5. Non-blocking observations (do NOT block PASS; carry to structure gate)

1. Tests touch full dimensions with stub fixtures (strict `no 1000x1024` note): `_struct_h()` builds a synthetic `1000x1024` weight-2 pattern and `T1_05` uses `zeros((1000,1024))` solely to exercise `audit_frozen_prefixes` field completeness and failure grading without any V31 builder, decoder, or output. This is NOT a real mother build and does NOT violate the no-default-builder / no-real-decoder / no-output rules, but a literal `no 1000x1024` reading would flag it. Future tests should prefer small-`h` field checks plus one explicitly labeled full-shape audit smoke if needed; no change required this round.
2. Operator structural note (reviewer concurs, no requirement change): weight-2 rows on a 1000x1024 bipartite graph cannot reach a single connected component (2024 nodes need >=2023 edges; 2000 available), so the minimum-PASS `components==1` + `largest==1.0` items deserve explicit attention at the structure gate. Execution will report the measured value; `V31_PREFIX_FAMILY_UNSUITABLE` + return-to-planner remains the frozen path if still FAIL after the single pre-registered reordering. No plan constant is questioned here.
3. `cycle_state.yaml` additive key: `p0_cost_execution_authorized: false` did not exist in the pre-packet state but is required by the frozen CLI phase set (`structure|g0|p0-cost|g1|g2`) and asserted by T1_15. Value is `false`; no authorization granted. No action.
4. `HEAD (bd1115a1) != origin/main (ed0adfc)` on branch `formal-ir-v72p1-addendum-clean` with implementation files untracked/uncommitted: expected for an uncommitted implementation candidate. No commit/push performed by reviewer (see section 6). Pre-EXECUTE gate must still verify `HEAD==origin/<branch>==implementation SHA` on the exact implementation SHA before any formal execution; that gate is NOT passed here.
5. AGENTS.md research-code policy compliance: no SHA-256/MD5/checksums/signatures, atomic writes, locks, retry frameworks, or excessive hardening added; T1_17 explicitly asserts no `hashlib/sha/md5/checksum/hmac` in code or result keys. Compliant.

## 6. State, gates, and prohibitions

- `cycle_state.yaml` REMAINS `state=IMPLEMENTATION_CANDIDATE`, `next_gate=INDEPENDENT_IMPLEMENTATION_REVIEW` (reviewer did NOT flip it, did NOT freeze/execute any structure packet).
- `STRUCTURE_AUTHORIZED=false`, `G0_AUTHORIZED=false`, `G1_AUTHORIZED=false`, `G2_AUTHORIZED=false`, `REAL_AUTHORIZED=false` (all phase authorizations remain false; `implementation_authorized=true` is code-writing only).
- No structure/G0/G1/G2/real/formal execution authorized by this review. Next gate on PASS is drafting a separate `STRUCTURE_EXECUTION_PACKET` (new authorization required); on FAIL it would be `revise-required`.
- `IMPLEMENTATION_COMMIT: NOT_COMMITTED` — reviewer did not commit or push. `REMOTE: origin=https://github.com/Placebo303/HD-QKD-Polar-Comparison.git (fetch/push), legacy-origin read-only; branch=formal-ir-v72p1-addendum-clean; HEAD=bd1115a1 != origin/main=ed0adfc (uncommitted candidate, expected)`.
- Blocking issues: NONE. Non-blocking suggestions: see section 5 only.
- Checklist: Matches OpenSpec spec [x]; Tests pass [x]; No scope creep [x]; docs/decision-log.md or docs/troubleshooting.md needs update? NO (no durable decision or reusable failure mode introduced by this T0/T1 review; operator connectivity note is a gate-watch item, not a log entry).

## 7. Verdict

PASS Gate A (IMPLEMENTATION_REVIEW, T0/T1 + I1-I14). Candidate may proceed to structure-packet drafting; structure execution remains UNAUTHORIZED pending its own packet + Pre-EXECUTE review on the exact implementation SHA.