# G7-STOP memo correction (append-only; original memo NOT edited)

The G7-STOP memo (`docs/research_cycles/V72P3G7-ROUTE/ROUTE_MEMO.md`) is
SUPERSEDED on its central arithmetic. The original memo file is NOT edited
(append-only discipline — the correction lives here).

## 1. Withdrawn bound

- The k-based `m < 57.6` impossibility is WITHDRAWN — it double-charged parity
  (subtracted parity once via k and again via disclosure).

## 2. Corrected picture

- Per-accept economics: S0 +76 / S1 +36 / S2 −24; exhausted frame −664.
- The route battle is exhausted fraction + stage distribution, NOT rate
  impossibility.
- Positive net needs accept-heavy low-stage profiles, e.g. all-S0 → +9728/128.

## 3. Revised verdict

- Route ALIVE as efficiency battle (STOP-positive-net downgraded to
  contentended — i.e. contested, no longer a stop on impossibility grounds).

## 4. Escapes restated

- Quieter data still helps (fewer exhausted frames, more low-stage accepts).
- The 2×-code bar is restated under correct accounting (halved disclosure
  against the n-based net, not against the double-charged k-based net).

## 5. Unchanged

- SKR still blocked until net-positive demonstrated (unchanged).
