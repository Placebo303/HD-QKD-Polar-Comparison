# D7-F Pre-RESULT review R1 (independent)

- Authority: `.workbuddy/tasks/D7_F_REVERSE_ORDER_EXECUTE_PRE_RESULT_R1_TASK_PACKET.md` §10 (STEP 7, R01–R22).
- Reviewer: independent D7-F Pre-RESULT reviewer (separate context from execution operator).
- Scope: review-only. Sole write is this document. No commits, no edits (beyond this file), no decoder/Model-F content reads, no roots/UUID generation, no verify re-run (already spent once — confirmed from persisted transcript, never re-executed).
- Branch: `formal-ir-v72p1-addendum-clean`; HEAD at review: `851efb92` (`chore(d7-f): consume and revoke one-shot execution authorization`).
- Ordered ancestry: `f4c06042` → `aca2b6b` → `7785366f` → `ffe94307` → `a099b257` → `851efb92` (verified via `git log --oneline`, `merge-base --is-ancestor`).
- Frozen refs: `D7_F_PREREG_R1.md`, `D7_F_EXECUTION_PACKET_R1.md`, both D7-F reviews (implementation PASS, Pre-EXECUTE PASS), `D7_F_AUTHORIZATION_RECORD_R1.md` (committed at `a099b257`), `D7_F_OPERATOR_RETURN_R1.md` (uncommitted — treated as claim, never as evidence), result root `workspace/d7_f_reverse_order_discriminator_b6d62184-fd15-483d-947e-01ea66ddc13c/` (seven files, read-only scalar checks only), `cycle_state.yaml`.
- Verdict token: `D7_F_PRE_RESULT_REVIEW_PASS_R1`
- Verify policy: transcript confirmed present INSIDE the operator return (literal command/timestamps/exit/stdout/stderr) and checked for consistency with the root; verify was NOT re-run by this reviewer (no decoder, no loader, no root mutation; current mtimes still at `22:57:26` prove no re-execution side effect).

## Method

All R01–R22 recomputed from scalar artifacts + frozen contract only:

- `manifest.json`, `summary.json`, `stratum_summary.csv`, `command_log.txt`, `report.md` (full read).
- `decoder_records.csv` (128 rows + header): slot contiguity/uniqueness, f/arm/role/condition/layer/rows order, provenance/finite/shape/eligibility, exact/syndrome/status, wall sum/max, RSS max.
- `arm_pairs.csv` (64 rows + header): pair contiguity, slot linkage `target=source+1`, per-arm layer identity, provenance, l1/l2 exact/syndrome, `both_layers_exact` AND recomputation, per-(f,seed) 2×2 join.
- `command_log.txt` / `report.md` / `manifest.command` / `summary.terminal` cross-consistency.
- `stat` sizes/mtimes + `find -mindepth 1 -type d | wc -l` + `wc -c`/`wc -l` + `grep` scalar scans (CSV brackets 0, `array(`/`dtype` 0).
- `git log`/`git ls-files`/`git diff`/`git status` for lifecycle, single UUID/root, protected roots (stat only, never contents), auth flags, no acceptance/promotion.
- Operator return used only as claim list; every numeric claim re-derived above. Main-run DIRECT exit/wall/stdout are operator-measured (no literal main transcript required by frozen schema); corroborated by complete root + manifest duration (see R01/R17 notes). Verifier transcript (R19) IS literal in the operator return and is checked byte-for-byte against the root.

## R01–R22 recomputation table

| ID | Frozen rule (§10) | Operator claim | Independently recomputed value | Verdict |
|----|-------------------|----------------|--------------------------------|---------|
| R01 | authorization lifecycle + one invocation | UUID `b6d62184-fd15-483d-947e-01ea66ddc13c`; auth `a099b257`, revoke `851efb92`; one foreground invocation, DIRECT exit 0, 44 s wall, stdout `terminal=D7_F_REVERSE_ORDER_REGRESSION`, stderr empty | UUID matches manifest `uuid`, manifest `out_root_name`, root dirname, auth-record target, operator-return verify command. Auth `a099b257` = `d7f_execution_authorized false→true` + auth record (50 lines, verbatim authorization, exact command with UUID); revoke `851efb92` = `true→false`, separate commit, before contents opened. Exactly one `workspace/d7_f_*` root exists; repo-wide UUID grep hits only this UUID + `<uuid>` placeholders; `manifest.authorization_consumed:true`, `retries/reruns/resumes=0` in manifest+summary. Manifest `start_utc 2026-09-11T14:56:44Z` → `end_utc 14:57:26Z` (42 s) corroborates outer 44 s (wrapper overhead, both ≪1830). Complete 128-row root + `D7_F_REVERSE_ORDER_REGRESSION` terminal corroborates exit-0/success claim; no second root/retry/resume/make-up evidence. Main-run DIRECT exit/stdout/stderr literals are operator-measured (frozen schema stores only `command_log.txt` scalar checkpoints, not full stdio) — consistent, no contradiction. | PASS |
| R02 | immutable command/UUID/scientific identity | frozen command with UUID, `.venv/bin/python`, model-f-root, out-root | Auth-record exact command `timeout -k 30 1800 .venv/bin/python scripts/v72p2d7_gf32_reverse_order_discriminator.py --model-f-root workspace/v72p2d5_model_f_input/20260907_r1 --out-root workspace/d7_f_reverse_order_discriminator_b6d62184-...` byte-matches frozen template (prereg §4 + execution packet) with `<uuid>`→`b6d62184-...` substitution only. Manifest `command` (inner, without timeout prefix) + `command_log.txt:1` identical: `scripts/... --model-f-root workspace/v72p2d5_model_f_input/20260907_r1 --out-root workspace/d7_f_...`. Manifest identity: `f_values [1.0,1.2]`, `block_seeds 2026091300..1315 (16)`, `l1_rows {1.0:49,1.2:59}`, `l2_rows {1.0:43,1.2:52}`, `n 64`, `graph_seeds L1 2026090501/L2 2026090502`, `mothers build_dv3_nested_support(64,64,49,2026090501)+assign... / (64,64,43,2026090502)+assign...`, `max_iter 90`, `damping 1.0`, `decoder_floor 1e-15`, `lambda_star 137.3823795883264`, `arms [FORWARD_L1_TO_L2,REVERSE_L2_TO_L1]`, `transfer_directions [L1_TO_L2,L2_TO_L1]`, `call_order for f in [1.0,1.2]: for seed ...: FWD_SRC,FWD_TGT,REV_SRC,REV_TGT`, budgets `max 128/mandatory 64/per-call 120/stored 1500/outer 1800+30/rss 2147483648`, `contract R1`, `change v72p2d7-reverse-order...`, `decoder_ids v35.decode_row_layered_fftqspa(...,max_iter=90,damping_alpha=1.0,warm_beliefs=None,...)` both keys. All match prereg B01/execution packet. | PASS |
| R03 | seven-file schema / no subdirs | seven files, sizes 3151/32429+129/8331+65/396+3/648/266/295, no subdirs | `ls` = exactly 7 names; `find -mindepth 1 -type d \| wc -l`=0. `wc -c`: 3151/32429/8331/396/648/266/295 — all match operator claim byte-for-byte. `wc -l`: decoder 129 (header+128), arm 65 (header+64), stratum 3 (header+2), command_log 4, report 9 — match. Manifest `seven_files` 7-tuple in frozen order; matches `ls`. No extra files, no subdirectories. | PASS |
| R04 | exact 32 identities / 128 slots / order | 128 slots frozen order, 32 (f,seed) identities | Decoder `slot_idx` = 1..128 contiguous, unique (128/128). Arm/role cycle every 4 = `FWD/SRC,FWD/TGT,REV/SRC,REV/TGT` — 0 violations. `f`: slots 1–64 = `1.0`, 65–128 = `1.2`. Per-f uniq seeds = `2026091300..1315` ascending, each 4× contiguous. Pairs `pair_idx` 1..64 contiguous unique; per (f,seed) exactly 2 arms (FWD then REV), 32 seeds ×2 f = 32 identities, 64 arm-rows. `layer/rows`: `(L1,49)×32,(L2,43)×32,(L1,59)×32,(L2,52)×32`; `condition`: `L1_MARGINAL×32,L2_TRANSFER×32,L2_MARGINAL×32,L1_TRANSFER×32`. Pair `source_slot+1=target_slot` 64/64; `source_layer/target_layer` = L1→L2 (FWD) / L2→L1 (REV) with matching rows. Matches frozen call order. | PASS |
| R05 | no missing/duplicate/retry/resume/make-up | 128/128/128 scheduled/attempted/completed, retries 0 | Summary `slots_scheduled 128/calls_attempted 128/calls_completed 128/calls_remaining 0`; manifest `retries/reruns/resumes 0/0/0`, summary same. Decoder 128 rows, slots unique contiguous, pairs 64 unique contiguous, no gap/dup. `command_log calls_completed:128`, `report calls_completed:128` consistent. No second root, no UUID reuse, no after-stop records. | PASS |
| R06 | exact provenance eligibility + blocked non-calls | all `CHECK_UPDATED`; invoked 64/blocked 0; mandatory 64/64 | Decoder `belief_provenance=CHECK_UPDATED` 128/128; `finite=True` 128/128; `belief_shape_ok=True` 128/128. SOURCE `transfer_eligible=True` 64/64; TARGET `transfer_eligible=''` 64/64 (empty per schema, verified 0 non-empty). Pairs `source_provenance/target_provenance=CHECK_UPDATED` 64/64 both. Summary `mandatory_completed 64/transfer_invoked 64/transfer_blocked 0`; 64+64=128. Gate (finite+non-crash+shape+exact-`CHECK_UPDATED`) satisfied everywhere → 0 blocked is arithmetically required, not a silent sweep. | PASS |
| R07 | source exact NOT a gate | source exact not gating (implied by all-eligible) | 64 sources all eligible despite majority inexact: decoder exact 13/128 total; per-role exact `FWD_SRC 3/REV_SRC 0/FWD_TGT 3/REV_TGT 7`. At f=1.0 all 32 sources inexact yet eligible+invoked (stratum f=1.0 16/16 eligible both arms). Pairs show inexact sources with invoked targets (e.g. f=1.0 pairs `source_exact False` → `target_slot` present). No blocked row attributable to inexactness. Consistent with `check_source_eligibility` having no exact arg (proved in implementation review). | PASS |
| R08 | corrected estimator identity | corrected per-Bob-column Model-F only | Manifest `estimator v72p2d5_gf32_rate_mother.prepare_model_f_prior_candidate(counts_ab, p_b)`, `lambda_star 137.3823795883264` — matches frozen per-Bob-column contract `(counts[:,b]+lambda*p_global)/(n_b[b]+lambda)`; legacy per-cell `build_f_model(counts+lambda)` absent from manifest/CSV/report/log (`grep array(/dtype` 0; `prior_*` hit only this estimator string). Formula-level proof rests with implementation review (`e01/e02/e03`); scalars show no contrary signal (correct rows/n/f/seeds/graph/mothers). | PASS |
| R09 | forward/reverse formulas + layer identity | FWD `P(U2\|B,s1)=sum q1·P(U2\|B,u1)`; REV `P(U1\|B,s2)=sum q2·P(U1\|B,u2)`; layer identity | Layer identity verified: FWD `source L1 (49/59)` → `target L2 (43/52)`; REV `source L2` → `target L1`; `transfer_directions [L1_TO_L2,L2_TO_L1]`, `call_order` string, decoder `condition` 4-way counts, pair `source_layer/target_layer/source_rows/target_rows` all match. Formula text itself is code-level (implementation `e01` direct-enumeration proved); scalars confirm the formula inputs/outputs were routed to the correct layers (no swapped arm, no third direction). | PASS |
| R10 | each syndrome consumed once / no feedback | syndrome-once, no feedback, cold target | Slot uniqueness (128 distinct) + pair linkage `+1` + no third stage + sequential `slot_idx` order prove each target slot consumed exactly its own syndrome once. Row asymmetry (49 vs 43, 59 vs 52) means within-arm source/target syndromes differ in shape and cannot be replayed. `retries/reruns/resumes 0`, no after-stop calls, no `warm_beliefs`/feedback fields in CSVs (scalar scan clean). Behavioral proof in implementation review (`s01/s02`); scalars show no extra/reused call. >2-stage alternating absent (only 2 stages/arm). | PASS |
| R11 | L1/L2 exact + syndrome isolation | `converged_exact×13/converged_no_syndrome×115`; exact/syndrome separate; `undetected` never merged | Decoder statuses `converged_exact 13/converged_no_syndrome 115` — recomputed via `Counter`, matches claim. `exact=True` 13/13 have `symbol_errors 0 + unsatisfied 0 + syndrome_ok True`; `exact=False` 115/115 have `converged_no_syndrome`. Row-wise `exact vs syndrome_ok` mismatch 0 in THIS run, but columns are distinct (`exact,syndrome_ok` in decoder; `l1_exact,l1_syndrome_ok,l2_exact,l2_syndrome_ok` in pairs) — isolation is structural, not coincidental. Pairs preserve both views; `both_layers_exact` derived only from exact (see R12). No syndrome-only row counted as exact. | PASS |
| R12 | both-exact AND semantics | `both=AND` only (2 arms, both forward f=1.2) | Recomputed `both_layers_exact == (l1_exact AND l2_exact)` for all 64 pairs — 0 violations. The 2 `True` rows are `pair 37 (f1.2/1302/FWD)` and `pair 41 (f1.2/1304/FWD)`, each `source_exact True + target_exact True + l1 True + l2 True`. All REV `both` False (even where `REV_TGT L1 True` with inexact L2 source — correctly False). | PASS |
| R13 | per-f paired 2×2 arithmetic | f1.0 0/0/0/16; f1.2 0/2/0/14; forward 2/reverse 0 | Join per (f,seed) FWD vs REV `both`: f1.0 `candidate 0/reference 0/both 0/neither 16` (forward_both 0, reverse_both 0); f1.2 `candidate 0/reference 2/both 0/neither 14` (forward_both seeds `2026091302,2026091304`, reverse_both none). Sums 16/16. Matches `stratum_summary.csv` (`forward_both 0/2, reverse_both 0/0, candidate 0/0, reference 0/2, both 0/0, neither 16/14`) and `summary.json` strata. | PASS |
| R14 | label first-match replay | f1.0 `NO_REVERSE_LIFT`; f1.2 `REVERSE_REGRESSION` | Replay B05 order: (1) `COVERAGE_BLOCKED` iff either arm <12/16 eligible → both f have 16/16 eligible (`COVERAGE_OK`), skip. (2) `REVERSE_REGRESSION` iff `ref>=2 & cand==0` → f1.0 (0,0) no; f1.2 (0,2) YES. (3–4) strong/weak not reached for f1.2; f1.0 falls to (5) `NO_REVERSE_LIFT`. Matches stratum labels byte-for-byte. | PASS |
| R15 | ten-terminal priority replay | `D7_F_REVERSE_ORDER_REGRESSION` (priority 9) | Replay in order: T1 pre-exec (complete root, no block) no; T2 watchdog 0 no; T3 crash 0/nonfinite 0 no; T4 resource (stored 41.28≤1500, RSS 133M<2G, per-call max 0.4735<120) no; T5 incomplete (128/128) no; T6 provenance-coverage (all CHECK, 16/16) no; T7 strong (need cand≥4&ref==0 either f) no; T8 weak (need cand>ref) no; T9 regression (f1.2 regresses) YES → `D7_F_REVERSE_ORDER_REGRESSION`. Manifest priority list matches frozen 10-order. Summary/command_log/report terminals all identical. | PASS |
| R16 | call/work arithmetic | 128 scheduled/attempted/completed, 64 mandatory, 64 invoked/0 blocked | Summary `128/128/128/0 + mandatory 64 + invoked 64/blocked 0`; 64+64=128. Decoder 64 SOURCE + 64 TARGET. `iterations`: `90×115` (non-converged cap) + 13 early (`3,4,5,7×2,8,9,10×2,13,15,31,80`) — consistent with 13 exact. No work hidden in blocked (blocked 0, no wall for non-calls by construction). | PASS |
| R17 | wall/timeout/RSS limits | stored 41.2816, max call 0.4735, peak 133021696, outer 44, all within budgets | Stored `41.2815843069402` == `sum(wall_s)` recomputed (41.28158...); `≤1500` PASS. Per-call max `0.473511...` (recomputed) `≪120` PASS. Peak `133021696` == `max(rss_bytes)` recomputed; `133021696 < 2147483648` (~126.9 MiB) PASS. Manifest budgets `120/1500/1800+30/2GiB` match frozen. Manifest `14:56:44→14:57:26` = 42 s; operator outer 44 s = wrapper-inclusive, corroborating (Δ 2 s overhead), both `≪1830` PASS. `watchdog 0/crash 0/nonfinite 0` consistent. | PASS |
| R18 | scalar-only payload + writer consistency | seven scalar files, no beliefs/vectors, writer consistent | No subdirs; CSV `[/{` grep 0/0/0 all three; `array(`/`dtype` 0 hits all seven. Only `belief_*` hit = column names (`beliefs_conditioned` boolean scalar, `belief_provenance/shape_ok/max/mean/entropy` scalars) + header; only `prior_*` hit = estimator identity string in manifest (not a persisted prior). No persisted beliefs/priors/symbols/syndromes/vectors/digests. Writer: manifest `seven_files` == `ls`; `command` == `command_log:1`; `summary.terminal` == `command_log:2` == `report:3` == `manifest` priority member; sizes/lines match R03; `report` 2 strata lines match `stratum_summary`. | PASS |
| R19 | single verifier transcript/exit + root immutability | verify once exit 0 `VERIFY_OK {ok True, problems [], records 128, terminal REGRESSION}`, root unchanged | Transcript EXISTS inside operator return §Verifier (lines 54–59): literal command `.venv/bin/python scripts/v72p2d7_gf32_reverse_order_discriminator.py --verify workspace/d7_f_...b6d62184-...` (cwd repo root), start local `2026-09-11 22:59:58 +0800`/UTC `14:59:58`, end local `23:00:00`/UTC `15:00:00` (≈2 s, after revoke `22:57:51`), exit `0`, stdout `VERIFY_OK {'ok': True, 'problems': [], 'records': 128, 'terminal': 'D7_F_REVERSE_ORDER_REGRESSION'}` (literal, with `records 128` == decoder rows, `terminal` == summary terminal), stderr `(empty)`. Immutability list (dir + 7 sizes/mtimes) matches CURRENT `stat` byte-for-byte (dir 4096 `22:57:26.380763500`; manifest 3151/...369021200; decoder 32429/...372679900; arm 8331/...374228400; stratum 396/...375766500; summary 648/...378880200; report 266/...379739300; cmdlog 295/...381764000) — still identical at review time, proving no post-verify mutation. Single transcript; no second-verify evidence; reviewer did NOT re-run (no new mtime, no decoder/loader bind). | PASS |
| R20 | protected roots + R1d/G2 absence | protected equality; R1d/G2 absent | `stat` (names/sizes/mtimes only, contents never read): Model-F `4096 2026-09-07 02:07:07`, D7-E `faa5dc1c-... 4096 2026-09-11 19:43:07`, D7-C `94c0ea15-... 4096 2026-09-11 00:33:39`, D7-B `c605d1e6-... 4096 2026-09-10 20:33:38`, D7-D `64660d16-... 4096 2026-09-11 07:30:05` — all equal Pre-EXECUTE §8. `ls workspace/d6_graph_mother_r1d_*` = absent; single `workspace/d7_f_*` root only; no `workspace/d7_f_*r1d*`/`*G2*` D7-F roots. `cycle_state r1d_state R1D_ABSENT_NOT_AUTHORIZED, g1/g2 false`. Old `workspace/*g1*` (d5_g1 etc.) are unrelated predecessors, not D7-F formal G1/G2. D7-F code committed at `7785366f`, `git diff HEAD -- <core/test/script>` empty. | PASS |
| R21 | all auth false + no acceptance/promotion mutation | all auth false, no acceptance | Current `cycle_state.yaml`: `d7f_execution_authorized false/decoder_executed false/result_created false/formal/synthetic/real false/scientific_promotion false/plan_accepted false/implementation_authorized false/g1 false/g2 false`, `attempts 0/completed 0/terminal NONE/pre_result PENDING/result_accepted false/accepted_scope NONE/next_gate D7_F_FROZEN_AWAITING_EXPLICIT_AUTHORIZATION` — all false/zero as required pre-solidification (attempts/completed/decoder/result update only at STEP 8 solidification, not yet). Auth record committed (`git ls-files` includes it, at `a099b257`); operator return uncommitted (`??`, untracked); result root gitignored (`workspace/` per `.gitignore:20`), uncommitted. No acceptance, promotion, or push. | PASS |
| R22 | supported claims limited to synthetic paired discriminator; no FER/leakage/key-rate/qualification/general claim | ceiling as stated | Operator return §Supported/Unsupported correctly limits to: single paired order observation on exact frozen synthetic contract (`f [1.0,1.2]`, seeds `...1300..1315`, corrected per-Bob-column Model-F, row-layered poly-37 cold 90/1.0) with `0 vs 0 (f1.0)` and `reverse 0 vs forward 2 (f1.2)`; explicitly unsupported: FER/leakage/efficiency/key-rate, qualification/promotion, real-data, general GF32/NB-LDPC, alternating-convergence, performance-estimate description. No broader claim in `report.md`/`summary.json`/`command_log.txt` (scalars only). | PASS |

## Exact-value cross-checks (recomputed, not trusted)

- Decoder header (26 cols): `slot_idx,f,seed,arm,role,condition,layer,rows,n,exact,syndrome_ok,iterations,status,finite,symbol_errors,unsatisfied_checks,wall_s,rss_bytes,belief_max_prob,belief_mean_true_p,belief_mean_entropy,beliefs_conditioned,current_belief_label,belief_provenance,belief_shape_ok,transfer_eligible`.
- Arm header (20 cols): `pair_idx,f,seed,arm,source_layer,target_layer,source_rows,target_rows,n,source_slot_idx,target_slot_idx,source_provenance,target_provenance,source_exact,target_exact,l1_exact,l1_syndrome_ok,l2_exact,l2_syndrome_ok,both_layers_exact`.
- Stratum header (17 cols) + 2 rows as in R13.
- Exact breakdown (13): `FWD_SRC L1 ×3 (slots 73,81,101)`, `FWD_TGT L2 ×3 (74,82,90)`, `REV_TGT L1 ×7 (76,84,92,96,104,116,120)`; `REV_SRC` exact 0. Operator breakdown matches.
- Wall sum `41.2815843069402` == stored; max `0.47351114499906544`; RSS max `133021696` == peak.
- Sizes/lines: manifest 3151; decoder 32429/129; arm 8331/65; stratum 396/3; summary 648; report 266; cmdlog 295; subdirs 0.

## Discrepancies

- None blocking. Two non-material observations (not discrepancies):
  1. Outer wall 44 s (operator-measured, wrapper-inclusive) vs manifest inner duration 42 s (`14:56:44→14:57:26Z`). Δ 2 s is expected timeout-wrapper overhead; both ≪1800+30. Corroborating, not contradicting.
  2. Main-run DIRECT exit/stdout/stderr are operator-measured claims; frozen schema persists only scalar checkpoints (`command_log.txt` 4 lines) rather than full stdio. Completeness of the 128-row root + `authorization_consumed:true` + non-void terminal corroborates success; no contrary signal. R19 (verifier) — the only transcript the frozen contract requires literally in the operator return — IS present and byte-consistent.

## Blocking Issues

- None.

## Non-Blocking Suggestions

- None required for PASS. Solidification (STEP 8) remains the main thread's decision; this review grants no acceptance, promotion, or push. Note for solidification: stage exactly the seven immutable files + auth record (if needed) + operator return + this review + factual D7-F state; keep `accepted/qualification/promotion` and all authorizations false; verify manifest + root bytes at commit time; no push.

## Checklist

- [x] Matches OpenSpec spec (prereg B01–B07 + execution packet + proposal/design/tasks/specs; R01–R22 all recomputed PASS)
- [x] Tests pass (not re-run here by design; implementation 41/41 + Pre-EXECUTE 41/41 already reproduced in their reviews; this Pre-RESULT is scalar-artifact review, no decoder/test execution)
- [x] No scope creep (single UUID/root, frozen command/identity/budgets/labels/terminals, no third stage/feedback/R1d/phase/G1/G2/CAL/VAL/real, no code/config change)
- [x] docs/decision-log.md or docs/troubleshooting.md needs update? No — discriminator observation only, no durable decision or new failure mode; solidification/acceptance (if any) is a separate gate.

Verdict: `D7_F_PRE_RESULT_REVIEW_PASS_R1`

(End of file)
