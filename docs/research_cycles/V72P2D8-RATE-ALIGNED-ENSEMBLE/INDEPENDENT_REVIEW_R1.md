# D8 rate-aligned GF32 ensemble feasibility — independent implementation + readiness review (E11) and scoped re-review (E12)

REVIEW_ID: D8-RATE-ALIGNED-ENSEMBLE-E11
EVIDENCE_ACCESS: VERIFIED
VERDICT: PASS_WITH_FINDINGS
REVIEWED_SCOPE: D8 packet; OpenSpec change (proposal/design/tasks/spec); READINESS_R1.md; EXPLORATION_LOG.md; adapter `v72p2d8_rate_aligned_ensemble.py`; runner `scripts/v72p2d8_rate_aligned_ensemble_development.py`; focused tests; reused modules (V26/V14/V9 common/V27 layer_rate_rho/V37 metrics + degree feasibility/v72p2d5 prior chain/v35 check-update convention/nonbinary_field); predecessor D6 docs; Model-F acceptance. Commands: read-only git checks, Fraction-based math re-derivation (56 PASS/0 FAIL), focused pytest rerun, `--verify` refusal path, `--de-sweep` refusal path (rc=2, before channel load/mkdir/call; sweep NOT executed), artifact checks. No repository file modified.
MATH_CHECKS: all identities re-derived exactly and matched — DV3 n64 f1.2 ρ={3:11/16,4:5/16} (Σρ/i=59/192); DV3 n64 f1.0 ρ={3:1/16,4:15/16} (49/192); tiny (8,5,λ={2:1}) ρ={3:3/4,4:1/4} (5/16); tiny (9,6,λ={3:1}) ρ={4:4/9,5:5/9} (2/9); R=1−m/n and width identities (49/64=98/128=196/256 etc., R=5/64 all widths); f=5m/(n·H_L1) (f1.2=1.20830583, f1.0=1.00350823); D5 ceil contrast 235/206 vs accepted D6 236/208 exact; 21-ID ascending grid no duplicates; max realized check degree 4 both conditions ≤8 bound.
CHANNEL_FIDELITY: implemented chain matches design §2 steps 1–6 exactly (artifact → pb → E2 P_F λ*=137.3823795883264 → P1 → floor_renorm 1e-15 → XOR centering); independent recomputation on the real accepted artifact gave maxdiff 0 for P_F/P1; sampler rows equal the manual floor+renorm XOR construction (np.array_equal) with mass 1; full 32-ary rows preserved; no scalar/BSC/AWGN/q-SC path; XOR centering is the exact linear-code symmetry; check-update convention (`inv(h)·y` permutation) matches the decoder's `_check_update_log_batch`; kernel floor 1e-300 confirmed; E2-anchor decision and D5 build_f_model out-of-scope note recorded; no BLOCKED_CHANNEL_REPRESENTATION_DECISION warranted.
GRID_AND_RUNNER: frozen-match PASS — `--de-sweep`/`--verify` present; fresh-root refusal before any channel read/call (rc=2 spot-checked); six root files; `--verify` zero-skip recomputation + tamper detection; budgets ≤126 DE calls + ≤20 setup, wall ≤1800 s, per-call ≤120 s, RSS <2 GiB strict, single process, no retry/resume/adaptive stop; seeds 2026091601..03 fresh; conditions f1.2 primary/f1.0 secondary; L1 only; six terminals; deterministic rank key; claim-ceiling markers; future root ABSENT before/after review.
EVIDENCE_BOUNDARY: the 4 E09 calls are labeled PROFILE_ONLY in both readiness §10 and the log (never sweep evidence, no root/records); no terminal/advancement classified from them; zero production-decoder calls (tests monkeypatch decoder/kernel raises); `--de-sweep` never executed; no production root exists.
TEST_RERUN: focused test file 14 passed (E11 base) with fresh basetemp; scratch math script 56 PASS/0 FAIL; `--verify` on missing root ⇒ rc=1.
SCOPE_AND_STATE: branch `formal-ir-v72p1-addendum-clean`; HEAD `278fdf0742255de0d030649965b6feffb11bc23d` unchanged; nothing staged; no D8 commit; D8 artifacts = allowed set only; V26/V27/V37/V14/V9/v35 have zero textual diff; two reused modules carry pre-existing D5/D6 working-tree deltas predating D8 (best-effort attribution); `AGENT_PROJECT_MEMORY.md` addition is D6-route context predating the D8 implementation (flagged for D8 memory triage); no push.
STOP_CONDITIONS §9: 1 PASS (D6 marker/root consistent, accepted facts match); 2 PASS (channel representable without new assumption; E2 anchor recorded; no surrogate); 3 PASS (rate mapping reconciled, D6 236/208 governs over D5 ceil 235/206); 4 PASS (only λ varies; channel/decoder frozen); 5 PASS (fixed grid, no adaptation, no results); 6 PASS (zero decoder/scientific-DE calls; only labeled profile calls; no root; D7-H untouched); 7 PASS (no blocking finding).
FINDINGS:
- [F1] NON-BLOCKING — design.md:141/spec.md:38 claimed "E08 asserts the actual maximum ≤4 at both conditions" without such a test; property independently holds (max realized check degree 4). APPLIED at E12: new `test_realized_max_check_degree_le_4_at_both_conditions` (21×2 grid, genuine recomputation, asserts ≤4 and ≤8); scoped re-review RESOLVED/PASS, 15 passed.
- [F2] NON-BLOCKING — `--verify` requires a complete 126-call set, so a legitimately partial RESOURCE_BLOCKED/EVIDENCE_INVALID root fails verification (fail-closed). Frozen zero-skip semantics; optional partial-group tolerance is a next-packet note. CARRIED.
- [F3] NON-BLOCKING — budget enforcement is between/after calls only (no single-call interruption) and `setup_calls` is hard-coded 0 (≤20 budget trivially satisfied). Negligible at profiled 0.8–2.0 s/call. CARRIED (note only).
- [F4] NON-BLOCKING — design.md:139 freezes coefficient-stream seeds for the finite-length construction; D8's DE uses the V26 random nonzero-coefficient ensemble convention and does not exercise the stream. Wording clarification for the next packet. CARRIED.
CLAIM_CEILING: establishes only that the D8 DE contract is mathematically correct/consistent, the Model-F L1 channel is represented exactly without surrogate, the runner/verifier implement the frozen command/root/budgets/seeds/terminals/markers, and the evidence boundary is respected. Establishes no decoder improvement, ensemble superiority, FER/leakage/SKR, qualification, promotion or real-data suitability; does not authorize the sweep.
TERMINAL_RECOMMENDATION: D8_RATE_ALIGNED_ENSEMBLE_READY_AWAITING_EXPLICIT_AUTHORIZATION
AUTHORITY_BOUNDARY: advisory review only; authorization and acceptance remain with the user/main thread.

## E12 scoped re-review

RE_REVIEW_ID: D8-E12-SCOPED
F1_STATUS: RESOLVED
TEST_RERUN: 15 passed, 0 failed (fresh basetemp, independent recomputation f1.2 entries=21 refused=0 realized_max=4; f1.0 entries=21 refused=0 realized_max=4)
SCOPE_CHECK: new test derives from `build_candidate_plan()`/implemented `make_rho`, covers 21×2 entries, not a constant; insertion +22 lines; only test file + log changed after the E11 review; adapter/runner/readiness/design/spec mtimes unchanged; no commit; future root absent.
VERDICT: PASS
TERMINAL_RECOMMENDATION: D8_RATE_ALIGNED_ENSEMBLE_READY_AWAITING_EXPLICIT_AUTHORIZATION (F2–F4 carried as non-blocking notes for the next packet)
