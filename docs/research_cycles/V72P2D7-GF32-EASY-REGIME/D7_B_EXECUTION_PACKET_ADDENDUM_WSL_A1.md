# D7-B execution packet addendum WSL-A1 (frozen; NOT authorized)

Supersedes ONLY the Windows shell/executable spelling of
`D7_B_EXECUTION_PACKET_R1.md`. Inner Python script, arguments, root pattern,
UUID rule, scientific matrix, budgets, one-attempt semantics, and all
prohibitions remain exactly as in R1.

## Canonical environment

WSL (distro/kernel, repository mount path, interpreter, and `timeout`
identity recorded at renewed Pre-EXECUTE in
`D7_B_PRE_EXECUTE_REVIEW_WSL_R2.md`; recorded as environment facts, never as
scientific parameters).

## Exact future command shape (frozen, not run in this rework)

```bash
timeout -k 30 1800 python scripts/v72p2d7_gf32_easy_regime.py --out-root workspace/d7_b_easy_regime_<uuid>
```

- `<uuid>` is a fresh UUID named/generated by a future explicit user
  authorization. No UUID is generated in this rework.
- Target must be absent before launch; the runner refuses overwrite and
  refuses subdirs. Authorization lifecycle (`d7b_execution_authorized`,
  consume-on-first-historical-decoder-attempt, no retry/rerun/resume/reuse),
  budgets/stop rules, root contract, and boundaries are unchanged from R1.
- This addendum authorizes no execution and no result acceptance.

(End of file.)
