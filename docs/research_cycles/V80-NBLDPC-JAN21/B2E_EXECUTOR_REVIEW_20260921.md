# B2E Executor Review (2026-09-21) — EXPLORE focused review, ONE record

- Reviewed NEW `v80_b2e_campaign.py` + NEW `test_v80_b2e_campaign.py` + `B2E_PREEXEC_20260921.md` against frozen `B2E_EXPERIMENT_PACKET_20260921.md`(+Amendment) and `L1_REWORK_MEMO_V3_20260921.md`. Read-only: no edits, no run, no switch/commit/push. Authorizes NOTHING.
- **Verdict: PASS_WITH_FINDINGS** (all findings non-blocking; none blocks the two-arm run).
- Tests re-run today: `test_v80_b2e_campaign.py` → 38 passed (16.6 s); frozen regression o1+l1b+s2c → 136 passed (70.6 s). Preexec claims confirmed.
- Scope: `git diff HEAD` EMPTY (o1/l1b/s2c frozen modules untouched); no tracked file modified; `ls -d workspace/b2e*` → 0 roots; `results/`+`outputs_comparison/` refused pre-write; no result artifacts.
- X2/X3: both arms built via `construct_arm("A208"|"A202",2026092001,20)` ⇒ identical (n=1024,m,λ={2:1},seed,trials) byte-identical paired instance; fc==0 AND rank==m AND construct-twice-identical GATED (mismatch → STOP-BLOCKED pre-decode, zero decodes); girth 8 recorded-not-gated.
- X4: û1 = `np.argmax(g1[:,b])` from the frozen bundle (no refit/decoder/DE/L1); `posterior_rows_l2(bundle,b,û1)` is the module's ONLY rows call — true u1 reaches `u1_mismatches` only (measurement-only, not-a-disclosure) ⇒ genie path unreachable here; XOR-centering via `center_rows_prior`; `decode_error_domain_posterior` ONLY (bare `decode_error_domain` absent, test-enforced); max_iter 300 / streak default 3.
- X5/X6: `d_u1_bits = Σ_i H(γ1(·|b_i))` is the amended gating input; gate (b) = arm-mean (m·5+64+mean d_u1)/852.544 ≤ 1.3; du0 and 5×k lines report-only (never enter `passed`); gate (a) fails/240 ≤ 12, early-stop at the 13th; one arm per invocation, no pooling.
- X7 labels: `campaign B2E-map-u1`, `u1_source MAP`, genie ceiling REMOVED (no D1 label), D-u1 block (rule + per-block + mean + p50/p90/p99/max + violating-block count), paired-seed note with explicit "NO independence claim", de-labels covered/exploratory (no new DE run).
- X1/X7 CLI: dual-flag gate; refuses non-frozen `--construct-seed`/`--construct-trials`/`--block-base`/`--n-blocks` rc=2 pre-anything (stricter than the frozen O1 CLI, which only type-checks); root prefix `workspace/b2e_`; ≤1 wall-partial resume (second resume refuses, zero decodes); no auto-relaunch; `max_blocks` probe-only, never a CLI flag.
- Dry-probe consistency: reproduced read-only/in-memory (no decode, no writes) block 2026095601 → `d_u1_bits` 20.508686, `u1_mismatches` 4 — exactly the preexec Q4 figures. The 300-iter/77.26 s wiring figure was not re-run (it would consume a real decode); accepted as recorded.
- **BXR-01** budget/window: §6's ~600–900 s/arm estimate rests on the genie 2.38 s/decode mean, while the dry MAP block burned all 300 iterations in 77.26 s. 240×77 s ≈ 18.5k s vs a 3600 s window (≤1 resume ⇒ ≤7.2k s) ⇒ INCOMPLETE-wall (no verdict) or FAIL(budget) is the likely outcome. Executor semantics are correct (never a false PASS) and preexec §6 discloses it, but one block cannot quantify the distribution. Any budget/window amendment is a main-thread decision (not granted here).
- **BXR-02** channel provenance: the CLI still accepts `--gamma`/`--source`, and the manifest records the constants (`source:"2M"`, `"gamma_f03.npz"`) instead of the actually bound path/source — an override would be mislabeled in the artifact. Default is the frozen bundle (precedent-consistent with O1) ⇒ non-blocking; recommend recording the resolved gamma path + `bundle["source"]` (or refusing non-default values) before Pre-RESULT.
- **BXR-03** record precision: preexec Q0 says "only the 2 NEW files"; `git status` also lists 4 untracked research-cycle docs (packet/prompt/L1 memo v3) and `.codebuddy/memory`. No code outside the two files — cosmetic.
- **BXR-04** root shape: `_allows_root` is a prefix match, so a non-uuid8 suffix under `workspace/b2e_` is accepted; fresh-root + forbidden-tree checks still guard (precedent: O1/O1R/P0). Optional tightening.
- **BXR-05** report-only nuance: `quarter_tally` counts error/overrun rows in a quarter's "blocks" cell (mirrors frozen O1/L1B); the du0 line is a counterfactual D-u1=0 reading, not a measurement — both labeled report-only.
- Checklist: [x] matches packet + Amendment (X1–X8) · [x] tests pass (38 new + 136 frozen regression) · [x] no scope creep (no frozen-module edit, no roots, no result artifacts) · [ ] decision-log/troubleshooting update not required — no durable decision or new failure mode; BXR-01 belongs to the main thread if it amends §6.
- The two-arm run may proceed under the standing pre-authorization; this review authorizes nothing itself and grants no execution.
