# O1 Pre-EXECUTE (2026-09-20) — Q0–Q6 record, NOT a grant

- Track EXPLORE synthetic only. ID G-O1: packet frozen, NOT granted; this
  record authorizes NOTHING — a fresh explicit user grant is still required.
- Q0 branch: `formal-ir-v72p1-addendum-clean` confirmed; no switch/commit/push.
- Q1 scoped cleanliness: new files only (`formal_ir/v80_o1_campaign.py`,
  `tests/test_v80_o1_campaign.py` + this record); `git diff` on all frozen
  modules (s2c/v10_peg/v28/v26_mcde/s2_peg) empty; unrelated dirty/untracked
  tree files left untouched.
- Q2 frozen contract (packet §§1–7): n=1024 GF(32) λ={2:1}; A188 m=188
  PRIMARY, A208 m=208 SECONDARY; seed 2026092001/trials 20; pins fc=0/g8/
  rank-full + construct-twice-identical (STOP on mismatch); triple sampler +
  y/rows/prior wiring into `decode_error_domain_posterior` (300/streak 3);
  GENIE true-u1 = D1 ceiling (upper bound only, §§4/9); single-code
  semantics — one block = one decode, no 4-frame grouping (block≠S2c-group);
  gates fails/60≤3 AND f_super≤1.3 (1.177652/1.294947); D_blind=0 measured +
  sensitivity; f_L2 informational; early-stop at 4th block fail.
- Q3 authorization: user pre-authorization 2026-09-20 covers executor build
  only. Campaign + DE precheck NOT run (zero decodes executed here).
- Q4 output absence: no `workspace/o1_*` root exists (`ls` none); `results/`,
  `outputs_comparison/` forbidden. Operator must prove fresh
  `workspace/o1_<uuid8>` absence again at run time (UUID generated then).
- Q5 focused tests green: 32 passed, 0 failed, ~6 s wall
  (`.venv/bin/python -m pytest comparison_bench/tests/test_v80_o1_campaign.py
  -p no:cacheprovider -q`); fake-only + one dry gate-binding test (T15 real
  A188 pins re-verified); no 1-block dry decode run yet — operator runs one
  before the window if required by the grant.
- Q6 commands + budgets (per arm, one arm/invocation, single window):
  `.venv/bin/python -m comparison_bench.src.comparison_bench.formal_ir.v80_o1_campaign
  --execute-real --execution-authorized --arm A188|A208 --root workspace/o1_<uuid8>`
  (60 decodes ≈ 660 s est; wall ≤3600 s; per-decode 300 s; RSS <4 GiB);
  ≤1 explicit `--resume-from` (same path, WALL-PARTIAL only; FAIL/early-stop
  never resume); `--de-label covered|exploratory` (default exploratory).
- rg proof 2026-09-20: `20260955` hits only packet+prompt (frozen literals)
  + new module/tests — disjoint from 2026096–98xx/20xx/70xx/72xx/75xx.
- A208-DE precheck (before the A208 window, own quick step, NOT run here):
  same dual flags + `--arm A208 --root <arm-root> --de-precheck` (ONE
  `run_mcde_posterior` q=32/λ={2:1}/ρ=make_rho(0.796875), S1-CONFIRM
  n=16000/100/1e-4/streak-20 seed 2026094951 → `de_precheck.json`);
  A188 refuses precheck (S1 rate-identical). Blocks proceed regardless.
