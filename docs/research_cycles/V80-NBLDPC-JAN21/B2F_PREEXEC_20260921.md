# B2F Pre-EXECUTE (2026-09-21) — EXPLORE synthetic; readiness ONLY, NOT a grant

- Track EXPLORE synthetic; branch `formal-ir-v72p1-addendum-clean` (no switch/commit/push). Acceptance ID G-B2F (packet frozen, NOT granted). This record authorizes NOTHING; a fresh explicit user grant per arm is required before ANY execution (packet §7).
- Entrypoint: `B2F_EXPERIMENT_PACKET_20260921.md` §§1–8; prompt `B2F_EXPERIMENT_PROMPT_20260921.md`. Executor: NEW `comparison_bench/src/comparison_bench/formal_ir/v80_b2f_campaign.py`; tests NEW `comparison_bench/tests/test_v80_b2f_campaign.py` (frozen modules untouched).

## Q0 — Branch / scope cleanliness
- Branch verified `formal-ir-v72p1-addendum-clean`; no switch/commit/push performed.
- Scope: only the 2 NEW files above are added (`git status --porcelain`); `git diff` on `v80_o1_campaign.py` / `v80_s2c_campaign.py` / `v80_l1b_campaign.py` / `v80_b2e_campaign.py` / kernels = EMPTY (frozen modules untouched; no other edits).

## Q1 — Frozen contract check (packet §§1–6)
- Arms EXACTLY F208/F202 (m=208/202 via the A208/A202 instance); n=1024 GF(32) λ={2:1}; construct seed 2026092001/trials 20; 240 blocks; block seeds 2026095601+idx; stream `o1_blk:`; bundle `gamma_f03.npz` read-only (no refit).
- Prior (frozen formula, verbatim): `g1c=g1[:,b]`; `cond=g2[:,:,b].transpose(2,0,1)`; `marg=np.einsum("nu,nuv->nv", g1c.T, cond)`; per-row renormalize (non-positive/non-finite row sum → delta-at-0); `prior=center_rows_prior(marg, y)`, y=b&31; feed `v28.decode_error_domain_posterior(field, y.tolist(), dense, s_x, prior, 300)`. NO genie u1, NO argmax u1, NO L1 code.
- Gates (AND): (a) fails/240 ≤ 12, early-stop at the 13th fail; (b) f_super=(5m+64)/852.544 ≤ 1.3 unchanged O1 basis (F208 1.294947, F202 1.259759; D-u1 = 0.0 MEASURED label + never-assume-zero note). No pooling across arms.
- CLI refuses any deviation of `--construct-seed`/`--construct-trials`/`--block-base`/`--n-blocks` (science-input change → STOP); dual-flag gate kept; root prefix `workspace/b2f_`; ≤1 wall-partial resume; no auto-relaunch.

## Q2 — Output absence + rg proofs (2026-09-21)
- `ls -d workspace/b2f*` → 0 roots (absence proven); `results/` + `comparison_bench/outputs_comparison/` forbidden by the executor (refuse rc=2 pre-write).
- rg `b2f_` repo-wide → only this change's 2 NEW files + the B2F packet/prompt; no roots, no other code.
- rg `2026095601|2026095840` → O1/O1R/P0/L1B/B2E/B2F modules+tests+docs, `AGENT_PROJECT_MEMORY.md`, `docs/decision-log.md`, S2 memo, SCAN docs (all declared paired reuse); no B2F root. This record performed no writes.

## Q3 — Dry-construct pins (REAL constructor, in-memory, no writes, no decodes)
- F208 → `construct_arm("A208", 2026092001, 20)`: n=1024 m=208 fc=0 girth=8 rank=208 (rank-full) sockets 2048 parity 0 family peg-irregular; construct-twice-identical TRUE (gate asserts).
- F202 → `construct_arm("A202", 2026092001, 20)`: n=1024 m=202 fc=0 girth=8 rank=202 (rank-full) sockets 2048 parity 0 family peg-irregular; construct-twice-identical TRUE.
- GATED: fc==0 + rank-full + twice-identical (mismatch → STOP-BLOCKED pre-decode); girth 8 RECORDED-not-gated (P0/R2-amendment precedent).

## Q4 — Dry 1-block probe (REAL bundle + REAL sampler + REAL marginal formula, FAKE kernel, in-memory, no writes, NO verdict)
- Bundle: `2M_gamma1_L1` (32,1024) + `2M_gamma2_L2condU1` (32,32,1024) + sidecar `2M_p_b` (sum=1); read-only, no refit. Marginal formula verified against an independent triple-loop contraction (max |Δ| < 1e-12) and against the d7/d5 `q @ P` precedents.
- Block seed 2026095601 (`o1_blk:` stream): **prior_entropy_bits = 853.0211** (MEASURED; report-only column). Genie-row entropy on the SAME draw = 832.6036 ⇒ mixing penalty 20.42 b/block.
- **DEVIATION REPORTED (not fixed; frozen formula implemented verbatim):** the packet §1 pre-registered parity anchor is 826.266 b (genie H_L2·n). The Bayes-marginal row IS P(U2|b_i), so Σ_i H(π_i) measures H(U2|B)·n ≈ H_full·n = 852.544 b/block; the §1 line (H_full·n − H(U1|B)) computes H(U2|U1,B)·n, i.e. the GENIE row entropy. Distributional dry check: E[H(U2|B)]·1024 = 852.544, E[H(U2|U1,B)]·1024 = 826.266, penalty = H(U1|B)·n = 26.278 b. Bayes marginalization can only FLATTEN the prior (γ1 p_max ≈ 0.71) ⇒ a weaker prior; the column is REPORT-ONLY, never gated, and BP convergence remains the open risk. Main-thread decision requested (packet §1 wording vs measured value).
- u1_mismatches on the same draw = 4 (report-only, b2e-tied, not a disclosure, not a criterion). Wiring check ONLY — one block, fake kernel, no verdict, no FER/SKR claim.

## Q5 — Commands (one arm per invocation; fresh root per arm)
- F208: `.venv/bin/python -m comparison_bench.src.comparison_bench.formal_ir.v80_b2f_campaign --execute-real --execution-authorized --arm F208 --root workspace/b2f_<uuid8>`
- F202: same with `--arm F202 --root workspace/b2f_<uuid8>` (fresh uuid8; absence re-proven at grant time).
- Resume (≤1, wall-partial only): add `--resume-from workspace/b2f_<same uuid8>` (root must equal it). Terminal FAIL/early-stop states never resume; no auto-relaunch.

## Q6 — Budgets / stop rules
- 240 blocks/arm; ONE window ≤3600 s/arm; per-decode ≤300 s; RSS <4 GiB; 1 CPU. First-block timing NOT yet measured (the dry probe used a FAKE kernel): MEASURE block 0 at execution (packet §6). b2e risk carried: 300-iter non-convergence ≈ 70–77 s/decode ⇒ a window admits ≤ ~46 worst-case decodes; higher rates surface as INCOMPLETE-wall (≤1 resume) or FAIL(budget), never as a PASS; per-decode >300 s = terminal overrun.
- STOP on any science-input change (n/m/λ/seeds/H anchors/thresholds/channel/decoder/hypothesis/data roles); no rerun, no tuning, no arm substitution; no cross-arm pooling; PASS ⇒ report only (no genie-retirement wording without a two-construct-seed replication).

## Focused tests (run 2026-09-21, this record)
- `.venv/bin/python -m pytest comparison_bench/tests/test_v80_b2f_campaign.py -p no:cacheprovider -q` → **39 passed** (~16 s): real dry-construct pins F208/F202 (fc0/rank-full/twice + girth recorded), marginal-formula correctness vs manual einsum/triple loop (incl. row-renormalize guard + delta-at-0), XOR-centering identity, û1/genie NOT used (monkeypatch capture), prior-entropy + mismatch report-only columns, gates/early-stop, refusals, root confinement, resume-once, column schema, manifest labels, dry 1-block wiring probe.
- Frozen-module regression: `test_v80_o1_campaign.py` + `test_v80_s2c_campaign.py` + `test_v80_l1b_campaign.py` + `test_v80_b2e_campaign.py` → **174 passed** (~81 s; frozen modules untouched).

## NO-GRANT marker
- This Pre-EXECUTE record + executor + tests establish readiness ONLY. Execution requires a fresh explicit user grant per arm (dual flags + fresh root uuid absence). NOT granted here; authorizes NOTHING. No campaign run, no root, and no result was produced by this record; batch-end review pending.
