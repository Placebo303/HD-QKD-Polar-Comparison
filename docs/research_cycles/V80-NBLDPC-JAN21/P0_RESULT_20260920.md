# P0 Result (2026-09-20) — EXPLORE synthetic, RAW only

- Frozen recap: paired 240 blocks `2026095601+idx` (idx 0..239) shared with O1R R1/R2 (paired-design reuse; NO independence claim P0↔O1R). Both arms n=1024 GF(32), λ={2:1}, genie-u1 = D1 ceiling, max_iter 300/streak 3, construct seed 2026092001/trials 20, de-label `exploratory` (no new DE).
- A202 PRIMARY: m=202, rate 0.802734375, leak 1074, f_super=1.25976. A200 SECONDARY: m=200, rate 0.8046875, leak 1064, f_super=1.24803. Construct pins both arms: fc=0 + rank-full + twice-identical (gated); girth 8 recorded-not-gated.

## Per-arm table (within-arm only; cross-arm pooling FORBIDDEN)

| arm | fails/240 | FER | quarters (fails per 60) | qtrs ≤3 | iter succ med/p95/max | f_super | gate (a) ≤12 | gate (b) ≤1.3 | verdict |
|---|---|---|---|---|---|---|---|---|---|
| A202 | 0 | 0.0 | 0/0/0/0 | 4/4 | 11/19/216 | 1.25976 | ✓ | ✓ | PASS |
| A200 | 2 (idx 42, 202) | 0.008333 | 1/0/0/1 | 4/4 | 12/20/44 | 1.24803 | ✓ | ✓ | PASS |

- A200 all-decode iterations med/p95/max incl. fails: 12/22/300 (fails hit the 300-iter cap). D_blind=0 measured placeholder; f_L2 informational only, never gated.

## Budget / ledger / windows

- Frozen: ≈600 s est ≤900 s/arm, 1 window ≤3600 s/arm, per-decode cap 300 s, RSS <4 GiB, 1 CPU; ≤1 `--resume-from` per arm, WALL-PARTIAL only.
- A202 actual: exit 0, wall 12:45 (765 s), RSS max 162.6 MiB, 1 window, 0 resumes; ledger {blocks 240, decodes 240}; root `workspace/p0_7a5c39e8`.
- A200 actual: exit 0, wall 15:37 (937 s), RSS max 163.5 MiB, 1 window, 0 resumes; ledger {blocks 240, decodes 240}; root `workspace/p0_a237aa12`.
- Each root: `manifest.json` + `rows.json` + `block_accounting.csv` + `group_accounting.csv` (byte-identical copy, O1R precedent). Logs: `/tmp/opencode/p0_A20{2,0}.{log,time,start}`.

## Provenance + cross-refs

- Branch `formal-ir-v72p1-addendum-clean` (no switch/commit/push). Code: `formal_ir/v80_o1_campaign.py` D1–D5 + `tests/test_v80_o1_campaign.py` T24 per frozen P0 pre-exec (54 passed).
- Cross-refs: O1R A208 PASS (0/240×2, f≈1.29495); L1 memo 20260920 (§1 cliff, §3 pre-arm); O1/O1R packets+results+batch-end-reviews; P0 packet/prompt/pre-exec/executor-review 20260920.

## §7 interpretation rule (packet verbatim; NO decision made here)

- "A202 PASS ⇒ L1 build MAY proceed with m2≤202 budget (P1–P3 packets still need freezing + grants). A202 FAIL ⇒ m2=202 unusable ⇒ L1 memo alternatives re-open."
- "A200 PASS keeps the m1=8 option alive; A200 FAIL restricts L1 to m1≤6 (or re-open). Cross-arm pooling FORBIDDEN (different m ⇒ different codes; paired frames aid contrast only)."
- "Outcome returned to main thread; P0 NEVER authorizes L1 construction, DE, or combined-FER work." Machine verdicts only; main thread decides.

## Does NOT establish

- No L1 construction/threshold; genie-u1 ceiling only (real blind disclosure unmeasured); synthetic only (no real/Jan-21 frames); single-code block ≠ S2c-group; no S3/qualification/publication/route decision; per-arm verdicts only (no pooled FER across arms).
