# B2F Experiment Packet (2026-09-21) — FROZEN, NOT GRANTED

- Track: EXPLORE synthetic only. Branch `formal-ir-v72p1-addendum-clean` (no switch/commit/push).
- Acceptance ID: G-B2F (packet frozen, NOT granted). Authorizes NOTHING; Pre-EXECUTE + fresh grant required (§7).
- Parents: `S2_ROUTE_DECISION_MEMO_20260921` (option A3) + `B2E_RESULT_20260921`/`B2E_BATCH_END_REVIEW_20260921` (MAP-û1 13/13 both arms, all `max_iter_reached`, ~70 s/decode) + `L1_REWORK_MEMO_V3_20260921` §2.1 (measured γ1 stats) + `O1R_RESULT_20260920` (A208 genie PASS 0/240 ×2 at f_super 1.294947) + `P0_RESULT_20260920` (A202 PASS) + `S2_ACCOUNTING_MAP_20260920` + read-only code (`v80_o1_campaign`, `v80_s2c_campaign`, `v80_b2e_campaign`, `nonbinary_v28`, `nonbinary_v10_fftqspa`, `v72p2d5_gf32_rate_mother`, `v72p2d7_gf32_cross_layer_discriminator`).
- Why: b2e falsified only the HARD argmax commitment. B2F replaces the u1 point estimate by exact Bayes marginalization — no L1 code, no estimator, no accounting change.

## 1. Hypothesis (to test, NOT a claim)
- H: the exact Bayes-marginal L2 prior π_i(e) = Σ_{u1} γ1(u1|b_i)·γ2(y_i⊕e | u1, b_i), y_i = b_i&31, carries (near-)identical entropy to the genie-conditioned prior and may restore A208-class decodability WITHOUT any u1 conditioning — no L1 code, no argmax, no accounting change (D-u1 = 0.0 measured label; gates unchanged).
- Entropy arithmetic (frozen anchors, quoted): chain rule H(U2|B) = H_full·n − H(U1|B) = 1024×0.83256272 − 1024×0.02566205 = 852.544 − 26.278 = **826.266 b** vs genie H_L2·n = 1024×0.80690067 = **826.266 b/block** (anchors `v80_s2_peg` L77/L79; memo's rounded line 826.27 vs 826.37 uses 0.807×1024) ⇒ agreement to ~2e-4 b ⇒ the marginalized prior carries the same entropy as the genie prior; b2e falsified only the hard commitment, not the information content.
- Pre-registered observation (not a claim): the mixture prior is flatter at the 31 uncertain columns (γ1 p_max≈0.71); BP convergence is the open risk (b2e: 300-iter non-convergence under MAP conditioning).

## 2. Frozen formula (every ambiguity resolved by QUOTED code, not invented)
- Bundle axes + normalization (quoted, `bind_empirical_bundle` s2c L194-247): g1[u1,b] (32,1024) colsums=1±1e-9; g2[u1,u2,b] (32,32,1024) axis-1 rowsums=1±1e-9 ("g2 IS the conditional, cond-rowsum over axis=1 == 1, bound at bind"); p_b sum=1±1e-9. `posterior_rows_l2` (s2c L288-316) returns (n,32) rows_i=γ2(·|b_i,u1_i) with the `norm=g1[u1,b]` zero-mass gate → delta-at-0.
- Precedent (quoted, frozen read-only): `app_fed_l2_prior` (v72p2d5 L354-376) "Production L2 prior `q @ P`": `seg = p2a[:, bob, :]`; `prior = np.einsum("nq,qnv->nv", q, seg)`; `_floor_renorm(prior, DECODER_FLOOR=1e-15)` (L48, L346-351). d7 `transfer_prior_l1_to_l2` (v72p2d7_gf32_cross_layer_discriminator L603-610): "`P_transfer(U2|B) = sum_u1 q1(u1) P(U2|B,u1)`; `(n, 32)` decoder prior"; `cond[i,u1,u2]=P(U2|b_i,u1)` via `seg = j[:, :, bob].transpose(2, 0, 1)` (L584); `out = np.einsum("nu,nuv->nv", q, cond)` (L609).
- EXACT op (frozen; per block, after the triple draw and y=b&31): `g1c = g1[:, b]` → (32,n); `cond = g2[:, :, b].transpose(2,0,1)` → (n,32,32) with cond[i,u1,u2]=γ2(u2|b_i,u1); `marg = np.einsum("nu,nuv->nv", g1c.T, cond)` ⇒ marg[i,v] = Σ_{u1} γ1(u1|b_i)·γ2(v|b_i,u1) — the d7 `q@P` contraction with q = γ1 column. Row guard: `decode_fftqspa` `_content_valid` (fftqspa L145-158) requires each prior row finite, non-negative, positive-sum, |sum−1|≤1e-9, while bind tolerates 1e-9+1e-9 accumulation ⇒ each marg row is divided by its row sum; a non-finite/non-positive row sum falls back to delta-at-0 (`posterior_rows_l2` zero-mass semantics; the d7 uniform-1/32 fallback is NOT adopted because bind already enforces rowsum=1). No extra floor: the kernel floors internally at 1e-15 (fftqspa L355), matching the O1/B2E prior wiring.
- Centering + feed (quoted): `prior = center_rows_prior(marg, y)` (s2c L319-331 = v28 `_center_rows` L189-197: `out[i] = arr[i, add[y_i, arange(32)]]`, GF(32) char-2 add = XOR) ⇒ π_i(e) = marg[i, y_i⊕e]. XOR-centering is a column permutation, hence commutes with the u1-sum: the two frozen ops realize the §1 formula exactly. Feed: `v28.decode_error_domain_posterior(field, y.tolist(), dense, s_x, prior, MAX_ITER)` (v28 L155-186; takes (n,q) prior; s_e=s_x+H·y; x_hat=y+e_hat); scalar-p `decode_error_domain` (v10) NEVER used. max_iter=300/streak 3; per-decode cap 300 s. Block accept = `exact_match` (x̂==u2).

## 3. Arms (EXACTLY two — no more, no substitutions)
- **F208 PRIMARY**: m=208 code via `construct_arm("A208", 2026092001, 20)` — the frozen A208 construction instance (byte-identical paired construction; recorded). Budget relevance: m=208 leaves NO L1-row funding ⇒ PASS ⇒ genie retires with no L1 code at all.
- **F202 SECONDARY**: m=202 code via `construct_arm("A202", 2026092001, 20)` — the frozen A202 instance. Budget relevance: m1=6 accounting room. Every other m is OUT OF SCOPE. Both: n=1024, GF(32), λ={2:1}. Pins: fc==0 AND rank-full AND construct-twice-identical GATED (mismatch → STOP-BLOCKED); girth RECORDED-not-gated (P0/R2-amendment precedent).

## 4. Blocks / seeds / gates
- 240 blocks/arm; literal paired base 2026095601+idx, idx=0..239 (SAME 240 frames as O1R/P0/L1B/b2e — paired contrast on identical (b,u1,u2) marginals; NO independence claim; no pooling across arms). Stream `o1_blk:{seed}`; construct seed 2026092001/trials 20.
- (a) fails/240 ≤ 12 (5% exact); early-stop at the 13th fail → FAIL, retain partials.
- (b) f_super = (5m+64)/852.544 ≤ 1.3 on the UNCHANGED O1 basis: F208 1104/852.544 = 1.294947; F202 1074/852.544 = 1.259759 — both PASS by construction (accounting identity); D-u1 = 0.0 MEASURED label (Bob's prior is computed from b alone ⇒ no u1 disclosure) with the NEVER-ASSUME-ZERO note retained. Gate (a) is the scientific test.

## 5. Executor delta (EXACT list; thin NEW wrapper, NOT new O1-family arms)
- X1 NEW `comparison_bench/src/comparison_bench/formal_ir/v80_b2f_campaign.py` mirroring `v80_b2e_campaign.py`; imports frozen helpers READ-ONLY: from `v80_o1_campaign` (O1_N, O1_CONSTRUCT_SEED, O1_MAX_TRIALS, MAX_ITER, WALL_CAP_S, PER_DECODE_CAP_S, RSS_CAP_GIB, `fail_bar`, `stream_seed`, `construct_arm`), from `v80_s2c_campaign` (`bind_empirical_bundle`, `empirical_triple_sampler`, `center_rows_prior`), plus `peg`/`v28`/`fftqspa`/`GF2mField`. NO edits to any existing module.
- X2 `marginal_prior_l2(bundle, b)` per §2 REPLACES both the genie path (`posterior_rows_l2(bundle,b,u1_true)`) and the MAP path (`posterior_rows_l2(bundle,b,û1)`); everything else (triple draw, y=b&31, syndrome, entrypoint, max_iter) identical to A208/A202.
- X3 Per-block REPORT-ONLY columns: `prior_entropy_bits` = Σ_i H(π_i) (1e-300 floor guard, same as b2e `d_u1_bits` L293) and `u1_mismatches` (û1=argmax_u g1[u,b]; measurement only, ties to b2e — NOT a disclosure, NOT a block criterion).
- X4 ARMS table F208/F202 only (§3). X5 Pins per §3. X6 Manifest labels: campaign `B2F-soft-marginal`, `u1_source: MARGINAL`, genie ceiling REMOVED (no D1 label), D-u1 = 0.0 measured label + never-assume-zero note, paired-seed note (2026095601+idx shared with O1R/P0/L1B/b2e), de-label `covered` (F208, carried from the A208-DE precheck) / `exploratory` (F202, P0 precedent) — no new DE run.
- X7 CLI hardening: refuse any non-frozen `--construct-seed`/`--construct-trials`/`--block-base`/`--n-blocks` (b2e precedent; science-input change → STOP); root prefix `workspace/b2f_` (fresh additive; `results/`+`outputs_comparison/` forbidden); dual-flag `--execute-real --execution-authorized` gate kept; checkpoint-per-block; ≤1 `--resume-from` (wall-partial only); no auto-relaunch.
- X8 Fake-only `tests/test_v80_b2f_campaign.py` (fake bundle + fake decode_fn: contraction arithmetic vs manual Σ_u γ1·γ2(y⊕e|u), XOR-centering identity, row-sum guard + delta-at-0 fallback, entropy column, bar derivation, early-stop, root refusal). No production decode in tests.

## 6. Budgets / scope / stop
- 240 blocks/arm; est ~600 s/arm (A208 genie measured 548–562 s); MEASURE the first block. RISK (b2e): non-convergence ran 300 iters ≈ 70 s/decode ⇒ 240 blocks ≈ 4.6 h ≫ 3600 s window. Protocol: gate (a) fires (13th fail) or the wall cap yields INCOMPLETE-wall with ≤1 continuation — NEVER a false PASS; a per-decode >300 s is a terminal overrun (L1B precedent). 1 window ≤3600 s; per-decode ≤300 s; RSS <4 GiB; 1 CPU.
- Roots `workspace/b2f_<uuid8>` fresh additive per arm (UUID + absence proven at Pre-EXECUTE); old roots untouched. STOP on any science-input change (n/m/λ/seeds/H anchors/thresholds/channel/decoder/hypothesis/data roles).

## 7. Entry evidence + interpretation (report to main thread; NO auto-proceed)
- Entry: (a) executor built per §5; (b) Q0–Q6 Pre-EXECUTE (branch; scope cleanliness; frozen contract §§1–6; authorization; output-absence + rg proofs — `2026095601–5840` hits only in O1R/P0/L1B/B2E/B2F docs+roots, `b2f_` absent; focused tests incl. dry-construct pins per arm + 1-block dry decode with the marginal prior).
- PASS (gate a on an arm) ⇒ report to main thread. NO genie-retirement wording without a two-construct-seed replication (e.g. F208 at a fresh construct seed) — stated explicitly. FAIL ⇒ return to the main thread for the A2/O-D decision (memo options). No rerun, no tuning, no arm substitution, no S3.

## 8. Deliverables + does-NOT-establish
- `B2F_RESULT_20260921.md` per arm (verdict, per-arm FER, per-60 tally report-only, prior-entropy mean + distribution, u1-mismatch mean/max report-only, both f_super lines, labels) + `rows.json` + `block_accounting.csv` per root. Prompt: `B2F_EXPERIMENT_PROMPT_20260921.md` (≤30 lines).
- Does NOT establish: no FER/route/S3/qualification/publication claim; genie retirement NOT established (single construct seed, single arm class); no L1 construction; no cross-arm pooling; paired-seed reuse carries NO independence claim; synthetic only (no real/Jan-21 frames); block ≠ S2c-group; prior-entropy parity is an entropy observation, not a decodability proof; this packet authorizes NOTHING.
