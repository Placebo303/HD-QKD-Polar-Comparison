# P0 Pre-EXECUTE (2026-09-20) — EXPLORE synthetic, FROZEN, NOT GRANTED

- Track EXPLORE synthetic only. Authorizes NOTHING; fresh explicit grant required before any execution. No campaign run occurred under this record.
- Q0 branch: `formal-ir-v72p1-addendum-clean` verified (`git branch --show-current`); no switch/commit/push performed or claimed.
- Q1 scope: exactly 2 files — `formal_ir/v80_o1_campaign.py` (D1–D5) + `tests/test_v80_o1_campaign.py` (T24); frozen channel/decoder/PEG modules untouched; unrelated worktree items unmodified by this delta.
- Q2 frozen contract (P0 packet §§1–7): A202 PRIMARY m=202/leak 1074/f≈1.259759 + A200 SECONDARY m=200/leak 1064/f≈1.248029, λ={2:1}, n=1024, genie-u1, max_iter 300/streak 3; gates/arm fails/240≤12 AND f_super≤1.3; early-stop at 13th fail; per-60 tally report-only; STOP on any science-input change.
- Q3 authorization: G-P0 frozen NOT granted; readiness only. Zero production decodes run (fake-path tests only; real binding limited to in-memory dry construction).
- Q4a rg `2026095[678]`: 9 files ONLY — scoped module+test, O1R docs ×5 (packet/prompt/preexec/result/review), P0 packet/prompt; zero workspace-root hits; any other hit → STOP.
- Q4b roots: `ls workspace/p0_*` → absent (No such file); run roots `workspace/p0_<uuid8>` fresh per arm, never pre-created here; `results/`/`outputs_comparison/` untouched.
- Q4c paired reuse (declared, packet §3): block base 2026095601+idx shared with O1R R1/R2 (same 240 frames at different m = paired-design norm, byte-identical sampler path); construct seed 2026092001 (O1 family; new m ⇒ disjoint outputs). NO independence claim P0↔O1R; cross-arm pooling FORBIDDEN.
- Q5 tests: `pytest test_v80_o1_campaign.py -p no:cacheprovider -q` → 54 passed (44 pre-existing + 10 new T24), wall 19.5 s: P0 table/pins vs measured; REAL dry-construct gate per arm (in-memory, no writes); girth-6 accepted-recorded + fc/rank mismatch STOP-BLOCKED zero-decode; unknown arm refuses; bar 12/n 240/base recorded; campaign 'P0'; R2 pins text byte-invariant.
- Dry pins (n=1024, seed 2026092001, trials 20, measured 2026-09-20): A202 fc=0/girth=8/rank=202/twice-identical; A200 fc=0/girth=8/rank=200/twice-identical. Policy: fc==0 + rank-full + twice-identical GATED; girth RECORDED-not-gated (R2-amendment precedent; P0 §5/D3).
- D4 DE: NO new MC-DE; `--de-label` defaults `exploratory` for P0; `run_de_precheck` is A208-only and refuses A202/A200 (tested) — precheck never forced; gates unchanged.
- D5 manifest: records m/rate/leak/f_super per arm (executor-recomputed, verified), pin table with girth-recorded flag, paired-base note (2026095601 shared with O1R), campaign `P0`.
- Note: packet §8 template value `exploratory-no-DE` is NOT a valid `--de-label` (frozen `covered|exploratory`); Q6 uses `exploratory` — main thread to reconcile packet text.

- Q6 A202 (single window, wall ≤3600 s):
  `.venv/bin/python -m comparison_bench.src.comparison_bench.formal_ir.v80_o1_campaign --execute-real --execution-authorized --arm A202 --root workspace/p0_<uuid8> --block-base 2026095601 --n-blocks 240 --construct-seed 2026092001 --de-label exploratory`
- Q6 A200: same with `--arm A200` + a second fresh `workspace/p0_<uuid8>`.
- Budgets: ≈240×2.5 s ≈ 600 s ≤ 900 s/arm; per-decode cap 300 s; RSS <4 GiB; 1 CPU. Continuation: ≤1 explicit `--resume-from` per arm, fresh window, WALL-PARTIAL only; terminal FAIL/early-stop never resume.

- Verdict: READY pending fresh grant. Does NOT establish L1/threshold/combined-FER/S3; P0 NEVER authorizes L1 construction, DE, or combined-FER work.
