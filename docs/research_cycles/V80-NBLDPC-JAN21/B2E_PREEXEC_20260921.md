# B2E Pre-EXECUTE (2026-09-21) — EXPLORE synthetic; readiness ONLY, NOT a grant

- Track EXPLORE synthetic; branch `formal-ir-v72p1-addendum-clean` (no switch/commit/push). Acceptance ID G-B2E (packet frozen, NOT granted). This record authorizes NOTHING; a fresh explicit user grant per arm is required before ANY execution (packet §8).
- Entrypoint: `B2E_EXPERIMENT_PACKET_20260921.md` §§1–9 + Amendment 2026-09-21 (entropy-sum D-u1 + arm-mean gate (b)); prompt `B2E_EXPERIMENT_PROMPT_20260921.md`. Executor: NEW `comparison_bench/src/comparison_bench/formal_ir/v80_b2e_campaign.py`; tests NEW `comparison_bench/tests/test_v80_b2e_campaign.py` (frozen modules untouched).

## Q0 — Branch / scope cleanliness
- Branch verified `formal-ir-v72p1-addendum-clean`; no switch/commit/push performed.
- Scope: only the 2 NEW files above are added (`git status`); `git diff` on `v80_o1_campaign.py` / `v80_s2c_campaign.py` / `v80_l1b_campaign.py` = EMPTY (frozen modules untouched; no other edits).

## Q1 — Frozen contract check (packet §§1–7 + Amendment)
- Arms EXACTLY B208/B202 (m=208/202 via the A208/A202 instance); n=1024 GF(32) λ={2:1}; construct seed 2026092001/trials 20; 240 blocks; block seeds 2026095601+idx; stream `o1_blk:`; bundle `gamma_f03.npz` read-only (no refit).
- u1 source MAP: û1_i = argmax_u γ1(u|b_i) (no decoder, no DE, no L1 construction); rows = `posterior_rows_l2(bundle,b,û1)`; XOR-centered prior; `decode_error_domain_posterior` ONLY; max_iter=300/streak 3; accept = `exact_match`; genie ceiling REMOVED (no D1 label).
- Gates: (a) fails/240 ≤ 12, early-stop at the 13th fail; (b) arm-mean f_super = (m·5+64+mean(d_u1_bits))/852.544 ≤ 1.3 (Amendment; mean-based). Report-only, never gated: f_super_du0 (B208 1.294947 / B202 1.259759) and the 5×k sensitivity (B208 ≈1.342453 / B202 ≈1.307264 at mean k=8.10).
- CLI refuses any deviation of `--construct-seed`/`--construct-trials`/`--block-base`/`--n-blocks` (science-input change → STOP); dual-flag gate kept; root prefix `workspace/b2e_`; ≤1 wall-partial resume; no auto-relaunch.

## Q2 — Output absence + rg proofs (2026-09-21)
- `ls -d workspace/b2e*` → 0 roots (absence proven); `results/` + `comparison_bench/outputs_comparison/` forbidden by the executor (refuse rc=2 pre-write).
- rg `b2e_` repo-wide → only B2E packet/prompt + L1 memo v3 (docs); no roots, no code before this change.
- rg `2026095601|2026095840` → O1R/P0/L1B/B2E docs+roots + o1/l1b tests (expected paired-reuse hits); no B2E root. This record performed no writes.

## Q3 — Dry-construct pins (REAL constructor, in-memory, no writes, no decodes)
- B208 → `construct_arm("A208", 2026092001, 20)`: n=1024 m=208 fc=0 girth=8 rank=208 (rank-full) sockets 2048 parity 0 family peg-irregular; construct-twice-identical TRUE (gate asserts).
- B202 → `construct_arm("A202", 2026092001, 20)`: n=1024 m=202 fc=0 girth=8 rank=202 (rank-full) sockets 2048 parity 0 family peg-irregular; construct-twice-identical TRUE.
- GATED: fc==0 + rank-full + twice-identical (mismatch → STOP-BLOCKED pre-decode); girth 8 RECORDED-not-gated (P0/R2-amendment precedent).

## Q4 — Dry 1-block probe (REAL bundle + REAL construction, in-memory, no writes)
- Bundle: `2M_gamma1_L1` (32,1024) + `2M_gamma2_L2condU1` (32,32,1024) + sidecar `2M_p_b` (sum=1); read-only, no refit.
- Anchors reproduced: p_b-weighted mean H(γ1) = 0.02566205 ⇒ 1024×H = 26.2779 b (≈26.28 ✓); p_b-weighted mean 1−p_max = 0.007909 ⇒ 8.099 ≈ 8.10 mismatches/block (✓ v3 memo §2.1).
- Block seed 2026095601 (`o1_blk:` stream): d_u1_bits = 20.508686 (single realization inside the pre-registered MC spread p10 20.47 / p50 26.09; the ARM MEAN is the gating quantity); u1_mismatches = 4 (single realization vs the 8.10 expectation).
- Dry MAP-u1 decode: status max_iter_reached, iterations 300, reconstruction_ok False, exact_match False, max_iter 300, wall 77.26 s (< 300 s per-decode cap). Wiring check ONLY — one block; no verdict, no FER/SKR claim.

## Q5 — Commands (one arm per invocation; fresh root per arm)
- B208: `.venv/bin/python -m comparison_bench.src.comparison_bench.formal_ir.v80_b2e_campaign --execute-real --execution-authorized --arm B208 --root workspace/b2e_<uuid8>`
- B202: same with `--arm B202 --root workspace/b2e_<uuid8>` (fresh uuid8; absence re-proven at grant time).
- Resume (≤1, wall-partial only): add `--resume-from workspace/b2e_<same uuid8>` (root must equal it). Terminal FAIL/early-stop states never resume; no auto-relaunch.

## Q6 — Budgets / stop rules
- 240 blocks/arm; ONE window ≤3600 s/arm; per-decode ≤300 s; RSS <4 GiB; 1 CPU. Observation (not a claim): the dry block used the full 300 iterations (77.26 s) — within the per-decode cap; a window admits ≤ ~46 such worst-case decodes, and a higher worst-case rate surfaces as INCOMPLETE-wall (≤1 resume) or FAIL(budget), never as a PASS.
- STOP on any science-input change (n/m/λ/seeds/H anchors/thresholds/channel/decoder/hypothesis/data roles); no rerun, no tuning, no arm substitution; no cross-arm pooling.

## Focused tests (run 2026-09-21, this record)
- `.venv/bin/python -m pytest comparison_bench/tests/test_v80_b2e_campaign.py -p no:cacheprovider -q` → **38 passed** (~18 s): real dry-construct pins B208/B202 (fc0/rank-full/twice + girth), MAP-argmax/mismatch/entropy-sum correctness, û1-not-genie capture, gates/early-stop, refusals, root confinement, resume-once, per-block column schema.
- Frozen-module regression: `test_v80_o1_campaign.py` + `test_v80_l1b_campaign.py` + `test_v80_s2c_campaign.py` → **136 passed** (frozen modules untouched).

## NO-GRANT marker
- This Pre-EXECUTE record + executor + tests establish readiness ONLY. Execution requires a fresh explicit user grant per arm (dual flags + fresh root uuid absence). NOT granted here; authorizes NOTHING. No campaign run, no root, and no result was produced by this record; reviewer batch-end review pending.
