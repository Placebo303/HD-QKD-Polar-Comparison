# B2G Pre-EXECUTE (2026-09-21) — EXPLORE synthetic; readiness ONLY, NOT a grant

- Track EXPLORE synthetic (EXPLORE_HEAVY cost annotation); branch `formal-ir-v72p1-addendum-clean` (no switch/commit/push). Acceptance ID G-B2G: packet FROZEN, NOT granted (CONTINGENCY RESOLVED 2026-09-21: `B2F_BATCH_END_REVIEW_20260921.md` = PASS). This record authorizes NOTHING; a fresh explicit user grant per arm is required before ANY execution (packet §7).
- Entrypoint: `B2G_EXPERIMENT_PACKET_20260921.md` §§1–8; prompt `B2G_EXPERIMENT_PROMPT_20260921.md`. b2g = b2f with EXACTLY ONE changed input: construct seed 2026092011 (O1R R2 second instance; series 2026092001→2026092011).
- Executor: NEW `comparison_bench/src/comparison_bench/formal_ir/v80_b2g_campaign.py`; tests NEW `comparison_bench/tests/test_v80_b2g_campaign.py` (no edits to any existing module).

## Q0 — Branch / scope cleanliness
- Branch verified `formal-ir-v72p1-addendum-clean`; no switch/commit/push performed.
- Scope: ONLY the 2 NEW files above added; the 2 B2G docs had exactly their 2 CONTINGENT header lines flipped (contingency-resolved line). `git diff` on ALL tracked files EMPTY — `v80_b2f_campaign.py`, `v80_o1_campaign.py`, `v80_s2c_campaign.py`, `v80_b2e_campaign.py`, kernels (`nonbinary_v28`/`nonbinary_v10_fftqspa`/`nonbinary_v10_peg`) and `test_v80_b2f_campaign.py` all CLEAN (pre-existing unrelated untracked: `.codebuddy/`, `LITERATURE_RECOMMENDATION_TRIAGE_20260921.md`).

## Q1 — Frozen contract check (packet §§1–6)
- Arms EXACTLY F208/F202 (m=208/202 via the A208/A202 code at construct seed 2026092011, trials 20); n=1024 GF(32) λ={2:1}; 240 blocks; block seeds 2026095601+idx; stream `o1_blk:`; bundle `gamma_f03.npz` read-only (no refit).
- Prior (frozen formula, verbatim): `g1c=g1[:,b]`; `cond=g2[:,:,b].transpose(2,0,1)`; `marg=np.einsum("nu,nuv->nv", g1c.T, cond)`; per-row renormalize (non-positive/non-finite row sum → delta-at-0); `prior=center_rows_prior(marg, y)`, y=b&31; feed `v28.decode_error_domain_posterior(field, y.tolist(), dense, s_x, prior, 300)`. NO genie u1, NO argmax u1, NO L1 code.
- Gates (AND): (a) fails/240 ≤ 12, early-stop at the 13th fail (retain partials); (b) f_super=(5m+64)/852.544 ≤ 1.3 on the UNCHANGED O1 basis (F208 1.294947, F202 1.259759; D-u1 = 0.0 MEASURED label + never-assume-zero note). No cross-arm or cross-instance pooling.
- CLI refuses ANY non-frozen `--construct-seed` (INCLUDING 2026092001 — b2f's value, not b2g's) / `--construct-trials` / `--block-base` / `--n-blocks` (science-input change → STOP, rc=2 pre-anything); arms F208/F202 only; dual-flag `--execute-real --execution-authorized`; root prefix `workspace/b2g_` (fresh additive; `results/`+`outputs_comparison/` forbidden); checkpoint-per-block; ≤1 `--resume-from` (wall-partial only, same root); no auto-relaunch; de-labels covered(F208)/exploratory(F202).

## Q2 — Output absence + rg proofs (2026-09-21)
- `ls -d workspace/b2g*` → 0 roots (absence proven); `results/` + `comparison_bench/outputs_comparison/` untouched (no file modified 2026-09-21); the executor refuses them rc=2 pre-write.
- rg `b2g_` repo-wide → ONLY this change's 2 NEW files + the 2 B2G packet/prompt docs; no roots, no other code.
- rg `2026092011` → O1R docs (PACKET/PROMPT/PREEXEC/RESULT/AMENDMENT) + `v80_o1_campaign.py` + `test_v80_o1_campaign.py` + `AGENT_PROJECT_MEMORY.md` + b2f/b2e tests (declared refusal-case hits) + this change's 2 NEW files + the 2 B2G docs — all declared reuse of the O1R R2 seed; no b2g root.
- rg `2026095601|2026095840` → O1/O1R/P0/L1B/b2e/b2f modules+tests+docs (declared paired reuse); no b2g root.

## Q3 — Dry-construct pins (REAL constructor, in-memory, no writes, no decodes)
- F208 → `construct_arm("A208", 2026092011, 20)`: n=1024 m=208 fc=0 **girth=6** rank=208 (rank-full) sockets 2048 parity 0 family peg-irregular; construct-twice-identical TRUE.
- F202 → `construct_arm("A202", 2026092011, 20)`: n=1024 m=202 fc=0 **girth=6** rank=202 (rank-full) sockets 2048 parity 0 family peg-irregular; construct-twice-identical TRUE.
- GATED: fc==0 + rank-full + twice-identical (mismatch → STOP-BLOCKED pre-decode); girth 6 RECORDED-not-gated (O1R R2 precedent: A208 girth 6 at this seed carried as recorded covariate; A202 girth 6 MEASURED here and recorded).

## Q4 — Dry 1-block probe (REAL bundle + REAL sampler + REAL marginal formula, FAKE kernel, in-memory, no writes, NO verdict)
- Bundle: `2M_gamma1_L1` (32,1024) + `2M_gamma2_L2condU1` (32,32,1024) + sidecar `2M_p_b` (sum=1); read-only, no refit.
- Block seed 2026095601 (`o1_blk:` stream; paired draw, construct-seed-independent): einsum marginal vs independent triple-loop contraction **max |Δ| = 0.0** (< 1e-12); row sums 1±2e-16.
- **prior_entropy_bits = 853.0211** (MEASURED; report-only; ≈ H_full·n = 852.544 — NOT parity with the genie 826.266 b; FXR-1: Bayes marginalization FLATTENS by ≈H(U1|B)·n ≈ 26 b/block; genie-row entropy on the same draw 832.6036, measured penalty 20.42 b/block). Column never gated.
- u1_mismatches on the same draw = **4** (report-only, b2e-tied, not a disclosure, not a criterion). Wiring check ONLY — one block, fake kernel, no verdict, no FER/SKR claim.

## Q5 — Focused tests (run 2026-09-21, this record)
- `.venv/bin/python -m pytest comparison_bench/tests/test_v80_b2g_campaign.py -p no:cacheprovider -q` → **39 passed** (~17.8 s): real dry-construct pins F208/F202 at seed 2026092011 (fc0/rank-full/twice + girth 6 recorded-not-gated), marginal-formula correctness vs manual einsum/triple loop (incl. row-renormalize guard + delta-at-0), XOR-centering identity, û1/genie NOT used (monkeypatched `posterior_rows_l2` raises — decode still returns the b-only marginal prior), report-only entropy/mismatch columns, gates/early-stop, refusals (2026092001 and every other seed/trials/block-base/n-blocks), root confinement, resume-once, column schema, manifest labels (second construction instance 2026092011 (O1R R2 precedent); never "byte-identical paired construction"), dry 1-block wiring probe. No production decode in tests.
- Frozen regression: `test_v80_o1_campaign.py` + `test_v80_s2c_campaign.py` + `test_v80_b2f_campaign.py` + `test_v80_b2e_campaign.py` → **165 passed** (~49.3 s; frozen modules untouched).
- Execution commands (NOT run; one arm per invocation; fresh root per arm): F208 `.venv/bin/python -m comparison_bench.src.comparison_bench.formal_ir.v80_b2g_campaign --execute-real --execution-authorized --arm F208 --root workspace/b2g_<uuid8>`; F202 same with `--arm F202` (fresh uuid8; ONLY if F208 terminal AND total elapsed at launch ≤50 min); resume ≤1 wall-partial via `--resume-from workspace/b2g_<same uuid8>` (root must equal it).

## Q6 — Budgets / stop rules
- 240 blocks/arm; ONE window ≤3600 s/arm; per-decode ≤300 s (overrun = terminal); RSS <4 GiB; 1 CPU. b2f MEASURED carried: F208 672.6 s / F202 1346.9 s (decode mean 2.77 s) — MEASURE block 0 at execution. b2e risk carried: 300-iter non-convergence ≈70 s/decode ⇒ gates must fire before the wall cap or the arm yields INCOMPLETE-wall with ≤1 continuation — NEVER a false PASS.
- STOP on ANY science-input change beyond the ONE preregistered construct-seed change (n/m/λ/block seeds/thresholds/channel/decoder/hypothesis/data roles); no rerun, no tuning, no arm substitution, no S3; no cross-arm/cross-instance pooling; PASS ⇒ report only.

## NO-GRANT marker
- This Pre-EXECUTE record + executor + tests establish readiness ONLY. Execution requires a fresh explicit user grant per arm (dual flags + fresh root uuid absence). NOT granted here; authorizes NOTHING. No campaign run, no root, and no result was produced by this record.
