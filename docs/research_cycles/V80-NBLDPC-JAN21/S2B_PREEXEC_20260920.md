# S2b Pre-EXECUTE Record (2026-09-20) — PREP ONLY, NOT A GRANT

- Track EXPLORE synthetic. G-S2B packet frozen, NOT granted; this record
  authorizes NOTHING. Execution needs a fresh explicit grant + acceptance
  of this Pre-EXECUTE (Q0–Q6) on the main thread.
- Q0 branch: `formal-ir-v72p1-addendum-clean` (no switch/commit/push done
  or planned by this task).
- Q1 scoped cleanliness: this task wrote ONLY
  `formal_ir/v80_s2_fer_campaign.py` + `tests/test_v80_s2_campaign.py`
  (S2b deltas E1–E6) + this new record. `troubleshooting.md` (M) and
  untracked journal/test files are pre-existing/out-of-scope, untouched;
  no `results/`/`outputs_comparison/` writes; no campaign run.
- Q2 frozen contract (S2b packet): single arm S2b; constructor
  `construct_l2(seed=2026092001)` on FIXED v10_peg with
  `four_cycles==0` assert (else STOP-BLOCKED), min_girth/rank recorded;
  λ={2:1} unchanged. Frame seeds `2026097001+idx`, idx=0..239
  (idx=4g+f). Sampler AND decode prior both QSC p*=0.081
  (0.40569+0.081×4.954196=0.80698, Δ+7.5e-5 vs H_L2=0.80690067),
  max_iter=300. 60 groups × 4 frames; group rule any-frame-fail⇒fail
  (`exact_match is True`); early-stop at 4th group failure → FAIL.
  Gates (AND): FER=fails/60 ≤ 3/60 AND f_super ≤ 1.3 (budget mapping
  1044/852.544≈1.2246); f_L2≈1.1376 informational only. D_blind=0
  MEASURED placeholder (NEVER-ASSUME-ZERO) + sensitivity
  Δf_super=D_blind/852.544 (16 bits≈+0.019) mandatory in result.
  B-limitation: entropy-anchored only, NOT same-law S1→S2 validation.
- Q3 authorization: user pre-authorization 2026-09-20 covers executor
  deltas + tests + this record ONLY. Quote: "do NOT run the campaign".
  No execution grant exists.
- Q4 output absence: run root MUST be a fresh `workspace/s2b_<uuid8>`
  (UUID + absence proven at run time); old roots, `results/` and
  `outputs_comparison/` forbidden. No root created by this task.
- Q5 tests green (fake-only, no production decode):
  `.venv/bin/python -m pytest comparison_bench/tests/test_v80_s2_campaign.py comparison_bench/tests/test_v80_s2_construction.py comparison_bench/tests/test_v10_peg_girth_fix.py -p no:cacheprovider -q`
  → **26 passed** (12 S2b campaign incl. new T12 + 7 construction +
  7 girth-fix), wall 5.60 s.
- Q6 command (ONLY on fresh grant + acceptance of this record):
  `.venv/bin/python -m comparison_bench.src.comparison_bench.formal_ir.v80_s2_fer_campaign --execute-real --execution-authorized --root workspace/s2b_<uuid8>`
  Budgets: wall ≤3600 s/window; per-decode cap 300 s; RSS <4 GiB;
  1 CPU. Continuation: ≤1 explicit `--resume-from` for WALL-PARTIAL
  only (same path, fresh window); terminal FAIL/early-stop never resume;
  no auto-relaunch. STOP on any science-input change.
- rg-absence (E3, 2026-09-20): `20260970xx` found ONLY in the frozen S2b
  packet+prompt docs; absent from src/tests/configs/docs-rest/openspec/
  tools/src/experiments/.codebuddy (no `.workbuddy` dir exists).
- Verdict: PREP COMPLETE — executor + tests + record ready; campaign
  execution BLOCKED pending fresh grant + Pre-EXECUTE acceptance.
