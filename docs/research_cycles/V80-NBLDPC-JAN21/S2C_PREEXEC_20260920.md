# S2c Pre-EXECUTE Record (2026-09-20) — EXPLORE synthetic

G-S2C frozen ONLY until grant; no run without fresh explicit go.
Executor: `formal_ir/v80_s2c_campaign.py` (NEW); `v80_s2_fer_campaign.py`
+ `v80_s2_peg.py` + `v10_peg.py` + kernel + `v26_channel` untouched.

Q0 branch: `formal-ir-v72p1-addendum-clean` verified; no switch/commit/push.
Q1 scoped cleanliness: tracked tree clean (`git status --porcelain`
empty); only additive untracked: module + tests + this record.
Frozen modules byte-untouched; no `results/`/`outputs_comparison/` writes.
Q2 frozen contract (packet 2026-09-20): 3 arms × fixed peg seed
2026092001 (L-A {2:1} fc0 / L-B {2:0.5,3:0.5} fc0 / L-C {3:1} fc2
PINNED; construct-twice-identical; STOP-BLOCKED); 60×4 frames
`2026097201+idx` paired; stream `s2c_emp:{seed}`; 2M empirical triple
(b~p_b; u1~g1; u2~g2; zero-mass→delta-at-0); y=b&31; GENIE true-u1 rows
(**D1: genie-aided L2 upper-bound ceiling, never a two-layer claim**);
`decode_error_domain_posterior` ONLY (scalar-p path NEVER); max_iter=300
streak default; gates FER≤3/60 AND f_super≤1.3 (f_L2≈1.1376
INFORMATIONAL; f_super≈1.2246 BUDGET MAPPING; D_blind=0 + sensitivity).
Q3 authorization: per-arm runs under user pre-authorization 2026-09-20
(continuing); one arm per invocation; STOP on any science-input change.
Q4 output absence: fresh root `workspace/s2c_<uuid8>` proven absent at
run time (`execute` refuses existing roots; old roots untouched).
Q5 focused tests: `test_v80_s2c_campaign.py` 24 passed (3.5 s, fake-only;
per-arm gates incl. L-C pinned 2, sampler/prior norms + zero-cell
fallback, refusal matrix, early-stop, checkpoint/resume, confinement).
Q6 per-arm commands (single window each; ≤1 explicit `--resume-from`
for WALL-PARTIAL only; terminal FAIL/early-stop never resumable):
`--execute-real --execution-authorized --arm L-A|L-B|L-C --root
workspace/s2c_<uuid8> [--gamma <path> --source 2M]`.
Budgets: wall ≤3600 s/window, per-decode 300 s, RSS <4 GiB, 1 CPU.
rg proof 2026-09-20: `20260972xx` only in frozen packet+prompt, new
module, new tests (all legitimate carriers); `2026098xxx` rejected
(2026098001 collides R23/v22b); no other src/tests/configs/docs hits.

Delta 2026-09-20 (wiring fix, zero execution consumed): `_construct_gate`
called `construct_fn(seed, trials)` (S2b 2-arg convention) while production
`construct_arm` takes `(arm, seed, max_trials)` → all arms refused as
`unknown arm 2026092001` pre-decode; fixed to `construct_fn(arm, seed,
trials)`, fakes reconciled to the production signature, T15 binds the
gate→REAL `construct_arm` (25 passed); dry-construct via gate path L-A
fc0/L-B fc0/L-C fc2 all PASS. Re-launch under standing pre-authorization;
the three refused roots were never created (UUIDs reusable).
