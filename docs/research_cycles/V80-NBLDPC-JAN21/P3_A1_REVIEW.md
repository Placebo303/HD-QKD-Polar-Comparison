# P3 A1 Census Arm — INDEPENDENT Pre-RESULT Review (Acceptance ID G-P3, arm A1)

- Reviewer: `reviewer-go` (taste instance, `step-5-preview`). Independent thread; **read-only** except this file. No file edited, no commit/push, `src/` untouched.
- Date: 2026-09-21. Track: **DECIDE**. Gate: **Pre-RESULT** (`AGENTS.md` §10.3), before main-thread acceptance.
- Contracts of record (read in full): `P3_CENSUS_PACKET.md` (§§1–12 incl. amendment A6/§3A), `P3_CENSUS_PREREG_AND_AUTH.md`, `P3_CENSUS_PROMPT.md`, `P3_STAGE05_PACKET.md` + `P3_STAGE05_REVIEW.md` (predecessor gate), `docs/CORRELATION_ALIGNMENT_CAPABILITY_20260921.md`, `docs/PREALIGN_CENSUS_PREFLIGHT_20260921.md`, `docs/TTBIN_MEMBER_SEMANTICS_20260921.md`.
- Evidence root verified: `workspace/p3_census_3954637c/` — **11 files exactly** as listed in RESULT_SUMMARY (`T2-{1M,1.5M,2M}.json`, `split_manifest.json`, `alignment_table.md`, `H_full_table.md`, `memory_stationarity.md`, `census_table.csv`, `census_table.json`, `PRE_EXECUTE.md`, `RESULT_SUMMARY.md`). No extra, no missing.
- Method: gates re-derived from recorded scalars and from the actual new code (`comparison_bench/src/comparison_bench/io/align_wrapper.py`, `cli/p3_census_a1.py`); frozen `src/` sign/bin conventions re-read at source; one header-accessor-only live spot check on the T2-2M base member (no drain, no histograms); fake-only tests re-run (18 passed); item-7 arithmetic recomputed with `.venv/bin/python`. The census artifacts are the object of review; no events were drained and no entropy/histogram was computed by this reviewer.

## Overall verdict: **PASS_WITH_FINDINGS**

Every recorded number re-derives; every §3A gate passes with margin; every prohibition holds; the control gate passes; protected roots and `src/` are untouched; the claim ceiling holds. No blocking science defect. However, (a) one prompt-required deliverable — the §9 conditional certification-arithmetic table — was **not emitted** by the operator (reconstructed verbatim in §7 below); (b) that table exposes the review's most consequential result: **the frozen A208 design point is OUT OF BOX for T2-1M and T2-1.5M, and A200 is out of box for T2-1M**; (c) the authorization paperwork has a gap (unsigned signature block; grant by verbatim chat message) the main thread should ratify. Items F-1 and the main-thread decisions below should be closed before `INDEPENDENT_ACCEPTANCE.md` is written.

## Numbered findings

| # | Item | Verdict | Evidence | Severity |
|---|---|---|---|---|
| 1 | Alignment procedure fidelity | **PASS** | `align_wrapper.derive_alignment` calls the frozen `compute_cross_correlation_histogram(events, ch_a=1, ch_b=5, bin_width_ps=100, max_lag_ps=819200)` → 16384 bins (frozen code re-read: `n_bins=ceil(2·819200/100)=16384`, edges from −819200 pinned at +819200, lag `t_B−t_A`), `pk=argmax(counts)`, `offset_ps=int(round(lag_center_ps[pk]))` — bin centre only, no interpolation. Sign `offset=+peak_center` matches `_pair_nearest_unique` (`src/qkd_io/ttbin_pipeline.py:219-249`: `a = t_a + offset_ps`). Once per dataset on the merged base-member stream (`read_ttbin_events(base)`; `parse_bases` rejects any `.1.ttbin` path). Recorded bin geometry matches §C2: bin 8191 = [−100,0) → −50.0 (T2-1M); bin 8192 = [0,+100) → +50.0 (1.5M/2M). | none |
| 2 | Acceptance gates | **PASS** | Re-derived from recorded scalars: p2bg 1186.35 / 748.23 / 546.07 ≥ 100 (margins 5.5–11.9×); crude sigma 66.41 / 70.71 / 70.92 ps ∈ [10,500]; single dominant mode — `n_secondary_violations=0` on all three (5317/5310/5372 local maxima, none >50% of peak outside ±1000 ps); channels non-empty (count_A 2.31M/3.29M/4.45M; count_B 3.23M/4.51M/6.06M). No STOP-BLOCKED occurred (align_status `ok` ×3). No fallback to 0 or to the recorded offset: pairing consumed `offset = aw.require_alignment_passed(align)` (the DERIVED value); prior offset appears only in `offsets_agree_one_bin`. | none |
| 3 | One-bin A1 comparison | **PASS** | `offsets_agree_one_bin` enforces |Δps| ≤ 100 AND \|Δbin\| ≤ 1; recorded `agree_one_bin=true`, `delta_ps=0`, `delta_bin=0` for all three. `write_shared_tables` emits a `FINDING` flag when `agree_one_bin` is false — a >1-bin disagreement would be surfaced, never adjusted. | none |
| 4 | Prohibitions | **PASS** | Base-member-only: single `FileReader(base)` per dataset + frozen loader on the same path; `.1.ttbin` paths rejected by `parse_bases`; repo-wide grep of the root and modules shows no both-member open and no stream concatenation (the only `np.concatenate` is bootstrap index assembly). No pairing/entropy before alignment pass: `require_alignment_passed` raises otherwise (fake-tested); all three passed before any pairing. No event arrays on disk: per-dataset JSONs carry scalars plus `per_frame_weight_hist` (3–4 entries) and `block_Hfull_series` (derived per-block H_full, required by §7.2); zero time/channel/event arrays. Families A/C/D never opened (hard-restricted `DATASET_IDS`). | none |
| 5 | Estimator rigor | **PASS** | N = raw coincidence counts 525831 / 735780 / 982182 (`frame.size`; +2.7/+3.9/+5.3% over pool operating N, recorded, not tuned). Support 2395/2439/2597; occupancy = K/1024² = 0.00228405/0.00232601/0.00247669 (arithmetic verified). Plug-in H_full 0.7981344445/0.8249782282/0.8314077735; H_L1+H_L2 = H_full exactly (0.02471853+0.77341591). MM = (K−1)/(2N·ln2) with K = occupied joint cells and N = TRAIN-pool count: back-solving recorded MM ⇒ implied TRAIN N 315504/441487/589461 vs 60%-of-full 315499/441468/589309 (frame-split residual) — frozen formula confirmed. Bootstrap: 200 resamples (floor), frozen seed 20260921 recorded, frame-level resampling with replacement; CI half-widths 0.00285/0.00221/0.00193 ≤ 0.02 support bar. Split side stated per row: "TRAIN plug-in (held-out gap reported separately; never quoted as held-out)"; anchor identified as the V49 TRAIN-pool plug-in with its own +0.0204 train→hold gap noted. Two transparency notes: (i) the bootstrap interval does **not** bracket the point estimate (e.g. T2-1M: point 0.79813 > CI_hi 0.79745; same pattern ×3) — expected downward duplicate-support shift of frame-resampled plug-in entropy; magnitude small, recorded transparently, CI_hi must not be read as an upper bound on H_full; (ii) the TRAIN-pool N is not directly tabulated (exactly recoverable from the MM formula). | non-blocking |
| 6 | Control gate | **PASS** | T2-2M (control; V80 frozen-channel source): 0.8314077735 − 0.83256272 = **−0.0011549**, \|Δ\| = 0.00115 ≤ 0.01 ⇒ **PASSES** (verdict correctly flagged conditional on the `[TO BE CONFIRMED]` tolerance). T2-1.5M: −0.0075849 ⇒ within ±0.01 (passes; not the control). T2-1M: −0.0344283 ⇒ **NOT** within ±0.01, correctly recorded `False` and reported as a per-source finding, not a machinery failure. | none |
| 7 | Derived design-point consequence | **PASS** (computed; see §7 below) | Recomputed with `.venv/bin/python` under the frozen accounting `f_super=(5m+64)/(1024·H) ≤ 1.3`, `m_max(H)=⌊(1.3·1024·H−64)/5⌋`, `N≥⌈3·4.785675/(1.3−f)⌉`. Thresholds: A208 requires H ≥ 0.829327 (=1104/1331.2); A200 H ≥ 0.799279; A199 H ≥ 0.795523. Result: **T2-1M is OUT OF BOX at m=208 (f=1.350806) and at m=200 (f=1.301864); T2-1.5M is OUT at m=208 (f=1.306853); only T2-2M supports A208 (f=1.296746, margin 0.0021 b/sym)**. At each source's own m_max, required N (3376/2881/4413) far exceeds the frozen per-source superframe counts 500/691/911 and the pool 2103 — reconfirming packet §9.2's structural uncertifiability. **Operator omission (F-1): this table is absent from the evidence root** (grep `f_super|headroom|m_max|4.785675|0.829327` → zero hits) although prompt item 11 requires it. | **high (decision-bearing); omission non-blocking-to-science** |
| 8 | Memory / stationarity | **PASS** (interpretive finding F-4) | Code re-read: mean-centered lag-k ACF; drift = per consecutive 4-frame-block H_full via the same F03 estimator, max−min + least-squares slope over block index. Recorded: weight mean 0.241/0.259/0.262, median 0, max 2/3/3, p99 1; slopes −3.2e−9/+1.7e−9/−4.7e−10; all \|acf\| ≤ 0.005 (mismatch lag1 0.0017/0.0004/0.0048; H_L1 lag1 −0.0010/−0.0016/−0.0007). Structural caveat recorded: measured pairs/frame = 1.013/1.019/1.026 ⇒ ~4 pairs per 4-frame block ⇒ block-H_full is small-sample noise; max−min must not be read as drift. | see F-4 |
| 9 | Budgets | **PASS** | `read_wall_s` 0.413/0.329/0.471 s ≤ 300; `dataset_wall_s` 687.6/1053.1/1246.4 s ≤ 1800 each; total 2987.4 s ≤ 5400; reads/dataset = 1 base read (+1 same-file liveness `FileReader` open, closed in `finally`) ≤ 2; peak RSS 776964 KiB ≈ 758.6 MiB < 4 GiB; 0 decoder/DE/graph/`tools/*` calls (module contains none); no repair-rerun used. Wall/RSS are printed/reported but not persisted as a machine artifact (cosmetic, same as Stage 0.5 finding 8b). | cosmetic |
| 10 | Protected roots + frozen baseline | **PASS** | Independently re-snapshotted: `results/` = **0 files / 0 B**; `comparison_bench/outputs_comparison/` = **1446 files / 554423395 B** — identical to the frozen snapshot (pre-existing permission-denied legacy pytest dirs excluded from both counts). `git diff -- src/` **EMPTY** (re-verified). Branch `formal-ir-v72p1-addendum-clean` @ `83cf848c` as recorded in Q0; no switch, no commit, no push. Packet/prereg/prompt mtimes (10:31/03:25/03:26) all **precede** the run (first artifact 10:53) — no post-hoc contract edits. | none |
| 11 | Claim ceiling | **PASS** | Grep over the root: the only FER/SKR/operating-point/route/qualification/publication strings are the negative ceiling statements in `RESULT_SUMMARY.md` L67–69; no artifact asserts any of them. `H_full` is stated CONDITIONAL on the derived alignment (RESULT_SUMMARY header; §3A.6). Split-side labelling present per row. | none |
| 12 | Pre-EXECUTE adequacy | **PASS** (governance finding F-3) | Q0–Q6 all recorded with actual outputs: branch/HEAD, alias smoke, absence proofs, before/after root snapshots (identical), 18 fake-only tests (independently re-run: **18 passed**), Stage 0.5 entry evidence, double Q6 absence proof. Content frozen pre-launch; post-run materialization into the root is documented (CLI owns root creation, refuses pre-existing roots). | see F-3 |

## Additional findings

- **F-1 — Missing §9 conditional-arithmetic deliverable (severity: high, decision-bearing; non-blocking-to-science).** Prompt item 11 requires reporting the `content/f_super/headroom/N` table per measured `H_full`; the evidence root contains no such table and `RESULT_SUMMARY.md` omits it. The arithmetic is deterministic and is reconstructed verbatim in §7 below. Recommended closure before `INDEPENDENT_ACCEPTANCE.md`: the operator appends the table (documentation completion; **no data re-read, no re-execution**) or the main thread adopts this review's table with provenance recorded.
- **F-2 — Bootstrap CI does not bracket the point estimate (severity: low, informational).** All three CIs sit entirely below the plug-in point estimate (e.g. T2-1M 0.79813 vs [0.79176, 0.79745]). Consistent with the duplicate-support downward shift of frame-resampled plug-in entropy; the half-widths (≤0.0029) remain the valid uncertainty magnitude and pass the support gate with 7× margin. Do not read CI_hi as an upper bound on H_full (the MM value sits above it by construction).
- **F-3 — Authorization paperwork gap (severity: moderate, governance).** `P3_CENSUS_PREREG_AND_AUTH.md` §7 signature block is **unsigned** (status `DRAFT_PENDING_AUTHORIZATION`); prompt item 1's STOP condition ("signed PREREG naming branch, dataset list, config path, seed, tolerances, wall ceiling") was not met literally. The actual grant is the verbatim user message "允许你开始Stage 0.5与A1" recorded in `PRE_EXECUTE.md`, which names the A1 branch (packet §12's requirement) but not the seed (20260921), tolerances (±0.01 / 0.02) or ceiling (5400 s) — those were operator-frozen from the packet's own proposed values and are recorded transparently, with verdicts flagged conditional. Scientific impact nil (control passes with 8.7× margin; CI with 7×), but the paperwork chain has a hole the main thread should ratify explicitly; OpenSpec task P3-T5 ("SIGNATURE BLOCK filled") is formally incomplete.
- **F-4 — Memoryless-assumption caveat is under-stated for the ACF battery (severity: moderate, interpretive).** The recorded caveat correctly neutralizes the block-H_full max−min column, but the RESULT_SUMMARY then says "the slopes (≈0) and autocorrelations carry the stationarity signal" without noting that at ~1 pair/frame the per-frame mismatch count is ~Bernoulli(0.25) and per-frame H_L1 is near-degenerate (0 for single-pair frames), so the ACF battery has power only against frame-to-frame correlations ≳0.005 (SE ≈ 1/√n_frames ≈ 0.0010–0.0014; all observed \|acf\| within ~3.5 SE of zero) and **cannot probe frame-internal memory** — precisely the mechanism that would break `gamma_f03` transferability (Q2). The memoryless symbol-level assumption is **NOT FALSIFIED**, but "not falsified" ≠ "validated": real acquisitions carry ~1 pair per 204800 ps frame while V80 synthetic frames carry 256 symbols, so the V80-symbol-level memory question remains OPEN. Bound any main-thread wording accordingly.
- **F-5 — Block-count source deviation, declared and caveated (severity: minor).** Packet §2 mandates the frozen pool block counts (2000/2767/3645 frames → 500/691/911 superframes); the census `n_superframes` column reports 129763/180536/239420 from the **measured raw-stream** frame counts (519054/722145/957682), with the structural caveat recorded. §7.2 ("split the acquisition") supports the measured choice and the census declared one source without mixing R14 — but the two superframe counts are different quantities and must not be conflated with §9's 500/691/911. No gate used `n_superframes`, so no conclusion is affected; main thread should ratify the source.
- **F-6 — Deliverable layout deviations (severity: cosmetic).** Prompt item 15 asks for separate per-dataset `ttbin_config.json` / `ttbin_metrics.json`; the census embeds `ttbin_config`, `qber`, `mutual_information_bits` inside the per-dataset JSON instead. Content complete; naming/layout differs. `P3_CENSUS_RESULT.md` is named `RESULT_SUMMARY.md` (as in Stage 0.5).
- **F-7 — Stale prompt text, no operator fault (severity: informational).** Prompt item 4 still carries the SUPERSEDED "read each member separately; exactly one yields events" rule; the operator correctly followed the amended packet §2/A1 + Stage 0.5 (base-only, never both). Main thread may annotate the prompt.
- **F-8 — Hold-gap convention (severity: informational).** Census hold gaps are covered-only (91/109/118 uncovered of 105130/147141/196458 HOLD pairs, counts reported); the anchor's own +0.0204 gap used a different split/pool. The RESULT_SUMMARY states "different split; reported, not pooled" — adequate; do not subtract the census gap from the anchor gap.

## 7. Item-7 design-point table (verbatim reconstruction; frozen accounting)

`content = 1024·H`; `m_max(H) = ⌊(1.3·1024·H − 64)/5⌋`; `f_super(m) = (5m+64)/content`; `N_req = ⌈3·4.785675/(1.3 − f)⌉` (∞ when f ≥ 1.3).

| dataset | H_full (TRAIN plug-in) | content | m_max(H) | f @ m=208 | f @ m=200 | f @ m=199 | N_req @208 | N_req @200 | N_req @199 | f @ m_max | N_req @ m_max |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T2-1M | 0.7981344445 | 817.2897 | **199** | **1.350806 — OUT** | **1.301864 — OUT** | 1.295746 | ∞ | ∞ | 3376 | 1.295746 | 3376 |
| T2-1.5M | 0.8249782282 | 844.7777 | **206** | **1.306853 — OUT** | 1.259503 | 1.253584 | ∞ | 355 | 310 | 1.295015 | 2881 |
| T2-2M (control) | 0.8314077735 | 851.3616 | **208** | 1.296746 (IN) | 1.249763 | 1.243890 | 4413 | 286 | 256 | 1.296746 | 4413 |
| anchor (V49 TRAIN) | 0.83256272 | 852.5442 | 208 | 1.294947 (IN) | 1.248029 | 1.242164 | 2842 | 277 | 249 | 1.294947 | 2842 |

Admissibility thresholds: **A208 needs H ≥ 0.829327**; A200 needs H ≥ 0.799279; A199 needs H ≥ 0.795523.

**Design-point implication (explicit):** on the census's measured H_full, the frozen V80 **A208 arm is inadmissible (out of box, f > 1.3) for T2-1M and T2-1.5M**, and the **A200 arm is inadmissible for T2-1M** (f = 1.301864); T2-1M first becomes admissible at m ≤ 199, where the zero-failure requirement (N ≥ 3376) still exceeds its available superframes (500 pool / 129763 measured raw blocks of ~4 pairs — neither is the 256-pair synthetic superframe). Only the 2M control supports A208, and even there N_req = 4413 (measured) / 2842 (anchor) ≫ 911. Combined with packet §9.3 ("lower H buys headroom" is false at fixed m), the only genuine lever is the (H_full, m_min) pair — and the census supplies H_full, not m_min. **The route/design-point decision must therefore not assume A208 (or A200) for the 1M/1.5M sources; any V80 design-point claim on real data must be re-derived per source from this table.** This is a property of the measured channel, not an operator error — but its absence from the operator's result record (F-1) is what this review must surface.

## Memoryless-assumption statement (item 8, honest assessment)

Supported to the limit of the battery's power: drift slopes ≈ 0 (|slope| ≤ 3.2e-9) and all frame-to-frame autocorrelations within ±0.005 (≤ ~3.5 SE) do not falsify the memoryless symbol-level assumption behind `gamma_f03`. But the battery operates at ~1 pair/frame, so it is nearly degenerate at the per-frame level and structurally blind to frame-internal memory; the recorded caveat covers the drift statistic only. Correct main-thread wording: "memorylessness not falsified at measurable granularity; V80-symbol-level memory question remains open."

## What the main thread must decide before accepting

1. **Confirm the two `[TO BE CONFIRMED]` tolerances** (±0.01 control, 0.02 support) — both gates pass with large margins, so confirmation is formal, not decision-relevant.
2. **Ratify the authorization chain**: accept the verbatim user grant in place of the unsigned signature block (or require signature retroactively); record that P3-T5 is thereby completed administratively.
3. **Confront the design-point implication (§7)**: A208/A200 out of box for 1M/1.5M; only 2M carries A208 with a thin 0.0021 margin and an uncertifiable N. Decide whether the headline source remains 2M and whether any arm below A199 is even worth evaluating.
4. **Close F-1**: require the operator to append the §9 conditional table to the root (no re-execution) or adopt this review's reconstruction with provenance — before `INDEPENDENT_ACCEPTANCE.md`.
5. **Bound the memory wording** per the statement above; decide whether a higher-power memory probe (e.g. pair-level burst statistics within coincidence windows, or a re-framed 256-symbol pool battery) is needed before synthetic→real transfer is claimed.
6. **Ratify the block-count source** (measured raw-stream frames vs frozen pool counts) and keep the two superframe quantities separate from §9's 500/691/911.

## Checklist

- [x] Matches OpenSpec/frozen spec (packet §§1–12 + §3A; contract unmodified post-run)
- [x] Tests pass (18/18 fake-only, independently re-run; no production invocation)
- [x] No scope creep (trio-only; additive modules; no forbidden computation)
- [x] Protected roots byte-identical; `src/` frozen (diff empty)
- [x] Claim ceiling holds (H_full CONDITIONAL on derived alignment; no FER/SKR/route claim)
- [ ] docs/decision-log.md or docs/troubleshooting.md update needed? — **Candidates, non-blocking**: (i) persist wall/RSS as a machine artifact in the root; (ii) bootstrap CI for sparse plug-in entropy does not bracket the point estimate; (iii) ~1 pair/frame real acquisitions make frame-level memory batteries near-degenerate. The A208/A200 out-of-box result is a decision-log-grade finding for the main thread's route decision, not a failure mode.
