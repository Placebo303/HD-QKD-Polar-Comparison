# R10 CTRL (m100, same-session control) — PREREG_AND_AUTH_CONDENSED (frozen)

Track: DECIDE (preregistration record only; no execution, no data access, no commit/push in this file).

## C-pop (frozen population)

- Same session as R9 prior source; C-pop window frames 1983..2046 (64 frames; 2046−1983+1=64) via shift rule after V55-hit at frame 1982, blocking K=2 even-odd → 128 calls.
- Same m100 candidate, graphs (seeds 4720-4721 pattern), decoder, T=64, 564-bit block disclosure, H-pair, F-gate, and budgets as R9 CONFIRM (no retune; control-contrast only).

## C-contrast (frozen decision rule)

- Contrast accepted-count R10 vs R9: ≥5 supports prior-match line; ≤2 routes to rate-adaptive track. Recorded, not re-judged.

## C-prior

- Prior-file-now-in-session: R10 prior source session == target window session (R9 cross-session caveat not carried).

## C-auth (frozen authorization)

- Execution grant "继续往下" consumed once — single invocation; no rerun/repair/retune. No SKR/qualification/promotion/generalization claim.
