# 2026-09-09（主线程：A3/A4 裁决 + R1c-A5 任务包）

## 一、A3/A4 复核（只读；对象 = HEAD `1a09220a`，branch `formal-ir-v72p1-addendum-clean`，ahead origin 62）

- A3 4 提交（`cfd5074f` prereg → `88d89631` impl+forensic → `fb6a42dd` review accept
  → `9d99d07f` evidence solidify）与 A4 5 提交（`c45fdb89` → `6c6dc68b` → `c11c852a`
  → `e142f560` → `1a09220a`）在本地均存在且未 push，文件范围与包一致。
- 六文件根 `workspace/d6_graph_mother_r1c_dd8c4defe67742a8b2bc1b634c116d6b` 存在、
  大小/mtime 与 A2 记录一致（15:17:52 等）；`cycle_state.yaml` 全执行键 false，
  `evidence_root`/`terminal` null，`next_gate=D6_GRAPH_MOTHER_R1C_A3_BLOCKED_AWAITING_MAIN_THREAD_ROUTE`。
- 结论：A3 `PASS_BLOCKED_RUN` 与 A4 `READY` 的固化、评审、memory/decision-log 均已落地，
  主线程**采纳**。R1c-A2 的 184 调用不得复用；topology-no-recovery 不得引用。

## 二、主线程新增只读审计（零 decoder，结构only）— 决定路线

脚本与产物（任务外，仅作交叉核对）：
`workspace/mainthread_a5_audit_7f3c1d9ab2e54a6f8c0d1e2f3a4b5c6d/`
（`audit_row_degree.py` / `row_degree_audit.json|csv` / `diagnose_blocked.py`）。

核心事实：冻结 eligibility（`d5.audit_prefix.passed` + runner 的 dup 规则）只检查
**变量度**，从不检查**检查行度**；`audit_extra` 只记 `row_degree_max/sumsq`。

| 臂 | f1.0 | f1.2 | square | 冻结闸门判定 |
|---|---|---|---|---|
| B0/B1 | 3 | 3 | 3 | n64 过；n128 L2 base-pair dup；n256 前缀不连通（components 11/41） |
| T1 | ≥2 | ≥2 | ≥2 | 全宽/层/前缀合格 |
| T2 | ≥2 | ≥2 | ≥2 | n64 rank 63/62≠64（不合格） |
| T3 (SC w4) | 3 | **1**（10–81 行） | **1**（12–81 行） | 补 I1 后不合格 |
| T4 (SC w8) | ≥2 | **1**（8–40 行） | **1**（8–77 行） | 同上 |
| M1 (forest MAX) | 2 | **1**（8–40 行） | **1**（14–83 行） | 同上 |
| M2 (forest HALF) | 2 | 2 | **1**（L2，6–25 行） | n64 rank 48/61；n256 L2 rank 200 |

- 机制：`[k_min, m_max)` 区间的行只可能被“扩展边”命中，窗口只覆盖 1 次即度=1。
- 交叉验证：用冻结 eligibility 复算得到的选中臂集合恰为 {B0,B1,T1,T3,M1}，
  与 A2 实际 dispatch 完全一致 ⇒ 审计路径可信。
- 关键推论：只补“最小检查行度≥2”闸门后 n64 仅剩 {B0,B1,T1}，且无合格 M 臂 ⇒
  §8.4 fallback 失去 M 腿、实验近乎空转。**因此路线必须是“修复 + 闸门”，不是纯闸门、
  也不是停线。**

## 三、主线程裁决与下一步

- 裁决：R1c-A3 采纳（blocked run 固化）；R1c-A4 采纳（READY，仅结构路径）；
  D6 路线 = **有效性收口 + 可采纳构造修复 + R1d 就绪包**（`D6 R1c-A5`）。
- 已下发任务包（零 decoder）：
  `.workbuddy/tasks/D6_R1C_A5_VALIDITY_REPAIR_R1D_READINESS_TASK_PACKET.md`
  + 同名 `_PROMPT.md`。
- A5 冻结的修复可采纳性：单一定律、无搜索/调参、保留全部冻结旋钮（arms/rows/SC w/
  M 的 N2/两区结构/系数流/族身份）、I1 与全部既有硬门在每宽度/层/前缀成立、
  报告结构差异；不可行则给 `STRUCTURALLY_INFEASIBLE_AS_FROZEN` + 最小重定义菜单
  （需主线程裁决，不得落地）。生产 builder 在 A5 保持字节不变；落地的只有 I1 闸门
  与执行期 crash 优先级终局。
- 待主线程下一步：A5 返回后裁决 (i) 修复是否落地为 R1d 生产路径、(ii) R1d 执行契约
  （同种子/同预算/新根/无复用）与 (iii) 是否给出 R1d Pre-EXECUTE + 显式授权。
  G2 仍未授权、未执行；所有执行键保持 false；不 push。

## 四、性能追问（用户提出“很多任务特别慢，能不能也加快”）→ A5-10 + A6

- 已解决：用户当初遇到的“structure n=256 卡 55+ 分钟”正是 A4 修掉的那一段
  （scaling 10897.7s→2.5s，n64 42.2s→21.0s；3× replay→2×；overflow 二次重建删除）。
  T1_PEG BFS 已不再重要（非 T2 臂 n256 合计 1.5s）。
- 唯一剩余慢点 = **T2_CYCLE_GREEDY**：候选 `|B|(|B|-1)/2*(|C|-2)` = 4.67M/76.7M/1.24B
  （n64/n128/n256），每候选 ~8.3–8.9µs（主线程 micro-bench：
  `workspace/mainthread_a5_audit_7f3c1d9ab2e54a6f8c0d1e2f3a4b5c6d/t2_cost_model.py`，
  four-only 前缀 0.27µs ≈ 5.3× 便宜），单层 wall ≈41s/650s/2.85h。
- 风险：A5 修好 T2 后它很可能成为冻结 `best_T` fallback ⇒ scaling 重新吃 2.85h/layer，
  撞 chunk 5400s 熔断。因此 A5 加 §7b/A5-10 结构成本投影，并新增 A6 性能包。
- 已下发：
  `.workbuddy/tasks/D6_R1C_A6_STRUCTURE_PERF_T2_ACCELERATION_TASK_PACKET.md` + `_PROMPT.md`
  （A5 之后执行；等价原理 = 键末字段排序三元组唯一标识候选 ⇒ argmin 唯一且顺序无关，
  故允许向量化/分阶段过滤，禁止改键/候选集/贪心顺序；目标 T2 n64≤5s、n128≤90s、
  n256≤600s，A4 收益不得回退，零 decoder；另含仓库级慢任务只读清单）。
- 科学诚实性记录：T1/B0/B1 两次 canary 均 0 信号；而 SC(T3/T4) 与 accumulator(M1/M2)
  族**从未产生过一条有效 decoder 记录**（全 crash 或从未派发），其科学状态是“未知”
  而非“阴性”——修复路线正是让 D6 问题变得可回答的唯一途径。

## 五、用户指令：A5/A6 合并为一次执行，之后主线程审查

- 已产出**唯一权威**任务包：
  `.workbuddy/tasks/D6_R1C_A5A6_VALIDITY_REPAIR_AND_PERF_TASK_PACKET.md` + `_PROMPT.md`。
- 单独的 A5/A6 包已加 `SUPERSEDED` 标注（被合并包整体吸收，仅追溯用，不再执行）。
- 合并包结构：§0 授权/裁决 → §1 决定事实 → §2 统一硬边界 → §3 Track A（A5-01..A5-10、
  A5-A01..A5-A18）→ §4 Track B（A6-01..A6-04、A6-A01..A6-A08）→ §5 三个独立只读评审
  （A5 实现 / A5 有效性就绪 / A6 性能，各最多一轮 rework）→ §6 七段提交 → §7 验收表+返回契约。
- 执行顺序硬约束：Track A 代码先落地（同一批文件），Track B 再在 A 闸门之上做基线→优化；
  A6 等价门第 7 条要求对 A5 沙箱修复版 T2 也重跑等价（无论 R1d 最终落地哪个 T2，
  加速都已被证明）。
- 主线程下一步（用户“然后再审查”）：operator 返回后由我做裁决——修复是否落地 R1d 生产路径、
  R1d 执行契约（同种子/同预算/新根/零复用）、是否给 R1d Pre-EXECUTE + 显式授权。
  G2 仍未授权、未执行。
