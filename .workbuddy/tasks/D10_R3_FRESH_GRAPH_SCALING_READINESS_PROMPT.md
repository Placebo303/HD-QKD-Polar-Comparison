# Copyable prompt — D10 R3 fresh-graph scaling readiness

Work in `D:\Code\HD-QKD_Polar_Comparison` and follow `AGENTS.md` plus
`.workbuddy/tasks/D10_R3_FRESH_GRAPH_SCALING_READINESS_TASK_PACKET.md` exactly.

Accept the reviewed A1 terminal unchanged. Implement R301--R310 as a bounded
readiness task for six fresh n128 graph pairs × 12 paired blocks, with n256 on
six other fresh graph pairs only if n128 meets the frozen R3_REPRODUCED gate.
Reuse the accepted D10 R2 constructor/decoder path; do not loosen A1 thresholds,
pool A1 observations, search seeds, or introduce a generalized framework.

This prompt authorizes OpenSpec/code/docs, focused fake tests and no-decoder
PROFILE_ONLY only. It does not authorize `--r3-batch`, any scientific decoder,
L2/APP, D7-H, real data, commit or push. Obtain one independent readiness review
with actual artifact access. Trust its traceable tests without ceremonial
duplication.

Return only at the packet ready terminal or first STOP condition, with the
specified evidence contract. Execution will require a later explicit grant.
