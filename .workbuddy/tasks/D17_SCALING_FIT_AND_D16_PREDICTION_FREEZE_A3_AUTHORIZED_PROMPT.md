# D17 Scaling Fit + D16 Prediction Freeze — A3 Authorized Prompt

I accept `D17_DE_R2_COMPLETE_REVIEWED_AWAITING_SCALING_FIT_DECISION` within
its independently reviewed scope and explicitly authorize
`D17_SCALING_FIT_AND_D16_PREDICTION_FREEZE_A3` once on branch
`formal-ir-v72p1-addendum-clean`, under:

`.workbuddy/tasks/D17_SCALING_FIT_AND_D16_PREDICTION_FREEZE_A3_TASK_PACKET.md`

Amend OpenSpec first and implement only the minimal fit/verify path. Read the
named A2 DE root and five finite roots explicitly; aggregate one binomial
cluster per root/profile/n/m/graph. Never fit pooled audit totals, APP/joint/
D16 data, syndrome-valid, undetected, residual covariates, or decoder rows as
independent graphs.

Freeze the exact `--scaling-fit --execution-authorized` command in code,
OpenSpec, prompt, and D17 log. Run the focused fake/math/aggregation/
identifiability/prediction tests and pre-dispatch checks. Only if all pass,
execute that frozen command exactly once into:

`workspace/d17_finite_scaling_fit_5b6d71c8-9e42-4e64-b1c3-73a1f20d8e95`

This grant permits zero DE/decoder/CAL/VAL calls, exactly six named input roots,
300 s wall, RSS strictly below 2 GiB, and one CPU process. No retry, resume,
model substitution, tuning, or outcome-driven change is authorized.

If all models identify, independently review and freeze the three D16
prediction records with all outcome fields `BLANK`, then return
`D17_SCALING_FIT_COMPLETE_PREDICTIONS_FROZEN_AWAITING_D16_AUTHORIZATION`.
If any model is not identifiable, write no filled predictions and return
`D17_SCALING_MODEL_NOT_IDENTIFIABLE_AWAITING_ROUTE_DECISION`.

This grant does not authorize D16 execution, another DE batch, decoder or
real-data work, commit/push, D7-H, route closure, or FER/leakage/SKR/
qualification/optimality/publication claims.
