# D7-D acceptance + BP interface readiness operator prompt R1

你是本轮可长时间自主托管的高级研究工程操作员。唯一权威任务包：

`D:/Code/HD-QKD_Polar_Comparison/.workbuddy/tasks/D7_D_ACCEPT_BP_INTERFACE_READINESS_R1_TASK_PACKET.md`

完整阅读任务包、`AGENTS.md`、`AGENT_PROJECT_MEMORY.md`、D7-D 冻结包/结果/Pre-RESULT、
layer-interface OpenSpec proposal/design/spec/tasks 与其独立 proposal review，然后按
A01→A20 连续完成。

主线程已经裁决：D7-D 仅按
`D7_D_RESULT_ACCEPTED_SCHEDULE_EFFECT_INCONCLUSIVE` 接受；43 vs 40、3 个 layered-only
只能报告，不能宣称 layered 优越。主线转 BP Alternative A；R1d 降为可选暂停，不执行。

你可以自主完成文档收口、OpenSpec delta、生产实现、测试、一次允许的 scoped rework、两个
独立评审和本地提交。不要因普通工程判断、测试修复、历史消费者数量、运行较慢或包外脏树
回来询问。只有全部完成，或出现会改变冻结科学/接口合同的单一 blocker 时返回。

硬边界：

- 零 claim-bearing/scientific decoder 调用；仅 tiny in-memory 测试 fixture。
- 不读 Model-F/CAL/VAL/real/raw/VOID 内容，不运行任何 `--phase`、R1d、G1/G2。
- Alternative A only：显式 provenance + cross-layer fail-closed；不得实现强制额外 sweep、
  warm-start 机制、alternating/joint BP 或 D7-E。
- 不改变 v35 hard-decision、stopping、belief 数值和 recurrence。
- 先 OpenSpec 后代码；按任务包四段 scoped commit，不 broad-stage、不 push。
- `docs/v35-algorithm-development-report.md` 的未提交 READY rewrite 已被主线程拒绝；记录处置后
  恢复 entry-HEAD 权威内容，不采信其中数字。
- 独立 reviewer 不得编辑；FAIL 只允许任务包规定的一次 scoped rework，之后重新评审。
- D7-D/D7-C/D7-B 与全部正式证据根 immutable；所有授权保持 false。

完成后只按 A20 报 delta，使用任务包指定的精确结束句。不要重复项目全史，不要自行冻结或
执行 D7-E。
