# D7-E Acceptance + D7-F Reverse-Order Readiness R1 Prompt

在 `HD-QKD_Polar_Comparison`、分支 `formal-ir-v72p1-addendum-clean` 执行：

`.workbuddy/tasks/D7_E_ACCEPT_D7_F_REVERSE_ORDER_READINESS_R1_TASK_PACKET.md`

完整读取任务包、根 `AGENTS.md`、D7-E prereg/packets/addenda、已固化七文件根、原 Pre-RESULT review、verify appendix、R18 addendum、D7-E state，以及 BP provenance OpenSpec。按 Phase A→F 长时间自主推进，只回 §10 delta。

先按冻结口径受限接受 D7-E，再在任何实现前冻结 D7-F。D7-F 只比较两个完整两层臂：forward L1→L2 与 reverse L2→L1；不得添加第三 stage、feedback、alternating、joint 或 turbo 框架。每次 transfer 必须由 exact `CHECK_UPDATED` posterior 触发，source exact 不是门；target 冷启动且自己的 syndrome 只消费一次，绝不回传 target posterior。

这是零 scientific decoder 的 readiness 任务。允许自主完成文档、最小实现、fake/tiny 测试、独立 implementation review、Pre-EXECUTE 和本地 scoped commits；不得生成 UUID、翻授权、创建 D7-F 科学根或执行 D7-F/R1d/G1/G2/CAL/VAL/real/raw。reviewer-go 的独立测试结果可直接作为有效证据，不重复完整套件。

任何科学歧义、前驱漂移、证据重复计数风险、scope 扩张或 in-scope regression 立即 STOP，不自行调参或扩修。双评审 PASS 后停在 fresh explicit authorization 门；不请求授权、不 push、不清理包外脏树。
