# OpenCode prompt — D6 mainline graph redirect heavy R1

Repository: `D:\Code\HD-QKD_Polar_Comparison`

This is the second of two independent sessions. Before doing anything, verify that the repository-wide workflow-optimization session has been main-thread accepted with durable marker `TWO_TIER_WORKFLOW_ACCEPTED_REPOSITORY_WIDE` (or its documented compatibility alias `TWO_TIER_WORKFLOW_ACCEPTED`). The policy must apply to all future project work, not just D6. If absent, return `BLOCKED_WORKFLOW_OPTIMIZATION_NOT_ACCEPTED` and make no edits.

Then read this packet completely:

`D:\Code\HD-QKD_Polar_Comparison\.workbuddy\tasks\D6_MAINLINE_GRAPH_REDIRECT_HEAVY_R1_TASK_PACKET.md`

Perform H01–H46 only. Reconcile the accepted X1–X4 evidence, close the current D5 rate-mother configuration, and return to the existing D6 R1d Option C graph discriminator with B0/B1 controls and T1 PEG-DV3 as the sole new candidate. Do not continue D7-H: X4 showed 0/600 base-layer syndrome-valid recovery at n256 and X3 showed only 1/156 recovery from longer iteration, so another alternation does not address the identified base-code failure.

Use the newly accepted EXPLORE workflow: one prereg/result log and one batch-end independent review, not per-arm paperwork. This prompt authorizes implementation, fake/injected tests and Pre-EXECUTE readiness only. Do not run the production decoder, D6 batch, D7-H, real data, G1/G2, or n1024; do not touch unrelated dirty files or push.

Return only `COMPLETE` at `D6_R1D_EXPLORE_READY_AWAITING_EXPLICIT_AUTHORIZATION`, or `BLOCKED` with the exact raw failure and one decision needed. Report deltas only.
