# Explicitly authorized prompt — D12 Finite L1 Degree Batch A1

I explicitly authorize `D12_FINITE_L1_DEGREE_BATCH_A1` exactly once in
`D:\Code\HD-QKD_Polar_Comparison` on branch
`formal-ir-v72p1-addendum-clean`, following `AGENTS.md` and
`.workbuddy/tasks/D12_FINITE_L1_DEGREE_BATCH_A1_TASK_PACKET.md`.

This grant names root
`workspace/d12_finite_l1_degree_94fb9d22-cadc-47f4-a96e-b2170bdba450`;
arms L045/L050/L055; n128 graph seeds `2026093001..06` and block seeds
`2026093201..12`; n256 graph seeds `2026093101..06` and block seeds
`2026093301..12`; both widths always run; frozen tables/gates/ranking. Budgets
are ≤432 scientific calls, ≤62 setup, ≤1800 s wall, ≤120 s/call, RSS strictly
below 2147483648 B, one process.

Run all eight pre-dispatch checks. If they pass, append them and execute the
exact `--d12-batch --execution-authorized` command once. The grant is consumed
at command start. No repair, rerun, seed replacement/search, tuning or input
change is authorized; retain any failed root and STOP.

After execution, complete one independent batch-end review with actual artifact
access and append it to the D12 log. Do not self-accept the selected route, run
forward/L2/D7-H/real data, commit or push. Return the exact packet terminal and
evidence contract.
