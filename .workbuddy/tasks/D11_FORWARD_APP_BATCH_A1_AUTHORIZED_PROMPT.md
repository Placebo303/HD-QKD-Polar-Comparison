# Explicitly authorized prompt — D11 Forward APP Batch A1

I explicitly authorize `D11_FORWARD_APP_BATCH_A1` exactly once in
`D:\Code\HD-QKD_Polar_Comparison` on branch
`formal-ir-v72p1-addendum-clean`, following `AGENTS.md` and
`.workbuddy/tasks/D11_FORWARD_APP_BATCH_A1_TASK_PACKET.md`.

This grant names root
`workspace/d11_forward_app_7c1878b5-23a8-4fd8-a395-b5a33a58ea64`; n128 first
and n256 only iff n128 is `D11_FORWARD_SIGNAL`; the frozen R3 L1 seeds/replay,
L2 seeds `2026092801..06` and conditional `2026092901..06`, CONTROL/MIX/shared
ORACLE design, canonical transfer semantics and frozen gates. Budgets are ≤720
scientific calls, ≤64 setup, ≤2400 s wall, ≤120 s/call, RSS strictly below
2147483648 B, one process.

Run all nine pre-dispatch checks first, including the current d5
`_run_layered_block` signature and `on_blocked_transfer="record"` behavior. If
all pass, append them and run the exact `--forward-batch
--execution-authorized` command once. The grant is consumed at command start.
No repair, rerun, seed replacement/search, tuning or input change is authorized.

After execution, complete one independent batch-end review with actual artifact
access and append it to the single D11 log. Do not self-accept a route, run
D7-H/reverse feedback/mixed L2/real data, commit or push. Return the exact packet
terminal and evidence contract.
