# Copyable prompt — D10 connectivity/rank R2

Work in `D:\Code\HD-QKD_Polar_Comparison`. Follow `AGENTS.md` and
`.workbuddy/tasks/D10_MIXED_DEGREE_L1_CONNECTIVITY_R2_TASK_PACKET.md` exactly.

Implement and independently review R201--R212 as one implementation/readiness
task. Trust the D10 R1 reviewer evidence; do not rerun broad suites for ceremony.
Main thread rejected R1 execution readiness because F4 is a scientific blocker:
9--68 graph components and deficient ranks confound the degree-profile test.

Preserve every R1 scientific input, exact degree table and original graph seed.
Use one deterministic connectivity-first degree-sequence PEG path for both arms.
Before decoder binding require one Tanner component, structural matching rank
`m`, exact GF32 row rank `m`, exact degrees/socket balance, simplicity and
deterministic replay. Never search/replace seeds or repair coefficients.

This authorizes code/docs, focused fake tests and no-decoder `PROFILE_ONLY` only.
It does not authorize `--batch`, decoder/DE/CAL/VAL/real-data calls, scientific
root creation, commit, push, D7-H, or a route conclusion. Keep execution false.

Return only when complete or at the first STOP condition with raw evidence and
the one decision needed. A ready result must be exactly
`D10_MIXED_DEGREE_L1_R2_READY_AWAITING_EXPLICIT_AUTHORIZATION`; otherwise use
`BLOCKED_D10_CONNECTED_FULL_RANK_CONSTRUCTION` as the packet specifies.
