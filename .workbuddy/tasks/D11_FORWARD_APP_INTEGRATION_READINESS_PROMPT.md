# Copyable prompt — D11 canonical forward APP readiness

Work in `D:\Code\HD-QKD_Polar_Comparison` and follow `AGENTS.md` plus
`.workbuddy/tasks/D11_FORWARD_APP_INTEGRATION_READINESS_TASK_PACKET.md` exactly.

Accept the reviewed D10 R3 wide L1 signal. Implement D1101--D1110 as the
smallest readiness change for a single canonical forward L1→L2 APP pass:
mixed-L1 versus DV3-L1, the same connected full-rank DV3 L2 graph within each
pair, and one shared L2-oracle diagnostic. Reuse canonical D5/D7 transfer and
D6 oracle semantics; do not invent another message interface or apply the L1
degree distribution to L2.

This message authorizes OpenSpec/code/docs, focused fake tests and no-decoder
PROFILE_ONLY only. It does not authorize the D11 batch, scientific decoder
calls, L2 results, D7-H, real data, commit or push. Use `reviewer-go` for the
independent readiness review when available; trust its traceable evidence.

Return only at the frozen ready terminal or first STOP condition with the
packet evidence contract. Execution requires a later explicit user grant.
