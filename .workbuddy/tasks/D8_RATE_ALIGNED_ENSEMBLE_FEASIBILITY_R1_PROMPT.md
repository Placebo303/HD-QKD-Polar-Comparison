# OpenCode prompt — D8 rate-aligned GF32 ensemble feasibility heavy R1

Repository: `D:\Code\HD-QKD_Polar_Comparison`

Read this packet completely before acting:

`D:\Code\HD-QKD_Polar_Comparison\.workbuddy\tasks\D8_RATE_ALIGNED_ENSEMBLE_FEASIBILITY_R1_TASK_PACKET.md`

Perform E01–E12. Accept the independently reviewed D6 R1d result only within its frozen claim ceiling: PEG-DV3 remained silent at n64/n128/n256, so close the eligible-only DV3 topology redirect and design the next rate-aligned GF32 ensemble/degree-distribution feasibility experiment.

Do not continue D7-H. X4, X3 and D6 show an unresolved base L1 recovery failure; alternation is not the next orthogonal variable. Audit and reuse the nearest accepted historical GF32/nonbinary density-evolution implementation. Do not silently replace the current Model-F 32-ary channel with BSC/AWGN/q-SC or another scalar surrogate.

This session authorizes OpenSpec/readiness work, minimal code, focused tiny/fake tests and bounded no-production-decoder profiling only. It does not authorize the DE scientific sweep, a finite-length decoder batch, CAL/VAL/raw/real data, n1024, D7-H, commit or push. Preserve protected roots and unrelated dirty files.

Use one compact EXPLORE readiness record and one independent reviewer-go review. Trust existing verified reviewer evidence within its scope; do not rerun broad suites for ceremony. If the channel representation, rate mapping or reuse contract is genuinely ambiguous, return the exact `BLOCKED` decision instead of inventing assumptions.

Return only `COMPLETE` at `D8_RATE_ALIGNED_ENSEMBLE_READY_AWAITING_EXPLICIT_AUTHORIZATION`, or `BLOCKED` with the raw failure and one decision needed. Report deltas only.
