# Authorized prompt — D10 Mixed-Degree L1 Batch A1

Work in `D:\Code\HD-QKD_Polar_Comparison` on branch
`formal-ir-v72p1-addendum-clean`. Follow `AGENTS.md`,
`.workbuddy/tasks/D10_MIXED_DEGREE_L1_BATCH_A1_TASK_PACKET.md`, and
`docs/research_cycles/V72P2D10-MIXED-DEGREE-L1/BATCH_A1_AUTHORIZATION.md`.

The main thread explicitly authorizes `D10_MIXED_DEGREE_L1_BATCH_A1` exactly
once: root
`workspace/d10_mixed_degree_l1_b2dd13e4-6600-4e27-90df-5c9038cf2c34`;
conditional n64→n128→n256 progression; ceilings 144 scientific L1 calls, 44
setup units, 1800 s wall, 120 s/call, RSS strictly below 2147483648 B, one
process. This satisfies the packet's explicit-authorization gate.

Reconfirm the seven pre-dispatch checks. If they still pass, append their raw
record and run the exact frozen `--batch --execution-authorized` command once.
The grant is consumed at command start. Do not repair, rerun, replace/search
seeds, tune, or change inputs. Preserve any failed root and STOP on a mismatch.

After the run, complete one independent batch-end review with actual artifact
access and append the evidence to the single exploration log. Do not self-accept
the route, revive D7-H, run L2/APP/real data, commit or push. Return exactly the
terminal and evidence contract from the A1 packet.
