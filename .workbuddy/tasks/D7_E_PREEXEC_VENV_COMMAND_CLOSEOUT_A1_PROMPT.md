# D7-E venv command closeout A1 operator prompt

执行唯一任务包：

`D:/Code/HD-QKD_Polar_Comparison/.workbuddy/tasks/D7_E_PREEXEC_VENV_COMMAND_CLOSEOUT_A1_TASK_PACKET.md`

完整阅读任务包、AGENTS.md、D7-E prereg/execution packet、OpenSpec tasks、实现评审与原
Pre-EXECUTE review，然后按 A01→A05 执行。

这是零 decoder 的文档/可达性收口，不重开科学设计：只把冻结命令的裸 `python` 改成
`.venv/bin/python`，补 addendum，修正 R14–R19 已完成的 OpenSpec checkbox，并由独立 reviewer
用 corrected interpreter 重验 help/dry-run/未授权拒绝/sentinel/根与授权状态。

严禁运行授权路径、decoder、Model-F 内容读取、R1d、`--phase`、G1/G2、CAL/VAL/real/raw；
不得生成 UUID/结果根，不改生产代码/tests，不 broad-stage，不 push。环境探针必须分开运行，
不得用 PATH activation 掩盖裸 python 缺失。

完成后按 §7 只报 delta 并使用精确结束句；失败贴 failing ID 和原始输出后停止。
