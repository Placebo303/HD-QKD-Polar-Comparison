# Explicitly authorized prompt — D10 R3 Fresh-Graph Scaling Batch A1

I explicitly authorize `D10_R3_FRESH_GRAPH_SCALING_BATCH_A1` exactly once in
`D:\Code\HD-QKD_Polar_Comparison` on branch
`formal-ir-v72p1-addendum-clean`, following `AGENTS.md` and
`.workbuddy/tasks/D10_R3_FRESH_GRAPH_SCALING_BATCH_A1_TASK_PACKET.md`.

This authorization names root
`workspace/d10_r3_fresh_graph_scaling_4d39ed0e-3cbb-49f6-a1df-1dcc10868a8d`;
n128 graph seeds `2026092401..06` and block seeds `2026092601..12`; conditional
n256 graph seeds `2026092501..06` and block seeds `2026092701..12`; n256 may run
only iff n128 is `R3_REPRODUCED`. Budgets are ≤288 scientific calls, ≤50 setup,
≤1800 s wall, ≤120 s/call, RSS strictly below 2147483648 B, one process.

Run the eight pre-dispatch checks first. If all pass, append them and execute the
exact frozen `--r3-batch --execution-authorized` command once. The grant is
consumed at command start. No repair, rerun, seed replacement/search, tuning,
adaptation or input change is authorized; retain a failed root and STOP.

After execution, complete one independent batch-end review with actual artifact
access and append it to the single R3 log. Do not self-accept a route, run
L2/APP, revive D7-H, use real data, commit or push. Return the exact packet
terminal and evidence contract.
