# Copyable prompt — D12 finite L1 degree refinement readiness

Work in `D:\Code\HD-QKD_Polar_Comparison` and follow `AGENTS.md` plus
`.workbuddy/tasks/D12_FINITE_L1_DEGREE_REFINEMENT_READINESS_TASK_PACKET.md`.

Accept the reviewed D11 wide forward result. Implement D1201--D1210 as the
smallest fresh-graph L1 comparison of λ2=0.45/0.50/0.55 at n128 and n256.
Reuse D9 realization and D10 construction/decoder paths; do not broaden the
grid, change schedule/rate, touch L2, or implement D7-H.

This message authorizes OpenSpec/code/docs, focused fake tests and no-decoder
PROFILE_ONLY only. It does not authorize `--batch`, scientific decoder calls,
real data, commit or push. Use independent `reviewer-go` with actual artifact
access and trust its traceable checks without ceremonial duplication.

Return only at the frozen ready terminal or first STOP condition with the
packet evidence contract. Execution requires a later explicit user grant.
