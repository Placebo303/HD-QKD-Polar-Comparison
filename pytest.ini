[pytest]
addopts = -p no:cacheprovider --basetemp=D:/Code/HD-QKD_Polar_Comparison/workspace/tmp_pytest
cache_dir = workspace/.pytest_cache
markers =
    slow: heavy test excluded from the default (T0) lane; run via the slow shard (perf-v38-triage-test-cost P4).
