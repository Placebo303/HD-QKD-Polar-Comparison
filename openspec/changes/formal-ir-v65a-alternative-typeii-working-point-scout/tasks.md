# OpenSpec Tasks: formal-ir-v65a-alternative-typeii-working-point-scout — V65AR1 备用 Type-II 工作点勘探（DECODER_FREE 同家族码率适配）

**Lifecycle**: `PLAN_CANDIDATE → IMPLEMENTATION_CANDIDATE / DECODER_FREE / EXECUTE_NOT_AUTHORIZED` — **固定顺序 162148→2500K→160254，4+4首个通过即停，256/64粗筛仅淘汰(永不READY)，1024/256正式+32 TEST规划，V65不动且V65AR1同家族码率适配；真实 Stage0/Stage1 未运行**
**Branch**: `formal-ir-mainline` HEAD `0131313177a6f2a52319b6354cc62477f4685b49` 已核 `HEAD==origin/formal-ir-mainline` data SHA `84d62779` (d1024 bw200 nearest legacy_v1 单点)
**Predecessor**: `formal-ir-v65-new-session-channel-compatibility` `DATA_NOT_READY` 非模型/码率失败 保持不动
**Method frozen**: `H1-16/Lane C m2 184/190/192→m_total 216/222/224/Δ8+Δ8/full-tag` 主方向冻结；V65AR1 仅允许同家族内 `P(U1|B)/P(U2|U1B)` 重估计调 `m1/m2` (`ceil(1.3*1024*CE/5)` 无 cap)，增量仅未来预注册相邻档位，本轮不创后继 change
**Boundary**: 固定三候选顺序首个通过即停；Batch 禁止（8帧逐一）；Stage0 4+4 materialization/provenance fail-closed；Stage1 256/64 coarse 仅淘汰不能 READY/RATE_READY；Stage2 1024/256 formal+32 TEST 仅规划(正式至少CAL1024/VAL256/TEST32才判FROZEN/ADAPTATION)；V65 零改；DECODE_FREE；旧 outcome 禁读；`m` 禁 cap/floor；MODEL_NOT_STABLE 优先。真实 Stage0/Stage1 本轮不运行。

## Phase A — 固定顺序与 4+4 最小验证（decoder-free，逐一，首个通过即停，Batch 禁止）

- [ ] **A1 冻结三候选顺序、真实相对路径与 tier 占位**：写入 `v65a_registry.json: {schema: "v65a_v1", lifecycle: "DRAFT→PLAN_CANDIDATE", data_sha: "84d62779", head: "TBD→新SHA", candidates_ordered: ["2026-01-13 162148","2026-01-07 2500K","2026-01-07 160254"], candidate_paths: {162148: "2026.1.13/SHG_Type2PPLN_3s_2_2026-01-13_162148", 2500K: "2026.1.7/Type2PPLN_2500K_3s_2026-01-07_174324.1.ttbin", 160254: "2026.1.7/Type2PPLN_3s_2026-01-07_160254.1.ttbin"}, provisional_tier: "B", guards: {batch_materialization: false, historical_result_files_read: false, decode_rg_0: true}, stages: {verify: "8 frames per candidate sequential", coarse: "256/64 selected only", formal: "1024/256+32 planned" }}`。顺序与路径冻结可复现，不以数据可用性重排；A/B/C 规则与每候选证据/不确定性一并落盘。
- [ ] **A2 编写 Stage0 4+4 验证逻辑（sequential, 8 frames only）**：`scripts/v65a_scout.py --stage verify` 对当前 `cand` 仅 `materialize_frames(cand, n=8)`（`4+4 frames = 2048 pairs`），校验 `dimension 1024 / bin200 / nearest double-pointer bin//1024 / legacy_v1 / candidate-specific channels / delay_used_ps / peak_center / sigma 50-150 / gate 200 / threshold 40000 / frame_start_ps / period 204800 floor_div / mapping legacy_v1 / 每帧256 A=32U1+U2 B=32V1+V2` 逐项，落盘 `per_candidate {cand_id, session_id, delay_used_ps, peak_center, sigma, gate, threshold, frame_anchor, mapping, channels_used, provenance_path, materialized_frames:8, materialized_pairs:2048, verify_pass:bool, fail_reason}`，`VERIFY_PASS = (dimension==1024 && bin==200 && pairing==nearest && assignment==double_pointer_bin_div_dimension && rule==legacy_v1 && channels_explicit_and_distinct && provenance_matches_candidate && sign(delay)==sign(peak) && |delay-peak|<50ps && sigma∈[50,150] && gate==200 && threshold==40000 && period==204800 && 每帧256 A/B映射)`，否则 `VERIFY_FAIL`；缺 path/sidecar/字段/冲突只报 `DATA_NOT_READY/INCOMPATIBLE`。
- [ ] **A3 实现首个通过即停与 Batch 禁止守卫**：`for idx, cand in candidates_ordered: verify 8 frames; if PASS: selected=cand; break`，脚本内 `assert materialized_candidates_count == (selected_idx+1 if selected else len(candidates_ordered))` 且 `assert materialized_frames_total == 8 * materialized_candidates_count` 在 Stage0 阶段，后续候选 `not_materialized` 已验，禁止 `for cand in candidates: load_all` 批量路径（`rg "for cand in candidates_ordered" 1 hit + break` 已验，`materialized_frames_total <=24` 最差），落盘 `v65a_manifest.json: stage0 {checked_candidates, selected, materialized_frames_total, batch_guard_pass, order_guard_pass}`
- [ ] **A4 落盘 Stage0 明细与 V65 不动校验**：`v65a_scout.json: stage0 {per_candidate[3] {verify_pass, provenance逐项, delay_peak_sign_ok, delay_peak_abs_ps, sigma, gate, threshold, frame256_ok}, selected, none_passed}` + `v65a_manifest.json: guards {batch_materialization==false, v65_unchanged==true}`，校验 `git diff -- openspec/changes/formal-ir-v65-new-session-channel-compatibility/ ==0` 且 `v65_frozen_session_binding.json` 未改且 `V65 overall` 仍 `DATA_NOT_READY`（只读校验，不修改）
- [ ] **A5 记录 provenance tier**：三候选分别写入 `provisional_tier`、`tier_basis`、`tier_uncertainty`；当前仓库未发现 V36–V64 NB-LDPC 注册表引用，暂列 B-provisional，`162148` 的 `D:\SPDC源测试` 路径冲突在 Stage0 阻断；后续账本发现当前 NB-LDPC 影响则改 C，A 需显式无影响证据。

## Phase B — 256/64 粗筛（selected only，decoder-free，仅淘汰不能 READY/RATE_READY，R65A-04/05）

- [ ] **B1 读 selected 256/64 切片（selected only）**：`--stage coarse` 仅对 `selected` 候选物化 `256 frames CAL (65536 pairs) +64 frames VAL (16384 pairs)`，`F03 5+5 natural U1=>>5, U2=&31` 分解，校验 `alice/bob ∈[0,1023]` 且每帧256连续，落盘 `per_selected {N_cal 65536/256, N_val 16384/64}`，`TEST 未参与` 已验，非 selected 候选零物化已验
- [ ] **B2 算 C_ab/P_global/Hierarchical 与 λ 粗筛**：`C_ab = bincount2d(a_cal,b_cal) 1024×1024 sum 65536 → N_b, P_global → P_λ(a|b)=(C_ab+λ P_global)/(N_b+λ)`，同时生成 `P_λ(U1|B) 32×1024` 与 `P_λ(U2|U1B)`，对 `log10 λ ∈ [-2,4]` 粗筛（`50-point log grid + Brent refine` 或 `2-fold CV` 粗筛），记录 `search_trace {logλ_grid, CV_NLL_grid, λ*, CV_NLL*}`，校验 `λ ∈ (1e-2,1e4)` 开区间 else `λ_at_boundary=True → REJECT`，触界不扩网格，不以 VAL 择优，单 hierarchical 一路
- [ ] **B3 算熵/CE 与 VAL 泛化（链式校验）**：`H_cal(A|B), H_cal(U1|B), H_cal(U2|U1B)=H-H1` 校验 `|H-H1-H2|<1e-9` 描述性，否则 `EVIDENCE_INVALID`；另算 `CE1=-E_VAL log P_λ*(U1|B), CE2=-E_VAL log P_λ*(U2|U1B), CE_full=-E_VAL log P_λ*(A|B)` 校验 `|CE_full-CE1-CE2|<1e-9` 门禁性，否则 `EVIDENCE_INVALID`，落盘 `per_selected {H, H1, H2, chain_delta_H, CE1, CE2, CE_full, chain_delta_CE, CV_NLL*, λ*, at_boundary}`
- [ ] **B4 泄漏与粗筛门禁（仅淘汰，R65A-05 禁 cap/floor/handfill 四分流）**：`f_target=1.3, n=1024, tag=64, log2q=5 → m1=ceil(1.3*1024*CE1/5), m2=ceil(1.3*1024*CE2/5), m_total=m1+m2, leak=5*m_total+64` 不 cap/floor/handfill，显式 `m_raw`，粗筛门禁 `G2 λ不触界, G3 ΔNLL≤0.75, G4 Val NLL≤H_cal+1.5, G5 unseen≤0.02, G6 m1≤16(CE), G7 m2≤200(CE), G7-aux m_total≤216, G8 provenance+CE链式`，任一 FAIL → `overall= V65A_COARSE_REJECTED` 停止，不进 Stage2；全过 → `overall= V65A_ELIGIBLE_FOR_FORMAL` 仅放行规划，绝不为 `READY/RATE_READY`，落盘 `per_selected {Val NLL, ΔNLL, MAP, q_mass_unseen, effective_contexts, m1/m2/m_total Δm/Δleak, required_rate_classification{MODEL_NOT_STABLE/FROZEN_RATE_COMPATIBLE/RATE_ADAPTATION_REQUIRED/FULL_DISCLOSURE_LAYER}, gates G1..G8 coarse, overall}`。`m` 计算禁 cap/floor/handfill，超容量且<1024→RATE_ADAPTATION_REQUIRED，≥1024→FULL_DISCLOSURE_LAYER，MODEL_NOT_STABLE 优先于率分流
- [ ] **B5 粗筛仅淘汰守卫（R65A-04 永不 READY）**：脚本内 `assert coarse_cannot_ready == true`（`coarse overall != READY/FORMAL_READY/RATE_READY`），`m_i CE-based ceil` 不 cap 已验（`rg "min(16" 0 hits` 且 `rg "floor" 0 hits` 对 m），Stage1 样本量不足时只能 `DATA_NOT_READY/COARSE_REJECTED`，`used_test_in_estimation==false` 已验，`rg "decode_" 0 hits` 已验

## Phase C — 1024/256 正式重表征与 32 TEST 密封规划（本轮仅规划，不执行全量至 READY，R65A-06/07）

- [ ] **C1 规划 1024/256 正式切片（planned）**：写入 `v65a_registry.json: formal_plan {CAL: 1024 frames 262144 pairs, VAL: 256 frames 65536 pairs, TEST: 32 frames 8192 pairs 8 blocks, session_id==selected session_id, frame_ids 互斥零重叠 per candidate (CAL 1024 + VAL 256 + TEST 32 互斥, 且与 Stage0/Stage1 已用帧零重叠), provenance 同 Stage0/Stage1 算法}`，本轮不物化 `1312 frames` 全量至 READY，仅 freeze 计划，正式执行需 `PLAN_ACCEPT` 后 `materialize_frames(selected, n=1312)`
- [ ] **C2 规划正式 hierarchical 与 λ 搜索（planned）**：`C_ab 1024×1024 sum 262144 → P_global → P_λ` 同 V65 算法，`λ` `Cal内4-fold log10[-2,4] 连续` 择优，正式阈 `G2 λ不触界, G3 ΔNLL≤0.50, G4 Val NLL≤H_cal+1.0, G5 unseen≤0.01, G6 m1≤16, G7 m2≤200, G7-aux m_total≤216, G8 provenance+CE链式<1e-9` 全过才 `FORMAL_READY`（本轮仅声明阈，执行后判定），触界不扩网格，禁第二 estimator，落盘 `formal_plan: {λ_domain [-2,4], folds 4, thresholds {G3 0.50, G4 H+1.0, G5 0.01, G6 16, G7 200, G7-aux 216, CE_chain 1e-9}, hierarchical_formula}`
- [ ] **C3 规划 32 TEST 密封（planned, 仅 identity）**：`v65a_registry.json: sealed_test {session_id==selected, frames[32] identity (frame_ids exact, blocks 8, pairs 8192, provenance), zero_overlap_verified (CAL 1024 ∩ VAL 256 ∩ TEST 32 ==∅ per candidate, key=(candidate, frame_id)), not_used_in_estimation}`，`TEST` 不计 `H/CE/NLL/MAP/q_mass/m` 任何统计，脚本内 `assert not used_test_in_estimation`，`32 TEST` 仅密封 identity 不读统计已验
- [ ] **C4 终态规划与 V65 隔离声明**：写入 `v65a_scout.json: overall_planned {if Stage0 none PASS: V65A_NO_CANDIDATE_PASSED_VERIFICATION else if Stage1 REJECT: V65A_COARSE_REJECTED else V65A_ELIGIBLE_FOR_FORMAL_RECHARACTERIZATION (本轮终态上限, not READY)}` + `v65_unchanged {git diff -- v65 ==0, V65 DATA_NOT_READY 保持}`，`FORMAL_READY` 仅正式执行后（需新 `PLAN_ACCEPT`）才可能，`V65A` 结论不 reinterpret V65 已验

## Phase D — 脚本与报告交付（PLAN_CANDIDATE / DECODER_FREE / EXECUTE_NOT_AUTHORIZED 至推送）

- [ ] **D1 编写 `scripts/v65a_scout.py`** (decoder-free, 本变更目录下): `python scripts/v65a_scout.py [--stage verify|coarse|formal|all] [--candidate-root PROJECT_DATA_ROOT] [--out v65a_scout.json] [--report V65A_SCOUT_REPORT.md]` → `candidates_ordered 冻结 → Stage0 8帧逐一 verify (首个PASS即停, 守卫 materialized_frames_total/batch) → Stage1 256/64 coarse (selected only, 仅淘汰, rg decode 0 hits) → Stage2 formal 1024/256+32 TEST 规划 (不读 TEST 统计, 不创建 run_01)`，仅 `numpy/pandas/pyarrow + ttbin_pipeline(TimeTagger optional)`，`py_compile PASS`，输出 `v65a_scout.json + V65A_SCOUT_REPORT.md + v65a_registry.json + v65a_manifest.json` + 控制台摘要，校验 `TEST 未参与` 及 `m_raw CE-based 未 cap` 及 `λ触界不扩` 及 `CE链式` 及 `固定顺序首个通过即停` 及 `旧 outcome 禁读`
- [ ] **D2 撰写 `V65A_SCOUT_REPORT.md`**：三候选 `VERIFY_PASS/FAIL` 明细（8帧 provenance 逐项 `dimension/bin/pairing/legacy_v1/channels/delay/peak/sigma/gate/threshold/frame anchor/mapping 每帧256`）+ `selected` `Stage1 256/64` 实报（`λ是否触界 + H_cal描述性 + CE1/CE2/CE_full chain_delta 门禁性 + CV/Val NLL/ΔNLL + MAP/q_mass/effective + m1/m2/m_total(CE) 相对冻结构 16/200/216 差额 + G1..G8 coarse 明细`）+ Stage2 `1024/256` 规划与 `32 TEST` 密封 identity（不含统计）+ `materialized_frames_total/batch_guard` + `V65不动` 声明 + `粗筛仅淘汰不能 READY` + `旧 outcome 禁读` + `首个通过即停`，数据与 `json` 一致，结论不扩大为 `FER/阈值/SKR/晋升`
- [ ] **D3 自检（G-verify + 粗筛 + 规划 + 守卫 + V65不动）**：`py_compile` PASS, `rg "decode_" 0 hits` 且 `rg "old_outcome|outcome\\.json" 0 hits` 已验, `git diff -- src/ ==0` 且 `git diff -- openspec/changes/formal-ir-v65-new-session-channel-compatibility/ ==0` (V65零改) 已验, `materialized_frames_total` 守卫（Stage0 `8*checked`, Stage1 `+320` selected only）已验, `candidates_ordered` 固定且首个通过即停已验, `|H-H1-H2|<1e-9` 描述性 + `|CE_full-CE1-CE2|<1e-9` 门禁性已验, `λ ∈ (1e-2,1e4)` 开区间且触界即 REJECT/MODEL_NOT_STABLE 不扩已验 (`search_trace` 落盘), `m_i(CE) ceil` 不 cap 已验 (`m_raw` 显式, `m_total 216` 辅助), `TEST 未读统计` 已验 (`used_test_in_estimation==false`), `overall` 互斥 `V65A_NO_CANDIDATE > V65A_COARSE_REJECTED > V65A_ELIGIBLE_FOR_FORMAL` 已落盘, 报告与 json 一致, 未建 `run_01`
- [ ] **D4 推送新 Plan SHA 并停留 `PLAN_CANDIDATE / DECODER_FREE / EXECUTE_NOT_AUTHORIZED`**，未创建任何 `.../v65a_*/run_01` 或 `.../v66_*/run_01` decoder 执行，未碰 `V65` 块，未启动正式重表征执行，仅改本目录 + `scripts/`，**推送后等待独立审核**（`Pre-RESULT` 独立线程复核 `HEAD/ACCEPTED_PLAN_SHA/rg 0 hits/run_01不存在/py_compile+TEST未读/批量物化守卫/固定顺序/首个通过即停/旧 outcome禁读/V65不动`），返回 `Plan SHA / implementation SHA / Stage0 8帧明细 / Stage1 256/64 粗筛 / Stage2 1024/256规划+32 TEST密封 / overall` 等待 `PLAN_ACCEPT`，正式执行需新 `PLAN_ACCEPT`

## 本变更显式禁止

decoder 调用 (`decode_*` / `construct_*` / `sample_uniform_gf32` 等)；批量物化三候选全量（禁止一次性 `3×全量 pairs.parquet` 预加载）；读取旧结果（性能/门禁/SKR）作排序/阈值；在选定候选外物化后续候选；以粗筛 `256/64` 宣称 `READY`（粗筛仅淘汰）；以 `8` 帧以外批量统计挑候选；调 `H1/Lane C/Δ8/decoder/m2/leak/prior/H_inc/H_total/verification` 任一冻结参数或新增矩阵；将 `floor/round` 当 `ceil`；将 `min(16, ceil(...))` cap 伪装当通过；将 `m_total≤200` 当总量（应 `216`）；用 `VAL` 择优 `λ` 或选平滑方式（`λ` 仅 `Cal内`）；擅自启动正式 `1024/256` 全量至 `READY`（仅规划）；改 `src/` 基线；宣称 `FER/阈值/SKR/晋升`；创建正式 `.../v65a_*/run_01` decoder 执行；任意 `bin/dimension/pairing/mapping` 网格；用第二 estimator 作门禁；覆盖已有输出；`V65` 语义 reinterpret；对任一候选预填或默认 channels/delay/peak/sigma/gate/threshold。

## 验收

- proposal/design/tasks/specs 一致 HEAD `TBD→新 Plan SHA` `84d62779` lifecycle `DRAFT→PLAN_CANDIDATE / DECODER_FREE / EXECUTE_NOT_AUTHORIZED` 终态互斥明确（`NO_CANDIDATE > COARSE_REJECTED > ELIGIBLE_FOR_FORMAL` 本轮上限，`FORMAL_READY` 需正式执行），显式固定三候选顺序首个通过即停、8帧验证、256/64粗筛仅淘汰、1024/256正式+32 TEST密封规划、V65不动、DECODE_FREE、禁批量物化、禁旧 outcome
- 三候选 `VERIFY_PASS/FAIL` 明细（8帧 `dimension/bin/pairing/legacy_v1/channels/delay/peak/sigma/gate/threshold/frame anchor/mapping 每帧256` 逐项 `sign/50ps` 门禁）已验，`candidates_ordered` 冻结，`materialized_frames_total` 守卫（Stage0 `8*checked` 首个PASS即停，后续未物化）已验
- `selected` 的 `256 CAL +64 VAL` 粗筛 `H/CE/λ/NLL/MAP/unseen/m` 已报，`λ ∈ (1e-2,1e4)` 开区间触界即 REJECT 不扩，`CE链式 |CE_full-CE1-CE2|<1e-9` 已验，`m_i(CE) ceil` 不 cap 且 `m_total 216` 辅助，粗筛 `FAIL→REJECT` 不进 Stage2，`PASS→ELIGIBLE_FOR_FORMAL` 绝非 `READY` 已验
- `1024 CAL(262144)+256 VAL(65536)+32 TEST(8192)` 正式重表征与 TEST 密封已规划（仅规划，本轮不执行至 READY），`TEST` 仅 identity 不含统计已验，`hierarchical λ [-2,4] Cal内4-fold` 与 `CE门禁` 阈冻结
- `scripts/v65a_scout.py` `rg "decode_" 0 hits` `rg "old_outcome" 0 hits` `py_compile` PASS，`materialized_frames_total` 守卫与 `V65不动` 守卫已验，报告与 json 一致，未建 `run_01`，已推新 Plan SHA `PLAN_CANDIDATE / DECODER_FREE / EXECUTE_NOT_AUTHORIZED` 仅改本目录 + `scripts/`，未启动正式执行，推送后等待独立审核
