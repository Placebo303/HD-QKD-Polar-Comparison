# OpenSpec Tasks: formal-ir-v54-two-stage-incremental-l2-rescue

**Lifecycle**: `PLAN_CANDIDATE / EXECUTE_NOT_AUTHORIZED` — **仅规划，不实现，不执行 decoder，不写产出。等待独立复审。本轮仅完成 decoder-free 二阶段嵌套 spike + 45块 registry + 四工件。**
**Execution status**: 本轮仅完成 decoder-free 二阶段嵌套 spike（含 H_inc2 rank/nesting）+ 45 fresh held-out registry；以下 P1..E 任务待独立 plan ACCEPT 后方可进入；当前不跑 decoder，不写产出。V54 冻结 45 held-out blocks `90-180` calls（每源15，`L1 45+base45+stage1≤45+stage2≤45`），门禁 `35/45 & 每源10/15 & undetected==0` 不因 V53 差两个改阈值。
**HEAD**: `bf5dd1686049156540328bac264296b17fee546c`（plan SHA；实现冻结时重绑至未来 implementation SHA）
**Predecessor**: `formal-ir-v53-rate-adaptive-l2-heldout-confirm` (`93c12fa5a8524eb5a8a52d071f135c653c746ebaf`, branch `formal-ir-mainline`)
**Delta m**: `Δm=8+8=16` per source 冻结（首阶段 8 复用 V52/V53 det1，第二阶段 8 新增 det2，总 `90-180` 硬帽180）

## Phase A — 语义冻结（plan ACCEPT 后）

- [ ] **A1** 冻结方法完全体（零改，首遍与首增量）：`n=1024, m2=184/190/192, GF32 poly37, Lane C ordinal-2二值support/标签/位置置换, H1-16 rank16 80b, L1-APP `q_i` via `BP_i` (TRAIN prior `channel_counts.npz`), `H_inc1 8×1024` det1 + `H_joint1 192/198/200`, `decoder 90/1.0 early-stop`, `verification syndrome_ok && tag_ok` L2-only `compute_tag_64(empty,x2)`；`leak_base=5*m2+80+64 →1064/1094/1104, leak_stage1=leak_base+40 →1104/1134/1144, leak_stage2=leak_base+80 →1144/1174/1184`；`exact_full=exact_u1&&exact_l2 oracle` 报告 promises；`Δm=8` 首阶段唯一，累计 `16` 不试 4/12 外多档。
- [ ] **A2** 冻结二阶段增量矩阵 `H_inc2`（唯一新增，`Δm=8` per source 第二张）：`8×1024 GF32 poly37`、`col_degree_inc2∈{0,1}`、`row_degree≤16`、`无零行`、`rank_joint1==m2+8`、`rank_total==m2+16`、`nested1 H_base==H_joint1[:m2]`、`nested2 H_base==H_total[:m2] && H_joint1==H_total[:m2+8]`、`independence_1==8` (rank_joint1 - rank_base)、`independence_2==8` (rank_total - rank_joint1)、确定性 PEG-增量 `SeedSequence([600004/600005/600006,1/2])` 各 source、禁止 seed 搜索、第二候选与调 `Δm`；`E_inc2≈96` 报告、`leak_stage2=leak_base+80` 公式冻结、`first_pass_success 已成功帧不增泄漏(1064/1094/1104)，stage1_success 为 leak_stage1(1104/1134/1144)，stage2 成功与最终失败均为 leak_stage2(1144/1174/1184)`。
- [ ] **A3** 冻结三阶段条件协议：`base(H_base)` → `verify_base(syndrome&&tag)` 通过则停 `leak_base`；否则 `s_inc1=H_inc1*u2_true`、`s_joint1=[s_base;s_inc1]`、`stage1(H_joint1)` → `verify_stage1` 通过则停 `leak_stage1`；否则 `s_inc2=H_inc2*u2_true`、`s_total=[s_base;s_inc1;s_inc2]`、`stage2(H_total)` → `verify_stage2`，`leak=leak_stage2` 无论成功/失败；`exact_*` 仅 oracle 统计不触发，三阶段均 `verification-only`；报告 `base_exact_full` 与 `verify_base` 分别计数、`stage1_call_exact_full` 与 `verify_stage1_call` 分别（仅 inc1 调用）、`stage1_final_exact_full_count (=base+inc1_rescued)` 与 `verify_after_stage1 (=verify_base OR (!verify_base AND verify_stage1_call))` 分别（45块累计）、`final_exact_full_count (=stage1_final+inc2_rescued)` 与 `verify_final` 分别，`stage1_rescued = rescued_by_inc1` 与 `stage2_rescued = rescued_by_inc2`，门禁仅用累计量，`rescue_rate_stage1=rescued_stage1/N_stage1_attempted`（`N_stage1_attempted=count(!verify_base)`）与 `rescue_rate_stage2=rescued_stage2/N_stage2_attempted`（`N_stage2_attempted=count(!verify_base && !verify_stage1)`）描述性，禁止用 `45-base_exact` 作分母。
- [ ] **A4** 冻结 45 fresh held-out blocks 枚举算法（排除 V53 后）：于 hold 区间 `1M H=400 base1600 / 1p5M H=554 base2213 / 2M H=729 base2916` 内，汇总 `V48 45区间 + V50 15 + V51 15 + V52 15 + V53 45 =135区间540帧` 已用 `frame_ids`（每区间 `[start,start+3]`），枚举 `all_starts=0..H-4` 的 `[s,s+3]` 窗口，过滤与已用区间重叠者得 `S2` 按 `ordinal` 排序 `K2=|S2|≥15`，以 `index_j=floor(j*(K2-1)/14) j=0..14` 分散选择 15/源，验证最终 45 间两两非重叠（gap≥4）且与已用零重叠；建议 IDs `395001-015/395101-115/395201-215` 仅标识，真实由 `frame_ids` 决定；每块写死 `4 frame_ids=[base+start .. base+start+3]` 与 `held_out_ordinal_start/end`、`pairs_count=1024`、`sampling_mode=deterministic_four_consecutive_frames_heldout_fresh_v54`、`BLOCK_LENGTH=1024`。
- [ ] **A5** 冻结 paired workload（三阶段共享 L1）：每块同 `bob` 同 `P_i(U2)`，`base L2` 1次 → 条件 `stage1 L2 ≤1` → 条件 `stage2 L2 ≤1`，共享 `L1 1`；每块 `L1 1+base 1+stage1 ≤1+stage2 ≤1`（L2 1-3）；总 `45 L1+45 base+≤45 stage1+≤45 stage2=90-180 硬帽180 (L2 45-135)`；冻结 `paired Δexact_stage1 = stage1 - base` 与 `Δexact_stage2 = final - stage1` 与 `Δexact_total = final - base` 描述性、McNemar `b/c` 仅描述。
- [ ] **A6** 冻结报告承诺 SHALL：`base_exact_full` 与 `verify_base` 分别计数（禁止假定相等）/ `stage1_exact_full` 与 `verify_stage1` 分别 / `final_exact_full` 与 `verify_final` 分别 / `stage1_rescued / stage2_rescued` / `N_stage1_attempted=count(!verify_base)` 与 `N_stage2_attempted=count(!verify_stage1&&!verify_base)`（overall & per-source）/ `rescue_rate_stage1=rescued_stage1/N_stage1` 与 `rescue_rate_stage2=rescued_stage2/N_stage2`（禁 `45-base_exact`）/ 每源分层 / `first_pass_success_leak(1064/1094/1104) / stage1_success_leak(1104/1134/1144) / stage2_leak(1144/1174/1184, 成功与失败同)` / `per_source_avg[s]=leak_base[s]+40×N_stage1[s]/15+40×N_stage2[s]/15` 与 `overall_avg=(Σ leak_base[source(block)]+40×N_stage1_total+40×N_stage2_total)/45`（禁单一 `+40N/45` 当 overall）/ `avg_disclosure_per_attempted_frame(=overall_avg/1024)` / `total_disclosed_bits(=Σ leak_total)` 与 `disclosure_per_final_exact_block=total/final_count（为0则null）` / `f_avg` 若保留则分母 `1024×(H(U1|B)+H(U2|U1,B))` 禁 `N_blocks×(H1+H2)` / `stage1_attempt_rate/stage2_attempt_rate` / 迭代/runtime/residual / 四类 `exact/detected/decoder_non_syndrome/undetected`；`V53 33/45` 仅历史描述，不作门禁依据。
- [ ] **A7** 冻结记录与聚合：`45` 块三阶段条件记录、`v54_summary.json` 含 `base/stage1/final` 三层 `exact` 与 `verify` 分别计数分层、每源、平均/条件泄漏（`per_source_avg` 与 `overall_avg` 区分，禁单一 `+40N/45` 当 overall）+`avg disclosure`+`total_disclosed_bits` 与 `disclosure_per_final_exact_block`（为0则null）+`H_inc1 joint1_rank/nested/independence` 与 `H_inc2 total_rank/nested/independence` provenance、`tag acceptance`、paired `base vs stage1 vs final`、`stage1/stage2 rescue_rate`、`Δexact`、四类/G3'、门禁明细（`stage1` 与 `final` 的 G1/G2/G3' 数值与 `V54_*` 四终态）、claim boundary。
- [ ] **A8** 冻结终态四者（互斥，`EVIDENCE_INVALID` 优先）：`V54_DELTA8_ALREADY_SUFFICIENT`（`stage1` 已过 `35/45 ∧ 每源10/15 ∧ undetected==0` 且 `final` 亦过）、`V54_DELTA16_ADDED_VALUE_SIGNAL`（`stage1` 未过但 `final` 过）、`V54_DELTA16_INSUFFICIENT`（`stage1` 与 `final` 均未过但完整性通过）、`V54_EVIDENCE_INVALID`（完整性/秩/嵌套/重叠/记账失败优先）；门禁 `35/45 & 10/15 & undetected==0` 不因 V53 差两个改阈值；`PASS` 系前两者（`ALREADY_SUFFICIENT` 或 `ADDED_VALUE_SIGNAL`），后两者为 `FAIL` 类，`EVIDENCE_INVALID` 单独。
- [ ] **A9** 冻结执行偏差防复发：不使用 600s 外部 timeout（建议≥3600s或不设）、若返回 session/cell ID 只轮询同一进程禁重启、中断时保留 raw partial 及调用计数不生成 aggregate、不得自动重跑；停止规则“仅四工件+decoder-free 二阶段 spike+45块 registry+泄漏/预算 report，不得实现/执行 decoder”。

> **Stage1/Final 累计口径冻结（45块累计，非调用记录）**：`stage1_final_exact_full_count = base_exact_full_count + inc1_rescued_exact_count`（`inc1_rescued = count(verify_stage1_call && exact_stage1 && !verify_base)`），对应 `verify_after_stage1_count = count(verify_base OR (!verify_base AND verify_stage1_call))`，`stage1_call_exact_full_count` 仅指 inc1 调用记录中 exact 数（=inc1_rescued），`verify_stage1_call` 仅指 inc1 调用中 verify 数；同理 `final_exact_full_count = stage1_final_exact_full_count + inc2_rescued_exact_count`（`inc2_rescued = count(verify_stage2_call && exact_stage2 && !verify_after_stage1)`），`stage2_call_exact_full_count = inc2_rescued`，`verify_final_count = verify_after_stage1_count + inc2_rescued_verify`；门禁与终态仅使用累计量 `stage1_final_exact_full_count / verify_after_stage1_count` 与 `final_exact_full_count / verify_final_count`，禁止用 `stage1_call_exact_full` 直接判门禁。
> 含糊的 `stage1_exact_full` 已区分更名为 `stage1_call_exact_full`（仅 inc1 调用记录） vs `stage1_final_exact_full_count`（累计），`final_exact_full_count = stage1_final + inc2_rescued`；终态只能使用后两个累计量。

## Phase B — 聚焦测试（仅 fake runner/decode_fn；零生产解码）

- [ ] **B1** Seed-registry 校验：拒重复/错形状/与已用 `frame_ids`（`V48 135区间540帧含 V53`）重叠 per source + 与 `FORBIDDEN 96` block ID 零重叠；接受新区建议 `395001-015/395101-115/395201-215` 但真实校验以 `frame_ids` 为准；校验 `K2≥15` 且 `index_j=floor(j*(K2-1)/14)` 分散选择 15/源、无内部重复、每源均分 15、与所有已用 `frame_ids` 零重叠 per source 可机械校验；校验最终 45 间两两非重叠（gap≥4）且与 `V53 394xxx` 零重叠。
- [ ] **B2** 嵌套矩阵重建匹配（二阶段）：`H_base m2×1024` 与 committed v38 常量一致；`H_inc1 8×1024` 与 V52/V53 det1 一致；`H_inc2 8×1024` 新增 deterministc 重建与 `joint1_rank==m2+8`、`total_rank==m2+16`、`nested1 H_base==H_joint1[:m2]`、`nested2 H_base==H_total[:m2] && H_joint1==H_total[:m2+8]`、`rank_increment_1==8`、`rank_increment_2==8`、`row_degree_max≤16`、`col_degree_inc1≤1`、`col_degree_inc2≤1`、`leak_stage1==leak_base+40`、`leak_stage2==leak_base+80`、`E_inc1≈96`、`E_inc2≈96` 均校验；任一漂移 → J 路径；重建一致；代表身份错→J。
- [ ] **B3** 泄漏与预算公式校验（三阶段）：`leak_base 1064/1094/1104`；`leak_stage1 1104/1134/1144`；`leak_stage2 1144/1174/1184`；`total=Σ base+40×N_stage1+40×N_stage2` 与 `avg=total/45` 可重算（`N_stage1=count(!verify_base)`，`N_stage2=count(!verify_base&&!verify_stage1)`，overall & per-source `N_stage1[s]/N_stage2[s]` 分别校验，因三源 `leak_base` 不同禁止用单一 `leak_base+40N/45` 当 overall）；`first_pass_success_leak==leak_base`、`stage1_success_leak==leak_stage1`、`stage2_leak==leak_stage2`（成功与失败同）、`failed_conditional==leak_stage2`、`avg_disclosure=overall_avg/1024`（per source 分层）、`total_disclosed_bits=Σ leak_total` 与 `disclosure_per_final_exact_block=total/final_count（为0则null）`、`f_avg` 若校验则分母 `1024×(H(U1|B)+H(U2|U1,B))` 禁 `N_blocks×(H1+H2)`；`rescue_rate_stage1=rescued_stage1/N_stage1（禁45-base_exact）` 与 `rescue_rate_stage2=rescued_stage2/N_stage2` 分别校验；校验不得再用 `successful_conditional==leak_base` 旧口径。
- [ ] **B4** Tag 复用与四类分流（L2-only 空前缀）：`compute_tag_64` 确定性、`hex[:16]`、`tag_scope==l2_only`；`base/stage1/stage2` 各自 `syndrome_ok&&!exact&&!tag_ok→detected` 等四类；`G3' undetected==0` 单独表；`base_exact` 与 `stage1_exact` 与 `final_exact` 均以 `tag_ok` 判公开成功但同时报告 `exact` 预留。
- [ ] **B5** 哨兵与嵌套与泄漏校验：每源首块建议 `395001/395101/395201` 各 `L1→P_i(U2)` 通路+`h_base_ok`+`h_inc1_nested_ok`+`h_inc2_nested_ok`+`joint1_rank_ok`+`total_rank_ok`+`independence_1_ok`+`independence_2_ok`+`leakage_accounted (base/stage1/stage2/avg/conditional/total)`+`tag_scope_l2_only`+`sampling_mode/frame_ids/ordinal/K2/index_j`。
- [ ] **B6** 样本一致性（fresh held-out, 排除 V53）：同 ID 重采样一致；缺/重 block →J6；校验 fresh 块非 `V48/V50/V51/V52/V53` 块且最终 45 间两两非重叠；校验 `45块 development held-out` 与门禁不因 V53 差两个改阈值。
- [ ] **B7** 真值表（四终态冻结）：枚举 integrity ok/failed × `V54_DELTA8_ALREADY_SUFFICIENT / V54_DELTA16_ADDED_VALUE_SIGNAL / V54_DELTA16_INSUFFICIENT / V54_EVIDENCE_INVALID` 互斥；另校验 `stage1_already_sufficient = stage1_final≥35/45∧每源10/15∧undetected0` 旗标与 `stage2_added_value = stage1_final未过∧final≥35/45∧每源10/15∧undetected0` 旗标可区分；`base_exact_full` 与 `verify_base` 分别计数、`stage1_call_exact_full` 与 `verify_stage1_call` 分别、`stage1_final_exact_full_count` 与 `verify_after_stage1` 分别、`final_exact_full_count` 与 `verify_final` 分别校验 `stage1_final = base + rescued_stage1` 且 `final = stage1_final + rescued_stage2`（`rescued_stage1` 仅对 `!verify_base` 者，`rescued_stage2` 仅对 `!verify_base&&!verify_stage1` 者）与门禁 `35/45 & 10/15 & undetected==0` 三条；校验 `V53 33/45` 仅历史描述不参与门禁。
- [ ] **B8** 预算与三阶段序校验：fake 结构校验每块 `L1 1+base 1+条件 stage1 ≤1+条件 stage2 ≤1` 冻结（L2 1-3，总 `45 L1+45 base+≤45 stage1+≤45 stage2=90-180 硬帽180，L2 45-135`）；源 1M/1p5M/2M、块升序、块内 `base→(条件)stage1→(条件)stage2` 序。
- [ ] **B9** 写出合约：文件集精确（45-135 L2行：45 base+≤45 stage1+≤45 stage2，含 `arm∈{base,stage1,stage2}/pass_index/used_inc1/used_inc2/matrix_id/joint`+`frame_ids/sampling_mode/ordinal`+`leak_total(1064/1094/1104 或 1104/1134/1144 或 1144/1174/1184)/leak_stage1/leak_stage2/leak_total/avg`；总调用 `45 L1+45 base+≤45 stage1+≤45 stage2=90-180 硬帽180；summary 含 `base/stage1/final` 三层 `exact` 与 `verify` 分别计数分层+每源+`first_pass/stage1_success/stage2_leak/avg` 泄漏分类（`per_source_avg` 与 `overall_avg` 区分，禁单一 `+40N/45` 当 overall）+`avg disclosure`+`total_disclosed_bits` 与 `disclosure_per_final_exact_block`（为0则null）+`joint1_rank/total_rank/nested/independence`+paired `base vs stage1 vs final`+`rescue_rate_stage1/stage2`+四类/G3'+`stage1_already_sufficient` 与 `stage2_added_value` 终态+门禁 `35/45 & 10/15 & undetected==0`）、CSV/JSON 行对等、已存在根 fail-closed、禁写 NPZ、provenance 含 `H_inc1 det1` 与 `H_inc2 det2` + fresh held-out 溯源含每块 `frame_ids/ordinal` 与 `K2/index_j`。
- [ ] **B10** 执行偏差防复发测试（仅 Python 单测可控）：fake 执行器模拟中断保留 raw partial 但不生成 aggregate → 通过（partial retention + no aggregate on interruption）；模拟中断后操作包禁重启/不生成新 session → 拒（操作包禁重启）；删除无法由 Python 单测控制的外部 600s timeout 包裹与 session/cell ID 外部轮询 fake 测试（该类外部超时/会话由执行层≥3600s 策略与轮询同一进程规则覆盖，不在单测中伪造）。

## Phase C — preflight（decoder-free，授权前，spike 复核）

- [ ] **P1** 重建 `Lane C base m2×1024` 三矩阵与 committed `rank/support` 比对；重建 `H_inc1 8×1024` 三矩阵与 `joint1_rank==m2+8 / nested / independence==8 / row≤16 / col≤1` 校验；重建 `H_inc2 8×1024` 三矩阵与 `total_rank==m2+16 / nested_stage2 / independence_2==8 / row≤16 / col≤1` 校验；`compute_tag_64` 可 import；校验 `leak_base/stage1/stage2` 公式（`leak_stage1==leak_base+40, leak_stage2==leak_base+80`）；校验 45 fresh 块枚举算法（`all_starts 0..H-4` 过滤已用 135 区间 540 帧得 `K2≥15`，`index_j` 分散选 15/源）与零重叠（与已用 135 区间零重叠且最终 45 间两两非重叠）可达；校验 TRAIN counts 形态；held-out fresh 池可达；零求值器、零写盘。
- [ ] **P2** 在首块建议 `395001/395101/395201` 跑 `L1 shared →P_i(U2)` 通路+`h_base/h_inc1/h_inc2/nested/rank/independence`+`tag_ok`+`leakage_accounted`+`tag_scope_l2_only`+`sampling_mode/frame_ids/ordinal/K2/index_j`；仅报 PASS/BLOCKED.
- [ ] **P3** 以拷贝已用区间机械校验 J2（新区建议 IDs `395xxx` 与已用 `frame_ids` 零重叠 per source + 最终45块两两非重叠、无重复、每源均分15且分散 `index_j`）并确认输出根缺席；仅报 PASS/BLOCKED.

## Phase D — 授权三阶段条件执行（需 EXECUTE_AUTH，45块三阶段，90-180 calls）

- [ ] **D1** 主线程获独立 plan ACCEPT + 显式用户 `EXECUTE_AUTH`（绑定 repository、分支 `formal-ir-mainline`、完整未来实现 SHA、cycle V54P0、scope `v54_two_stage_rescue_45_blocks_triple_exactly_once`，plan 引用 HEAD `bf5dd1686049156540328bac264296b17fee546c`）。
- [ ] **D2** 恰好一次：`python scripts/execute_v54_two_stage_rescue.py --execution-authorized --authorized-target-sha <sha>`；按 design §2.3 三阶段协议执行：`45块 × (base 1 + 条件 stage1 ≤1 + 条件 stage2 ≤1)`，每块 `L1 1+base L2 1+stage1 ≤1+stage2 ≤1`，总 `45 L1+45 base+≤45 stage1+≤45 stage2=90-180 硬帽180 (L2 45-135)`；**执行偏差防复发**：不设 600s 外部 timeout（建议≥3600s或不设）、若返回 session/cell ID 只轮询同一进程禁重启、中断时保留 raw partial 及调用计数不生成 aggregate、不得自动重跑；每条 L2 记录含 `arm∈{base,stage1,stage2}/pass_index/used_inc1/used_inc2/matrix_id/target_tag/candidate_tag/tag_ok/reclassified` (L2-only) 与 `exact_u1/exact_l2/exact_full/errors/syndrome/entropy/||q-p||1/leak_total(1064/1094/1104 或 1104/1134/1144 或 1144/1174/1184)/leak_stage1/leak_stage2`；保留 `run_01`；出错止、原样保留 raw partial 无聚合、返回 blocker；不 rerun/resume/tuning/加块/改增量/重实现 canonical；无论结果不做第二轮；同块 base→stage1→stage2 同 `bob` 同 `P_i(U2)`；采样恒 deterministic fresh held-out `K2/index_j` 分散。
- [ ] **D3** 只读 postcheck：记账 `base_exact_full` 与 `verify_base` 分别计数（禁止假定相等）、`stage1_exact_full` 与 `verify_stage1` 分别、`final_exact_full` 与 `verify_final` 分别、`stage1_rescued / stage2_rescued / final`、`.stage1/stage2 attempt counts`、`45` 块三阶段记录与序每块 `base→(条件)stage1→(条件)stage2` 正确、增量 `H_inc1` 与 `H_inc2` provenance、fresh held-out `frame_ids/ordinal` 逐块比对 design §2.4 `K2/index_j` 分散选择、无 NPZ、`V38–V53` 输出 byte-identical、summary 完备（含 `base/stage1/final` 三层 `exact` 与 `verify` 分别分层+每源+`first_pass_success_leak/stage1_success_leak/stage2_leak/per_source_avg[s]=leak_base[s]+40×N_stage1[s]/15+40×N_stage2[s]/15 / overall_avg=(Σ leak_base+40×N_stage1_total+40×N_stage2_total)/45（分母 45 禁单一 `+40N/45` 当 overall）/avg_disclosure/total_disclosed_bits 与 disclosure_per_final_exact_block（为0则null，`f_avg` 分母 `1024×(H(U1|B)+H(U2|U1,B))`）+`joint1_rank/total_rank/nested/independence`+paired( 三阶段 )+四类/G3'+`rescue_rate_stage1/stage2`+`tag acceptance`+四终态+门禁 `35/45 & 10/15 & undetected==0`+`leakage_formula(per_source/overall区分)`）、独立重算；校验门禁不因 V53 差两个改阈值。

## Phase E — 结果复核

- [ ] **E1** 写 `OPERATOR_RETURN.md` 与 `DEVELOPMENT_RESULT.md` 候选；lifecycle 保持 result-candidate；含 `base_exact_full` 与 `verify_base` 分别计数（禁止假定相等）/ `stage1_exact_full` 与 `verify_stage1` 分别 / `final_exact_full` 与 `verify_final` 分别 / `stage1_rescued / stage2_rescued` (overall & per-source, 45块)、`N_stage1_attempted=count(!verify_base)` 与 `N_stage2_attempted=count(!verify_stage1&&!verify_base)`（overall & per-source, 45块）、`rescue_rate_stage1=rescued_stage1/N_stage1（禁45-base_exact）` 与 `rescue_rate_stage2=rescued_stage2/N_stage2` / `first_pass_success_leak/stage1_success_leak/stage2_leak/per_source_avg[s]=leak_base[s]+40×N_stage1[s]/15+40×N_stage2[s]/15 / overall_avg=(Σ leak_base+40×N_stage1_total+40×N_stage2_total)/45（禁单一 `+40N/45` 当 overall）/avg_disclosure_per_attempted/total_disclosed_bits 与 disclosure_per_final_exact_block（为0则null）描述性、`joint1_rank/total_rank/nested/independence` 实测、`paired base vs stage1 vs final` per block 描述性、McNemar `b/c` 仅描述、四类计数、`l1` 执行期诊断上下文真实 `q≠p` 严格在 claim boundary 内（含 L2-only 工程近似与 `leak_base 1064/1094/1104` / `stage1 1104/1134/1144` / `stage2 1144/1174/1184` / `per_source_avg[s]/overall_avg` 且 first_pass 已成功帧不增泄漏、stage1 已成功帧不进入 stage2 说明，不宣称 FER/晋升，`V53 33/45` 仅历史描述，门禁不因 V53 差两个改阈值）。
- [ ] **E2** 主线程/reviewer 独立重算 `base/stage1/final/rescued_stage1/rescued_stage2`、泄漏、嵌套秩、配对与终态与四类；verdict 入 `REVIEW_VERDICT.md`.
- [ ] **E3** Memory triage 单独里程碑（本规划轮禁改 `AGENT_PROJECT_MEMORY.md`）；**不启动下一阶段**。

## 本变更期间显式禁止

plan ACCEPT 前实现；以 outcomes 回搜增量位置/调 `Δm` 或多候选择优；改首遍 support/标签/先验/度分布/m2/H1/泄漏/decoder；重复比较 prior、标签或另一全新 MET 图；新增第三增量或档位；以 `V48/V50/V51/V52/V53` outcomes 选持块或调 `Δm` 或用 V53 outcomes 选 H_inc2 行；写 NPZ；把 wrong 计为 exact；作晋升排名；FER/阈值/SKR/安全/资格/晋升陈述；结果后加块/加 seed/改阈值/改机制；rerun/resume/补偿 partial；任何结果下开第二轮；自接受；自动启动后继（含下一阶段）；import `v50/v51/v52/v53` 模块作生产解码；任何 `compute_tag_64(x1_true,…)` 形态；改 H1/`m2` 泄漏或 `90/1.0` 或引入第二变量外泄漏变更；创建正式 `.../v54_*/run_01` 输出（P0 仅规划）；运行 decoder（P0 仅 decoder-free spike）；声称 `V53 33/45` 为门禁依据或因差两个改阈值至 33/45；使用 600s 外部 timeout 包裹执行；对返回的 session/cell ID 重启新进程而非轮询同一进程；中断后生成 aggregate 或自动重跑；为凑 30 块小样本而降低门禁至 `24/30` 等；将 `development confirmation` 宣称为 `qualification`；新造 H_inc1 或用不同 `Δm=4/12/16` 代替 `8+8` 二阶段。
