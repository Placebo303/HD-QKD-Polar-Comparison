# OpenSpec Tasks: formal-ir-v72p1-soft-joint-binary-adapter — 最小 adapter 衔接 1024 local factor 自排除 ↔ binary IRA mother 9036×10240 增量 syndrome ↔ exact 64-bit tag (PLAN_CANDIDATE / SYNTHETIC_ONLY / EXECUTE_NOT_AUTHORIZED)

**Lifecycle**: `PLAN_CANDIDATE / SYNTHETIC_ONLY / EXECUTE_NOT_AUTHORIZED` — **10 步 S1-S10 + 3 类型 Config/Result/Adapter，P1A plumbing / P1B tiny 端到端 64/512 8trials tree-only 1e-9 c3观测 / P1C full-size 9036×10240 1/3/10 iter smoke 30s / P1D small loopy n12 k3 描述性，边/内存/复杂度探针，5 态 2 态，四工件双报告 + test，守卫 R72P1-01~10，不跑 decoder 不改 V70/V72P0 不启 V72**

**HEAD**: `TBD (freeze-time git rev-parse HEAD; EXTERNAL_BINDING until Pre-RESULT)` + data `84d62779 synthetic_v72p1` — **P1A plumbing 确定性/前缀/增量/tag / P1B tiny 64/512 c3负向 1e-9 / P1C 1/3/10 smoke wall<1/5/30s / P1D loopy n12 k3 capacity_warning / edges≈49698 CSR≈235KB O(nnz+Q·1024)**

**Predecessor**: `formal-ir-v72p0-soft-joint-binary-synthetic 5591e16bf35b03c3df30a003bee12011a796d73e (V72P0 64ca2f1e)` + `formal-ir-v71-soft-joint-factor-kernel e038114db5095a57158b0e1cd36884d6a1a5d8be` + `formal-ir-v70-binary-soft-joint-feasibility 9bc34be6` → `V72P1-ADP`

**Method frozen**: `Q1024 N1024 Nbit10240 M9036 r0 160 Δ8 max9036 f1.3 NOT_MEASURED used_2m false tag64b exact SHA256 LE col sym*10+bit bit_i(s)=(s>>i)&1 LF去self Σ_{j≠i} logsumexp mother 9036×10240 sparse CSR IRA dual-diagonal det1 rank9036` 零改；10 步 S1-S10 + 3 类型 + P1A-D 四层 synthetic

**Boundary**: 仅验证 adapter 无损衔接 + 边内存复杂度，不改主体；S1 synthetic_bits → S2 prior → S3 LF去self T_LF01-08 1e-12/1e-9 → S4 extrinsic打包 sym*10+bit → S5 mother前缀 H_r → S6 增量syndrome s_{r+8}=s_r∪new8 → S7 披露 r·1+64 f NOT_MEASURED → S8 exact tag SHA256 LE → S9 边内存估算 → S10 5态2态，P1A plumbing 确定性/前缀/增量/tag + P1B tiny 64/512 c3观测负向 1e-9 + P1C 1/3/10 smoke wall<1/5/30s + P1D loopy n12 k3 描述性，每 case 5 终端总体 2 态，不跑 decoder 不启 V72

## Phase A — 合成注册表（SYNTHETIC_ONLY，不读 2M，不启 V72）

- [ ] **A1 冻结合成注册表 `v72p1_data_registry_synthetic.json`**：写入 `schema v72p1_synthetic_v1, lifecycle PLAN_CANDIDATE, data_sha 84d62779, head TBD (freeze-time git rev-parse HEAD), reused_from v72p0_synthetic_v1 (5591e16b), successor_v72_not_started true, frozen {Q 1024,N 1024,Nbit 10240,M 9036,r0 160,delta 8,max 9036,f 1.3,f_actual NOT_MEASURED,tag 64b exact, col sym*10+bit, seed_mother V72P1-ADP-MOTHER, P1A {plumbing deterministic/prefix/incremental/tag}, P1B {k2/3 n6/9 64/512 8trials tree-only c3_obs worst 1e-9}, P1C {9036×10240 sparse_CSR_IRA 1/3/10 smoke wall<1/5/30s C1-C6 pivot 160/168/176/9036}, P1D {n12 k3 12bits loopy descriptive cycle1}, edges {nnz≈49698}, memory {CSR≈235KB}, used_2m false, used_test false}`，校验 `Q1024 N1024 M9036 f1.3 NOT_MEASURED used_2m false successor_v72_not_started==true`，且 `rg -i "2M.*real|read.*2M" v72p1_data_registry_synthetic.json 0 hits` 已验，且 `V72_not_started` 已显式。
- [ ] **A2 10 步 S1-S10 与 3 类型冻结**：在 `v72p1_manifest.json:frozen_body` 写入 `S1 synthetic_bits 10240 / S2 log_prior 1024 / S3 LF去self Σ_{j≠i} T_LF01-08 / S4 extrinsic打包 sym*10+bit / S5 mother前缀 H_r / S6 增量syndrome s_{r+8}=s_r∪new8 / S7 披露 r·1+64 f NOT_MEASURED / S8 exact tag SHA256 LE / S9 边内存 nnz/CSR/peak / S10 5态分流` 10 步定义，且 `AdapterConfig / AdapterResult / Adapter` 3 类型签名稳定已验，`rg -i "v72|qualification" 0 hits`（除 successor 注释），`V72_not_started` 已显式。

## Phase B — 冻结主体零改（SYNTHETIC_ONLY，不构业务矩阵，不启 V72）

- [ ] **B1 主体明文化（逐项显式，冻结零改，不启 V72）**：写入 `v72p1_manifest.json:frozen_body {Q1024,N1024,Nbit10240,M9036,r0 160,Δ8,max9036,f 1.3,f_actual NOT_MEASURED,used_2m false,tag 64b exact SHA256 LE,col sym*10+bit,bit_i(s)=(s>>i)&1,LF 去self Σ_{j≠i} logsumexp,T_LF01-08 1e-12/1e-9,mother 9036×10240 sparse CSR IRA dual-diagonal det1 rank9036 info 3-4 H_p2, Rs r0 160 Δ8,leak r·1+64, adapter pack/syndrome/tag/prefix/incremental, 10步 S1-S10, 3类型 Config/Result/Adapter, S1-S10 3类型已冻结, successor_v72_not_started true}`，显式 `not Gray/not MET/protograph/SC/not H business construction/not V72`，`git diff -- src/ ==0 && git diff -- experiments/ ==0 && git diff -- tools/ ==0 && git diff -- comparison_bench/src/comparison_bench/formal_ir/ldpc_v5* ==0` 已验。
- [ ] **B2 权威算法只读复用（SYNTHETIC_ONLY）**：SYNTHETIC_ONLY 合成 bits→H syndrome→tag→LF→wall 链路复用 `V72P0` 的 `legacy_v1` synthetic 生成与 `scipy.sparse CSR` 逻辑，不读 2M，不重写 V72P0 的 tree/small 校验；`10-bit位展开` 仅在 `a` 的 `s` 上比特置换，不改帧边界，不读 TEST，不启 V72，不执行 `run_ldpc_formal_v5`。
- [ ] **B3 不构业务矩阵守卫 + 不改 V70/V72P0 守卫 + 不启 V72 守卫**：`rg "decode_" scripts/v72p1_soft_joint_binary_adapter.py 0 hits && rg "decode_" scripts/v72p1_backend_audit.py 0 hits` 且不调用任何业务 `H` 构造/`nested`（`ldpc_v5*` 只读探针不属业务 `construct`，仅 AST/signature 探针），且 `rg -i "v72|qualification" scripts/v72p1_soft_joint_binary_adapter.py 0 hits && rg -i "v72|qualification" scripts/v72p1_backend_audit.py 0 hits`（除 successor 注释 `V72_not_started`），且 `git diff -- openspec/changes/formal-ir-v70-binary-soft-joint-feasibility/ ==0 && git diff -- openspec/changes/formal-ir-v72p0-soft-joint-binary-synthetic/ ==0` 已验，`py_compile` 双脚本校验；脚注 `ponytail:` 标明 ceiling（若需后续真码率自适应，需另起 OpenSpec）。

## Phase C — 去self 8 测试 T_LF01-08（SYNTHETIC_ONLY，10 步 S3）

- [ ] **C1 去self 定义（S3）**：写入 `v72p1_manifest.json:local_factor {def: "LLR_{→i}=logsumexp_{bit_i=1} Σ_{j≠i} bits_j·llr_j - logsumexp_{bit_i=0} Σ_{j≠i}", logsumexp: "numpy.logaddexp.reduce 手写", self_exclusion: "Σ_{j≠i}"}`，与 `T_LF01-08` 阈 `1e-12/1e-9` 已显式，`S3` 与 `S4 打包` 语义一致已验。
- [ ] **C2 纯函数 log-domain 实现（枚举 1024，logsumexp，无 numba）**：在 `scripts/v72p1_soft_joint_binary_adapter.py` 实现 `validate_local_factor(log_prior_1024, llr_10) → log_post_excl`（枚举 1024 态，`logsumexp` 实现，`i∈{0,5,9}` 三点探针，`K=1e6` 稳定性 isfinite），校验 `T_LF01 completeness / T_LF02 normalization 1e-12 / T_LF03 marginal 1e-12 / T_LF04 delta 1e-9 / T_LF05 self_exclusion 1e-12 / T_LF06 stability K1e6 isfinite / T_LF07 determinism==0 / T_LF08 brute 1e-12` 正交 8 test 全 PASS，已验 `max|Δ|` 与 `isfinite`。
- [ ] **C3 落盘 T_LF01-08**：在 `v72p1_results.json:kernel {T_LF01..T_LF08 per case, KERNEL_PASS}` 已验 `KERNEL_PASS = T_LF01∧T_LF02∧...∧T_LF08`，且 `v72p1_table.csv` 行对等，`V72P1_SYN_REPORT.md` 与 json 一致。

## Phase D — P1A plumbing（确定性/前缀/增量/tag，SYNTHETIC_ONLY）

- [ ] **D1 P1A plumbing 执行（S1+S4+S5+S6+S8，确定性）**：在 `scripts/v72p1_soft_joint_binary_adapter.py` 执行 P1A 4 probes：`pack_determinism 同输入二次 max|Δ|==0 / prefix_nested ∀r∈{160,168,176,9036} H_r==H_mother[0:r] / incremental s_{r+8} 异或一致 (s_{r+8}[0:r]==s_r 且新增 8 行独立) / tag_exact SHA256 LE 64 samples 全 exact`，各 probe `PASS/FAIL` 已验，`wall≤30s peak≤2048MiB` 已验，落盘 `v72p1_results.json:P1A {pack_ok,prefix_ok,incremental_ok,tag_ok,deterministic_ok,wall,peak}`。
- [ ] **D2 P1A 落盘与校验**：`P1A_PASS = pack_ok ∧ prefix_ok ∧ incremental_ok ∧ tag_ok ∧ deterministic_ok ∧ wall≤30` 已验，`v72p1_table.csv` 行对等，`V72P1_SYN_REPORT.md:P1A` 章节与 json 一致。

## Phase E — P1B tiny 端到端（k2/3 n6/9 64/512 各8 trials tree-only 1e-9，含负向测试）

- [ ] **E1 P1B tiny 执行（S1+S3+S4+S6+S8，经 adapter 端到端，tree-only）**：在 `scripts/v72p1_soft_joint_binary_adapter.py` 执行 `configs (k=2,n=6)→total_bits 6 exhaustive 64 与 (k=3,n=9)→total_bits 9 exhaustive 512 各8 trials 种子0..7，H_small tree forest (each var degree 1 无环) 保证无环 BP exact` 经 adapter 到 `syndrome+tag` 端到端，每 trial 计 `c1 isfinite / c2 brute exists isfinite / c3 observed_zero&&observed_one fail-closed (负向测试：observed_one==false ⇒ c3 false 且数值 64/512 wall delta 保持) / c4 brute exists / c5 BP-marg vs brute max|Δ|<1e-9 tree-only / c6 tree / c7 total≤9 exhaustive` 7 checks，全 trials 全 m 需全 PASS，且 `syndrome增量 && tag exact` 亦 PASS，`wall≤30s peak≤2048MiB` 已验，落盘 `worst<1e-9`。
- [ ] **E2 tiny tag exact + incremental 落盘**：`v72p1_results.json:P1B {k2_n6 {c1..c7,c3_observed,worst,wall,peak,syndrome_ok,tag_ok}, k3_n9 {...}, P1B_PASS}` 已验 `P1B_PASS = ∀trials ∀(k,m) c1..c7 true ∧ c3_obs true ∧ worst<1e-9 ∧ syndrome_ok ∧ tag_ok ∧ wall≤30`，`v72p1_table.csv` 行对等。
- [ ] **E3 负向测试保留（c3 fail-closed）**：构造 `observed_one==false` 人工 case（全零 observed）探针 `c3==false` 且 `64/512 exhaustive 计数、wall、delta 数值保持` 已验，落盘 `negative_test {input observed_one false ⇒ c3 false, wall unchanged, delta unchanged}`，`V72P1_SYN_REPORT.md:P1B` 章节 `c3_obs` 与负向测试已显式。

## Phase F — P1C full-size 1/3/10 iter smoke + P0B re-validate（9036×10240 sparse CSR IRA）

- [ ] **F1 mother 生成（S5，sparse CSR IRA dual-diagonal）**：在 `scripts/v72p1_soft_joint_binary_adapter.py` 以 `SeedSequence("V72P1-ADP-MOTHER")` 生成 `H_mother 9036×10240 sparse CSR (scipy.sparse.csr_matrix, dtype uint8/int32)`，`H_p dual-diagonal (diag+subdiag) det1 ⇒ rank9036`，`info 每行 3-4 列随机`，`nnz≈49698 (9036*5.5)` 已验 `nnz ∈ [49200,50200]`，落盘 `edges {nnz, row_deg_min>0, row_deg_max≤6, col_deg, zero_cols 0, dup 0}`，`memory {CSR_bytes≈235KB, indptr, LLR_bytes≈80KB, peak}`。
- [ ] **F2 C1-C6 re-validate + pivot（S5+S6+S8，复用 V72P0）**：校验 `C1 rank9036 (H_p det1) / C2 row_min>0 / C3 dup0 (行 hash 去重) / C4 prefix_nested H_r==H_mother[0:r] / C5 incremental s_{r+8} 异或一致 / C6 tag64 exact 64 samples SHA256 LE` 全 PASS，且 `pivot 160/168/176/9036 Rs r0 160 Δ8→9036 prefix_nested true` 已验，落盘 `P0B_revalidate {C1..C6, pivot4, nnz, zero0, dup0}`，`v72p1_table.csv` 行对等。
- [ ] **F3 P1C 1/3/10 iter smoke（S9，三档 wall/peak/边/内存/增量/tag）**：在 `H_mother 9036×10240` 上对固定 `synthetic_bits 10240 种子0` 跑 `1 iter / 3 iter / 10 iter` 三档 smoke（各 iter 的消息传递，仅计量不判收敛），每档计 `wall_s, peak_MiB, per_invocation_ns, nnz/row_deg/col_deg/CSR_bytes, syndrome_inc_ok, tag_ok`，阈 `1 iter <1s && 3 iter <5s && 10 iter ≤30s && peak≤2048MiB` 已验，`syndrome增量 && tag exact` 三档全 PASS 已验，落盘 `P1C {iter1 {...}, iter3 {...}, iter10 {...}, P1C_PASS}`，`P1C_PASS = C1-C6∧pivot4∧1/3/10三档 wall/peak/增量/tag 全PASS`，`V72P1_SYN_REPORT.md:P1C` 章节与 json 一致。

## Phase G — P1D small loopy（n12 k3 含环描述性，不入硬门禁）

- [ ] **G1 P1D small loopy 执行（n12 k3 12bits 8 trials 含单环）**：在 `scripts/v72p1_soft_joint_binary_adapter.py` 执行 `n=12 k=3 total_bits 12 ×8 trials 种子0..7，H_small_loopy 含单环 (cycle_count 1, 非 tree)` 经 adapter 到 `syndrome+tag`，每 trial 计 `syndrome_ok (H·b mod2 异或一致) / tag_ok (SHA256 LE exact) / cycle_count (DFS 手写计数 1) / BP_residual (BP 后残差描述性) / wall`，落盘 `P1D_descriptive {n12_k3_loopy: {syndrome_ok, tag_ok, cycle_count, BP_residual, wall, capacity_warning}}`。
- [ ] **G2 capacity_warning 正交旗标**：`capacity_warning = (cycle_count>0 || BP_residual>1e-9)` 正交旗标已落盘 `v72p1_results.json:P1D.capacity_warning`，报告 `V72P1_SYN_REPORT.md:P1D` 章节单独 `descriptive_diagnostics` 与 `capacity_warning` 已显式，**不入 `ADAPTER_PLAN_READY` 硬门禁**，仅描述性，`V72P1_BACKEND_AUDIT_REPORT.md` 一致。

## Phase H — 5 态 2 态 + 边内存复杂度 + T0-T3

- [ ] **H1 5 终态判定（S10，per-case，first-match 互斥）**：`if !synthetic_bits_ok or !H_mother_exists → EVIDENCE_INCOMPLETE / elif !T_LF01_02_PASS → KERNEL_FAIL / elif !P1A_PASS or !P1B_PASS → PLUMBING_TINY_FAIL / elif !P0B_revalidate_PASS or !P1C_PASS → MATRIX_SMOKE_FAIL / elif T_LF01-08_PASS∧P1A_PASS∧P1B_PASS∧P1C_PASS∧wall≤30∧peak≤2048 → ADAPTER_PLAN_READY (需 4 hard PASS，P1D 仅描述)` first-match 优先级已验，每 case `classification + successor` 已落盘 `v72p1_results.json:classification`，`P1D capacity_warning` 正交不过门禁已验。
- [ ] **H2 总体 2 态**：`overall = ADAPTER_PLAN_READY iff ∀cases classification==ADAPTER_PLAN_READY else NOT_READY` 基于 `5 orthogonal counts {EVIDENCE/KERNEL/PLUMBING_TINY/MATRIX_SMOKE/ADAPTER_PLAN_READY: n}` 已判定，落盘 `overall + 5 counts + edges/memory/wall`，`v72p1_table.json` 行对等，报告 `overall 2 态` 章节与 json 一致。
- [ ] **H3 边内存复杂度审计表**：落盘 `audit {per_case classification, overall, 5 counts, per_case P1A/P1B/P1C/P1D, edges {nnz,row_deg,col_deg,zero0,dup0}, memory {CSR_bytes,peak_MiB}, wall {P1A,P1B,P1C iter1/3/10, per_invocation_ns}, complexity {O(nnz+Q·1024) ponytail ceiling}, f1.3 NOT_MEASURED, successor per case, capacity_warning}`，报告 `V72P1_SYN_REPORT.md:edges/memory/complexity` 审计章节显式三档 `1/3/10 wall/peak/CSR` 与 `nnz≈49698`。
- [ ] **H4 T0-T3（SYNTHETIC_ONLY，适配器新增）**：`T0 compile/import/tiny-math 1e-12 (py_compile + T_LF01-08 小矩阵)` / `T1 unit & tamper 8test+enum+P1A plumbing+P1B负向` / `T2 fake/test-only replay (fake H_small 跑全量 64/512 不触 real)` / `T3 regression rg decode_ 0 hits && rg "ADAPTER_PLAN_READY" 0 hits (枚举用 IntEnum) && V70/V72P0 只读` 已验，落盘 `T0_T3 {T0 true, T1 true, T2 true, T3 true}`。

## Phase I — 四件双报告 + 复杂度探针 + T0-T3（SYNTHETIC_ONLY）

- [ ] **I1 编写 `scripts/v72p1_soft_joint_binary_adapter.py`** (SYNTHETIC_ONLY, 本变更目录下): `python scripts/v72p1_soft_joint_binary_adapter.py [--out v72p1_results.json]` → `T_LF01-08 → P1A plumbing deterministic/prefix/incremental/tag → P1B tiny 64/512 8trials c3观测负向测试 → P1C 9036×10240 1/3/10 smoke C1-C6 pivot → P1D loopy descriptive → edges/memory/complexity → 5态2态`，`rg "decode_" 0 hits` `rg -i "met|protograph|sc_coupling" 0 hits`（除 adapter 注释）仅 `numpy/pandas/pyarrow/scipy.sparse/hashlib`，`py_compile PASS`，输出 `v72p1_results.json + v72p1_table.(csv|json) + v72p1_manifest.json + V72P1_SYN_REPORT.md` + 控制台 5 态/2 态摘要。
- [ ] **I2 编写 `scripts/v72p1_backend_audit.py`** (read-only 3类型+10步+5态): `python scripts/v72p1_backend_audit.py [--out v72p1_audit_report.json]` → `AdapterConfig/AdapterResult/Adapter 3 类型校验 + S1-S10 10步完整性 + 5态枚举探针 (IntEnum)`，`rg "decode_" 0 hits` `rg "\"ADAPTER_PLAN_READY\"" 0 hits`（枚举用 `==BackendState.ADAPTER_PLAN_READY` 比较）仅 `ast/importlib`，`py_compile PASS`，输出 `v72p1_audit_report.json + V72P1_BACKEND_AUDIT_REPORT.md` + 控制台摘要。
- [ ] **I3 执行 SYNTHETIC_ONLY 回填（含 T_LF + P1A/B/C/D + 边内存）**：运行 `scripts/v72p1_soft_joint_binary_adapter.py` 与 `scripts/v72p1_backend_audit.py` 得每 case `T_LF01-08/P1A/P1B/P1C/P1D/edges/memory/wall/class` 与 `overall 2 态`，落盘 `v72p1_results.json` 与 `v72p1_table.csv/.json`（CSV 行对等，含 `capacity_warning` 正交 + `S1-S10/3类型/边内存`）与 `v72p1_audit_report.json`，`T_LF 1e-12` 已验，`P1A plumbing` 已验，`P1B 64/512 c3观测负向 worst<1e-9` 已验，`P1C 1/3/10 30s` 已验，`P1D loopy` 描述性已验，`edges≈49698 CSR≈235KB` 已验，`f NOT_MEASURED` 已验。
- [ ] **I4 撰写双报告**：`V72P1_SYN_REPORT.md`（`per case T_LF01-08 / P1A plumbing / P1B 64/512 c3_obs worst / P1C 1/3/10 smoke edges/CSR/wall / P1D loopy capacity_warning + overall 2 态 + S1-S10 3类型 + 10步数据流 + 边内存复杂度 + complexity spike`）与 `V72P1_BACKEND_AUDIT_REPORT.md`（`3类型/10步/5态 IntEnum`）与 `json/csv` 一致，不扩大为 `FER/SKR`，显式 `SYNTHETIC_ONLY used_2m false f1.3 NOT_MEASURED`，`O(nnz+Q·1024) ponytail ceiling` 已标。
- [ ] **I5 自检（5态2态+守卫+R72P1-01~10，不启 V72）**：`py_compile` 双脚本 PASS, `rg "decode_" 0 hits`, `rg -i "met|protograph" 0 hits`, `rg -i "v72|qualification" 0 hits`（除 V72_not_started 注释），`git diff -- src/ ==0` 未改码, `git diff -- openspec/changes/formal-ir-v72p0-soft-joint-binary-synthetic/ ==0` 未改 V72P0, `git diff -- openspec/changes/formal-ir-v70-binary-soft-joint-feasibility/ ==0` 未改 V70, `T_LF 1e-12` 已验, `P1A plumbing` 已验, `P1B 64/512 c3_obs 负向 worst<1e-9` 已验, `P1C 1/3/10 9036×10240 C1-C6 pivot wall<1/5/30s` 已验, `P1D loopy` 描述性已验, `edges≈49698 CSR≈235KB peak<2GiB` 已验, `5态优先级互斥` 已验, `overall 2态` 已验, `3类型10步` 已验, `TEST未读 2M未读` 已验, `V72_not_started` 已验, 报告与 json/csv 一致 **无 TBD**, **S1-S10 已闭合**。
- [ ] **I6 小测试**：`pytest -p no:cacheprovider -q` 小测试（`test_v72p1_soft_joint_binary_adapter_small.py`）验证 `bit展开 sym*10+bit / T_LF01-08 小矩阵 1e-12 / P1A plumbing mock / P1B tiny c3观测负向 5态枚举 / P1C smoke mock (fake H 4×8) 1/3/10 wall 模拟 / P1D loopy cycle_count / edges/memory mock + TEST隔离 + V72_not_started`，`py_compile` 双 PASS。
  - ponytail: `adapter` 为 `sym*10+bit` 纯移位/掩码 + `scipy.sparse.csr_matrix` 模 2 异或 + `logsumexp`，不引 `numba`；`P1B tiny` 小矩阵为 `tuple` 校验，`O(64/512)` 足够；`P1C smoke` 小测试仅 `fake H 4×8` 不测 `9036×10240` 全量以控成本；`P1D loopy` mock 用 `n12 k3` 单环手写 DFS。

## Phase J — 守卫 R72P1-01~10 + 单独提交推送新 Plan SHA + 不启 V72

- [ ] **J1 守卫 R72P1-01~10 落盘验证**：在 `v72p1_manifest.json:guards {R72P1-01..R72P1-10}` 逐项 `true`，见 Design §8 / Spec §10。
  - R72P1-01 冻结主体 +2M 禁 +10 步+3 类型+不启 V72
  - R72P1-02 去self 8 测试 1e-12/1e-9
  - R72P1-03 P1A plumbing 确定性前缀增量 tag
  - R72P1-04 P1B tiny 64/512 c3 观测 1e-9
  - R72P1-05 P1C full-size 1/3/10 smoke 9036×10240 C1-C6 pivot wall<1/5/30s
  - R72P1-06 P1D small loopy 描述性 capacity_warning
  - R72P1-07 边内存复杂度估算 nnz≈49698 CSR≈235KB O(nnz+Q·1024)
  - R72P1-08 per-case 5 终端总体 2 态 wall≤30s
  - R72P1-09 四工件+双报告完整
  - R72P1-10 SYNTHETIC_ONLY 不构业务矩阵 + 不读 2M/TEST + 不创 run_01 + 不改 V70/V72P0 + 不启 V72
- [ ] **J2 单独提交推送四工件+registry+双脚本+双报告（V72P1 provenance — 新 Plan SHA）**：`git add openspec/changes/formal-ir-v72p1-soft-joint-binary-adapter/ scripts/v72p1_soft_joint_binary_adapter.py scripts/v72p1_backend_audit.py v72p1_data_registry_synthetic.json v72p1_results.json v72p1_table.csv v72p1_table.json v72p1_audit_report.json V72P1_SYN_REPORT.md V72P1_BACKEND_AUDIT_REPORT.md test_v72p1_soft_joint_binary_adapter_small.py v72p1_manifest.json && git commit -m "formal-ir-v72p1: soft-joint binary adapter 10step 3type P1A/B/C/D 64/512 9036×10240 1/3/10 smoke loopy edges" && git push origin formal-ir-mainline`，返回新 `Plan SHA`（40 位），记录于 `proposal/design/tasks` HEAD 占位替换，**不创建 run_01，不启 V72**。
- [ ] **J3 停留 `PLAN_CANDIDATE / SYNTHETIC_ONLY / EXECUTE_NOT_AUTHORIZED` + `V72_NOT_STARTED`**，未创建任何 `.../v72p1_*/run_01` 且未创建任何 `.../v72_*/run_01`，未构业务矩阵，不比较，不碰 `V48-V72P0` 块外，未转 qualification，未启动 V72，仅改本目录 + `scripts/`，**推送后等待独立审核**（`Pre-RESULT` 独立线程复核 `HEAD/ACCEPTED_PLAN_SHA/rg 0 hits/run_01 不存在/py_compile/2M 未读/T_LF 1e-12/P1A plumbing/P1B 64/512 c3观测 1e-9/P1C 9036×10240 1/3/10 smoke/C1-C6 pivot/边内存/5态2态/10步3类型/V72_not_started`），返回 `Plan SHA / per-case P1A/B/C/D/edges/memory/wall + 各分流计数 + overall 2 态` 等待 `PLAN_ACCEPT`。

## 本变更显式禁止

decoder/业务矩阵构造（`decode_*` / `construct_*_business` / `gf_rank_business` / `nested_business` 等）；读密封 `TEST` 或 `2M` 的 `H/CE/NLL/MAP` 统计或将其用于 adapter 选择；调 `Q/N/M/f/tag/col/LF/mother/Rs/Δ` 任一冻结参数或新增业务矩阵；跨 acquisition 拼接；将 floor/round 当 ceil；将 `min(1024, ceil(...))` cap 伪装当通过；将 `+8` 外 degree/seed 网格当自适应；用 `VAL/TEST` 择优；将不足 3 伪判为 complete；将 acquisition 未去重多计；将 1024 枚举剪枝；将纯函数全零/ delta 单极当全验证（必须双极 1e-12）；将 `T_LF 1e-12` FAIL 当可行；将 P1C smoke wall 阈放宽（必须 1<1s 3<5s 10≤30s）；引 MET/protograph/SC/Gray（仅 adapter）；改 `src/` 基线；宣称 LDPC 证伪或 `FER/阈值/SKR/晋升`；创建正式 `.../v72p1_*/run_01`；任意 `bin_width/dimension/pairing/mapping` 网格或阈网格（处理点单点）；用第二 estimator 作门禁；覆盖已有输出；`V55 90 / V48-V72P0` 永久禁用违反；主观“显著可行”替代硬阈 `T_LF 1e-12 ∧ P1A∧P1B 1e-9 ∧ P1C 1/3/10 wall`；擅自调 V72P1 以外码（仅 adapter）；保留 TBD 占位不回填；启动 V72（任何 `V72_*/run_01`、`QUALIFICATION_PLAN_READY`、`V72` OpenSpec 预冻结均禁止）；执行 `run_ldpc_formal_v5`（仅只读审计）。

## 验收

- proposal/design/tasks/specs 一致 `V72P0 5591e16b→新 Plan SHA` `84d62779` lifecycle `PLAN_CANDIDATE / SYNTHETIC_ONLY / EXECUTE_NOT_AUTHORIZED` + `V72_NOT_STARTED` 5 态 2 态按优先级互斥明确，显式 10 步 S1-S10 + 3 类型 Config/Result/Adapter + P1A plumbing / P1B tiny 64/512 c3观测 1e-9 / P1C full-size 9036×10240 1/3/10 smoke C1-C6 pivot / P1D small loopy 描述性 + 边内存复杂度 10 步数据流已声明，严格复用 V56 权威算法，冻结分段/熵-CE 与 f 公式/分流阈 + V72_not_started
- 10 步数据流 + 3 类型已验，`T_LF01-08 1e-12` 已验，`P1A plumbing` 已验，`P1B 64/512 c3观测 负向 worst<1e-9` 已验，`P1C 1/3/10 9036×10240 C1-C6 pivot wall<1/5/30s` 已验，`P1D loopy` 描述性已验，`edges≈49698 CSR≈235KB` 已验，`5态` 已验，`overall 2态` 已验，`wall≤30s` 已验，不扩，禁第二 estimator 已验
- 合同 `Q1024 N1024 Nbit10240 M9036 r0 160 Δ8 max9036 tag64 f1.3 NOT_MEASURED col sym*10+bit` 算法一致已验，`10-bit位展开` 每 case 已验，`S1-S10` 无丢已验
- 每 case `S1 bits→S2 prior→S3 LF去self→S4 pack→S5 mother→S6 syndrome→S7 leak→S8 tag→S9 边内存→S10 5态` 已算且 `SYNTHETIC_ONLY` 不读 2M 已验，`P1A/B/C/D` 已算已验，`T_LF` 双极 `1e-12` 已验
- 每 case `5 终端 final_classification + successor` 已落盘，`overall 2 态` 已统计
- `cases (P1A+tiny+smoke+loopy) + overall 2 态 / T_LF/P1A/B/C/D/edges/memory/wall/classification/successor/capacity_warning/descriptive` 已回填，`报告表CSV行对等JSON` 已验，`overall 2 态` 已验，`V72_not_started` 已验
- 守卫 `R72P1-01~10` 已验（`10步3类型 / T_LF 8test / P1A plumbing / P1B tiny c3观测 / P1C 9036×10240 1/3/10 smoke C1-C6 pivot / P1D loopy / 边内存复杂度 / 5态2态wall / 四工件双报告 / SYNTHETIC_ONLY不构业务矩阵不读TEST不创run_01不改V70/V72P0不启V72`）
- 双脚本 `rg "decode_" 0 hits` `rg -i "met|protograph" 0 hits` `rg -i "v72|qualification" 0 hits`（除 V72_not_started 注释） `py_compile` PASS `pytest小测试` PASS `f NOT_MEASURED` 已验 `2M未读` 已验 `T_LF 1e-12` 已验 `P1A plumbing` 已验 `P1B 64/512` 已验 `P1C 1/3/10` 已验 `P1D loopy` 已验 `edges≈49698` 已验 `capacity_warning` 正交与 `descriptive` 已落盘报告与 json/csv 一致未建 `run_01` 未启 `V72` 已停留 `PLAN_CANDIDATE / SYNTHETIC_ONLY / EXECUTE_NOT_AUTHORIZED` 仅改本目录 + `scripts/`（`src/` 零改），未启动 decoder/业务矩阵，**四工件+registry+双脚本+双报告已单独提交推送，新 Plan SHA + per-case P1A/B/C/D/edges/memory/wall + 各分流数 + overall 2 态已返回**，推送后等待独立审核
