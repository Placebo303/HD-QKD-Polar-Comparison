# OpenSpec Tasks: formal-ir-v66-single-segment-adaptive-nbldpc — 单 session 单源自适应 NB-LDPC (PLAN_CANDIDATE / DEVELOPMENT_BENCHMARK / EXECUTE_NOT_AUTHORIZED)

**Lifecycle**: `PLAN_CANDIDATE / DEVELOPMENT_BENCHMARK / EXECUTE_NOT_AUTHORIZED` — **单 session 20260123_1M_600k_0dB 24+24+24=72 overall 单源，不比较，可复用旧数据但 development_replay 标记，冻结主体 U=32*U1+U2 LaneC Δ8，P重估+VAL CE+raw m+同家族+8，decoder-free 先验 m1<1024&&m2<1024 满秩嵌套 disclosure constructibility 否则 RATE_NOT_FEASIBLE/MATRIX_NOT_CONSTRUCTIBLE；EVAL 19/24 undetected0 仅 overall 密封度量（已删 per-source 6/8）；本轮仅四工件+只读调查+注册表+spike 回填无 TBD+终态机+独立 review packet + 单独提交推送新 Plan SHA，禁 decoder/run_01/比较/改 baseline/转 qualification/自行 ACCEPT**
**HEAD**: `TBD_NEW_PLAN_SHA` (本次修订后单独提交推送的新 40位 SHA，`git rev-parse HEAD == origin/formal-ir-mainline` 重核) + data `84d62779`
**Predecessor**: `formal-ir-v64-full-symbol-verification-correction` `22/24 PASS` + `formal-ir-v65-new-session-channel-compatibility` `cabc928f` → `TBD_NEW`
**Method frozen**: `n1024 q1024 GF32 poly37 H1 16×1024 rank16 80b U=32*U1+U2 F03 5+5 natural Lane C ordinal-2 s38310x m2 184 (单源) H_inc Δ8 家族 decoder 90/1.0 full-tag canonical 32*U1+U2 leak 5*(m1+m2)+64` 零改；处理点 `84d62779` 单点 单 session
**Boundary**: 单 session 72 overall 单源 24/段，内部零重叠键 `(source,session,frame)`，不跨 session 拼接，少 72 或任段 !=24 → `DATA_NOT_READY`；仅允许 `P(U1|B)/P(U2|U1B) VAL CE1/CE2 → m_raw ceil → +8 家族化`，不依 EVAL 调参，**m1<1024 && m2<1024** 满秩嵌套 disclosure constructibility 先验否则 `RATE_NOT_FEASIBLE`/`MATRIX_NOT_CONSTRUCTIBLE`；EVAL 密封 `19/24 overall && undetected0`（**无 per-source**）；五态优先级 `EVIDENCE_INVALID > DATA_NOT_READY > RATE_NOT_FEASIBLE/MATRIX_NOT_CONSTRUCTIBLE > ADAPTIVE_EVAL_FAIL > ADAPTIVE_EVAL_PASS`；`DEVELOPMENT_BENCHMARK` 可复用旧 session 但标记；**spike 已回填无 TBD，四工件单独提交推送**

## Phase A — 单段单源冻结与只读调查（decoder-free，单 session 72）

- [ ] **A1 定义 CAL/VAL/EVAL 冻结窗口（单 session 单源 24/段 72 overall，可复用旧数据但标记）**：选定单 `session_id=20260123_1M_600k_0dB`（复用 `v55_intake_20260828/pairs/20260123_1M_600k_0dB` 的连续 72 blocks：`CAL=前24 blocks 0-23 (96 frames 24576 pairs)`, `VAL=中24 blocks 24-47 (96 frames)`, `EVAL=后24 blocks 48-71 (96 frames)`），`BLOCK 4×256` 顺序切，`frame_id∈[0,2129]`，`BLOCK 4 连续`，写入 `v66_data_registry.json` (`per_source {"20260123_1M_600k_0dB": {CAL_blocks[24], VAL_blocks[24], EVAL_blocks[24], session_id, F_s=2130, K=532, development_replay:true, replay_source:"v55_intake_20260828", zero_overlap_key="(source,session,frame)"}}`)，`overall 24/段` 单源。
- [ ] **A2 零重叠与单 session 隔离机械校验（硬门，多重，元组键）**：脚本启动即 `assert |CAL|==24 && |VAL|==24 && |EVAL|==24 overall`，`assert CAL_session_id==VAL_session_id==EVAL_session_id=="20260123_1M_600k_0dB"`，`assert not cross_session_spliced`，`assert CAL_key∩VAL_key==∅ && CAL∪VAL_key∩EVAL_key==∅ && CAL∪VAL∪EVAL_key ∩ (V13∪V48..V64)_key==∅` (exact `(source,session,frame)` 键，来自 `v55_stratified_registry_candidate.json + v64_fresh_registry.json + V13 sidecars`)，落盘 `v66_manifest.json: zero_overlap_proofs` 全 true，失败则 `V66_EVIDENCE_INVALID`（若属拼接/键误用）或 `V66_DATA_NOT_READY`（若属 K<72/段 !=24）零估计。
- [ ] **A3 单 session 容量判定与 DATA_NOT_READY 门（优先级 2）**：枚举 `K = F_s//4 =532`，若 `K<72` 或 `F_s<288` 或任段 `!=24` 则 `overall=V66_DATA_NOT_READY` 并写 `v66_data_readiness.json {K,F_s,required 72/24 per segment,status}` 后零估计停止，不伪造，不以旧 `pairs.parquet` 硬凑；若 `≥72` 则进入 B-H。**实测 532>>72 已过**。
- [ ] **A4 注册表落盘与帧 256 校验（含 development_replay 标记）**：`v66_data_registry.json + v66_manifest.json` 含 `schema v66_data_v1, lifecycle PLAN_CANDIDATE/DEVELOPMENT_BENCHMARK, data_sha 84d62779, head TBD_NEW_PLAN_SHA, materialization_rule legacy_v1, provenance_note, created_at, development_replay true, replay_source v55_intake_20260828, zero_overlap_verified, single_session true`；对每段每帧校验 `pairs_per_frame 256` 且 `alice/bob ∈[0,1023]` 且 `F03 U1>>5 &31` 可分解，失败则 `EVIDENCE_INVALID`；`EVAL` 仅 identity 不参与 `P/m` 已校验 `used_eval_in_estimation==False`。

## Phase B — 冻结主体 U=32*U1+U2 LaneC conditional Δ8（decoder-free，不引 MET，单源）

- [ ] **B1 主体明文化（逐项显式，单源，冻结零改）**：写入 `v66_manifest.json:frozen_body {n1024, q1024, GF32 poly37, H1 16×1024 rank16, U=32*U1+U2 F03 5+5 natural, U1>>5/U2&31 per frame 256, Lane C ordinal-2 s38310x m2 184 (单源), H_inc Δ8 family (H_inc1 8×1024 + H_inc2 8×1024 nested), decoder 90/1.0 poly37 early-stop (禁用), full-tag canonical 32*U1+U2 compute_tag_64 trunc64 single 64b, leak=5*(m1+m2)+64, materialization legacy_v1}`，显式 `not MET/protograph/SC`，`git diff -- src/ ==0` 已验。
- [ ] **B2 权威算法只读复用**：直接复用 `src.reconciliation.run_nbldpc_demo_point` 的 `legacy_v1` 物化算法不重写；`CAL/VAL/EVAL` 三段均按该链物化后 `frame_id=row//256` 切片，禁直接按 `pairs.parquet` 猜帧；`V13` 溯源失败则 `EVIDENCE_INVALID`。
- [ ] **B3 帧级 256 映射校验（硬门）**：每帧 `alice[256], bob[256]` 校验 `s=32*u1+u2` 且 `u1,u2∈[0,31]` 且 `BLOCK 4 连续`，失败则 `EVIDENCE_INVALID`。

## Phase C — 单一 P 重估与 VAL CE + raw m + 同家族 +8 decoder-free 校验（不依 EVAL，m1<1024&&m2<1024 满秩嵌套 constructibility）

- [ ] **C1 读 CAL 切片（24 blocks 96 frames 24576 pairs 单源）**：`--pairs-root` 的 `alice_symbol/bob_symbol` 按 `CAL 24` 切 `24576 rows`，`F03 U1=>>5, U2=&31` 分解，校验 `alice/bob ∈[0,1023]` 且每帧 256。
- [ ] **C2 算 C_ab/P_global/P(U1|B)/P(U2|U1,B)**：`C_ab = bincount2d(a_cal,b_cal) 1024×1024 int32 sum 24576` → `N_b, P_global(a)=Σ_b C_ab/N_cal` → 构造 `P(a|b)=(C_ab+λ P_global)/(N_b+λ)`（`N_b==0→P_global`），同时生成 `P(U1|B) 32×1024` 与 `P(U2|U1,B) 扁平 32×32×1024`，落盘 `C_shape, N_cal 24576, effective_contexts, P_global_entropy`。
- [ ] **C3 VAL CE 链式与 raw m（VAL 门禁，24 blocks 24576 pairs，EVAL 隔离）**：对 `VAL 24` 计 `CE_full=-E_VAL log2 P(A|B)`, `CE1=-E_VAL log2 P(U1|B)`, `CE2=-E_VAL log2 P(U2|U1,B)`，校验 `|CE_full-CE1-CE2|<1e-9` 否则 `EVIDENCE_INVALID`，落盘 `CE1,CE2,CE_full,chain_delta`；随后 `m1_raw=ceil(1.3*1024*CE1/5)`, `m2_raw=ceil(1.3*1024*CE2/5)`, `m_total_raw=m1_raw+m2_raw` 显式 **不 cap**，校验 `m_raw` 未 `min(1024, ...)` 截断；**已回填 spike 无 TBD**。
- [ ] **C4 同家族 +8 调整（唯一允许自适应，不依 EVAL，m1/m2 分别）**：将 `m1_raw/m2_raw` 上调至 `H_inc Δ8` 家族中 `≥m_raw` 且 `≡base (mod 8)` 的最小可构造行数 `m1=ceil_to_family(m1_raw,8)`, `m2=ceil_to_family(m2_raw,8)`, `m_total=m1+m2`，显式报告 `m_raw→m_family` 映射表，若族内无覆盖则 `MATRIX_NOT_CONSTRUCTIBLE`，禁止 `round/floor/EVAL 重选`，`EVAL 未参与` 已校验 `used_eval==False`，禁第二 estimator；**回填 aligned m1/m2/constructibility**。
- [ ] **C5 decoder-free 码先验校验（进 EVAL 前，m1<1024 && m2<1024 满秩嵌套 disclosure 否则 RATE_NOT_FEASIBLE/MATRIX_NOT_CONSTRUCTIBLE）**：对 `gf_rank(H1(m1))` 与 `gf_rank(H2(m2))` (GF32 poly37) 校验 `m1<1024 && m2<1024` 且 `rank_m1==m1 && rank_m2==m2` 且 `nested(H_base, H_inc)`（`H(m)` 行集合嵌套，增量 `Δ8` 行独立）且 `disclosure==5*(m1+m2)+64` 双校验且 `constructible==true`，全过才 `DEVELOPMENT_BENCHMARK_READY` 可申请 `EVAL` 度量；任一失败则 `overall=V66_RATE_NOT_FEASIBLE`（子 `MATRIX_NOT_CONSTRUCTIBLE` 若族不能覆盖）零 decoder，不启动 EVAL，落盘 `m1_raw,m2_raw,m1,m2,rank_m1,rank_m2,nested,disclosure,constructible,feasible`。

## Phase D — EVAL 密封门禁（24 blocks 单源，未执行前仅冻结判定框架，decoder-free 预检，已删 per-source）

- [ ] **D1 EVAL 密封度量框架冻结（不读统计前）**：声明 `EVAL 24 blocks (96 frames)` 仅一度量，公式 `exact_full = (exact_u1 && exact_l2)`, `verified = syndrome_ok && tag_ok_full`, `undetected = verified && !exact_full`，`disclosure=5*(m1+m2)+64`, `efficiency=disclosure/(1024*CE_full)`，落盘 `EVAL_identity 24 blocks single source, m1/m2, disclosure`，校验 `used_eval_in_estimation==False`。
- [ ] **D2 门禁阈冻结（19/24 overall, undetected 0, tag, disclosure, efficiency，已删 per-source 6/8）**：`G_exact: exact_full ≥19/24 overall (79.17%)`, `G_undetected: undetected_full_tag==0`, `G_tag: all exact_full → tag_ok_full==True`, `G_disclosure: disclosure==5*(m1+m2)+64`, `G_efficiency: efficiency 报告` 已显式，五态优先级已含 `ADAPTIVE_EVAL_FAIL/PASS`，本轮 `EVAL` 未执行故 `D2` 为框架待验。

## Phase E — 终态机五选一（优先级互斥，含 DATA_NOT_READY 与 RATE_NOT_FEASIBLE/MATRIX_NOT_CONSTRUCTIBLE）

- [ ] **E1 EVIDENCE_INVALID 最高优先**：`if not materialization_ok or frame_256_violation or CE_chain_not_closed>1e-9 or provenance_fabricated or cross_spliced: overall=V66_EVIDENCE_INVALID`。
- [ ] **E2 DATA_NOT_READY 次优先**：`elif K<72 or |CAL|!=24 or |VAL|!=24 or |EVAL|!=24: overall=V66_DATA_NOT_READY`。
- [ ] **E3 RATE_NOT_FEASIBLE 三优先（含 MATRIX_NOT_CONSTRUCTIBLE）**：`elif m1>=1024 or m2>=1024 or rank_m1!=m1 or rank_m2!=m2 or not nested or disclosure!=5*(m1+m2)+64 or not constructible or no family H: overall=V66_RATE_NOT_FEASIBLE`（若 `not constructible` 子状态记 `MATRIX_NOT_CONSTRUCTIBLE`），decoder-free 失败，不启动 decoder。
- [ ] **E4 ADAPTIVE_EVAL_FAIL/PASS（需 EVAL 执行后，已删 per-source）**：`elif EVAL executed && (exact_full<19/24 or undetected!=0 or not tag_ok): overall=V66_ADAPTIVE_EVAL_FAIL`；`elif EVAL executed && exact_full≥19/24 && undetected==0 && disclosure ok: overall=V66_ADAPTIVE_EVAL_PASS`。
- [ ] **E5 DEVELOPMENT_BENCHMARK_READY（EVAL 未执行但前三态已过）**：`else if not EVAL executed && E1-E3 全过: overall=V66_DEVELOPMENT_BENCHMARK_READY`（允许申请 `EXECUTE_AUTH` 后度量 `EVAL`），落盘 `overall` 与 `PASS` 至 `v66_spike_summary.json:verdict`，优先级严格先到先得，已验；本轮 `EVAL` 未执行故终态为前三态之一或 `READY`。

## Phase F — spike 与报告交付（PLAN_CANDIDATE / DEVELOPMENT_BENCHMARK / EXECUTE_NOT_AUTHORIZED，回填无 TBD）

- [ ] **F1 编写 `scripts/v66_data_readiness.py`** (decoder-free, 本变更目录下): `python scripts/v66_data_readiness.py [--pairs-root comparison_bench/outputs_comparison/v55_intake_20260828/pairs/20260123_1M_600k_0dB] [--out v66_data_registry.json]` → 单 session 72 检测 + `24/段` 切分与三重零重叠（键 `(source,session,frame)`） + `frame 256` 校验 + `development_replay` 标记，`rg "decode_" 0 hits` 仅 `numpy/pandas/pyarrow + ttbin_pipeline`，`py_compile PASS`，输出 `v66_data_registry.json + v66_data_readiness.json` + 控制台 spike 摘要。
- [ ] **F2 编写 `scripts/v66_spike.py`** (decoder-free, 本变更目录下): `python scripts/v66_spike.py [--registry v66_data_registry.json] [--out v66_spike_summary.json]` → `CAL C_ab/P_global → P(U1|B)/P(U2|U1B) → VAL CE1/CE2/CE_full 链式 → m_raw ceil → m_family +8 aligned → m1<1024 && m2<1024 rank nested disclosure constructibility` 校验，`rg "decode_" 0 hits` 仅 `numpy/pandas/pyarrow`，`py_compile PASS`，不依 EVAL，输出 `v66_spike_summary.json + V66_ADAPTIVE_REPORT.md` 占位 + 控制台摘要，**已回填 CE1/CE2/raw/aligned/rank/nested/disclosure/constructibility 无 TBD**。
- [ ] **F3 执行 decoder-free spike 回填**：运行 `scripts/v66_spike.py` 得 `CE1/CE2/CE_full, m1_raw/m2_raw, m1/m2 aligned (+8), rank_m1/rank_m2, nested, disclosure, constructibility`，若族不能覆盖则 `MATRIX_NOT_CONSTRUCTIBLE`，清除所有 `TBD` 占位，`v66_spike_summary.json` 与 `V66_ADAPTIVE_REPORT.md` 同步回填。
- [ ] **F4 撰写 `V66_ADAPTIVE_REPORT.md` 占位并回填**：`CAL/VAL/EVAL 24/段 单 session 单源 provenance + development_replay + CE1/CE2/CE_full + chain_delta + m_raw/m_family/Δm + rank/nested/disclosure/constructibility + EVAL 密封门禁 19/24 undetected0 overall 预冻结 + 五态` 附 `EVAL identity 附录（不含统计）` + `V64 22/24` 对照章节，数据与 `json` 一致，**无 TBD**，结论不扩大为 `FER/SKR/晋升`，显式 `development_replay` + `EVAL 未参与` + `仅 PASS 允许后续 EXECUTE_AUTH` + `m_raw 未 cap + m1<1024 && m2<1024 + 满秩嵌套 constructibility`。
- [ ] **F5 自检（五态+守卫+ A-E 约束）**：`py_compile` 双脚本 PASS, `rg "decode_" 0 hits` 双脚本, `git diff -- src/ ==0` 且 `git diff -- comparison_bench/src/comparison_bench/formal_ir/ ==0` (未改码) 已验, 三重零重叠（键 `(source,session,frame)`）+ 单 session 单源已验, `|CE_full-CE1-CE2|<1e-9` 已验, `m_raw ceil` 未 cap 且 `m_family +8` 显式已验, `m1<1024 && m2<1024 rank==m nested disclosure constructibility` 已验否则 `RATE_NOT_FEASIBLE`/`MATRIX_NOT_CONSTRUCTIBLE`, `EVAL 未读` 已验, `24/段 72 overall 单源` 已验, `development_replay` 已标记, `EVAL 19/24 undetected0 overall` 预冻结已声明, 报告与 json 一致 **无 TBD**, **A-E 已闭合**。
- [ ] **F6 单独提交推送四工件+registry+spike+脚本结果**：`git add openspec/changes/formal-ir-v66-single-segment-adaptive-nbldpc/ scripts/v66_*.py && git commit -m "formal-ir-v66: single-session 20260123_1M_600k_0dB 72 single-source, m1/m2<1024, spike backfilled, no per-source" && git push origin formal-ir-mainline`，返回新 `Plan SHA`（40位），记录于 `proposal/design/tasks` HEAD 占位替换。
- [ ] **F7 停留 `PLAN_CANDIDATE / DEVELOPMENT_BENCHMARK / EXECUTE_NOT_AUTHORIZED`**，未创建任何 `.../v66_*/run_01`，不比较，不碰 `V48-V65` 块外，未转 qualification，仅改本目录 + `scripts/`，**推送后等待独立审核**（`Pre-RESULT` 独立线程复核 `HEAD/ACCEPTED_PLAN_SHA/rg 0 hits/run_01不存在/py_compile/EVAL 未读/m_raw 显式/rank_nested/constructibility/单 session 72/development_replay`），返回 `Plan SHA / 冻结段数量 / raw m1/m2 / 可构造性 / 下一步` 等待 `PLAN_ACCEPT`。

## 本变更显式禁止

decoder 调用 (`decode_*` / `construct_*` 等)；读密封 `EVAL` 的 `H/CE/NLL/MAP` 统计或将其用于 `P/m/阈值` 选择；在原 `V55 90-block` 或 `V48-V64` 已用 `(source,session,frame)` 上重跑先验估计；调 `H1/Lane C/Δ8/decoder/m2/m_total/leak/prior/H_inc/H_total/verification` 任一冻结参数或新增矩阵；**跨 session 拼接凑 72**；**将 floor/round 当 ceil**；**将 `min(1024, ceil(...))` cap 伪装当通过**（`m_raw` 必须显式）；**将 `+8` 外 degree/seed 网格当自适应**（仅 `Δ8` 家族 +8 档）；**用 `EVAL` 择优阈或选平滑**（`P/m` 仅 `CAL/VAL`，EVAL 隔离）；**将不足 72 伪判为可 EVAL**（应 `DATA_NOT_READY`）；**将 EVAL 另选新 session**（EVAL 必须单 session 内后 24 段）；**将零重叠仅比 `frame_id`**（必须 `(source,session,frame)` 元组）；**引 MET/protograph/SC**；**改 `src/` 基线**；宣称 LDPC 证伪或 `FER/阈值/SKR/晋升`；创建正式 `.../v66_*/run_01`；任意 `bin_width/dimension/pairing/mapping` 网格或阈网格（处理点单点）；用第二 estimator 作门禁；`I` 总体平均替代；覆盖已有输出；`V55 90 / V48-V64` 永久禁用違反；**主观“显著恢复”替代 19/24 硬阈**；**擅自启动 EVAL**（仅 `READY` 后另起 `EXECUTE_AUTH` 才允）；**保留 per-source 6/8 门禁**（已删）；**用 m_total<1024 替代 m1<1024&&m2<1024**；**保留 TBD 占位不回填**。

## 验收

- proposal/design/tasks/specs 一致 HEAD `TBD_NEW_PLAN_SHA`（已推送） `84d62779` lifecycle `PLAN_CANDIDATE / DEVELOPMENT_BENCHMARK / EXECUTE_NOT_AUTHORIZED` 终态五选一按优先级互斥明确（`EVIDENCE_INVALID > DATA_NOT_READY > RATE_NOT_FEASIBLE/MATRIX_NOT_CONSTRUCTIBLE > ADAPTIVE_EVAL_FAIL > ADAPTIVE_EVAL_PASS`，未执行时 `READY`），显式单 session 单源 72、development_replay、可复用旧数据、冻结主体 `U=32*U1+U2 LaneC Δ8`、仅 P 重估 + VAL CE + 同家族 +8、不依 EVAL、**m1<1024 && m2<1024** 满秩嵌套 disclosure constructibility 先验、EVAL 19/24 undetected0 overall（**无 per-source**），严格复用 V56 materialization 算法，冻结分段/熵-CE 与 m 公式/门禁/终态机，**无 TBD**
- 单 session 单源 `CAL 24 + VAL 24 + EVAL 24 =72 overall` 每段 `24` 可验三重零重叠（键 `(source,session,frame)`，`CAL∩VAL==∅ && CAL∪VAL∩EVAL==∅ && ∩V13..V64==∅` + 单 session 单源 + `development_replay`），`frame 256 A=32U1+U2` 每帧已验，少 72 或任段 !=24 → `DATA_NOT_READY` 不伪造，注册表已落盘（单源 72），`EVAL` 未读已验，**新 Plan SHA 已推送**
- 合同 `U=32*U1+U2 / dimension 1024 / bin200 / nearest legacy_v1 / channels/frame anchor/mapping 每帧256` 算法一致已验，偏则 `EVIDENCE_INVALID` 已验
- **单源** `CAL C_ab 1024×1024 → P(U1|B)/P(U2|U1B) → VAL CE1/CE2/CE_full 链式 |CE_full-CE1-CE2|<1e-9 → m1_raw/m2_raw ceil → m1/m2 aligned +8 → m1<1024 && m2<1024 rank_m1==m1 rank_m2==m2 nested disclosure constructible` 已重算并回填无 TBD，`m_raw` 未 cap，`m_family` 显式，若族不能覆盖则 `MATRIX_NOT_CONSTRUCTIBLE`，`EVAL` 未参与，不扩，禁第二 estimator 已验
- `Cal/Val pairs/frames/size, CE1/CE2/CE_full+chain_delta, m_raw/m_family/Δm, rank/nested/disclosure/constructible` 已回填无 TBD，`EVAL 仅 identity` 已验，`m ceil` 未 cap 且 `disclosure=5*(m1+m2)+64` 一致已验
- 终态机 `EVIDENCE_INVALID > DATA_NOT_READY > RATE_NOT_FEASIBLE/MATRIX_NOT_CONSTRUCTIBLE > ADAPTIVE_EVAL_FAIL > ADAPTIVE_EVAL_PASS`（未执行时 `DEVELOPMENT_BENCHMARK_READY`）已落盘，仅 `PASS` 允许后续 `EXECUTE_AUTH` 且 `EVAL 19/24 overall undetected0` 预冻结已声明（**无 per-source**）
- 双脚本 `rg "decode_" 0 hits` `py_compile` PASS `m_raw` 未 cap `EVAL 未读` `CE 链式` `单 session 单源 72` `development_replay` 已验 报告与 json 一致 **无 TBD** 未建 `run_01` 已停留 `PLAN_CANDIDATE / DEVELOPMENT_BENCHMARK / EXECUTE_NOT_AUTHORIZED` 仅改本目录 + `scripts/`（`src/` 零改），未启动 EVAL，**四工件+registry+spike 已单独提交推送，新 Plan SHA 已返回**，推送后等待独立审核
