{
  "schema": "v10_budget_amendment_v1",
  "change": "formal-nonbinary-ldpc-v10-de-peg-fftqspa",
  "amendment_id": "AMEND-2026-08-05-01",
  "approved_at_utc": "2026-08-05T21:00:00Z",
  "approved_by": "main thread (user decision); planner/coder operator role unchanged",
  "trigger": "V10-S06 budget blocker returned by the implementation operator after microbenchmark #1: worst-case V10A official execute estimated 31.3 h > 24 h hard limit. Microbenchmark #1 measured 19.03 s/evaluation at n_samples=500, max_iter=30 (0.634 s/iteration) and confirmed GF(1024) p=.20 MC-DE convergence needs ~80 iterations at n_samples=500, so max_iter<=80 screen budgets were rejected as scientifically unsound (zero margin).",
  "change_made": {
    "import_amendment": "numba (already a root requirements.txt dependency, version 0.64.0 verified compatible with numpy 2.4.0) is allowed as a production import for nonbinary_v10_de.py hot-loop kernels only (WHT butterfly, check convolution, variable/belief updates, normalization). No other module gets numba. No new dependency is installed.",
    "microbenchmark_authorization": "exactly one re-measurement microbenchmark (microbenchmark #2, same non-scientific seed rule) is authorized to re-freeze pre_run_plan.json budgets with measured numba numbers.",
    "correctness_requirement": "njit kernels MUST be bit-identical to the numpy reference semantics. Tests MUST assert allclose(rtol=1e-12) against a small pure-numpy reference and a known-answer butterfly regression case (delta input -> all-ones output) that catches the half-block butterfly bug.",
    "design_freeze_update": "design.md section 15 import discipline updated accordingly; v10_freeze_acceptance.json allowed_imports updated."
  },
  "unchanged_scientific_parameters": [
    "all seeds (202610xx allocation; disjoint from V8 20260804xx and V9 20260901xx)",
    "gates (S1 .22, S2 .215, S3 .32, S4 .32; V10-0 |threshold-0.069|<=0.012)",
    "K=8 degree+logit representation, degree range [2,40], min weight 0.01, sum=1 within 1e-12",
    "6-tier lexicographic objective with entropy gate screen",
    "hard caps: 600 screen evaluations per search, 12 refinement candidates, peak RSS 3 GiB, workers=1",
    "validation semantics: >=3 disjoint validation seeds, conservative = min of per-seed thresholds",
    "no-tuning/no-rerun rules; failed evidence immutable",
    "V10-0 q=4 reference contract (DET 0.069, tol 0.012, n_samples 100000, max_iter 150)"
  ],
  "no_other_changes": true,
  "note": "This amendment is frozen BEFORE any formal scientific result, per the same protocol precedent as V9 plan v2. Budgets in pre_run_plan.json remain immutable after formal results exist."
}
