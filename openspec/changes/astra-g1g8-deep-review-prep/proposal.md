# Proposal — Astra G1–G8 Deep-Review Prep (docs-only)

- Ownership: main thread owns requirements; operator transcribes planner P1–P5 only, no new science, no self-acceptance.
- Lifecycle: documentation-only analysis; neither `EXPLORE` nor `DECIDE` (no execution, no data, no claim).
- Branch `formal-ir-v72p1-addendum-clean` stays as-is. No commit/push.

## D1.1 Why G1–G8 first, Q1–Q3 scope

- Per `ASTRA_D19_REPLY_AND_PROJECT_EXPANSION_ZH.md` §§1–5: D19 is one local
  finite-length validation gate, not the project decision. The next review must
  cover the full evidence chain (channel population → source model → asymptotic
  potential → finite graph/decoder → cross-layer messages → real-frame yield),
  i.e. G1–G8, with Q1–Q3 answered first as one connected decision.
- Q4 stays paused until finite-L2 evidence is credible; Q5 is design-only and
  must not authorize real data (packet §§2–3).
- G1–G8 redirect (from expansion §4): G1 channel-population sufficiency; G2
  code-family investment; G3 DE→finite theory bridge; G4 cross-layer extrinsic
  contract; G5 rate-adaptation value; G6 minimum real-data DECIDE design; G7
  project decision function; G8 process-debt triage.

## D1.2 D19 stays STOP-closed

- Terminal `D19_L2_FINITE_ENGINEERING_BLOCKED` (READINESS_R1 §§3–6) is accepted.
  The old STOP is immutable: frozen seed `2026094408` (n256 DV3,
  `no eligible check placement at variable 255 socket 2`) cannot be replaced
  under the frozen packet; 23/24 cells admit; zero decoder/DE calls; no ready
  terminal issued.
- No 4408 replacement pretending the same population. Any seed-stream,
  admission-population, statistic, or budget change belongs to a new
  amendment/packet, never a rewrite of the old STOP (expansion §2, P0-2).

## D1.3 D18 winner stays frozen

- `lam_d2_0.20_d3_0.80` is the single L2 finite-validation representative by a
  frozen deterministic tie-break (max-check-degree then ID), not a demonstrated
  optimum. The 0.15/0.20/0.25 near-tie family is not reopened; no extra arms.

## D1.4 Q1-option mapping (planner P-classes, no new science)

1. Single-seed replacement under a preregistered non-adaptive next-seed rule —
   rejected as a same-population fix (conditions on a post-freeze event; breaks
   or redefines pairing; risks seed shopping).
2. Admitted-conditioning design (fixed admitted-graph count from a preregistered
   seed stream, admission rate reported) — admissible only as explicitly
   secondary/conditional evidence, never the primary pipeline estimand.
3. Pipeline estimand (constructor failure retained as pipeline failure /
   zero-yield) — recommended default: preserves paired identical graph lists
   and answers the deployable-pipeline question.
4. Constructor redesign first — required if the failure is judged a systemic
   robustness defect rather than an isolated trajectory dead-end; then no
   decoder comparison is authorized until the constructor question is settled.

## D1.5 Explicit non-authorizations

- No D19 execution, no decoder/DE calls, no constructor redesign, no new seeds,
  no T=72 or new budgets, no real data, no SKR/FER-qualification/publication/
  route-closure claims, no commit/push.
- The user's autonomous-advance approval is NOT a DECIDE Pre-EXECUTE
  authorization. Any future execution needs its own explicit grant with
  Pre-EXECUTE.
