# OpenSpec Tasks: formal-ir-v55-two-stage-rescue-independent-test-qualification-preparation

**Lifecycle**: `QUALIFICATION_PLAN_READY / EXECUTE_NOT_AUTHORIZED` — **intake 3/3 READY 已固化、G1-G7 全 PASS、authoritative 90-block 已冻，仅等待独立 plan review，不实现 runner，不执行 decoder，不创建正式 TEST 输出。**
**Execution status**: 本轮已完成 intake 紧凑证据 (`intake_compact_evidence.md` + `v55_authoritative_registry.json` authoritative 90-block) + 四工件修订至 QUALIFICATION_PLAN_READY；Phase G 及之后待独立 plan ACCEPT + EXECUTE_AUTH 后方可进入（90 块三阶段 180-360 calls）；当前不跑 decoder，不创建 `.../v55_*/run_01`
**HEAD**: `efd34ef318014e1d0505605062057b042e2180eb`（plan SHA；实现冻结时重绑至未来 implementation SHA） + data SHA `84d62779603e62de50ded5182ed65b65d3dc6084` (84d62779 200ps legacy_v1)
**Predecessor**: `formal-ir-v54-two-stage-incremental-l2-rescue` (`bf5dd1686049156540328bac264296b17fee546c`, branch `formal-ir-mainline`)
**Delta m**: `Δm=8+8=16` per source 冻结（V54 已冻，V55 不新增），总预算 `180-360 硬帽360` (L1 90+base90+stage1≤90+stage2≤90, 30/source 共90)
**Data status**: `QUALIFICATION_PLAN_READY` (G1-G7 全 PASS, authoritative 90-block 已冻, 1M_600k_0dB F2130 / 1p5M F5125 / 2M_1p2M_0dB F5513 非同分布 2026-01-21, 200ps 单点)
**Registry**: `v55_authoritative_registry.json` 唯一 authoritative (30/source 共90, K=2127/5122/5510, index_j floor 分散 gap≥4, frame_ids/ordinal frozen, 禁换块)

## Phase A — 语义冻结（已冻结，零改）

- [x] **A1** 冻结方法完全体（零改，首遍与二阶段）：`n=1024, m2=184/190/192, GF32 poly37, Lane C ordinal-2二值support/标签/位置置换, H1-16 rank16 80b, L1-APP q_i via BP_i (TRAIN prior channel_counts.npz), H_inc1 8×1024 det1 + H_joint1 192/198/200, H_inc2 8×1024 det2 + H_total 200/206/208, decoder 90/1.0 early-stop, verification syndrome_ok&&tag_ok L2-only compute_tag_64(empty,x2), leak_base 5*m2+80+64 →1064/1094/1104, leak_stage1+40, leak_stage2+80, exact_full=exact_u1&&exact_l2 oracle, Δm=8+8 唯一**；V55 **不测新矩阵/标签/prior/阈值/decoder参数**，新增即 EVIDENCE_INVALID。
- [x] **A2** 冻结二阶段增量矩阵（唯一已冻变量，V55 不新增）：`8×1024 GF32 poly37` ×2 per source、`col_degree_inc∈{0,1}`、`row_degree≤16`、`无零行`、`rank_joint1==m2+8`、`rank_total==m2+16`、`nested1 H_base==H_joint1[:m2]`、`nested2 H_base==H_total[:m2] && H_joint1==H_total[:m2+8]`、`independence_1==8` (rank_joint1 - rank_base)、`independence_2==8` (rank_total - rank_joint1)、确定性 PEG-增量 `SeedSequence([600001-600003,1/2])` 与 `SeedSequence([600004-600006,1/2])` 各 source、禁止 seed 搜索/第二候选/调 Δm；`E_inc≈96` 报告、`leak_stage1==leak_base+40, leak_stage2==leak_base+80` 公式冻结。
- [x] **A3** 冻结三阶段条件协议（未来执行，预冻结）：`base(H_base)` → `verify_base` 通过则停 `leak_base`；否则 `s_inc1=H_inc1*u2_true`、`s_joint1=[s_base;s_inc1]`、`stage1(H_joint1)` → `verify_stage1` 通过则停 `leak_stage1`；否则 `s_inc2=H_inc2*u2_true`、`s_total=[s_base;s_inc1;s_inc2]`、`stage2(H_total)` → `verify_stage2`，`leak=leak_stage2` 无论成功/失败；`exact_*` 仅 oracle 统计不触发，三阶段均 `verification-only`；报告 `base_exact_full` 与 `verify_base` 分别计数（per source & coverage）、`stage1` 与 `verify_stage1` 分别、`final` 与 `verify_final` 分别，**主判完整 base→Δ16 (final)**，`base/stage1` 仅分层报告。
- [x] **A4** 冻结合格 TEST 样本 (authoritative 90-block)：三源标签 `1M_600k_0dB (F2130) / 1p5M (F5125) / 2M_1p2M_0dB (F5513)` 非同分布 2026-01-21，`d=1024 bw=200ps pairing=nearest rule=legacy_v1` 单点 (84d62779)，大型 sidecar/pairs 已在 `comparison_bench/outputs_comparison/v55_intake_20260828/` (parquet 545280/1312000/1411328 行) 结构验证；authoritative `90 blocks` (30/source) 于 `all_starts=0..F-4` 得 `K=2127/5122/5510 ≥30`，`index_j=floor(j*(K-1)/29)` 分散，gap≥4，与 V13 及 V48-V54 零重叠 per source；`v55_authoritative_registry.json` 为唯一合法块集，`sampling_mode=deterministic_four_consecutive_frames_independent_test_v55_authoritative`，`BLOCK_LENGTH=1024`，**禁换块**。
- [x] **A5** 冻结 paired workload（已冻结，三阶段共享 L1）：每块同 `bob` 同 `P_i(U2)`，`base L2` 1次 → 条件 `stage1 ≤1` → 条件 `stage2 ≤1`，共享 `L1 1`；每块 `L1 1+base 1+stage1 ≤1+stage2 ≤1`（L2 1-3）；`90 块` 时总 `90 L1+90 base+≤90 stage1+≤90 stage2=180-360 硬帽360 (L2 90-270)`。
- [x] **A6** 冻结报告承诺 SHALL（未来执行，分层）：`base_exact_full` 与 `verify_base` 分别计数（禁止假定相等）/ `stage1_exact_full` 与 `verify_stage1` 分别 / `final_exact_full` 与 `verify_final` 分别 / `stage1_rescued / stage2_rescued` / `N_stage1_attempted=count(!verify_base)` 与 `N_stage2_attempted=count(!verify_stage1&&!verify_base)`（per source & coverage）/ `rescue_rate_stage1/stage2` per source / 分层 `leakage` 三档与 `per_source_avg[s]=leak_base[s]+40×N_stage1[s]/30+40×N_stage2[s]/30` 与 `coverage_avg=(Σ leak_base+40×N_stage1_total+40×N_stage2_total)/90` / `avg_disclosure_per_attempted` / `total_disclosed_bits` 与 `disclosure_per_final_exact_block` / `Wilson 95%` 仅报告 per source & coverage / 迭代/runtime/residual / 四类；`base/stage1` 仅分层报告，主判 `base→Δ16`。
- [x] **A7** 冻结记录与聚合（预冻结）：`90` 块三阶段条件记录、`v55_summary.json` 含 `base/stage1/final` 三层 `exact` 与 `verify` 分别计数（per source + coverage 90）+`per_source_avg` 与 `coverage_avg` +`Wilson 95%` per source & coverage+`H_inc1 joint1_rank/nested/independence` 与 `H_inc2 total_rank/nested/independence` provenance、`tag acceptance` per source、paired `base vs stage1 vs final`、`rescue_rate` per source、四类/G3'、门禁明细（`final` 的 per-source 20/30 与 coverage 70/90 数值与 `V55_*` 终态，undetected==0 全局）、claim boundary (independent cross-session, non-iid 2026-01-21, not FER/SKR/promotion)。
- [x] **A8** 冻结终态三者+QUALIFICATION_PLAN_READY（互斥）：`QUALIFICATION_PLAN_READY` (G1-G7 全 PASS, 已达成，等待 plan review) / `V55_EVIDENCE_INVALID`（完整性/秩/嵌套/重叠/记账失败）/ `V55_INDEPENDENT_TEST_PASS`（`coverage ≥70/90 ∧ per-source ≥20/30 ∧ undetected==0 ∧ rank/nested/verification/记账` 满足）/ `V55_INDEPENDENT_TEST_FAIL`（完整性通过但未过门禁）；PASS 仅称 independent cross-session qualification evidence，非 FER/SKR/promotion；不宣称同分布复现。
- [x] **A9** 冻结执行偏差防复发与数据就绪门前置：不使用 600s 外部 timeout（建议≥3600s或不设）、若返回 session/cell ID 只轮询同一进程禁重启、中断时保留 raw partial 及调用计数不生成 aggregate、不得自动重跑；**G1-G7 全 PASS 已达成** (`QUALIFICATION_PLAN_READY`)，输出根已存在即拒；“仅四工件+compact intake 证据 (authoritative)，不得实现/执行 decoder” 已冻结。

## Phase B — Intake 紧凑证据固化 (已完成 3/3 READY, decoder-free)

- [x] **B1** Intake 3/3 READY 已固化：`workspace/v55_intake_20260828/v55_decoder_free_intake.py` (SHA 84d62779, d=1024 bw=200 legacy_v1) 已执行，`comparison_bench/outputs_comparison/v55_intake_20260828/` 下 `sidecars/a_eff.npy|b_eff.npy|sidecar_meta.json` + `pairs/pairs.parquet` (545280/1312000/1411328 行) 已生成 (命令/路径/行数/重建方法见 `intake_compact_evidence.md` §4)，结构统计 `frames×256` `0..1023` 排序无缺失已验 (见 §3)，三源 provenance `.ttbin/.1.ttbin` 同源 sha256 已记录，decoder-free P1-P3 仍 PASS。
- [x] **B2** 紧凑证据已提交 Git：`intake_compact_evidence.md` (三源 provenance/结构统计/生成命令/路径/行数/重建方法) + `v55_authoritative_registry.json` (90-block authoritative, 30/source, frame_ids/ordinal frozen) 已进 `openspec/changes/.../` (大型 sidecar/pairs 不进 Git，但报告可重建)，满足 compact evidence 要求。
- [x] **B3** 非同分布声明已冻结：三源 `1M_600k_0dB / 1p5M / 2M_1.2M_0dB` 与 `2026-01-21` V13 非同分布，三层绑定实际 acquisition，不宣称同分布复现，PASS 仅独立跨 session 资格证据。
- [x] **B4** HOLD 未复用：V13 HOLD 已污染结论仍保留，未用 HOLD 冒充 TEST。

## Phase C — authoritative 90-block 注册冻结 (已完成，唯一合法)

- [x] **C1** 枚举算法已 freeze 每源：`F=2130/5125/5513` → `K=2127/5122/5510 ≥30`，`remaining=0..F-4`，`selected_starts=[remaining[floor(j*(K-1)/29)] for j in 0..29]` (见 registry)，验证 `gap≥4` 两两非重叠且与 V13 零重叠，已写入 `v55_authoritative_registry.json`
- [x] **C2** 标识已 freeze：`selected_frame_ids[4]=[s..s+3]`，`held_out_ordinal_start/end=s/s+3`，`sampling_mode=deterministic_four_consecutive_frames_independent_test_v55_authoritative`，`pairs_count=1024, BLOCK_LENGTH=1024`，30/source 共90，**禁换块**

## Phase D — 数据就绪门 G1-G7 decoder-free 全 PASS (已验证)

- [x] **D1** `intake_compact_evidence.md` 已记录 G1-G7 per source 全 PASS (G1文件可读 frames×256, G2 provenance 双文件, G3 三源明确非同分布, G4 256/frame 1024/block 0..1023 排序无缺失, G5 与 V13 零重叠, G6 V25 prior只读, G7 authoritative registry gap≥4)，零 `import decoder`，零 `decode_*` 调用
- [x] **D2** `v55_authoritative_registry.json` 已为 G7 的 authoritative 冻结产物 (K/index_j 分散结果 per source 已固化)
- [x] **D3** 裁决：三源 G1-G7 全 PASS ⇒ `QUALIFICATION_PLAN_READY` (已达成)，停留 `EXECUTE_NOT_AUTHORIZED` 等待独立 plan review

## Phase E — 预算/门禁/claim 冻结 (已冻结，authoritative 90-block)

- [x] **E1** 预算已冻结：`90 块` 时 `L1 90 + base 90 + stage1 0–90 + stage2 0–90 =180–360 硬帽360 (L2 90–270)`，`base`兼old不重复，每块 `L1 1+base1+stage1≤1+stage2≤1`
- [x] **E2** 门禁已冻结：`coverage ≥70/90 (77.8%) ∧ per-source ≥20/30 (66.7%) ∧ undetected==0 全局 ∧ rank/nested/verification/记账` 均过才 `PASS`，否则 `FAIL`，完整性失败 `EVIDENCE_INVALID`；**主判完整 base→Δ8→Δ16 (base→Δ16)**，`base` 与 `stage1` 仅分层报告不取代最终；`Wilson/rescue/runtime/leakage` 仅报告（按源分层）；PASS 仅称 independent cross-session qualification evidence
- [x] **E3** 终态已冻结：`QUALIFICATION_PLAN_READY` (已达成) / `V55_EVIDENCE_INVALID` / `V55_INDEPENDENT_TEST_PASS` / `V55_INDEPENDENT_TEST_FAIL` 互斥；已就绪后不产生 `DATA_NOT_READY`
- [x] **E4** Claim 边界已收紧：独立跨 session TEST，三层绑定实际 acquisition (第2章 provenance + 第3章结构统计 + 第5章 registry)，**不宣称复现 2026-01-21 同分布**，PASS 仅称 independent cross-session qualification evidence，非 FER/SKR/阈值/安全/资格/晋升

## Phase F — 本轮交付 (QUALIFICATION_PLAN_READY)

- [x] **F1** 四工件已修订至 `QUALIFICATION_PLAN_READY / EXECUTE_NOT_AUTHORIZED`：`proposal.md, design.md, tasks.md, specs/spec.md` lifecycle 已更新，HEAD `efd34ef...` + data SHA `84d62779` 双绑定，明确 authoritative 90-block 与 budget/gate/claim
- [x] **F2** 紧凑证据已交付：`intake_compact_evidence.md` (provenance/结构/生成命令/路径/行数/重建方法) + `v55_authoritative_registry.json` (唯一 authoritative 90-block) + 原 `data_readiness_report.md` 保留历史，零 decoder 调用，无正式 output，已达 `QUALIFICATION_PLAN_READY`
- [x] **F3** 已推送并停在 `QUALIFICATION_PLAN_READY` 等待独立 plan review，未自授 `EXECUTE_AUTH`

## Phase G — 授权三阶段条件执行（需独立 plan ACCEPT + EXECUTE_AUTH，90 块三阶段，180-360 calls，未来轮次，本轮不执行）

- [ ] **G1** 主线程获独立 plan ACCEPT + 显式用户 `EXECUTE_AUTH`（绑定 repository、分支 `formal-ir-mainline`、完整未来实现 SHA、cycle V55P0、scope `v55_two_stage_rescue_independent_test_90_blocks_triple_exactly_once_authoritative`，plan 引用 HEAD `efd34ef318014e1d0505605062057b042e2180eb` + data SHA `84d62779` + authoritative registry `v55_authoritative_registry.json`，且 G1-G7 已为 `QUALIFICATION_PLAN_READY`）
- [ ] **G2** 恰好一次：`python scripts/execute_v55_independent_test.py --execution-authorized --authorized-target-sha <sha>`；按 design 三阶段协议执行：`90 块 × (base 1 + 条件 stage1 ≤1 + 条件 stage2 ≤1)`，每块 `L1 1+base L2 1+stage1 ≤1+stage2 ≤1`，总 `180-360 硬帽360 (L2 90-270)`；**执行偏差防复发**：不设 600s 外部 timeout（建议≥3600s或不设）、若返回 session/cell ID 只轮询同一进程禁重启、中断时保留 raw partial 及调用计数不生成 aggregate、不得自动重跑；每条 L2 记录含 `stratum/source/arm∈{base,stage1,stage2}/pass_index/used_inc1/used_inc2/matrix_id/target_tag/candidate_tag/tag_ok/reclassified` (L2-only) 与 `exact_u1/exact_l2/exact_full/errors/syndrome/entropy/leak_total/status/runtime`；保留 `run_01`；出错止、原样保留 raw partial 无聚合、返回 blocker；不 rerun/resume/tuning/加块/改增量/重实现 canonical；无论结果不做第二轮；同块 base→stage1→stage2 同 `bob` 同 `P_i(U2)`；采样恒 deterministic authoritative `K/index_j` 按源分散 gap≥4
- [ ] **G3** 只读 postcheck（分层，authoritative）：记账 `base_exact_full` 与 `verify_base` 分别计数（禁止假定相等，per source & coverage 90）、`stage1_exact_full` 与 `verify_stage1` 分别、`final_exact_full` 与 `verify_final` 分别、`stage1_rescued / stage2_rescued / final`、`.stage1/stage2 attempt counts` per source & coverage、`90` 块三阶段记录与序每块 `base→(条件)stage1→(条件)stage2` 正确、增量 `H_inc1` 与 `H_inc2` provenance、authoritative `frame_ids/ordinal` 逐块比对 design G1-G7 每源分散 gap≥4、无 NPZ、`V38–V54` 输出 byte-identical、summary 完备（含 `base/stage1/final` 三层 `exact` 与 `verify` 分别分层 per source + coverage +`first_pass/stage1_success/stage2_leak/per_source_avg/coverage_avg`/`Wilson 95%` per source & coverage/paired 四类/G3' 等）、独立重算；校验分层门禁 `coverage 70/90 ∧ per-source 20/30 ∧ undetected==0` 且 `base→Δ16` 主判
- [ ] **G4** 写 `OPERATOR_RETURN.md` 与 `DEVELOPMENT_RESULT.md` 候选；lifecycle 保持 result-candidate；含 `base/stage1/final` 三层 `exact` 与 `verify` 分别计数（per source & coverage）、`rescued`、分源、`N_stage1/N_stage2` per source & coverage、`rescue_rate` per source、`per_source_avg/coverage_avg`、`Wilson 95%` per source & coverage、`total_disclosed_bits` 与 `disclosure_per_final_exact_block` 等描述性；Claim 仅 independent cross-session qualification evidence，非 FER/SKR/promotion，不宣称同分布复现

## 本变更期间显式禁止

plan ACCEPT 前实现；以 outcomes 回搜增量位置/调 Δm 或多候选择优；改首遍 support/标签/先验/度分布/m2/H1/泄漏/decoder；重复比较 prior、标签或另一全新 MET 图；新增第三增量或档位；以 V48-V54 outcomes 选持块或调 Δm 或用 V13 HOLD 剩余帧冒充独立 TEST；写 NPZ；把 wrong 计为 exact；作晋升排名；FER/阈值/SKR/安全/资格/晋升扩大陈述；结果后加块/加 seed/改阈值/改机制；rerun/resume/补偿 partial；任何结果下开第二轮；自接受；自动启动后继（含下一阶段）；import `v50/v51/v52/v53/v54` 模块作生产解码；任何 `compute_tag_64(x1_true,…)` 形态；改 H1/`m2` 泄漏或 `90/1.0` 或引入第二变量外泄漏变更；创建正式 `.../v55_*/run_01` 输出（P0 仅规划+intake）；运行 decoder（当前仅 decoder-free P1-P3 已 PASS，intake 3/3 READY 已验）；声称 `1M_600k_0dB/1p5M/2M_1p2M_0dB` 与 `2026-01-21` 同分布；用模拟 TEST 数据冒充真实采集；创建 production module/CLI/tests/正式 output root（当前禁止）；自授 `EXECUTE_AUTH` 或伪造 `QUALIFICATION_PLAN_READY`（已由 G1-G7 全 PASS + authoritative 90-block 达成）；将块换作非 authoritative 或重采样；将 P1-P3 由 PASS 改写；实现 runner 后未授权即运行。
