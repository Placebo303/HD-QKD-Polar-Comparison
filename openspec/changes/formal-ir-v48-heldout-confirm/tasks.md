# OpenSpec Tasks: formal-ir-v48-heldout-confirm

**Lifecycle**: `PLAN_CANDIDATE / EXECUTE_NOT_AUTHORIZED` — **只规划，不实现，不执行，不启动 V49。等待独立评审。**
**Execution status**: 本轮不授权任何实现/执行；以下任务待独立 plan ACCEPT 后方可进入 A-phase；当前不跑 decoder，不写产出。V48 冻结 H1-16 held-out 单臂 45 块 (90 calls) 推荐，30 块备案。
**HEAD**: `cb4f9990f7864e12dd69c89ee87ec8a58e77470d`（实现冻结时重绑）

## Phase A — 语义冻结（plan ACCEPT 后）

- [ ] **A1** 冻结 H1-16 单臂与主候选语义：`H1-16=V31-H1 (16×1024,80b)` 母矩阵经 `nonbinary_v31.build_matrix_packet(m1=16,QC-cyclic-projective)` 确定性构造，`rank16` 已验证（GF32 poly37）、`capacity_ok/projective_safe`；单臂仅 `m1=16`，不改 Lane C L2、不改 `90/1.0`、不改 L1APP 公式与 L2-only verification；V48 不新增第二候选/第二变量。
- [ ] **A2** 冻结 L1APP + verification 同构与 prior 隔离：`p_i=P(U1|B)` (V25 TRAIN C `floor 1e-15`)、`s1=H1·u1^Alice`、`BP_i=decode_row_layered_fftqspa(H1,p_i,s1).bp_posterior_beliefs` (APP approximation, 冻结 early-stop)、`q_i=softmax(BP_i)`、`P_i(U2)=Σ q_i P(U2|B,u1)`；`tag_scope=l2_only`，`compute_tag_64(empty_uint8,x2)` 空前缀复用 V35（`b1+b2→SHA256→hex[:16]`），不重实现 canonical；**prior 始终只由 TRAIN `channel_counts.npz` 经 `load_v25_channel_counts()` 构建**，evaluation blocks 来自 held-out 连续 4 frames=1024 pairs（按 `split_manifest` 60/20/20 hold 区间），**禁止用 held-out 重估 prior**。
- [ ] **A3** 冻结四类与门禁等比语义：四类 `exact / detected_verification_failure (syndrome_ok&&!exact&&!tag_ok) / decoder_non_syndrome_failure (!syndrome_ok&&!exact) / undetected_accepted_wrong (tag_ok&&!exact, ≈2^-64)`；G3' 为 `undetected==0` 单臂；**推荐 45 块** `G1 exact_full≥35/45 (77.8%)`、`G2 每源 exact_full≥10/15 (66.7%)`、`G3' undetected==0`，通过当且仅当三条全满足；30 块备案 `G1≥24/30` 且 `G2≥7/10` 算术亦冻结；`L1 wrong` 单独报告不入 G3'；`exact_full=exact_u1&&exact_l2` 为门禁主判据，同时报告 `exact_u1/exact_l2`；tag 只验 L2，`exact_full` oracle 不经 tag。
- [ ] **A4** 冻结 held-out 块与隔离根：**推荐 45 块** `390128-142/390228-242/390328-342`（每源 15，x28–x42 连续，`390328+` 代表 2M 侧），**备案 30 块** `390128-137/390228-237/390328-337`（每源 10，x28–x37）；均与 `FORBIDDEN 96 = 87 (78+V46 9) + V47 9 (390125-127/225-227/325-327)` 零重叠、无内部重复、每源均分、连续（J2）；增量根 `comparison_bench/outputs_comparison/formal_ir_methods/v48_heldout_confirm/run_01/` fail-closed；最小文件集含 `target_tag/candidate_tag/tag_ok/tag_scope/reclassified/leak_total` 四类与 TRAIN/held-out 双溯源；无 NPZ；库存富余 1683 frames / 430k pairs (1M 400 / 1p5M 554 / 2M 729) 已冻结。
- [ ] **A5** 冻结 V47 边界：不追溯重放 V47 54-call 记录，不改 V47 终态/文件；V48 用 held-out 45 块验证泛化，对照为 TRAIN 同候选历史结果，不在同 run 内重跑 TRAIN。
- [ ] **A6** 冻结其余同构常量：Lane C 三矩阵 ordinal-2、`90/1.0 poly37`、BP posterior beliefs / APP approximation、泄漏口径 **单臂 1064/1094/1104** (`5·m2+5·16+64`, m2=184/190/192, `f_total=leak/[N(H1+H2)] N=1024`)、J6 `errors_initial` 严格 per-block 单值门（单臂退化为一致性检查）、译码合约 90/1.0 early-stop。
- [ ] **A7** 冻结终态：三终态 `V48_HELDOUT_PASS / V48_HELDOUT_FAIL / V48_EVIDENCE_INVALID`，`V48_HELDOUT_PASS` 当且仅当 `G1∧G2∧G3'` 通过，`V48_EVIDENCE_INVALID` 优先；orthogonal `needs_heldout_structure_branch` 当且仅当 held-out `exact_full <35/45` 或某源 `<10/15` 时立旗。

## Phase B — 聚焦测试（仅 fake runner/decode_fn；零生产解码）

- [ ] **B1** Seed-registry 校验：拒重复、错形状、分别与各族重叠（V36_A3 15、V39 15、V40 probe 3、V41 9、V42 9、V43 9、V44 9、V45 9 共78 + V46 9 + V47 9 共96）；接受冻结新区 **45** `390128-142/390228-242/390328-342`（及备案 30）；校验新区连续 x28+ 每源均分且与 96 零重叠 per source。
- [ ] **B2** H1 重建匹配（单臂）：H1 母矩阵 `16×1024` 注入 metric 漂移（含 `rank/capacity/projective`）→ J 路径；重建一致；代表身份错→ J 路径；校验 `rank16` 与 `capacity_ok/projective_safe`。
- [ ] **B3** Tag 复用与分流测试（单臂 L2-only 空前缀）：`compute_tag_64` 确定性、`hex[:16]` 长度、`v35_tag_import_ok`（仅 `compute_tag_64`）；不变性：改变 `x1_true` 不得改变 L2 tag；敏感性：改变 `x2` 必须改变锚点 tag；四类分流：`syndrome_ok&&!exact&&!tag_ok→detected`、`!syndrome_ok&&!exact→decoder_non_syndrome`、`tag_ok&&!exact→undetected`、`exact→exact`；G3' 仅 `undetected==0` 触发，单臂。
- [ ] **B4** 哨兵与 prior 隔离与泄漏测试：每源首块 `390128/390228/390328` 单臂六项/七项 fake-beliefs 通路 + `prior_train_only_ok`（TRAIN loader 隔离，未触 held-out）+ `heldout_reachable_ok`（1683/430k 池可达，4 frames/block）+ `v35_tag_import_ok` + `tag_scope_l2_only` + `leakage_accounted`（**1064/1094/1104** 单臂，`leakage_already_accounted` 声名，工程 verification 声名 `SHA-trunc64 random-hash approx, L2-only`）+ `exact_full` oracle 声名（不经 tag）；校验 `leak_total =5·m2+5·16+64`；校验不重实现 `canonical_bytes` 且不宣称 symbols 等价；**禁止 held-out 重估 prior** 的负向测试。
- [ ] **B5** 样本一致性（单臂 held-out）：同 seed 重采样一致（样本算一次）；缺/重 block →J6/J12；J6 严格门注入：篡改 `errors_initial`→J6→`V48_EVIDENCE_INVALID` 且零 decoder calls；字段 `pairing_errors_initial_equal` 仍上报（单臂为自洽）；跨块 outcome 差异不判完整性失败；`tag_ok` 差异为诊断量；**校验 held-out 帧来自 hold 区间非 TRAIN**。
- [ ] **B6** 真值表测试（单臂门禁）：枚举 integrity ok/failed × `pass` 布尔 {pass→V48_HELDOUT_PASS, fail→V48_HELDOUT_FAIL}；各落且仅落一终态；`EVIDENCE_INVALID` 优先；穷尽互斥；另校验 orthogonal `needs_heldout_structure_branch` 当且仅当 `exact_full <35/45` 或某源 `<10/15` 时为 true (45 块；30 块则对应 24/30 与 7/10)；校验四类计数完备与 G3' `undetected==0`。
- [ ] **B7** Wrong/undetected 二态（基于 G3' 单臂）：(i) 注入 `detected`→ `undetected==0` 仍通过 G3'；(ii) 注入 `undetected`→ G3' 失败、`undetected_anomaly=true`→`V48_HELDOUT_FAIL`；无跨臂否决。
- [ ] **B8** 预算帽：fake 模式结构拒第 91 call（J10，总硬帽 90 @45 块；30 块则硬帽 60，区分 l1 45 / l2 45 / total 90 或 l1 30 / l2 30 / total 60）；planned/started/completed actuals 一致且分层记录；校验 `C01-C45` 顺序冻结（源 1M/1p5M/2M、块升序；30 块则 C01-C30）。
- [ ] **B9** 科学 preflight 失败路径：J2/J3/J4 (含 J4a TRAIN prior 与 J4b held-out 可达)/J5 各注入→建增量根、三件套（invalid notice、空 records、summary planned 90 / l1 45 / l2 45 / total 90、无聚合、terminal EVIDENCE_INVALID；30 块则 planned 60 / l1 30 / l2 30 / total 60）且零 decoder calls。
- [ ] **B10** Partial 保留：注入中途 `BaseException`→预建根内原样保留 raw partial（含 `tag_ok/tag_scope/partial`）、started/completed actuals（分 l1/l2/total）、仅失败标记、无性能聚合/门禁/终态，重抛可观测。
- [ ] **B11** CLI 守卫：缺旗→非零、零 calls、不创建；target SHA 与 HEAD (`cb4f9990f7864e12dd69c89ee87ec8a58e77470d`) 或 origin 分支任一不等→拒；四文件任一 SCOPED dirty（含 v48 替代 v47/v35）→拒（J1）；输出根已存在→拒且不创建（J7，Tier 0）。
- [ ] **B12** 记录 schema 完备含 `target_tag/candidate_tag/tag_ok/tag_scope=l2_only/reclassified∈{exact,detected,decoder_non_syndrome,undetected}/exact_u1/exact_l2/exact_full/bp_posterior_entropy/mean_abs_diff_q_p/leak_total` 单臂；`exact_full` 不经 tag（oracle）；译码参数断言：每记录 poly 37、90/1.0、无 warm-start 键、early-stop 冻结（J9）；`P_i(U2)=Σ q_i P(U2|B,u1)`（`q_i=softmax decode(H1,p_i,s1).bp_posterior_beliefs`）；断言无 `x1_true` 进 candidate；**断言 prior loader 为 TRAIN-only**。
- [ ] **B13** 写出合约：文件集精确（records 45 行含 tag_ok/tag_scope/reclassified/leak；30 块则 30 行、summary 含分层记账 90(或60) + 四类聚合 + G3' + 终态）、CSV/JSON 行对等、已存在根 fail-closed（J7）、不写 NPZ、summary 含 `leakage_already_accounted` + 工程 verification L2-only 声名 + provenance 含 `H1 16×1024` 与 tag 源 `v35:compute_tag_64(empty,x2)[:16] tag_scope=l2_only` + TRAIN `channel_counts` 与 held-out 1683/430k 双溯源；并校验单臂泄漏 `1064/1094/1104` (syndrome 920/950/960, `f_total` 含 N) 与 `35/45` 与 `10/15` 门禁声名与 `≥78%` 绝对阈值说明；校验 `tag_match&&!exact` 为工程近似声名且 `exact_full` oracle 声名。

## Phase C — preflight（decoder-free，授权前）

- [ ] **P1** 重建 H1 母矩阵 `16×1024`；与 committed 结构权威含 `full_row_rank/capacity/projective` 严格匹配（秩 16 校验）；校验 `compute_tag_64` 可 import 且空前缀 `empty+x2` 可用、`hex[:16]`、x1 不变/x2 敏感通过；校验 TRAIN `channel_counts.npz` 经 accepted TRAIN loader 可达且溯源为 TRAIN；校验 held-out 池按 `split_manifest` hold 区间可达且帧数 1683/430k (1M 400 / 1p5M 554 / 2M 729) 与 FORBIDDEN 96 零重叠；零求值器调用、零写盘。
- [ ] **P2** 在 `390128/390228/390328` 跑单臂绑定 preflight（单臂 fake beliefs 通路 `p_i→s1→BP_fake→q_fake` + `prior_train_only_ok` + `heldout_reachable_ok` + `v35_tag_import_ok` + `leakage_accounted` + `tag_scope_l2_only`）；仅报 PASS/BLOCKED；真实 `q≠p` 不在此 gate。
- [ ] **P3** 以拷贝 96 registries（87+9）机械校验 J2（新区 45 (或 30) 与 96 零重叠、无重复、每源均分、连续 x28+）并确认输出根缺席；校验 4 frames/block =1024 pairs 按 hold 帧序拼接可行；仅报 PASS/BLOCKED。

## Phase D — 授权诊断执行（需 EXECUTE_AUTH，90 invocations @45 块，held-out fresh-block）

- [ ] **D1** 主线程获独立 plan ACCEPT + 显式用户 `EXECUTE_AUTH`（绑定 repository、分支 `formal-ir-mainline`、完整实现 SHA、cycle V48P0、scope `v48_diagnostic_90_invocations_heldout_exactly_once` (45×2) 或 `v48_diagnostic_60_invocations_heldout_exactly_once` (30×2)，HEAD `cb4f9990f7864e12dd69c89ee87ec8a58e77470d`）。
- [ ] **D2** 恰好一次：`python scripts/execute_v48_heldout_confirm.py --execution-authorized --authorized-target-sha <sha>`；**90 invocations（45 L1 + 45 L2: 单臂 45 L1 +45 L2，推荐）**，45 L2 records（每源 15），每条 L2 含 `target_tag/candidate_tag/tag_ok/tag_scope/reclassified` 四类（L2-only `empty+x2`）与 `exact_u1/exact_l2/exact_full/bp_posterior_entropy/mean_abs_diff_q_p/leak_total`；或 **60 invocations (30 L2)** 备案；保留增量 run_01 文件；出错/partial 止、原样保留 raw partial 无性能聚合、返回 blocker；不 rerun/resume/repair/tuning/加块/改 seed/机制/加权重/重实现 canonical；无论结果如何不做第二轮诊断；执行期测量真实 `q≠p`/`iterations`/`entropy`/`||q-p||1` 与 `tag_ok` 分流；prior 始终 TRAIN-only。
- [ ] **D3** 只读 postcheck：记账（planned 90 / l1 45 / l2 45 / total 90，除非完整性停止；30 块则 60 / l1 30 / l2 30 / total 60）、L2 记录数 45(或30) 与序 C01-C45(C01-C30) 单臂正确、L1 45(30)次、`tag_ok`/`tag_scope` 与四类计数完整、**prior loader 溯源 TRAIN**、held-out 块来自 hold 区间、无 NPZ 写、V38-V47 输出 byte-identical、summary 完备（含分层记账与 exact/detected/decoder_non_syndrome/undetected + G3' `undetected==0`、`exact_full`/`exact_u1`/`exact_l2`、终态、迭代/运行时/entropy/||q-p||1、L2-only 声名、TRAIN/held-out 双溯源）、门禁与终态独立重算（含 `needs_heldout_structure_branch` orthogonal 校验与 `f_total` 核验与工程 verification 声名与 `35/45`/`10/15` (或 24/30/7/10) 阈值核验）。

## Phase E — 结果复核

- [ ] **E1** 写 `OPERATOR_RETURN.md` 与 `DEVELOPMENT_RESULT.md` 候选；lifecycle 保持 result-candidate；含 terminal（V48 三终态）、路由轨迹、单臂 per-source 四类聚合与 `exact_full`/`exact_u1`/`exact_l2`、per-block 结果、门禁明细（基于 `undetected==0` 单臂，L2-only，`L1 wrong` 单独表，35/45 与 10/15 权威，≥78% 绝对阈值说明）、`l1` 执行期诊断上下文（真实 `q≠p`/entropy/iterations/||q-p||1/syndrome/tag_ok）及有界方向性分流（`HELDOUT_PASS / HELDOUT_FAIL` 二格冻结门禁 + orthogonal `needs_heldout_structure_branch`）严格在 claim boundary 内（含 L2-only、工程近似与单臂固定泄漏 1064/1094/1104 说明，不宣称信息论 2^-64，`exact_full` oracle，H1-16 冻结，TRAIN prior 隔离，held-out 1683/430k 溯源）。
- [ ] **E2** 主线程/reviewer 独立重算信号/机器/终态与四类计数；verdict 入 `REVIEW_VERDICT.md`。
- [ ] **E3** Memory triage 单独里程碑（本规划轮禁改 `AGENT_PROJECT_MEMORY.md`）；**不启动 V49**。

## 本变更期间显式禁止

plan ACCEPT 前实现；对 V47 54-call 记录重放或复用历史 96 块作 held-out 样本；**用 held-out 重估 prior / 更新 channel_counts / 校准 P(U1|B) 或 P(U2|B,u1)**；重新实现 `canonical_bytes/tag_of` 或不复用 V35 `compute_tag_64` 或宣称 `compute_tag_64_symbols` 与空前缀调用等价；宣称固定公开 SHA-256 截断的信息论 `2^-64` 安全界（仅随机哈希模型工程近似，严格界需 universal2+seed）；把 `detected` 计为 exact 或把 `undetected` 静默为 success；保留旧 G3 `wrong_codewords==0`（应为 `undetected==0`）；新增 decoder/矩阵/参数网格/调参/多臂；加事后权重；改写/覆盖 V47 输出或追溯改其终态；以非 accepted TRAIN loader 读 NPZ 或以 held-out counts 作 prior；写任何 NPZ；把 wrong 计为 exact；作多臂优劣排名；FER/阈值/SKR/安全/资格/晋升/真帧全集陈述；结果后加块/加 seeds/改阈值/改机制/加权重；rerun/resume/补偿 partial；任何结果下开第二轮诊断；任何结果下调 decoder 参数；把跨块 outcome 差异判为完整性失败；自接受；**自动启动后继（含 V49）**；import v39/v40/v41/v42/v43/v44/v45/v46/v47 模块作生产解码；任何 `compute_tag_64(x1_true, x_hat)` 形态；**改 H1/L2 矩阵或 `90/1.0` 或引入第二变量或用 held-out 更新 prior**。
