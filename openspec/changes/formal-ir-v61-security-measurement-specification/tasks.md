# OpenSpec Tasks: formal-ir-v61-security-measurement-specification — V61 最小安全测量采集规范

**Lifecycle**: `PLAN_CANDIDATE / DECODE_FORBIDDEN / MEASUREMENT_SPEC_ONLY` — 仅定义“新实验需要记录什么”，不跑 decoder，不进 V62，不实现脚本、不启动 V62，普通推送新 Plan SHA 后停止
**HEAD**: `7b476f62368a410722ab1e0d65ea2709e423d848` → 新 Plan SHA (branch `formal-ir-mainline`, 实施前以 `git fetch && git rev-parse HEAD == origin/formal-ir-mainline == 7b476f62368a410722ab1e0d65ea2709e423d848` 重核，不一致阻塞) + data SHA `84d62779` (200ps legacy_v1 nearest 1024)
**Predecessor**: `formal-ir-v60-composable-security-input-readiness` `9f1fb02c` 已固化 `V60_DATA_NOT_READY / ell=null` (m1 981/1024/1024, m2 424/451/516, leak 7089/7439/7764, n1024, tag64, GF32 5bits) — V60 缺口为 `composable theorem missing + decisive PE missing`；本修订起点 `7b476f62368a410722ab1e0d65ea2709e423d848`，`R61-01 dependency-aware readiness` + `R61-02 verification 口径冻结` 最小修订，不实现脚本、不启动 V62
**Boundary**: 每 block 1024 符号，64-bit tag 已含不得重复扣除，仅定义采集；`H1/Lane C/H_inc/Δ/decoder 90/1.0/poly37/estimator` 全冻；禁把 `IAB/H/MAP/visibility shadow` 当 `H_min`；`R61-01` 将 10 类 `全部READY` 改为 `required_core / required_if_theorem_applicable / not_applicable_with_theorem_reason / missing` 四类，`V62_OPEN` 仅要求 `required_core` 全 ready + 所选定理条件依赖 ready，无关 `visibility/decoy` 允许 `N/A` 不阻塞；`R61-02` 冻结 `leak_IR 已含 tag64 / leak_other = max(0,actual-64) / epsilon概率不入bits` 并新增 `tag64-only / extra-verification / epsilon-not-bits` 三边界测试；仅新数据 `READY` 允许 `ell=1024*hmin_lower - leak_IR - leak_other - finite` 三源独立比 `10% margin`；缺 `theorem` 或 `decisive core PE` 则 `V62 PENDING`，禁旧数据伪造 PE，禁 `decoder/V62数值`；三源分别、`rg "decode_" 0 hits`、`git diff -- src/ ==0`

## Phase A — 10 类采集规范 Schema 与模板（decoder-free，禁止自创，proxy 不升级，missing→null，R61-01/02）

- [ ] **A1 fetch 与 HEAD 自检（阻塞门）**：`git fetch origin && git rev-parse HEAD == origin/formal-ir-mainline == 7b476f62368a410722ab1e0d65ea2709e423d848` 完整 40 位，不一致则阻塞；记录 `HEAD/origin/implementation SHA` 至 `V61` 报告 `provenance`，`rg "9f1fb02c" 0 hits` 旧 SHA 无残留（除历史记录），`rg "7b476f62368a410722ab1e0d65ea2709e423d848"` 新起点已验；`data SHA 84d62779` 已验
- [ ] **A2 V60 缺口溯源（只读）**：阅读 `openspec/changes/formal-ir-v60-*/v60_composable_security_readiness.json` 与 `tools/security_reports/round3_build_proof_gap_matrix.py` 的 `per_point_franson_pe_chain: missing / conjugate_basis_stats: missing / decoy_state_PE: missing / protocol_specific_composable_constants: missing`，确认 10 类中 `theorem/e_ph/conjugate/n_PE/vis_chain` 为 `missing/proxy` 且 `V60_DATA_NOT_READY`，沉淀为 `V61` 10 类 `minimal_new_measurement` 输入；记录 `R61-01` 依赖分类与 `R61-02` verification 口径为增量必验
- [ ] **A3 协议/安全定理及适用假设 字段定义（R61-01）**：定义 `theorem_id`（`Renner/Niu 2016/Tomamichel/Lim` 等枚举+原文行号）、`assumptions[]`（`collective/coherent, PE 模型, finite-key, auth, EV`）、`domain`（适用维数/信道假设）+ 新增 `visibility_dependence: bool` 与 `decoy_dependence: bool` 显式声明，要求新实验提供定理原文绑定 `source(file:line:expr)` 并声明是否依赖 `visibility/decoy`，无声明则 `authority=missing → V62 PENDING`，`readiness: required_core`
- [ ] **A4 conjugate-basis/phase-error 观测字段定义（R61-01）**：定义 `e_ph`（相位错误率 `[0,1]`）+ `conjugate_basis_stats`（`X/Z` 基计数、`n_X/n_Z`、`e_X/e_Z` 若适用）`readiness: required_core` + `decoy_chain`（若用 decoy-state 则 `decoy 强度/统计链`）`readiness: required_if_theorem_applicable`（否则 `not_applicable_with_theorem_reason` 带定理行号），明确 `e_ph` 估计方法与误差区间来源，禁止 `SER/Shannon H/visibility` 代理字段，代理则 `EVIDENCE_INVALID`；`decoy` 无关定理时 `N/A` 不阻塞 `V62_OPEN`
- [ ] **A5 n_PE 与抽样规则字段定义**：定义 `n_PE`（权威 `PE` 样本数 `integer`）+ `sampling_rule`（`random_without_replacement` / `p_Z/p_X` 基选择概率 / `PE 帧标记` / `seed_or_counter` 可重放链），与 `n_eff`（`shadow proxy`）严格区分，模板列 `n_PE_source, p_Z, p_X, sampling_rule, seed`，`readiness: required_core`
- [ ] **A6 visibility 逐源区间字段定义（R61-01）**：定义每源每 `loss/session` 的 `[vis_low, vis_high]`（`float [0,1]`）+ `vis_source` + `cal_chain`（干涉可见度校准链），`readiness: required_if_theorem_applicable`（仅当 `theorem_id` 显式以 `chi_from_visibility(d, vis)` 绑定 `vis→chi_E` 时为 `required`，否则 `not_applicable_with_theorem_reason` 需定理行号为据），明确 `chi_E = h2((1-vis)/2)+e*log2(d-1)` 输入 `vis` 的实测链来源，`global 0.95 shadow` 仅 `proxy` 不可升 `composable`，无关定理时 `N/A` 不阻塞
- [ ] **A7 eps_sec/eps_cor 分配字段定义（R61-02）**：定义 `eps_sec, eps_cor` 总预算及分解 `eps_sec >= eps_PE+eps_PA+eps_EC`、`eps_cor >= eps_EV+eps_auth`，每项与 `DeltaFK/EV/auth` 公式 `eps` 一致性可校验列，`epsilon_EV/EC` 保留为概率、未经定理显式 `bits` 映射不得直接加进 `leak_other/finite`，缺分解则 `missing`，`readiness: required_core`
- [ ] **A8 verification/authentication 泄漏字段定义（R61-02 冻结口径）**：定义 `actual_verification_bits_per_block`（`verification transcript` 实测值）+ `leak_verification_extra = max(0, actual-64)`（仅超出 64 的部分入 `leak_other`）+ `leak_auth_bits_per_block`（认证开销）+ `epsilon_EC: probability`（概率不入 `bits`）+ `tag_included_in_IR: true`（`leak_IR` 已含 `tag64` 不重复扣除），明确 `leak_total =5*m_total+64` 中 `tag` 不重复扣除，`epsilon` 概率未映射不得转 `bits`，`readiness: required_core`；新增三边界断言：`tag64-only / extra-verification / epsilon-not-bits`
- [ ] **A9 finite-size / post-selection / 有效帧计数字段定义（R61-02）**：定义 `DeltaFK_formula`（`4*sqrt(log2(2/eps_sec)/n_eff)+...`）+ `n_eff`（有效计数）+ `coeff_authority`（系数权威）+ `accepted_frame_fraction` + `n_block`（`N_pairs→frames→blocks` 映射）+ `post_sel_penalty`（`bits/block` 或 `null`），单位 `bits/pair` vs `bits/block` 显式，`epsilon` 概率未映射不得入 `finite`，`readiness: required_core`
- [ ] **A10 单位/时间戳/session/source 绑定字段定义**：定义 `unit_map`（`bits/block` vs `bits/symbol` vs `bits/pair` vs 无量纲/计数的每项映射）+ 每 `block` 的 `session_id/source_id/delay_used_ps/block_id/pairing=legacy_v1 nearest 200ps` 四元绑定，要求新实验数据 `data_inventory.json` 中 `session/source/timestamp` 可追溯，绑定缺失则 `EVIDENCE_INVALID`，`readiness: required_core`

## Phase B — 验收公式与阈值（`ell` 仅新数据 READY 可算，三源独立，tag 不重复，R61-02 口径）

- [ ] **B1 验收公式冻结（仅新数据 READY 可算，R61-02 口径）**：冻结 `ell_s = 1024 * hmin_lower_s - leak_IR_s - leak_other_s - finite_s`，其中 `leak_IR_s = leak_total_s =5*m_total+64` 已含 `tag64` 不重复扣除（R61-02），`hmin_lower` 为 `bits/symbol` composable 下界，非 `READY` 时 `ell_* = null` 不冒充；`leak_other_s = max(0, actual_verification_bits-64) + PE_s + auth_s + ...`, `finite_s = DeltaFK_new_s + ...` 单位全 `bits/block`，`epsilon_EV/EC` 为概率未经定理显式 `bits` 映射不得入 `leak_other/finite`；仅 `V61_NEW_DATA_READY`（`required_core` 全 ready + 所选定理条件依赖 ready）时以新实验权威 `hmin_lower` 计算 `ell_s`，其余 `ell=null`；三边界断言待验：`tag64-only / extra-verification / epsilon-not-bits`
- [ ] **B2 GF32 与 tag 不重复校验（R61-02）**：逐源校验 `m_total == m1+m2` (`1405/1475/1540`) 且 `leak_total ==5*m_total+64` (`7089/7439/7764`)，`leak_without_tag =5*(m1+m2)` (`7025/7375/7700`)，`tag=64` 单计不重复，`leak_IR 已含 tag64` 且 `leak_other = max(0,actual-64)` 仅计超出部分已验；`log2 q=5` 显式记录；`hmin ≤10 bits/symbol (log2 d)` 上界校验；失败则 `SPEC_INVALID`；附加 `tag64-only: actual 64→extra 0` 已验
- [ ] **B3 Schema/模板落盘（≥10 类_exact，R61-01 四类）**：`v61_measurement_schema.json`（`JSON Schema draft-07`，`required` 含 10 类，`readiness` 四类 `required_core / required_if_theorem_applicable / not_applicable_with_theorem_reason / missing`）与 `v61_minimal_measurement_template.csv`（列 `source, field, symbol, meaning, unit, required_class, authority, example_value, source_trace, readiness_detail`）逐项精确落盘新 `docs/research_cycles/V61P0/`，每项 `missing → composable:null + minimal_new_measurement`，`visibility/decoy` 条件依赖 `N/A` 带定理依据已验，`proxy 不升级为 composable` 已验，`rg "decode_" 0 hits`
- [ ] **B4 `H/IAB/MAP/vis ≠ H_min` 审计**：检查若 `hmin_lower` 借用 `H(A|B)` / `IAB_est` / `MAP_acc` / `visibility shadow` 则显式 `proxy_missing` 并报告语义差，且 `composable` 保持 `null` 不填；正宗 `H_min` 需新实验权威文件行号支撑，否则 `missing`；脚本 `assert proxy_not_upgraded` 已验；`epsilon_not_bits: assert epsilon 概率未入 bits` 已验
- [ ] **B5 不跨 source 平均校验**：`schema/template` 与 `decomposition table` 三源独立列，禁止 `mean(hmin)` 替代逐源 `hmin_break_even`；`assert` 三源分别计算，报告不出现总体平均替代
- [ ] **B6 decomposition 阈值表落盘**：落盘 `break_even` 阈值表（`source, leak_total, floor, margin_5%, margin_10%, hmin_lower_authority, ell_or_null, gate, verification_caliber`）三源独立，`py_compile` 前已验；`floor = leak/1024 = 6.9238/7.2637/7.5820` 等已算，`leak_IR含tag / leak_other=max(0,actual-64)` 显式

## Phase C — 三档 break-even 阈值（optimistic floor 必给 / 10% margin 锚点，每源三源独立，R61-02 口径）

- [ ] **C1 optimistic floor（必给，other=0 finite=0，description，R61-02）**：逐源 `hmin_break_even_floor_s = leak_total_s /1024` bits/symbol，`1M 6.9238, 1p5M 7.2637, 2M 7.5820`，与 `leak` 锚点双校验（`floor*1024 == leak`），`log2 d=10` 上界校验（`floor <10`），落盘 `break_even.optimistic_floor[]`，为新数据 `READY` 与非 `READY` 共用门限参考，但非 `READY` 不升为密钥余量；校验 `leak_IR=leak_total 已含 tag64`
- [ ] **C2 margin 阈值（10% 必比，每源三列，R61-02）**：`h_m(margin) = (leak_IR+other+finite)/(1024*(1-margin))`，`margin 0% = floor, 5% => /0.95, 10% => /0.90`，逐源三行三列成表，非 `READY` 时 `other=0 finite=0`（`other` 中 `max(0,actual-64)` 占位为 0）仅作锚点：
  - `1M: 6.9238 / 7.2882 / 7.6931` (floor/5%/10% optimistic)
  - `1p5M: 7.2637 / 7.6460 / 8.0707`
  - `2M: 7.5820 / 7.9811 / 8.4245`
  `READY` 时以真实 `hmin_lower/leak_other(=max(0,actual-64)+...)/finite` 覆盖，未 `READY` 时保持 `hmin_lower_authority=null` 且 `ell=null`；三源独立 `log2 d=10` 上界校验
- [ ] **C3 composable 显式 null（缺则 null，不填 0）**：若 `H_min` / `e_ph` / `composable theorem` 缺决定性 `required_core` 输入，则 `hmin_lower_s = null` 且 `ell_s = null`（JSON `null`）而非 `0`，落盘 `break_even.composable = [null]`，并显式 `composable_missing_reasons[] = {theorem missing / decisive core PE missing / proxy not H_min / finite authority missing ...}`，条件依赖 `visibility/decoy` `N/A` 不计入缺失原因
- [ ] **C4 单位换算与锚点双校验（R61-02）**：`leak_total_s ==5*m_total+64` 且 `h_floor_s *1024 == leak_total_s` 整点校验；`bits/block = bits/symbol *1024` 显式换算记录；`h_floor*1024/(1-margin)` 反算校验已验；`tag已含不重复` 与 `max(0,actual-64)` 口径已验
- [ ] **C5 紧凑CSV（阈值锚点）**：`v61_break_even_thresholds.csv` 列 `source, leak_total, floor, margin_5%, margin_10%, hmin_lower_authority, hmin_lower_null_or_value, ell_or_null, gate, verification_extra`，`composable` 列显式空或 `null`，`floor / 5% / 10%` 数值已落盘且与 `schema` 一致，`verification_extra = max(0,actual-64)` 显式
- [ ] **C6 仅新数据 READY 比阈值判定 ell>0（R61-01/02）**：仅当新数据 `V61_NEW_DATA_READY`（`required_core` 全 ready + 所选定理条件依赖 ready + `R61-02` 口径已验）时逐源比较 `hmin_lower` vs `floor`（`6.9238 vs`）与 `10% margin`（`7.6931 vs`）判定 `ell>0` 与 `margin≥0.10`，并报告 `ell_final / per_pair / margin_ratio`（其中 `leak_other` 按 `max(0,actual-64)` 口径）；非 `READY` 时仅报告阈值锚点，不宣称 `ell>0 / SKR`，模板中以 `null` 占位；`epsilon` 概率不得入判定

## Phase D — V61 规范就绪与 V62 门禁（按序优先级，不可共存，proxy 不升级，R61-01/02，仅新数据 READY 进 V62）

- [ ] **D1 总体判定（first-match 互斥，R61-01 dependency-aware，R61-02 口径）**：
  ```
  if not spec_10items_ge10 or not unit_ok or leak !=5*m+64 or m_total != m1+m2 or IAB/H/MAP/vis_as_Hmin or not formula_self_consistent or tag_repeated or proxy_upgraded or old_data_fake_PE or verification_caliber_mismatch_R61_02:
      overall = V61_SPEC_INVALID  # 硬完整性：规范缺项、单位不一致、tag 重复、m 不自洽、H/IAB/MAP/vis 当 H_min、proxy 升级、旧数据伪造 PE、R61-02 口径不验（leak_IR含tag / max(0,actual-64) / epsilon不入bits）
  elif composable_theorem_missing_in_new_data_spec or decisive_core_PE_missing_in_new_data_spec:  # 缺定理或缺 required_core 中 e_ph/conjugate/n_PE
      overall = V61_SPEC_READY__V62_PENDING  # 规范已就绪但缺 core 新数据，需新实验补采集，ell=null，不进 V62，立即停；条件依赖 visibility/decoy N/A 不判此分支
  elif not all_required_core_ready_spec or not conditional_deps_ready_for_selected_theorem or not hmin_lower_composable_defined:
      overall = V61_SPEC_READY__V62_PENDING  # 规范已落盘但 core 或所选定理条件依赖仍 PENDING，已输出清单与阈值，未 ready 前 ell=null；无关 visibility/decoy N/A 不计入
  elif spec_all_required_core_ready and new_data_all_required_core_ready and conditional_deps_ready_for_selected_theorem and hmin_lower_composable and break_even_floor_done and checklist_done and unit_ok and not old_data_fake and verification_caliber_ok_R61_02:
      overall = V61_SPEC_READY__V62_OPEN  # 唯一允许另起 V62 数值后继（需新 OpenSpec + 独立授权），可算 ell=1024*hmin - leak_IR - leak_other(=max(0,actual-64)+...) - finite
  else:
      overall = V61_SPEC_READY__V62_PENDING
  ```
  实际落盘逻辑为严格按序 `V61_SPEC_INVALID > V61_SPEC_READY__V62_PENDING(缺theorem或decisive core PE) > V61_SPEC_READY__V62_OPEN(required_core全ready + 条件依赖ready, 唯一可进V62)`，`first_match` 显式记录，**仅新数据缺 `H_min` 而未补清单与阈值不得判 `OPEN`，仅 `OPEN` 允 `V62`，其余 `ell=null`** 已验；`R61-01` 例：`visibility` 对不依赖 `vis` 的定理以 `not_applicable_with_theorem_reason` 不阻塞 `OPEN` 已验
- [ ] **D2 互斥性与旧数据伪造审计（R61-01/02）**：`assert overall in [SPEC_INVALID, SPEC_READY__V62_PENDING, SPEC_READY__V62_OPEN] 且仅一态`，`proxy_missing 保持 missing/null`，`shadow 不升级为 composable`，`missing 不填 0`，`rg "H_min.*=.*IAB|IAB.*H_min|MAP.*H_min|visibility.*H_min" 0 hits` 无自行等价，`rg "old.*PE|fake.*PE|pseudo.*PE"` 审计旧数据 `e_ph/n_PE` 是否源自 `V55` 重算（若是则 `EVIDENCE_INVALID`），`only_NEW_DATA_READY_ell: assert (overall==OPEN) == (ell_not_null)` 已验；`R61-01` 断言：`visibility N/A` 时 `conditional_deps_ready` 仍可为真；`R61-02` 断言：`epsilon概率未入bits` 已验
- [ ] **D3 仅 `OPEN` 可进 V62，其余 stop**：报告显式声明“**仅 `V61_SPEC_READY__V62_OPEN`（新数据 `required_core` 全 ready + 所选定理条件依赖 ready + `hmin_lower` composable 均 ready，R61-02 口径已验）允许另起 `V62` 数值计算 `ell =1024*hmin_lower - leak_IR - leak_other(=max(0,actual-64)+...) - finite` 逐源判定 `ell>0` 与 `margin≥0.10`，其余 `PENDING/INVALID` `ell=null` 仅输出 floor/5%/10% 门限与缺口清单，立即停，不进 decoder，不重跑 V55，不改 LDPC**”，`PENDING` 与 `INVALID` 区分已验，无关的 `visibility/decoy` `N/A` 不得阻塞已验
- [ ] **D4 停止条件校验（R61-01/02）**：脚本模拟 `composable theorem missing` 注入 → `V62 PENDING` 且 `ell=null`，`decisive core PE missing` 注入 → `V62 PENDING` 且未以 shadow 填 `null` 已验，`visibility N/A` 对不依赖 `vis` 定理注入 → 仍可 `OPEN` 已验（`R61-01`），`decoy N/A` 同理；`proxy 补 null` 注入 → `SPEC_INVALID` 已验；`old_data_fake_PE` 注入 → `EVIDENCE_INVALID` 已验；`R61-02` 三边界：`tag64-only: actual 64→extra 0`、`extra 128→extra 64`、`epsilon概率不入bits` 已验

## Phase E — 脚本与报告交付（PLAN_CANDIDATE / DECODE_FORBIDDEN，R61-01/02，普通推送后停止）

- [ ] **E1 编写/预留 `scripts/v61_measurement_spec_check.py`** (decoder-free, 可选，R61-01/02)：`python scripts/v61_measurement_spec_check.py [--schema docs/research_cycles/V61P0/v61_measurement_schema.json] [--template docs/research_cycles/V61P0/v61_minimal_measurement_template.csv]` → A1 fetch/HEAD(`7b476f62368a410722ab1e0d65ea2709e423d848`) → A2 V60 缺口溯源 → A3-A10 10 类字段定义（四类 readiness）→ B1-B6 验收公式/阈值表（R61-02 口径 `max(0,actual-64)`）→ C1-C6 三档 floor/5%/10% + null → D1-D4 first-match 门禁（R61-01 dependency-aware 仅 core+条件依赖进 V62）→ 含 `tag64-only / extra-verification / epsilon-not-bits` 三边界测试 → 输出 `v61_measurement_schema.json + v61_minimal_measurement_template.csv + v61_minimal_new_measurement_checklist.csv + V61_SECURITY_MEASUREMENT_SPEC_REPORT.md` + 控制台摘要，`rg "decode_" 0 hits`，`py_compile` PASS，未创建 `run_01`，不改 `src//experiments//tools/` 且 `openspec/changes/formal-ir-v5[5-9]/` 与 `openspec/changes/formal-ir-v60*/` 零改，不进 `V62`
- [ ] **E2 撰写 `docs/research_cycles/V61P0/V61_SECURITY_MEASUREMENT_SPEC_REPORT.md`**：`V60 缺口溯源`段（`V60_DATA_NOT_READY` + `proof_gap missing` 原文）、`10 类字段定义表`（symbol/meaning/unit/required_class/readiness四类/authority/source_trace/depends_on）、`unit/timestamp/session/source 绑定`、`验收公式`（`ell =1024*hmin - leak_IR(已含tag64) - leak_other(=max(0,actual-64)+...) - finite` 三源独立，`epsilon概率不入bits`）、`break_even` `floor/5%/10%` 阈值表（含 `6.9238/7.2637/7.5820` floor 与 `7.6931/8.0707/8.4245` 10% 锚点）、`V62 门禁（R61-01 dependency-aware）`、`R61-02 verification口径与三边界测试`、`minimal 新增测量优先级清单`（按优先级排序含 `depends_on`，最小可执行采样/校准/定理验证）、`overall` 终态与规范闭合声明，数据与 `json/csv` 一致，显式 `V57-V60 m/leak 冻结` + `tag已含不重复 / leak_other=max(0,actual-64) / epsilon不入bits` + `三源分别不平均` + `proxy 不升级` + `missing→null` + `仅READY(core+条件依赖)算ell` + `visibility/decoy N/A不阻塞` + `V62 PENDING`，不扩大为 `FER/SKR/晋升`
- [ ] **E3 生成 `v61_minimal_new_measurement_checklist.csv`**：列 `priority, item, missing_reason, minimal_new_measurement, required_sample_or_proof, acceptance_criterion, depends_on`，按 `composable theorem > decisive core PE (e_ph/conjugate/n_PE) > visibility[conditional]/decoy[conditional] > eps/finite > EV/auth/post_sel > units/binding` 优先级排序，其中 `visibility/decoy` 标注 `depends_on=theorem_id` 条件依赖，精确列出需新采集参数（无 proxy 填充），与 `schema/template` 四类一致
- [ ] **E4 自检（gate，R61-01/02）**：`py_compile` PASS, `rg "decode_" 0 hits && rg "import.*decoder" 0 hits && rg "construct_" 0 hits`，`git diff -- src/ ==0 && git diff -- experiments/ ==0 && git diff -- tools/ ==0 && git diff -- openspec/changes/formal-ir-v5[5-9]/ ==0 && git diff -- openspec/changes/formal-ir-v60*/ ==0`（除本变更外零改），三源 `leak_without_tag/tag` 不重复已验，`GF32 5bits` 已验，`H/IAB/MAP/vis !=H_min` 已验，不跨源平均已验，`break_even 锚点 leak/1024` 与 `h*1024/(1-margin)` 已验，`missing→null` 已验，`proxy 不升级` 已验，`10 类覆盖` 四类已验，`R61-01 visibility/decoy N/A不阻塞` 已验，`R61-02 tag已含/max(0,actual-64)/epsilon不入bits` 三边界已验，`仅新数据CORE+条件依赖READY算ell` 已验，`旧数据伪造PE` 零验，`HEAD==origin (7b476f62368a...)` 已验，`run_01` 不存在已验，`V62` 未触发已验
- [ ] **E5 普通推送新 Plan SHA 并停留 `PLAN_CANDIDATE / DECODE_FORBIDDEN`**（基于 `7b476f62368a410722ab1e0d65ea2709e423d848`），未创建任何 `run_01` decoder 执行，不碰 `V57-V60 m/leak`，`src//experiments//tools//V54-V60` 零改，普通推送（非 force）至 `formal-ir-mainline`，推送后等待独立审核（`Pre-RESULT` 独立线程复核 `HEAD/ACCEPTED_PLAN_SHA/rg 0 hits/run_01不存在/py_compile+关键测试 PASS`），`V62` 仍 `PENDING`，返回 `Plan SHA / implementation SHA / 10类 schema / 三源阈值 / V62 门禁 / 终态`，**不自动进入 `V62`，不实现脚本、不启动 V62**

## 本变更显式禁止

decoder 调用（`decode_* / construct_*` 等）；改 `V57-V60 m1/m2/leak` 或 `H1/Lane C/Δ/decoder` 冻参或新增矩阵；改 `LDPC` 码族；重跑 `V55`；自创 `H_min / finite-key / composable` 公式或造 `eps_sec/eps_cor/vis/phase-error` 参数；用旧数据伪造 `PE`（`e_ph/n_PE/conjugate` 重算无新观测）；重复扣除 `tag 64`；把 `IAB/H/MAP/vis` 当 `H_min` 不加 `missing`；将 `shadow` 升级为 `composable`；将 `missing` 填 `0` 而非 `null`；跨 source 平均 `hmin`；网格调参；把 `epsilon_EV/EC` 概率未经定理映射直接加进 `leak_other/finite`；把 `actual_verification_bits==64` 仍计入 `leak_other` 或把 `extra` 不计；把与定理无关的 `visibility/decoy` `N/A` 判为阻塞；宣称 `FER/SKR/阈值/晋升`；创建正式 `run_01` decoder 执行；改 `src//experiments//tools//V54-V60`；将总体平均掩盖单源阈值；在无新安全测量数据时创建数值型 `V62`；实现脚本/启动 V62（本修订仅 4 工件，普通推送新 Plan SHA 后停止）。

## 验收

- proposal/design/tasks/specs 一致 HEAD `7b476f62368a410722ab1e0d65ea2709e423d848`→新 Plan SHA 84d62779 lifecycle PLAN_CANDIDATE DECODE_FORBIDDEN MEASUREMENT_SPEC_ONLY（R61-01/02 已融）；10 类采集规范 `schema+template` 已落盘 `V61_SPEC_READY`，四终态按 `V61_SPEC_INVALID > V61_SPEC_READY__V62_PENDING(缺theorem或decisive core PE) > V61_SPEC_READY__V62_OPEN(required_core全ready + 条件依赖ready, 唯一可进V62)` 互斥先匹配，**缺 composable theorem 或 decisive core PE 即 V62 PENDING**，无关 `visibility/decoy` 以 `not_applicable_with_theorem_reason` 不阻塞已验，显式三源分别、tag 不重复（`leak_other=max(0,actual-64)`）、proxy 不升级、missing→null、仅新数据CORE+条件依赖READY算ell、禁旧数据伪造PE、epsilon不入bits三边界已验
- Phase A 10 类字段定义 `protocol_theorem、conjugate/phase-error、n_PE+sampling_rule、visibility逐源区间、eps_sec/cor、verification/auth、finite-size、post-selection/effective帧、unit/timestamp/session/source、acceptance_formula_inputs` 已覆盖，每类 `symbol/meaning/unit/required_class/authority/source_trace/readiness_detail/depends_on` 已定义（四类 `required_core / required_if_theorem_applicable / not_applicable_with_theorem_reason / missing`），`H_min` 下界是否明确声明已验，禁止 `IAB/H/MAP/vis` 当 `H_min`，`H1/L2/Δ/decoder` 零改已验，`old_data_fake_PE` 已审，`R61-02 epsilon不入bits` 已验
- Phase B 验收公式 `ell =1024*hmin - leak_IR(已含tag64) - leak_other(=max(0,actual-64)+...) - finite` 三源独立 `leak_without_tag 7025/7375/7700 +tag64=7089/7439/7764` 自洽，`GF32 5bits` 与 `H≠H_min` 已验，不跨源平均，`only_NEW_DATA_READY_ell` 已验，`R61-02` 三边界 `tag64-only / extra-verification / epsilon-not-bits` 已验
- Phase C `hmin_break_even` 阈值已落盘 `floor 6.9238/7.2637/7.5820 + 5% 7.2882/7.6460/7.9811 + 10% 7.6931/8.0707/8.4245`，`h_m = (leak_IR+other+finite)/(1024*(1-m))` 已算，锚点 `leak/1024` 与 `h*1024/(1-m)` 双校验，`missing→null` 已验，`leak_other` 口径 `max(0,actual-64)` 已验
- Phase D `first-match` 已验（含 `R61-01 visibility/decoy N/A 不阻塞`），`proxy 不升级` 已验，`仅新数据CORE+条件依赖READY进V62` 已验，`R61-02 三边界` 已验，无网格，`old_data_fake` 审计已验，stop 条件已验
- Phase E 脚本预留 `rg 0 hits` `py_compile` PASS 报告与 json/csv 一致 未建 `run_01` 未进 `V62` 未重跑 `V55` 未改 `LDPC` 已普通推送新 Plan SHA `PLAN_CANDIDATE/DECODE_FORBIDDEN` 仅改本目录+`scripts/`+`docs/research_cycles/V61P0/`（`src//experiments//tools//V54-V60` 零改），`HEAD==origin (7b476f62368a...)` 已验，推送后等待独立审核，**不自动进入 `V62`，不实现脚本、不启动 V62**
