# Tasks: repository-wide two-tier research workflow

- [x] W01 — Create this complete OpenSpec proposal/design/tasks/delta spec before editing workflow rules.
- [x] W02 — Record explicit supersession of the per-task review-frequency reading of `standardize-task-packet-review-loop` and extension of `research-cycle-sop-single-user-simplification`; preserve paired packet+prompt handoff and all DECIDE safety gates.
- [x] W03 — Amend `AGENTS.md` scope, mandatory processes, and §10 so the two tracks are the repository-wide default, EXPLORE batch-end review is explicitly valid, and the every-result Pre-RESULT wording applies to DECIDE, not every synthetic diagnostic arm.
- [x] W04 — Update the SOP lifecycle, task types, artifact expectations, authorization loop, and examples for EXPLORE/DECIDE.
- [x] W05 — Update the two reusable prompts so operators/reviewers must name the track and apply the matching evidence contract.
- [x] W06 — Add the compact DECIDE three-document option and EXPLORE single-log option; add at most one new template.
- [x] W07 — Record the D7 process lesson and the rule that machine terminal labels are scoped pre-registered classifications, not immutable scientific facts.
- [x] W08 — Remove or reconcile conflicting text across edited files; scoped `rg` shows no remaining unconditional per-arm independent-review requirement.
- [x] W09 — Run Markdown/link/path consistency checks and `git diff --check`; no numerical/decoder tests.
- [x] W10 — Obtain one independent workflow review; stop at `TWO_TIER_WORKFLOW_CANDIDATE_AWAITING_MAIN_ACCEPTANCE`; do not self-accept or start D6.
- [x] W11 — Add the compact repository-wide applicability matrix (synthetic diagnostics, synthetic route gates, real-data development, parameter scans, formal qualification, publication/report numbers, security calculations, implementation-only changes, documentation-only changes).
- [x] W12 — Inspect all repository-level workflow authorities and reusable generic prompts plus `rg`-discovered `README.md`/`AGENT_PROJECT_MEMORY.md` references; update an allowed authoritative file or list each intentionally unchanged non-authoritative historical reference; do not scan or rewrite every historical cycle.
- [x] W13 — Set the durable accepted terminal to `TWO_TIER_WORKFLOW_ACCEPTED_REPOSITORY_WIDE`; keep `TWO_TIER_WORKFLOW_ACCEPTED` only as a compatibility alias for the D6 prerequisite.
