# OpenSpec Tasks: formal-ir-v53-rate-adaptive-l2-heldout-confirm

**Lifecycle**: `PLAN_REVISE_REQUIRED / EXECUTE_NOT_AUTHORIZED` — **修订中，仅规划，不实现，不执行 decoder，不写产出。等待独立复审。本轮仅完成decoder-free样本注册表与嵌套矩阵复核（已按本次修订完成六文件口径修正）。**
**Execution status**: 本轮仅完成decoder-free样本注册表+嵌套秩复核；以下P1..E任务待独立plan ACCEPT后方可进入；当前不跑decoder，不写产出。V53冻结45 held-out blocks `90-135` calls（每源15，`L1 45+base45+rescue≤45`），门禁`35/45 & 每源10/15 & undetected==0`。
**HEAD**: `93c12fa5a8524eb5a8a52d071f135c653c746ebaf`（revised from `d61d5a3189b54fc0b82e2df688dae3e9bde5ff8e`；实现冻结时重绑至未来implementation SHA）
**Predecessor**: `formal-ir-v52-rate-adaptive-l2-rescue` (`6aa33eadc872bb4551f458ee750a94cd24566314`, branch `formal-ir-mainline`)
**Delta m**: `Δm=8` per source冻结（总90-135 硬帽135，去重后，复用V52 det1）

## Phase A — 语义冻结（plan ACCEPT后）

- [ ] **A1** 冻结方法完全体（零改）：`n=1024, m2=184/190/192, GF32 poly37, Lane C ordinal-2二值support/标签/位置置换, H1-16 rank16 80b, L1-APP `q_i` via `BP_i` (TRAIN prior `channel_counts.npz`), `H_inc 8×1024` det1 + `H_joint 192/198/200`, `decoder 90/1.0 early-stop`, `verification syndrome_ok && tag_ok` L2-only `compute_tag_64(empty,x2)`；`leak_base=5*m2+80+64 →1064/1094/1104, leak_joint=leak_base+40 →1104/1134/1144`；`exact_full=exact_u1&&exact_l2 oracle`报告promises；`Δm=8`唯一且禁止新造/试4/12/16。
- [ ] **A2** 冻结增量矩阵`H_inc`（复用V52 det1, `Δm=8` per source）：`8×1024 GF32 poly37`、`col_degree_inc∈{0,1}`、`row_degree≤16`、`无零增量行`、`rank_joint==m2+8`、`nested H_base==H_joint[:m2]`、`independence==Δm` (rank增量)、确定性PEG-增量`SeedSequence([60000x,1/2/3])`、禁止seed搜索、第二候选与调`Δm`；`E_inc≈96`报告、`leak_joint=leak_base+40`公式冻结、`first_pass_success已成功帧不增泄漏(1064/1094/1104)，rescued/仍失败均为leak_joint(1104/1134/1144)`。
- [ ] **A3** 冻结两遍条件协议：`pass1(H_base)` → `verify1(syndrome&&tag)`通过则停`leak_base`；否则`s_inc=H_inc*u2_true`、`s_joint=[s_base;s_inc]`、`pass2(H_joint)` → `verify2`，`leak=leak_base if verify1 else leak_joint`；报告`base_exact_full`与`verify_base=count(syndrome_ok&&tag_ok)`分别计数（禁止假定相等）、`rescued_by_increment / final_exact_full`，`rescue_rate=rescued/N_rescue_attempted`（`N_rescue_attempted=count(!verify_base)` overall，per source `N_rescue_attempted[s]=count(!verify_base) per source`，禁止用`45-base_exact`作分母）描述性；每块`L1 1+base 1+rescue≤1`，总`45 L1+45 base+≤45 rescue=90-135 硬帽135 (L2 45-90)`。
- [ ] **A4** 冻结45 fresh held-out blocks枚举算法：于hold区间`1M H=400 base1600 / 1p5M H=554 base2213 / 2M H=729 base2916`内，汇总V48/V50/V51/V52已用`frame_ids`（60区间360帧+早期FORBIDDEN 96 block IDs），枚举`all_starts=0..H-4`的`[s,s+3]`窗口，过滤与已用区间重叠者得`S`按`ordinal`排序`K=|S|≥15`，以`index_j=floor(j*(K-1)/14) j=0..14`分散选择15/源，验证最终45间两两非重叠（gap≥4）且与已用零重叠；建议IDs `394001-015/394101-115/394201-215`仅标识，真实由`frame_ids`决定；每块写死`4 frame_ids=[base+start .. base+start+3]`与`held_out_ordinal_start/end`、`pairs_count=1024`、`sampling_mode=deterministic_four_consecutive_frames_heldout_fresh_v53`、`BLOCK_LENGTH=1024`。
- [ ] **A5** 冻结paired workload（`base兼old`去重）：每块同`bob`同`P_i(U2)`，`base L2/pass1`同一次确定性译码兼作`old` baseline与V53 `pass1`（old Lane C与V53 pass1完全同参同算子同输入，结果必然相同，不重复译码，`old_exact=base_exact`）；每块`L1 1+base 1+条件rescue ≤1`（L2 1-2）；总`45 L1+45 base+≤45 rescue=90-135 硬帽135`；冻结`paired Δexact = final_V53 - base`描述性、McNemar `b/c`仅描述。
- [ ] **A6** 冻结报告承诺SHALL：`base_exact_full`与`verify_base`分别计数（禁止假定相等）/ `incremental_rescue_count / final_exact_full (45 & per-source)` / `first_pass_success_leak(1064/1094/1104) / rescued_success_leak(1104/1134/1144) / final_failure_leak(leak_joint) / per_source_avg[s]=leak_base[s]+40×N_rescue_attempted[s]/15 / overall_avg=(Σ leak_base[source(block)]+40×N_rescue_total)/45（`N_rescue_total=count(!verify_base)`，因三源`leak_base`不同禁单一`+40N/45`当`overall`） / avg_disclosure_per_attempted_frame(=overall_avg/1024) / total_disclosed_bits(=Σ leak_total) / disclosure_per_final_exact_block=total_disclosed_bits/final_exact_full_count（为0则null，删除含糊`final_accepted_bits`，`f_avg`若保留则分母`1024×(H(U1|B)+H(U2|U1,B))`禁`N_blocks×(H1+H2)`） / iterations/runtime/residual / reclassified四类`；`first_pass_success_leak=leak_base、rescued_success_leak=leak_joint、final_failure_leak=leak_joint、per_source_avg[s]=leak_base[s]+40×N_rescue_attempted[s]/15、overall_avg=(Σ leak_base+40×N_rescue_total)/45`；不得再用`successful_conditional=leak_base`单一口径；`V52 12/15`仅历史描述，不得作门禁依据。
- [ ] **A7** 冻结记录与聚合：`45`块paired记录、`v53_summary.json`含`base_exact_full`与`verify_base`分别计数分层、每源分源、`rescue_rate=rescued/N_rescue_attempted（禁45-base_exact）`、平均/条件泄漏（`per_source_avg`与`overall_avg`区分，禁单一`+40N/45`当`overall`）+`avg disclosure`+`total_disclosed_bits`与`disclosure_per_final_exact_block`（为0则null，删除含糊`final_accepted_bits`，`f_avg`若保留分母`1024×(H(U1|B)+H(U2|U1,B))`）+`H_inc provenance (Δm=8 det1 joint_rank/nested/independence/E_inc/row_max)`、`tag acceptance`、`paired base vs final Δexact(去重)`、`四类/G3'`、门禁明细（G1/G2/G3'数值与PASS/FAIL，`35/45`与`10/15`权威，`undetected==0`）；`tag_scope=l2_only`；`V52 12/15`历史描述单独段。
- [ ] **A8** 冻结终态：`V53_EVIDENCE_INVALID`优先（完整性/秩/嵌套/重叠/记账失败）、`V53_HELDOUT_CONFIRM_PASS`（`G1∧G2∧G3'∧rank/nested/记账`全满足）、`V53_HELDOUT_CONFIRM_FAIL`（完整性通过但未满足门禁）；不加复杂终态；`PASS`仍仅`development confirmation`，下一阶段qualification需新采集/独立TEST（`split_manifest`外），不能直接称qualification。
- [ ] **A9** 冻结执行偏差防复发：不使用600s外部timeout（建议≥3600s或不设）、若返回session/cell ID只轮询同一进程禁重启、中断时保留raw partial及调用计数不生成aggregate、不得自动重跑；停止规则“仅四工件+decoder-free样本注册表+矩阵复核，不得实现/执行decoder”。

## Phase B — 聚焦测试（仅fake runner/decode_fn；零生产解码）

- [ ] **B1** Seed-registry校验：拒重复/错形状/与已用`frame_ids`重叠per source + 与`FORBIDDEN 96` block ID零重叠；接受新区建议`394001-015/394101-115/394201-215`但真实校验以`frame_ids`为准；校验`K≥15`且`index_j=floor(j*(K-1)/14)`分散选择15/源、无内部重复、每源均分15、与`V48/V50/V51/V52` `frame_ids`零重叠per source可机械校验（`BLOCK_WINDOWS`比对）；校验最终45间两两非重叠（gap≥4）。
- [ ] **B2** 嵌套矩阵重建匹配：`H_base m2×1024`与committed v38常量一致；`H_inc 8×1024`注入`rank_joint`/`nested`/`independence`漂移 → J路径；重建一致；代表身份错→J；校验`rank_joint==m2+8`、`H_base==H_joint[:m2]`、`rank_increment==8`、`row_degree_max≤16`、`col_degree_inc≤1`、`leak_joint==leak_base+40`、`E_inc≈96`。
- [ ] **B3** 泄漏公式校验（三类+平均，区分per-source/overall）：`leak_base 1064/1094/1104`；`leak_joint 1104/1134/1144`；`per_source_avg[s]=leak_base[s]+40×N_rescue_attempted[s]/15`与`overall_avg=(Σ leak_base[source(block)]+40×N_rescue_total)/45`可重算（`N_rescue_attempted[s]=count(!verify_base) per source`，`N_rescue_total=count(!verify_base) overall`，因三源`leak_base`不同禁止用单一`leak_base+40N/45`当`overall`）；`first_pass_success_leak==leak_base`、`rescued_success_leak==leak_joint`、`final_failure_leak==leak_joint`、`failed_conditional==leak_joint`、`avg_disclosure_per_attempted = overall_avg/1024`（per source分层）、`total_disclosed_bits = Σ leak_total`与`disclosure_per_final_exact_block=total_disclosed_bits/final_exact_full_count（为0则null，删除含糊final_accepted_bits）`、`f_avg`若校验则分母`1024×(H(U1|B)+H(U2|U1,B))`禁`N_blocks×(H1+H2)`；`rescue_rate=rescued/N_rescue_attempted（禁45-base_exact，base_exact与verify_base分别校验）`；校验不得再用`successful_conditional==leak_base`旧口径。
- [ ] **B4** Tag复用与四类分流（L2-only空前缀）：`compute_tag_64`确定性、`hex[:16]`、`tag_scope==l2_only`；`pass1/pass2`各自`syndrome_ok&&!exact&&!tag_ok→detected`等四类；`G3' undetected==0`单独表；`base_exact`以`tag_ok`判但同时报告`exact`预留。
- [ ] **B5** 哨兵与嵌套与泄漏校验：每源首块建议`394001/394101/394201`各`L1→P_i(U2)`通路+`h_base_ok`+`h_inc_nested_ok`+`joint_rank_ok`+`independence_ok`+`leakage_accounted (base/joint/avg/conditional/total)`+`tag_scope_l2_only`+`sampling_mode/frame_ids`+`K/index_j`分散性。
- [ ] **B6** 样本一致性（fresh held-out）：同ID重采样一致；缺/重block →J6；校验fresh块非V50/V51/V52块且最终45间两两非重叠；校验`45块是最小确认规模`与`V53通过仍仅development confirmation` claim boundary。
- [ ] **B7** 真值表（门禁冻结）：枚举integrity ok/failed × `V53_HELDOUT_CONFIRM_PASS / V53_HELDOUT_CONFIRM_FAIL / V53_EVIDENCE_INVALID`互斥；另校验`rescue_gain = final > base`描述性旗标可区分；`base_exact_full`与`verify_base`分别计数（禁止假定相等）、`final = base_exact_full + rescued`（rescue仅对未通过`verify_base`者，`N_rescue_attempted=count(!verify_base)` overall，per source `N_rescue_attempted[s]=count(!verify_base) per source`，`rescue_rate=rescued/N_rescue_attempted`禁`45-base_exact`）与门禁`35/45 & 10/15 & undetected==0`三条；校验`V52 12/15`仅历史描述不参与门禁。
- [ ] **B8** 预算与配对序校验（去重后）：fake结构校验每块`L1 1+base_shared(兼old/pass1)1+条件rescue ≤1`冻结（L2 1-2，总`45 L1+45 base+≤45 rescue=90-135 硬帽135，L2 45-90`）；源1M/1p5M/2M、块升序、块内`base_shared(兼old/pass1)→(条件)rescue`序；`old`与`pass1`同记录不重复。
- [ ] **B9** 写出合约（去重后）：文件集精确（45-90 L2行：45 base_shared(兼old/pass1)+≤45 rescue，含`arm∈{base_shared,rescue}/pass_index/used_increment/matrix_id/joint`+`frame_ids/sampling_mode/ordinal`+`leak_total(1064/1094/1104或1104/1134/1144)/leak_joint/avg`；总调用`45 L1+45 base+≤45 rescue=90-135 硬帽135；summary含`base_exact_full`与`verify_base`分别计数分层+每源+`first_pass/rescued/failure/avg`泄漏分类（`per_source_avg`与`overall_avg`区分，禁单一`+40N/45`当`overall`）+`avg disclosure`+`total_disclosed_bits`与`disclosure_per_final_exact_block`（为0则null，删除含糊`final_accepted_bits`，`f_avg`分母`1024×(H(U1|B)+H(U2|U1,B))`）+`joint_rank/nested/independence`+paired `base vs final(去重)`+四类/G3'+`rescue_rate=rescued/N_rescue_attempted（禁45-base_exact）`+终态+门禁明细`35/45 & 10/15 & undetected==0`）、CSV/JSON行对等、已存在根fail-closed、禁写NPZ、provenance含`H_inc Δm=8 det1`+fresh held-out溯源含每块`frame_ids/ordinal`与`K/index_j`。
- [ ] **B10** 执行偏差防复发测试（仅Python单测可控）：fake执行器模拟中断保留raw partial但不生成aggregate → 通过（partial retention + no aggregate on interruption）；模拟中断后操作包禁重启/不生成新session → 拒（操作包禁重启）；删除无法由Python单测控制的外部600s timeout包裹与session/cell ID外部轮询fake测试（该类外部超时/会话由执行层≥3600s策略与轮询同一进程规则覆盖，不在单测中伪造）。

## Phase C — preflight（decoder-free，授权前，spike复核）

- [ ] **P1** 重建`Lane C base m2×1024`三矩阵与committed `rank/support`比对；重建`H_inc 8×1024`三矩阵与`joint_rank==m2+8 / nested / independence==8 / row≤16 / col≤1`校验；`compute_tag_64`可import；校验`leak_base/joint`公式；校验45 fresh块枚举算法（`all_starts 0..H-4`过滤已用得`K≥15`，`index_j`分散选15/源）与零重叠（与已用`frame_ids`零重叠且最终45间两两非重叠）可达；校验TRAIN counts形态；held-out fresh池可达；零求值器、零写盘。
- [ ] **P2** 在首块建议`394001/394101/394201`跑`L1 shared →P_i(U2)`通路+`h_base/h_inc/nested/rank/independence`+`tag_ok`+`leakage_accounted`+`tag_scope_l2_only`+`sampling_mode/frame_ids/ordinal/K/index_j`；仅报PASS/BLOCKED.
- [ ] **P3** 以拷贝已用区间机械校验J2（新区建议IDs与已用`frame_ids`零重叠per source + 最终45块两两非重叠、无重复、每源均分15且分散`index_j`）并确认输出根缺席；仅报PASS/BLOCKED.

## Phase D — 授权两遍条件执行（需EXECUTE_AUTH，45块paired，90-135 calls）

- [ ] **D1** 主线程获独立plan ACCEPT + 显式用户`EXECUTE_AUTH`（绑定repository、分支`formal-ir-mainline`、完整未来实现SHA、cycle V53P0、scope `v53_heldout_confirm_45_blocks_paired_exactly_once`，plan引用HEAD `93c12fa5a8524eb5a8a52d071f135c653c746ebaf` revised from `d61d5a3189b54fc0b82e2df688dae3e9bde5ff8e`）。
- [ ] **D2** 恰好一次：`python scripts/execute_v53_heldout_confirm.py --execution-authorized --authorized-target-sha <sha>`；按design §2.3去重两遍协议执行：`45块 × (base 1(兼old/pass1，同一次确定性译码)+条件rescue ≤1)`，每块`L1 1+base L2 1+rescue ≤1`，总`45 L1+45 base+≤45 rescue=90-135 硬帽135 (L2 45-90)`；**执行偏差防复发**：不设600s外部timeout（建议≥3600s或不设）、若返回session/cell ID只轮询同一进程禁重启、中断时保留raw partial及调用计数不生成aggregate、不得自动重跑；每条L2记录含`arm∈{base_shared,rescue}/pass_index/used_increment/matrix_id/target_tag/candidate_tag/tag_ok/reclassified` (L2-only)与`exact_u1/exact_l2/exact_full/errors/syndrome/entropy/||q-p||1/leak_total(1064/1094/1104或1104/1134/1144)/leak_joint`；`old`基准由`base`结果确定性复用，不另译码；保留`run_01`；出错止、原样保留raw partial无聚合、返回blocker；不rerun/resume/tuning/加块/改增量/重实现canonical；无论结果不做第二轮；同块base与rescue同`bob`同`P_i(U2)`；采样恒deterministic fresh held-out `K/index_j`分散。
- [ ] **D3** 只读postcheck：记账`base_exact_full`与`verify_base`分别计数（禁止假定相等）、`rescued / final / old(derived from base)`、`45`块去重记录与序每块`base_shared→(条件)rescue`正确（old与pass1同记录不重复）、增量`H_inc` provenance、fresh held-out `frame_ids/ordinal`逐块比对design §2.4 `K/index_j`分散选择、无NPZ、`V38–V52`输出byte-identical、summary完备（含`base_exact_full`与`verify_base`分别分层+每源+`first_pass_success_leak/rescued_success_leak/final_failure_leak/per_source_avg[s]=leak_base[s]+40×N_rescue_attempted[s]/15 / overall_avg=(Σ leak_base+40×N_rescue_total)/45（`N_rescue_total=count(!verify_base)`禁单一`+40N/45`当`overall`）/avg_disclosure/total_disclosed_bits与disclosure_per_final_exact_block（为0则null，删除含糊final_accepted_bits，`f_avg`分母`1024×(H(U1|B)+H(U2|U1,B))`）+`joint_rank/nested/independence`+paired(去重)+四类/G3'+`rescue_rate=rescued/N_rescue_attempted（禁45-base_exact）`+`tag acceptance`+终态+门禁`35/45 & 10/15 & undetected==0`+`leakage_formula(per_source/overall区分)`）、独立重算；校验`45块最小确认规模`与`V53通过仍仅development confirmation` claim boundary。

## Phase E — 结果复核

- [ ] **E1** 写`OPERATOR_RETURN.md`与`DEVELOPMENT_RESULT.md`候选；lifecycle保持result-candidate；含`base_exact_full`与`verify_base`分别计数（禁止假定相等）/ `incremental_rescue / final_exact_full / old_exact_full(derived)` (overall & per-source, 45块)、`rescue_rate=rescued/N_rescue_attempted（禁45-base_exact，N_rescue_attempted=count(!verify_base)）`、`first_pass_success_leak/rescued_success_leak/final_failure_leak/per_source_avg[s]=leak_base[s]+40×N_rescue_attempted[s]/15 / overall_avg=(Σ leak_base+40×N_rescue_total)/45（禁单一`+40N/45`当`overall`）/avg_disclosure_per_attempted/total_disclosed_bits与disclosure_per_final_exact_block（为0则null，删除含糊final_accepted_bits，`f_avg`若保留分母`1024×(H(U1|B)+H(U2|U1,B))`）描述性`、`joint_rank/nested/independence`实测、`paired base vs final Δexact(去重)` per block描述性、McNemar `b/c`仅描述、四类计数、`l1`执行期诊断上下文真实`q≠p`严格在claim boundary内（含L2-only工程近似与`leak_base 1064/1094/1104` / `rescued/failure 1104/1134/1144` / `per_source_avg[s]/overall_avg`且first_pass已成功帧不增泄漏说明，不宣称FER/晋升，`V52 12/15`仅历史描述，`45块最小确认规模`与`V53通过仍仅development confirmation下一阶段需新采集/独立TEST`边界）。
- [ ] **E2** 主线程/reviewer独立重算`base/final/rescued/old`、泄漏、嵌套秩、配对与终态与四类；verdict入`REVIEW_VERDICT.md`.
- [ ] **E3** Memory triage单独里程碑（本规划轮禁改`AGENT_PROJECT_MEMORY.md`）；**不启动下一阶段**。

## 本变更期间显式禁止

plan ACCEPT前实现；以outcomes回搜增量位置/调`Δm`或多候选择优；改首遍support/标签/先验/度分布/m2/H1/泄漏/decoder；重复比较prior、标签或另一全新MET图；新增第二增量候选或档位；以`V48/V50/V51/V52` outcomes选持块或调`Δm`；写NPZ；把wrong计为exact；作晋升排名；FER/阈值/SKR/安全/资格/晋升陈述；结果后加块/加seed/改阈值/改机制；rerun/resume/补偿partial；任何结果下开第二轮；自接受；自动启动后继（含下一阶段）；import `v50/v51/v52`模块作生产解码；任何`compute_tag_64(x1_true,…)`形态；改H1/`m2`泄漏或`90/1.0`或引入第二变量外泄漏变更；创建正式`.../v53_*/run_01`输出（P0仅规划）；运行decoder（P0仅decoder-free spike）；声称`V52 12/15`为门禁依据；使用600s外部timeout包裹执行；对返回的session/cell ID重启新进程而非轮询同一进程；中断后生成aggregate或自动重跑；为凑30块小样本而降低门禁至`24/30`等；将`development confirmation`宣称为`qualification`。
