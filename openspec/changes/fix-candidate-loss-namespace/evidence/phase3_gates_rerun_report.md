# Phase 3 门校验复跑报告（G3 二次修订版）— fix-candidate-loss-namespace（E2）

**状态：G2/G4 PASS；G3 二次修订门在新数据上 FAIL（条件③：2 个倒置 ≥2%）⇒ 按停止规则就地停止，未进入 E3/E4。**
复跑时间：2026-08-26；任务根：`workspace/fix-candidate-loss-namespace/p4_20260826_010754/gates_rerun/`。
门脚本：`tools/verify_candidate_namespace_gates.py`（E1 二次修订版，self-test 全场景 PASS、试点 pytest 4 passed）。

## 1. G1 抽档复核 — 按任务书略过

## 2. G2 跨档字节唯一性

### 新档互检（修复有效性主门）

| 比较范围 | 比较对数 | 碰撞 | 结论 |
|---|---|---|---|
| 新6 × 新10 × 新16（全格） | **574** | **0** | **PASS** |

### 任务书指定的旧参照对（6 对，记录性质）

| 比较对 | 数组数 | 碰撞 | 结论 |
|---|---|---|---|
| 新6 vs 旧20_t15 | 242 | 0 | PASS |
| 新10 vs 旧20_t15 | 242 | 0 | PASS |
| 新16 vs 旧20_t15 | 166 | 0 | PASS |
| 新10 vs 旧16 | 242 | 70 | 与 Phase 3 完全一致（已归因：污染方向证据） |
| 新6 vs 旧10 | 242 | 6 | 同上 |
| 新6 vs 旧16 | 242 | 14 | 同上 |

## 3. G3 物理合理性（二次修订语义）

门配置：新 6/10/16 三档 + 旧20dB_t15 作 20dB 参照；可比格（四档全覆）= 83；
相对差定义 = 对称差 |a−b| / ((|a|+|b|)/2)；阈值 2%（design §4）。

| 条件 | 结果 | 判定 |
|---|---|---|
| ① 跨档 ser 零精确相等 | **0** 起 | ✓ |
| ② 档均值严格有序 | 6dB=0.240816 > 10dB=0.225529 > 16dB=0.207552 > 20dB=0.172464 | ✓ |
| ③ 倒置分类 | noise(<2%) = **11** 条；需解释(≥2%) = **2** 格 | ✗ ⇒ 门 FAIL |

isolation 清单（verdict≠PASS，非门控）：355 条（与 Phase 3 一致）。

**≥2% 需解释清单（2 格，均为 [6dB>10dB] 倒置）：**

| 格 | ser(6dB) | ser(10dB) | rel_diff |
|---|---|---|---|
| d1024_bw150 | 0.23191869496914921 | 0.23714954504655134 | **2.2303%** |
| d2048_bw150 | 0.23191869496914921 | 0.23714954504655134 | **2.2303%** |

明细 CSV：`gates_rerun/g3_new_detail.csv`（kind ∈ isolation/noise/needs_explanation）。

### 附带取证（非阻塞观察）

- 两格六数组（3 档 × 2 格）sha256 **全部互异** ⇒ 无字节级共享，G2 保证未被破坏；
- d1024_bw150 与 d2048_bw150 的 ser 在 6/10dB 逐位相同、16dB 末三位分离
  （0.23300801 vs 0.23295416）——高维带映射层巧合，与 Phase 3 已知模式一致，非污染签名。

### 旧四档数据同门对照（记录性质，预期 FAIL）

精确相等 **152** 起（如 d1024_bw150: ser(6dB)==ser(16dB)==0.10502…，污染直接签名）、
noise 96 条、≥2% 需解释 29 格。均值序 0.211116>0.200442>0.194812>0.17006（均值序新旧都成立，
判别力来自条件①）。新数据零精确相等 vs 旧数据 152 起 ⇒ 二次修订判据对污染的区分度成立。

## 4. G4 溯源完整性

新三档全量 **325** sidecar：metrics/joint 指纹、source_ttbin_path 匹配本档原始记录目录、
pairing_v2、sequence_is_sampled=0、params 快照 —— **PASS**。（old20 的 121 个已在 Phase 3 验证过。）

## 5. BLOCKER（单一决策需求）

**G3 条件③ FAIL：2 格倒置 rel_diff=2.2303%，恰好越过 2% 阈值。**

诊断：

1. 两格即 Phase 3 §7 引用的典型简并格 d1024_bw150 及其 ser 孪生格 d2048_bw150，
   位于已裁决的高维近简并区（d512–d2048）；无精确相等、数组互异、均值序干净、20dB 带清晰分离
   （0.172 vs 0.208–0.241）；
2. design §4 二次修订记载"新违例全部 <1%"，实际最大倒置为 **2.23%** ——
   裁决时的量级估计偏低，2% 阈值恰好卡在真实最大值之下约 0.23 个百分点；
3. 非重建缺陷、非脚本 bug（self-test/pytest 全绿；同门下旧数据以 152 起精确相等显著区分）。

按停止规则未进入 E3/E4、未调阈值、未绕过。可选处置（示例，非提案）：
(a) 阈值上调至覆盖实测简并带（如 2.5%，毫秒级重跑本门即可验证）；
(b) 对该 2 格作书面物理解释后白名单放行；
(c) 将 ≥2% 清单改为纯记录项（回到"仅①②硬门控"语义）。
裁决后无需重新物化，仅需重跑本门（秒级），随后方可恢复 E3/E4。
