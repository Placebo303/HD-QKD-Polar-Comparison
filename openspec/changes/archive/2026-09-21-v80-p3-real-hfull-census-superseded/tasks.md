# V80-P3 Real-Data H_full Census — Tasks

Planning-only change: no code/test edits in this task. T3–T6 need the signed authorization
before execution.

- [x] P3-T1 packet freeze — acceptance: `docs/research_cycles/V80-NBLDPC-JAN21/P3_CENSUS_PACKET.md` written (§§1–12: questions, 10-dataset manifest with families A–D, DECIDE justification + mandatory gate chain, alignment gating decision tree with the four branches and the identifiability limit, single frozen estimator definition with comparability reconciliation, bias/uncertainty rule, memory & stationarity battery, per-source reporting schema, conditional certification arithmetic, budgets, stop rules, forbidden list, explicit user gate); every number traces to a frozen source or is marked `[TO BE MEASURED]` / `[BLOCKING: needs user input]`; no alignment parameter, seed literal, or block count is invented.
- [x] P3-T2 preregistration + authorization template — acceptance: `P3_CENSUS_PREREG_AND_AUTH.md` written with the exact command placeholder, the frozen budget table, the output-absence checklist, the frozen decision criteria, and a SIGNATURE BLOCK the user must fill; nothing granted by default.
- [x] P3-T3 operator prompt — acceptance: `P3_CENSUS_PROMPT.md` written, copy-paste ready, stop conditions and forbidden list explicit, authorizes nothing.
- [ ] P3-T4 thin executor + fake-only tests (implementation-only, no data access) — acceptance: new thin module only, `src/qkd_io/ttbin_pipeline.py` and all frozen modules read-only; fake-only tests cover estimator identity (MM formula, TRAIN/HOLD accounting, per-plane sum), split-manifest-written-before-statistics, per-frame statistics asserting equality with `compute_ttbin_metrics` aggregates on a fake event stream, root refusal, and the §4.3 Branch-B acceptance arithmetic; no production decode/DE in tests.
- [ ] P3-T4a mandatory alignment wrapper + fake-only alignment tests (implementation-only, no data access) — acceptance: NEW ADDITIVE wrapper module only (never `src/`; `git diff -- src/` empty) implementing the frozen §3A order (histogram `bin100/max_lag819200` → `argmax` → gates → `offset_ps=+peak_center` → metrics); fake-only tests cover argmax/bin-centre conversion sign, `peak_to_bg ≥ 100` gate, single-mode rule (>50% secondary outside ±1000 ps ⇒ BLOCKED), sigma 10–500 ps gate, STOP-BLOCKED with no fallback to 0/borrowed/recorded offset, A1 one-bin agreement check vs recorded −50/+50 (bins 8191/8192) reported as finding, per-row alignment fields incl. bin index, and call-order refusal (pairing before passed alignment refused); no production decode/DE in tests.
- [ ] P3-T5 Pre-EXECUTE + signed grant (needs the user's branch choice and signature) — acceptance: Q0–Q6 recorded (branch context, scoped cleanliness, frozen contract, output-absence + `rg` proofs, `workspace/p3_census_*` absence, focused tests, TimeTagger import check, target-output absence with the final UUID); SIGNATURE BLOCK filled naming the branch, dataset list, config path, seed, tolerances and wall ceiling.
- [ ] P3-T6 gated execution (needs T5 AND the separately-signed Stage 0.5 PASS) — acceptance: Stage 0 + Stage 0.5 file-identity + span-continuity probe completed with the frozen rule (ONLY `X.ttbin` opened, never both concatenated; span > 0 and consistent with `.1`−base mtime gap; `duration_measured_s`/`filename_duration_tag`/`tag_disputed` recorded; Jan-12 quarantined at 30.0 s); one bounded run per dataset list within wall/per-dataset/RSS caps; `split_manifest.json` written before any statistic; per-source §8 table emitted with `comparable_by_anchor` and `status`; no forbidden action; wall-partial retained as `INCOMPLETE`.
- [ ] P3-T6a gated alignment-first execution (needs T5 + T4a + Stage 0.5 PASS) — acceptance: per dataset, §3A alignment derived on the merged base-member stream and passed (`peak_to_bg ≥ 100`, single-mode, sigma 10–500 ps, ok status) BEFORE any pairing; `offset_ps_derived`/`peak_bin_index`/`peak_center_ps`/`peak_to_bg`/`sigma_crude_ps`/`align_status` + prior-offset comparison recorded; any gate failure ⇒ STOP-BLOCKED with no pairing/histogram/entropy and no fallback; A1 agreement within one bin checked and reported; `H_full` reported conditional on the derived offset; PM/EB negative result + SHG-A channel-plan rule (`getChannelList()` over `registered channels`) observed.
- [ ] P3-T7 independent Pre-RESULT + main-thread acceptance — acceptance: an independent reviewer re-checks the actual artifacts against the frozen plan (estimator definition and split side, support/occupancy and bias diagnostics, disclosure/accounting semantics, per-source breakdown, no pooling, `undetected` isolation where applicable, alignment provenance labels) and records `INDEPENDENT_ACCEPTANCE.md`; issues ⇒ immediate rework, never publish-then-patch; main-thread acceptance closes the batch and owns any route decision.
